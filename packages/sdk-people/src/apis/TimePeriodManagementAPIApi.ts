/* tslint:disable */
/* eslint-disable */
/**
 * POS Human Resources Service API
 * Human Resources service for employee management, payroll, and benefits administration
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  CreateTimePeriodRequest,
  TimePeriodDto,
  TransitionTimePeriodRequest,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    CreateTimePeriodRequestFromJSON,
    CreateTimePeriodRequestToJSON,
    TimePeriodDtoFromJSON,
    TimePeriodDtoToJSON,
    TransitionTimePeriodRequestFromJSON,
    TransitionTimePeriodRequestToJSON,
} from '../models/index';

export interface CreateTimePeriodOperationRequest {
    createTimePeriodRequest: CreateTimePeriodRequest;
}

export interface TransitionTimePeriodOperationRequest {
    timePeriodId: string;
    transitionTimePeriodRequest: TransitionTimePeriodRequest;
}

/**
 * 
 */
export class TimePeriodManagementAPIApi extends runtime.BaseAPI {

    /**
     * Creates a pay period for a tenant with an inclusive start and end date and an initial lifecycle status. Use this tool for corrections and off-grid periods; do not create routine periods manually, the scheduled rollover opens those on the configured cadence instead. Preconditions: the range must not overlap any existing period for the tenant, and endDate must not be before startDate. Required inputs: a body with tenantId (UUID), startDate, and endDate; status is optional and defaults to OPEN. Emits PEOPLE_TIME_PERIOD_CREATE. Returns 400 when the range is invalid, and 409 when the range overlaps an existing period. 
     * Create Pay Period
     */
    async createTimePeriodRaw(requestParameters: CreateTimePeriodOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TimePeriodDto>> {
        if (requestParameters['createTimePeriodRequest'] == null) {
            throw new runtime.RequiredError(
                'createTimePeriodRequest',
                'Required parameter "createTimePeriodRequest" was null or undefined when calling createTimePeriod().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people:timePeriod:create"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/people/time-periods`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateTimePeriodRequestToJSON(requestParameters['createTimePeriodRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TimePeriodDtoFromJSON(jsonValue));
    }

    /**
     * Creates a pay period for a tenant with an inclusive start and end date and an initial lifecycle status. Use this tool for corrections and off-grid periods; do not create routine periods manually, the scheduled rollover opens those on the configured cadence instead. Preconditions: the range must not overlap any existing period for the tenant, and endDate must not be before startDate. Required inputs: a body with tenantId (UUID), startDate, and endDate; status is optional and defaults to OPEN. Emits PEOPLE_TIME_PERIOD_CREATE. Returns 400 when the range is invalid, and 409 when the range overlaps an existing period. 
     * Create Pay Period
     */
    async createTimePeriod(requestParameters: CreateTimePeriodOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TimePeriodDto> {
        const response = await this.createTimePeriodRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Moves a pay period to a new lifecycle status along the allowed transitions, including reopening a SUBMISSION_CLOSED period for corrections. Use this tool for manual corrections; do not drive routine closure with it, the scheduled rollover advances statuses on the configured cadence instead. Preconditions: allowed transitions are OPEN to SUBMISSION_CLOSED or PAYROLL_CLOSED, SUBMISSION_CLOSED to PAYROLL_CLOSED or back to OPEN; PAYROLL_CLOSED is terminal. Required inputs: timePeriodId (UUID) path parameter and a body with the target status. Emits PEOPLE_TIME_PERIOD_TRANSITION. Returns 404 when the period does not exist, and 409 when the transition is not allowed. 
     * Transition Pay Period Status
     */
    async transitionTimePeriodRaw(requestParameters: TransitionTimePeriodOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TimePeriodDto>> {
        if (requestParameters['timePeriodId'] == null) {
            throw new runtime.RequiredError(
                'timePeriodId',
                'Required parameter "timePeriodId" was null or undefined when calling transitionTimePeriod().'
            );
        }

        if (requestParameters['transitionTimePeriodRequest'] == null) {
            throw new runtime.RequiredError(
                'transitionTimePeriodRequest',
                'Required parameter "transitionTimePeriodRequest" was null or undefined when calling transitionTimePeriod().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people:timePeriod:transition"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/people/time-periods/{timePeriodId}/status`.replace(`{${"timePeriodId"}}`, encodeURIComponent(String(requestParameters['timePeriodId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: TransitionTimePeriodRequestToJSON(requestParameters['transitionTimePeriodRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TimePeriodDtoFromJSON(jsonValue));
    }

    /**
     * Moves a pay period to a new lifecycle status along the allowed transitions, including reopening a SUBMISSION_CLOSED period for corrections. Use this tool for manual corrections; do not drive routine closure with it, the scheduled rollover advances statuses on the configured cadence instead. Preconditions: allowed transitions are OPEN to SUBMISSION_CLOSED or PAYROLL_CLOSED, SUBMISSION_CLOSED to PAYROLL_CLOSED or back to OPEN; PAYROLL_CLOSED is terminal. Required inputs: timePeriodId (UUID) path parameter and a body with the target status. Emits PEOPLE_TIME_PERIOD_TRANSITION. Returns 404 when the period does not exist, and 409 when the transition is not allowed. 
     * Transition Pay Period Status
     */
    async transitionTimePeriod(requestParameters: TransitionTimePeriodOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TimePeriodDto> {
        const response = await this.transitionTimePeriodRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
