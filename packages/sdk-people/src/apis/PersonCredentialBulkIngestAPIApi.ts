/* tslint:disable */
/* eslint-disable */
/**
 * POS Human Resources Service API
 * Human Resources service for employee management, payroll, and benefits administration
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  BulkIngestRequestPersonCredentialBulkIngestRecord,
  BulkIngestResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    BulkIngestRequestPersonCredentialBulkIngestRecordFromJSON,
    BulkIngestRequestPersonCredentialBulkIngestRecordToJSON,
    BulkIngestResponseFromJSON,
    BulkIngestResponseToJSON,
} from '../models/index';

export interface BulkIngestPersonCredentialsRequest {
    bulkIngestRequestPersonCredentialBulkIngestRecord: BulkIngestRequestPersonCredentialBulkIngestRecord;
}

/**
 * 
 */
export class PersonCredentialBulkIngestAPIApi extends runtime.BaseAPI {

    /**
     * Records many credentials at once, resolving each row\'s employee number to a person and its vendor code (sourceCode + sourceCredentialCode, such as ASE T4-BRAKES) to a registry skill through the cross-reference, or taking a Durion skillCode such as DOT-INSPECTOR directly. Use this tool when loading an HR feed\'s certifications or seeding an environment; read the registry at GET /v1/people/skills instead of guessing codes. Preconditions: each employee number must resolve to a person with an employee record, and each vendor code must be in the cross-reference — an unknown code rejects its row rather than becoming a skill nobody holds. Required inputs: jobId, and per record employeeNumber, issuer, issuedOn and either skillCode or sourceCode + sourceCredentialCode; expiresOn, proficiency (1-5) and evidenceRef are optional, and expiresOn may not precede issuedOn. Semantics: upsert by natural key (person, skill, issuer, issuedOn), so a re-send updates the expiry, proficiency and evidence, a renewal with a later issuedOn is a new row, and status is derived from the dates rather than taken from the row. With supersedeAbsent=true, each person in the batch has their credentials from this job\'s sourceSystem that the batch no longer lists marked SUPERSEDED — never deleted. Emits a PEOPLE_CREDENTIAL_BULK_INGEST event and a person-credential fact per row written. Returns 200 with a per-record result, where a refused row carries CREDENTIAL_EMPLOYEE_UNKNOWN or CREDENTIAL_INGEST_REJECTED with the reason. 
     * Record People\'s Credentials in Bulk
     */
    async bulkIngestPersonCredentialsRaw(requestParameters: BulkIngestPersonCredentialsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkIngestResponse>> {
        if (requestParameters['bulkIngestRequestPersonCredentialBulkIngestRecord'] == null) {
            throw new runtime.RequiredError(
                'bulkIngestRequestPersonCredentialBulkIngestRecord',
                'Required parameter "bulkIngestRequestPersonCredentialBulkIngestRecord" was null or undefined when calling bulkIngestPersonCredentials().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        const response = await this.request({
            path: `/v1/people/credentials/bulk-ingest`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkIngestRequestPersonCredentialBulkIngestRecordToJSON(requestParameters['bulkIngestRequestPersonCredentialBulkIngestRecord']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkIngestResponseFromJSON(jsonValue));
    }

    /**
     * Records many credentials at once, resolving each row\'s employee number to a person and its vendor code (sourceCode + sourceCredentialCode, such as ASE T4-BRAKES) to a registry skill through the cross-reference, or taking a Durion skillCode such as DOT-INSPECTOR directly. Use this tool when loading an HR feed\'s certifications or seeding an environment; read the registry at GET /v1/people/skills instead of guessing codes. Preconditions: each employee number must resolve to a person with an employee record, and each vendor code must be in the cross-reference — an unknown code rejects its row rather than becoming a skill nobody holds. Required inputs: jobId, and per record employeeNumber, issuer, issuedOn and either skillCode or sourceCode + sourceCredentialCode; expiresOn, proficiency (1-5) and evidenceRef are optional, and expiresOn may not precede issuedOn. Semantics: upsert by natural key (person, skill, issuer, issuedOn), so a re-send updates the expiry, proficiency and evidence, a renewal with a later issuedOn is a new row, and status is derived from the dates rather than taken from the row. With supersedeAbsent=true, each person in the batch has their credentials from this job\'s sourceSystem that the batch no longer lists marked SUPERSEDED — never deleted. Emits a PEOPLE_CREDENTIAL_BULK_INGEST event and a person-credential fact per row written. Returns 200 with a per-record result, where a refused row carries CREDENTIAL_EMPLOYEE_UNKNOWN or CREDENTIAL_INGEST_REJECTED with the reason. 
     * Record People\'s Credentials in Bulk
     */
    async bulkIngestPersonCredentials(requestParameters: BulkIngestPersonCredentialsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkIngestResponse> {
        const response = await this.bulkIngestPersonCredentialsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
