/* tslint:disable */
/* eslint-disable */
/**
 * POS Human Resources Service API
 * Human Resources service for employee management, payroll, and benefits administration
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  CreateJobRoleRequest,
  JobRoleDto,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    CreateJobRoleRequestFromJSON,
    CreateJobRoleRequestToJSON,
    JobRoleDtoFromJSON,
    JobRoleDtoToJSON,
} from '../models/index';

export interface CreateJobRoleOperationRequest {
    createJobRoleRequest: CreateJobRoleRequest;
}

/**
 * 
 */
export class JobRoleAPIApi extends runtime.BaseAPI {

    /**
     * Adds a job role to the calling tenant\'s own list: a code, a display name and an optional description. HR master data only -- it carries no permissions and is never evaluated by security decisions. Use this tool to set up the tenant\'s job titles before assigning them to employees; do not use it to create an application role or a permission, which are pos-security-service concepts. Preconditions: the caller holds people:jobRole:manage; the code must not already be used by another job role in this tenant (another tenant may reuse the same code freely). Required inputs: code and name; description is optional. Emits a PEOPLE_JOB_ROLE_CREATE event; no other state changes. Returns 422 when the code is already in use within this tenant. 
     * Add A Job Role To The Tenant\'s List
     */
    async createJobRoleRaw(requestParameters: CreateJobRoleOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<JobRoleDto>> {
        if (requestParameters['createJobRoleRequest'] == null) {
            throw new runtime.RequiredError(
                'createJobRoleRequest',
                'Required parameter "createJobRoleRequest" was null or undefined when calling createJobRole().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people:jobRole:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/people/job-roles`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateJobRoleRequestToJSON(requestParameters['createJobRoleRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => JobRoleDtoFromJSON(jsonValue));
    }

    /**
     * Adds a job role to the calling tenant\'s own list: a code, a display name and an optional description. HR master data only -- it carries no permissions and is never evaluated by security decisions. Use this tool to set up the tenant\'s job titles before assigning them to employees; do not use it to create an application role or a permission, which are pos-security-service concepts. Preconditions: the caller holds people:jobRole:manage; the code must not already be used by another job role in this tenant (another tenant may reuse the same code freely). Required inputs: code and name; description is optional. Emits a PEOPLE_JOB_ROLE_CREATE event; no other state changes. Returns 422 when the code is already in use within this tenant. 
     * Add A Job Role To The Tenant\'s List
     */
    async createJobRole(requestParameters: CreateJobRoleOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<JobRoleDto> {
        const response = await this.createJobRoleRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every active job role on the calling tenant\'s own list, ordered by name. Use this tool to populate a job-role picker when creating or editing an employee, or to look up the id createJobRole needs; do not use it to look up application roles or permissions, which live in pos-security-service and are a different concept entirely. Preconditions: the caller holds people:jobRole:view. Required inputs: none; there are no parameters and no request body. No events are emitted beyond the audit read and no state changes; this is a read-only projection scoped to the caller\'s tenant. Returns 200 with the list, empty when the tenant has defined none, and 403 without the authority. 
     * List The Tenant\'s Job Roles
     */
    async listJobRolesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<JobRoleDto>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people:jobRole:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/people/job-roles`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(JobRoleDtoFromJSON));
    }

    /**
     * Returns every active job role on the calling tenant\'s own list, ordered by name. Use this tool to populate a job-role picker when creating or editing an employee, or to look up the id createJobRole needs; do not use it to look up application roles or permissions, which live in pos-security-service and are a different concept entirely. Preconditions: the caller holds people:jobRole:view. Required inputs: none; there are no parameters and no request body. No events are emitted beyond the audit read and no state changes; this is a read-only projection scoped to the caller\'s tenant. Returns 200 with the list, empty when the tenant has defined none, and 403 without the authority. 
     * List The Tenant\'s Job Roles
     */
    async listJobRoles(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<JobRoleDto>> {
        const response = await this.listJobRolesRaw(initOverrides);
        return await response.value();
    }

}
