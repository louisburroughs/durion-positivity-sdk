/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Price API
 * Price service for managing product pricing and discounts
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  BulkIngestRequestLaborRateAdjustmentBulkIngestRecord,
  BulkIngestResponse,
} from '../models/index';
import {
    BulkIngestRequestLaborRateAdjustmentBulkIngestRecordFromJSON,
    BulkIngestRequestLaborRateAdjustmentBulkIngestRecordToJSON,
    BulkIngestResponseFromJSON,
    BulkIngestResponseToJSON,
} from '../models/index';

export interface BulkIngestLaborRateAdjustmentsRequest {
    bulkIngestRequestLaborRateAdjustmentBulkIngestRecord: BulkIngestRequestLaborRateAdjustmentBulkIngestRecord;
}

/**
 * 
 */
export class LaborRateAdjustmentBulkIngestAPIApi extends runtime.BaseAPI {

    /**
     * Imports a batch of labor-matrix adjustment steps in one call, storing each whose scope, code and start instant are not already taken, with a per-row verdict. Use this tool to load a whole matrix at once, where the steps\' sequences relative to each other are the point; do not use createLaborRateAdjustment, which stores one step at a time and cannot show that ordering. Preconditions: a row whose location, category, adjustmentCode and effectiveFrom are already held is answered with the stored step unchanged, because a step that has priced an invoice is never rewritten. Required inputs: jobId (UUID), locationId (UUID) and a non-empty records array; per record, adjustmentCode, adjustmentType, adjustmentValue, sequence and effectiveFrom, with the step\'s own locationId, operationCategory, description and effectiveTo optional. Emits a PRICE_LABOR_RATE_ADJUSTMENT_BULK_INGEST event; percentage steps compound in sequence order, so a batch that renumbers them changes what the matrix charges. Returns 200 even when rows fail, so callers must inspect successCount, failureCount and the per-row results rather than the HTTP status. A row pricing refused carries errorCode LABOR_RATE_ADJUSTMENT_INGEST_FAILED and the reason; a row lost to a server-side fault carries INTERNAL_ERROR and a correlationId to quote. 
     * Bulk Import Labor Matrix Adjustments
     */
    async bulkIngestLaborRateAdjustmentsRaw(requestParameters: BulkIngestLaborRateAdjustmentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkIngestResponse>> {
        if (requestParameters['bulkIngestRequestLaborRateAdjustmentBulkIngestRecord'] == null) {
            throw new runtime.RequiredError(
                'bulkIngestRequestLaborRateAdjustmentBulkIngestRecord',
                'Required parameter "bulkIngestRequestLaborRateAdjustmentBulkIngestRecord" was null or undefined when calling bulkIngestLaborRateAdjustments().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "pricing:labor_rate:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/labor-rate-adjustments/bulk-ingest`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkIngestRequestLaborRateAdjustmentBulkIngestRecordToJSON(requestParameters['bulkIngestRequestLaborRateAdjustmentBulkIngestRecord']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkIngestResponseFromJSON(jsonValue));
    }

    /**
     * Imports a batch of labor-matrix adjustment steps in one call, storing each whose scope, code and start instant are not already taken, with a per-row verdict. Use this tool to load a whole matrix at once, where the steps\' sequences relative to each other are the point; do not use createLaborRateAdjustment, which stores one step at a time and cannot show that ordering. Preconditions: a row whose location, category, adjustmentCode and effectiveFrom are already held is answered with the stored step unchanged, because a step that has priced an invoice is never rewritten. Required inputs: jobId (UUID), locationId (UUID) and a non-empty records array; per record, adjustmentCode, adjustmentType, adjustmentValue, sequence and effectiveFrom, with the step\'s own locationId, operationCategory, description and effectiveTo optional. Emits a PRICE_LABOR_RATE_ADJUSTMENT_BULK_INGEST event; percentage steps compound in sequence order, so a batch that renumbers them changes what the matrix charges. Returns 200 even when rows fail, so callers must inspect successCount, failureCount and the per-row results rather than the HTTP status. A row pricing refused carries errorCode LABOR_RATE_ADJUSTMENT_INGEST_FAILED and the reason; a row lost to a server-side fault carries INTERNAL_ERROR and a correlationId to quote. 
     * Bulk Import Labor Matrix Adjustments
     */
    async bulkIngestLaborRateAdjustments(requestParameters: BulkIngestLaborRateAdjustmentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkIngestResponse> {
        const response = await this.bulkIngestLaborRateAdjustmentsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
