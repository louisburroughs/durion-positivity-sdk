/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Price API
 * Price service for managing product pricing and discounts
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  LaborRateAdjustmentRequest,
  LaborRateAdjustmentResponse,
  LaborRateRequest,
  LaborRateResponse,
} from '../models/index';
import {
    LaborRateAdjustmentRequestFromJSON,
    LaborRateAdjustmentRequestToJSON,
    LaborRateAdjustmentResponseFromJSON,
    LaborRateAdjustmentResponseToJSON,
    LaborRateRequestFromJSON,
    LaborRateRequestToJSON,
    LaborRateResponseFromJSON,
    LaborRateResponseToJSON,
} from '../models/index';

export interface CreateLaborRateRequest {
    laborRateRequest: LaborRateRequest;
}

export interface CreateLaborRateAdjustmentRequest {
    laborRateAdjustmentRequest: LaborRateAdjustmentRequest;
}

/**
 * 
 */
export class LaborRatesApi extends runtime.BaseAPI {

    /**
     * Stores an hourly labor rate for a scope and a time window: a location and an operation category, either of which may be omitted to widen the rate to every location or every category. Use this tool to set what an hour of shop labor costs; do not use it to set how long an operation takes, which is the pos-catalog labor standard. Preconditions: none beyond authorization. Rates are never edited in place — correcting one means closing its window and creating a new rate, so an invoice quoted at the old rate stays explainable. Required inputs: currency, hourlyRate greater than zero, and effectiveFrom; locationId, operationCategory and effectiveTo are optional. Emits a PRICE_LABOR_RATE_CREATE event. Returns 201 with the stored rate, 422 when a field is not storable as asked, and 409 when a rate already opens the same scope at the same instant. 
     * Create A Labor Rate
     */
    async createLaborRateRaw(requestParameters: CreateLaborRateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LaborRateResponse>> {
        if (requestParameters['laborRateRequest'] == null) {
            throw new runtime.RequiredError(
                'laborRateRequest',
                'Required parameter "laborRateRequest" was null or undefined when calling createLaborRate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["pricing:labor_rate:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/labor-rates`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LaborRateRequestToJSON(requestParameters['laborRateRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LaborRateResponseFromJSON(jsonValue));
    }

    /**
     * Stores an hourly labor rate for a scope and a time window: a location and an operation category, either of which may be omitted to widen the rate to every location or every category. Use this tool to set what an hour of shop labor costs; do not use it to set how long an operation takes, which is the pos-catalog labor standard. Preconditions: none beyond authorization. Rates are never edited in place — correcting one means closing its window and creating a new rate, so an invoice quoted at the old rate stays explainable. Required inputs: currency, hourlyRate greater than zero, and effectiveFrom; locationId, operationCategory and effectiveTo are optional. Emits a PRICE_LABOR_RATE_CREATE event. Returns 201 with the stored rate, 422 when a field is not storable as asked, and 409 when a rate already opens the same scope at the same instant. 
     * Create A Labor Rate
     */
    async createLaborRate(requestParameters: CreateLaborRateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LaborRateResponse> {
        const response = await this.createLaborRateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Stores one step of a shop\'s labor matrix: a percentage or flat adjustment that a quote opts into by naming its code, for conditions a guide time assumes away such as corrosion, restricted access, after-hours work or a fleet contract. Use this tool to configure what a shop charges beyond its base rate; do not use it to change the base rate itself, which is createLaborRate. Preconditions: none beyond authorization. Steps apply in sequence order and percentage steps compound, so the sequence changes the resulting rate and is part of the configuration, not a display hint. Required inputs: adjustmentCode, adjustmentType (PERCENT or FIXED), adjustmentValue, sequence and effectiveFrom; locationId, operationCategory, description and effectiveTo are optional. Emits a PRICE_LABOR_RATE_ADJUSTMENT_CREATE event. Returns 201 with the stored step, 422 when a field is not storable as asked, and 409 when the same code already opens the same scope at the same instant. 
     * Create A Labor Matrix Step
     */
    async createLaborRateAdjustmentRaw(requestParameters: CreateLaborRateAdjustmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LaborRateAdjustmentResponse>> {
        if (requestParameters['laborRateAdjustmentRequest'] == null) {
            throw new runtime.RequiredError(
                'laborRateAdjustmentRequest',
                'Required parameter "laborRateAdjustmentRequest" was null or undefined when calling createLaborRateAdjustment().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["pricing:labor_rate:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/labor-rates/adjustments`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LaborRateAdjustmentRequestToJSON(requestParameters['laborRateAdjustmentRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LaborRateAdjustmentResponseFromJSON(jsonValue));
    }

    /**
     * Stores one step of a shop\'s labor matrix: a percentage or flat adjustment that a quote opts into by naming its code, for conditions a guide time assumes away such as corrosion, restricted access, after-hours work or a fleet contract. Use this tool to configure what a shop charges beyond its base rate; do not use it to change the base rate itself, which is createLaborRate. Preconditions: none beyond authorization. Steps apply in sequence order and percentage steps compound, so the sequence changes the resulting rate and is part of the configuration, not a display hint. Required inputs: adjustmentCode, adjustmentType (PERCENT or FIXED), adjustmentValue, sequence and effectiveFrom; locationId, operationCategory, description and effectiveTo are optional. Emits a PRICE_LABOR_RATE_ADJUSTMENT_CREATE event. Returns 201 with the stored step, 422 when a field is not storable as asked, and 409 when the same code already opens the same scope at the same instant. 
     * Create A Labor Matrix Step
     */
    async createLaborRateAdjustment(requestParameters: CreateLaborRateAdjustmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LaborRateAdjustmentResponse> {
        const response = await this.createLaborRateAdjustmentRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists every stored labor-matrix step in application order, including windows that have closed. Use this tool to see which conditions a shop prices and by how much; do not use it to find which steps apply to one job, which is resolveLaborRate — that applies the caller\'s opted-in codes and returns the resulting rate. Preconditions: none beyond authorization. Required inputs: none. Emits a PRICE_LABOR_RATE_ADJUSTMENT_LIST event; no state changes. Returns 200 with the steps, and an empty list when none are stored. 
     * List Labor Matrix Steps
     */
    async listLaborRateAdjustmentsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<LaborRateAdjustmentResponse>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["pricing:labor_rate:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/labor-rates/adjustments`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(LaborRateAdjustmentResponseFromJSON));
    }

    /**
     * Lists every stored labor-matrix step in application order, including windows that have closed. Use this tool to see which conditions a shop prices and by how much; do not use it to find which steps apply to one job, which is resolveLaborRate — that applies the caller\'s opted-in codes and returns the resulting rate. Preconditions: none beyond authorization. Required inputs: none. Emits a PRICE_LABOR_RATE_ADJUSTMENT_LIST event; no state changes. Returns 200 with the steps, and an empty list when none are stored. 
     * List Labor Matrix Steps
     */
    async listLaborRateAdjustments(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<LaborRateAdjustmentResponse>> {
        const response = await this.listLaborRateAdjustmentsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Lists every stored labor rate, newest effective window first, including windows that have closed. Use this tool to see what a location charges and when each rate took effect; do not use it to price a specific job, which is resolveLaborRate — this returns the whole table, not the one applicable rate. Preconditions: none beyond authorization. Required inputs: none. Emits a PRICE_LABOR_RATE_LIST event; no state changes. Returns 200 with the rates, and an empty list when none are stored. 
     * List Labor Rates
     */
    async listLaborRatesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<LaborRateResponse>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["pricing:labor_rate:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/labor-rates`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(LaborRateResponseFromJSON));
    }

    /**
     * Lists every stored labor rate, newest effective window first, including windows that have closed. Use this tool to see what a location charges and when each rate took effect; do not use it to price a specific job, which is resolveLaborRate — this returns the whole table, not the one applicable rate. Preconditions: none beyond authorization. Required inputs: none. Emits a PRICE_LABOR_RATE_LIST event; no state changes. Returns 200 with the rates, and an empty list when none are stored. 
     * List Labor Rates
     */
    async listLaborRates(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<LaborRateResponse>> {
        const response = await this.listLaborRatesRaw(initOverrides);
        return await response.value();
    }

}
