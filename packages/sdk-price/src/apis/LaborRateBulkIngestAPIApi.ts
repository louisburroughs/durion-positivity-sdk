/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Price API
 * Price service for managing product pricing and discounts
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  BulkIngestRequestLaborRateBulkIngestRecord,
  BulkIngestResponse,
} from '../models/index';
import {
    BulkIngestRequestLaborRateBulkIngestRecordFromJSON,
    BulkIngestRequestLaborRateBulkIngestRecordToJSON,
    BulkIngestResponseFromJSON,
    BulkIngestResponseToJSON,
} from '../models/index';

export interface BulkIngestLaborRatesRequest {
    bulkIngestRequestLaborRateBulkIngestRecord: BulkIngestRequestLaborRateBulkIngestRecord;
}

/**
 * 
 */
export class LaborRateBulkIngestAPIApi extends runtime.BaseAPI {

    /**
     * Imports a batch of hourly labor rates in one call, storing each whose scope and start instant are not already taken, with a per-row success or failure verdict. Use this tool to load a rate card across several locations and categories; do not use createLaborRate, which stores one rate at a time and reports its errors as HTTP statuses rather than row results. Preconditions: a row whose location, category and effectiveFrom are already held is answered with the stored row unchanged, because a rate that has priced an invoice is never rewritten. Required inputs: jobId (UUID), locationId (UUID) and a non-empty records array; per record, currency, hourlyRate and effectiveFrom, with the rate\'s own locationId, operationCategory and effectiveTo optional. Emits a PRICE_LABOR_RATE_BULK_INGEST event; a changed rate is a new row in a new window rather than an edit of the old one. Returns 200 even when rows fail, so callers must inspect successCount, failureCount and the per-row results rather than the HTTP status. A row pricing refused carries errorCode LABOR_RATE_INGEST_FAILED and the reason; a row lost to a server-side fault carries INTERNAL_ERROR and a correlationId to quote. 
     * Bulk Import Labor Rates
     */
    async bulkIngestLaborRatesRaw(requestParameters: BulkIngestLaborRatesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkIngestResponse>> {
        if (requestParameters['bulkIngestRequestLaborRateBulkIngestRecord'] == null) {
            throw new runtime.RequiredError(
                'bulkIngestRequestLaborRateBulkIngestRecord',
                'Required parameter "bulkIngestRequestLaborRateBulkIngestRecord" was null or undefined when calling bulkIngestLaborRates().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "pricing:labor_rate:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/labor-rates/bulk-ingest`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkIngestRequestLaborRateBulkIngestRecordToJSON(requestParameters['bulkIngestRequestLaborRateBulkIngestRecord']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkIngestResponseFromJSON(jsonValue));
    }

    /**
     * Imports a batch of hourly labor rates in one call, storing each whose scope and start instant are not already taken, with a per-row success or failure verdict. Use this tool to load a rate card across several locations and categories; do not use createLaborRate, which stores one rate at a time and reports its errors as HTTP statuses rather than row results. Preconditions: a row whose location, category and effectiveFrom are already held is answered with the stored row unchanged, because a rate that has priced an invoice is never rewritten. Required inputs: jobId (UUID), locationId (UUID) and a non-empty records array; per record, currency, hourlyRate and effectiveFrom, with the rate\'s own locationId, operationCategory and effectiveTo optional. Emits a PRICE_LABOR_RATE_BULK_INGEST event; a changed rate is a new row in a new window rather than an edit of the old one. Returns 200 even when rows fail, so callers must inspect successCount, failureCount and the per-row results rather than the HTTP status. A row pricing refused carries errorCode LABOR_RATE_INGEST_FAILED and the reason; a row lost to a server-side fault carries INTERNAL_ERROR and a correlationId to quote. 
     * Bulk Import Labor Rates
     */
    async bulkIngestLaborRates(requestParameters: BulkIngestLaborRatesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkIngestResponse> {
        const response = await this.bulkIngestLaborRatesRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
