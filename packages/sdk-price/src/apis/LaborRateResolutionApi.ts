/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Price API
 * Price service for managing product pricing and discounts
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  LaborRateQuoteRequest,
  LaborRateQuoteResponse,
} from '../models/index';
import {
    LaborRateQuoteRequestFromJSON,
    LaborRateQuoteRequestToJSON,
    LaborRateQuoteResponseFromJSON,
    LaborRateQuoteResponseToJSON,
} from '../models/index';

export interface ResolveLaborRateRequest {
    laborRateQuoteRequest: LaborRateQuoteRequest;
}

/**
 * 
 */
export class LaborRateResolutionApi extends runtime.BaseAPI {

    /**
     * Answers the one hourly labor rate in force for a location and operation category at a moment, then applies the labor-matrix steps the caller opted into and itemises each one with the rate it produced. Use this tool from the approved pos-workorder client to price a LABOR estimate line, paired with the catalog labor time that says how many hours; do not use it to browse what rates exist, which is listLaborRates. Preconditions: none. Scope narrows from the location\'s rate for the category through the location default and the platform category rate to the platform default; an unknown category or an adjustment code with no in-force step widens or is skipped rather than failing. Required inputs: a body, which may be empty — every field is optional, and omitting locationId resolves the platform default rate. Emits a PRICE_LABOR_RATE_QUOTE event; no state changes. Returns 200 always for a well-formed request — no rate in force is the typed NO_RATE_AVAILABLE status, never an error, and callers degrade to a blank price. 
     * Resolve The Labor Rate For A Job
     */
    async resolveLaborRateRaw(requestParameters: ResolveLaborRateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LaborRateQuoteResponse>> {
        if (requestParameters['laborRateQuoteRequest'] == null) {
            throw new runtime.RequiredError(
                'laborRateQuoteRequest',
                'Required parameter "laborRateQuoteRequest" was null or undefined when calling resolveLaborRate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "pricing:labor_rate:quote"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/labor-rates/quote`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LaborRateQuoteRequestToJSON(requestParameters['laborRateQuoteRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LaborRateQuoteResponseFromJSON(jsonValue));
    }

    /**
     * Answers the one hourly labor rate in force for a location and operation category at a moment, then applies the labor-matrix steps the caller opted into and itemises each one with the rate it produced. Use this tool from the approved pos-workorder client to price a LABOR estimate line, paired with the catalog labor time that says how many hours; do not use it to browse what rates exist, which is listLaborRates. Preconditions: none. Scope narrows from the location\'s rate for the category through the location default and the platform category rate to the platform default; an unknown category or an adjustment code with no in-force step widens or is skipped rather than failing. Required inputs: a body, which may be empty — every field is optional, and omitting locationId resolves the platform default rate. Emits a PRICE_LABOR_RATE_QUOTE event; no state changes. Returns 200 always for a well-formed request — no rate in force is the typed NO_RATE_AVAILABLE status, never an error, and callers degrade to a blank price. 
     * Resolve The Labor Rate For A Job
     */
    async resolveLaborRate(requestParameters: ResolveLaborRateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LaborRateQuoteResponse> {
        const response = await this.resolveLaborRateRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
