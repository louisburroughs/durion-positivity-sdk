/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Documents API
 * API documentation for document rendering and format conversion
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  RenderRequest,
} from '../models/index';
import {
    RenderRequestFromJSON,
    RenderRequestToJSON,
} from '../models/index';

export interface RenderDocumentRequest {
    renderRequest: RenderRequest;
}

/**
 * 
 */
export class DocumentRenderAPIApi extends runtime.BaseAPI {

    /**
     * Renders the supplied source content into a PDF document and returns the PDF bytes inline. Use this tool when a printable document is needed from structured content; do not use it to store or retrieve documents, because nothing is persisted and every call re-renders from the request alone. Preconditions: none beyond an authenticated caller with documents:render; when templateId is supplied the named template must exist. Required inputs: format (JSON, XML, MARKDOWN, TEXT or CSV, describing the source content) and content, the raw text to render; templateId is optional and applies the named layout. Emits a DOCUMENT_RENDER event; no document record is created and the output exists only in the response body. Returns 400 when the format is missing or unsupported, 404 when the named template does not exist, 422 when the content is blank or exceeds the maximum input size, and 500 when PDF rendering itself fails. 
     * Render document to PDF
     */
    async renderDocumentRaw(requestParameters: RenderDocumentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Blob>> {
        if (requestParameters['renderRequest'] == null) {
            throw new runtime.RequiredError(
                'renderRequest',
                'Required parameter "renderRequest" was null or undefined when calling renderDocument().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["documents:render"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/documents/render`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RenderRequestToJSON(requestParameters['renderRequest']),
        }, initOverrides);

        return new runtime.BlobApiResponse(response);
    }

    /**
     * Renders the supplied source content into a PDF document and returns the PDF bytes inline. Use this tool when a printable document is needed from structured content; do not use it to store or retrieve documents, because nothing is persisted and every call re-renders from the request alone. Preconditions: none beyond an authenticated caller with documents:render; when templateId is supplied the named template must exist. Required inputs: format (JSON, XML, MARKDOWN, TEXT or CSV, describing the source content) and content, the raw text to render; templateId is optional and applies the named layout. Emits a DOCUMENT_RENDER event; no document record is created and the output exists only in the response body. Returns 400 when the format is missing or unsupported, 404 when the named template does not exist, 422 when the content is blank or exceeds the maximum input size, and 500 when PDF rendering itself fails. 
     * Render document to PDF
     */
    async renderDocument(requestParameters: RenderDocumentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Blob> {
        const response = await this.renderDocumentRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
