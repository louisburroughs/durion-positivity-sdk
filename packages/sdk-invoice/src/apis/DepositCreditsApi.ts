/* tslint:disable */
/* eslint-disable */
/**
 * POS Invoice API
 * API documentation for invoice and billing rules management
 *
 * The version of the OpenAPI document: v1
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CreateDepositRequest,
  DepositCreditResponse,
} from '../models/index';
import {
    CreateDepositRequestFromJSON,
    CreateDepositRequestToJSON,
    DepositCreditResponseFromJSON,
    DepositCreditResponseToJSON,
} from '../models/index';

export interface CreateDepositCreditRequest {
    createDepositRequest: CreateDepositRequest;
}

export interface GetDepositCreditRequest {
    depositCreditId: string;
}

export interface ListDepositCreditsBySourceRequest {
    sourceType: string;
    sourceId: string;
}

export interface RefundDepositCreditRequest {
    depositCreditId: string;
    reason?: string;
}

/**
 * 
 */
export class DepositCreditsApi extends runtime.BaseAPI {

    /**
     * Creates an AVAILABLE deposit credit for a down-payment taken at pos-order checkout, holding the amount against its source document until a settlement invoice draws it down. Use this tool when a deposit was tendered against an estimate, workorder, or order; do not use refundDepositCredit, which returns a credit\'s remaining balance when the source is cancelled. Preconditions: none beyond a priced source document — the call is idempotent on orderId, so a replay for an order that already registered a credit returns the existing record unchanged. Required inputs: orderId (UUID, idempotency anchor), sourceType (ESTIMATE, WORKORDER or ORDER), sourceId (UUID) and amount (positive); partyId is optional and currencyCode defaults to USD. Emits an INVOICE_DEPOSIT_CREATE event; no invoice records are touched until settlement applies the credit. Returns 201 with the credit (existing or new), and 422 when the amount is not positive or the sourceType is not a known value. 
     * Register a Deposit Credit
     */
    async createDepositCreditRaw(requestParameters: CreateDepositCreditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DepositCreditResponse>> {
        if (requestParameters['createDepositRequest'] == null) {
            throw new runtime.RequiredError(
                'createDepositRequest',
                'Required parameter "createDepositRequest" was null or undefined when calling createDepositCredit().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/invoices/deposits`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateDepositRequestToJSON(requestParameters['createDepositRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DepositCreditResponseFromJSON(jsonValue));
    }

    /**
     * Creates an AVAILABLE deposit credit for a down-payment taken at pos-order checkout, holding the amount against its source document until a settlement invoice draws it down. Use this tool when a deposit was tendered against an estimate, workorder, or order; do not use refundDepositCredit, which returns a credit\'s remaining balance when the source is cancelled. Preconditions: none beyond a priced source document — the call is idempotent on orderId, so a replay for an order that already registered a credit returns the existing record unchanged. Required inputs: orderId (UUID, idempotency anchor), sourceType (ESTIMATE, WORKORDER or ORDER), sourceId (UUID) and amount (positive); partyId is optional and currencyCode defaults to USD. Emits an INVOICE_DEPOSIT_CREATE event; no invoice records are touched until settlement applies the credit. Returns 201 with the credit (existing or new), and 422 when the amount is not positive or the sourceType is not a known value. 
     * Register a Deposit Credit
     */
    async createDepositCredit(requestParameters: CreateDepositCreditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DepositCreditResponse> {
        const response = await this.createDepositCreditRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns a single deposit credit with its status (AVAILABLE, PARTIALLY_APPLIED, FULLY_APPLIED or REFUNDED), original amount and remaining balance. Use this tool when the depositCreditId is already known; use listDepositCreditsBySource instead to find the credits held against an estimate, workorder or order. Preconditions: the deposit credit must exist. Required inputs: depositCreditId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no deposit credit exists for the supplied id. 
     * Get a Deposit Credit
     */
    async getDepositCreditRaw(requestParameters: GetDepositCreditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DepositCreditResponse>> {
        if (requestParameters['depositCreditId'] == null) {
            throw new runtime.RequiredError(
                'depositCreditId',
                'Required parameter "depositCreditId" was null or undefined when calling getDepositCredit().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/invoices/deposits/{depositCreditId}`.replace(`{${"depositCreditId"}}`, encodeURIComponent(String(requestParameters['depositCreditId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DepositCreditResponseFromJSON(jsonValue));
    }

    /**
     * Returns a single deposit credit with its status (AVAILABLE, PARTIALLY_APPLIED, FULLY_APPLIED or REFUNDED), original amount and remaining balance. Use this tool when the depositCreditId is already known; use listDepositCreditsBySource instead to find the credits held against an estimate, workorder or order. Preconditions: the deposit credit must exist. Required inputs: depositCreditId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no deposit credit exists for the supplied id. 
     * Get a Deposit Credit
     */
    async getDepositCredit(requestParameters: GetDepositCreditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DepositCreditResponse> {
        const response = await this.getDepositCreditRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists every deposit credit held against one source document, identified by its type and id. Use this tool when checking what down-payments a settlement can draw on; use getDepositCredit instead when a specific depositCreditId is already known. Preconditions: none — an unknown or credit-free source returns an empty list rather than an error. Required inputs: sourceType query parameter (ESTIMATE, WORKORDER or ORDER, case-insensitive) and sourceId (UUID) query parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 422 when sourceType is not one of the known values. 
     * List Deposit Credits by Source
     */
    async listDepositCreditsBySourceRaw(requestParameters: ListDepositCreditsBySourceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<DepositCreditResponse>>> {
        if (requestParameters['sourceType'] == null) {
            throw new runtime.RequiredError(
                'sourceType',
                'Required parameter "sourceType" was null or undefined when calling listDepositCreditsBySource().'
            );
        }

        if (requestParameters['sourceId'] == null) {
            throw new runtime.RequiredError(
                'sourceId',
                'Required parameter "sourceId" was null or undefined when calling listDepositCreditsBySource().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['sourceType'] != null) {
            queryParameters['sourceType'] = requestParameters['sourceType'];
        }

        if (requestParameters['sourceId'] != null) {
            queryParameters['sourceId'] = requestParameters['sourceId'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/invoices/deposits`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(DepositCreditResponseFromJSON));
    }

    /**
     * Lists every deposit credit held against one source document, identified by its type and id. Use this tool when checking what down-payments a settlement can draw on; use getDepositCredit instead when a specific depositCreditId is already known. Preconditions: none — an unknown or credit-free source returns an empty list rather than an error. Required inputs: sourceType query parameter (ESTIMATE, WORKORDER or ORDER, case-insensitive) and sourceId (UUID) query parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 422 when sourceType is not one of the known values. 
     * List Deposit Credits by Source
     */
    async listDepositCreditsBySource(requestParameters: ListDepositCreditsBySourceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<DepositCreditResponse>> {
        const response = await this.listDepositCreditsBySourceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Zeroes the remaining balance of a deposit credit and marks it REFUNDED, recording that the deposit is returned because its source document was cancelled after the deposit was taken. Use this tool on source cancellation (spec R7.4); do not use createDepositCredit, which registers a new deposit, and note the cash disbursement itself is not performed here. Preconditions: the deposit credit must exist; a credit already REFUNDED is left unchanged, so the call is idempotent. Required inputs: depositCreditId (UUID) as a path parameter; the reason query parameter is optional and defaults to \"source cancelled\"; there is no request body. Emits an INVOICE_DEPOSIT_REFUND event and sets the credit\'s status to REFUNDED with a zero remaining balance. Returns 200 with the refunded credit, and 404 when no deposit credit exists for the supplied id. 
     * Refund a Deposit Credit Balance
     */
    async refundDepositCreditRaw(requestParameters: RefundDepositCreditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DepositCreditResponse>> {
        if (requestParameters['depositCreditId'] == null) {
            throw new runtime.RequiredError(
                'depositCreditId',
                'Required parameter "depositCreditId" was null or undefined when calling refundDepositCredit().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['reason'] != null) {
            queryParameters['reason'] = requestParameters['reason'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/invoices/deposits/{depositCreditId}/refund`.replace(`{${"depositCreditId"}}`, encodeURIComponent(String(requestParameters['depositCreditId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DepositCreditResponseFromJSON(jsonValue));
    }

    /**
     * Zeroes the remaining balance of a deposit credit and marks it REFUNDED, recording that the deposit is returned because its source document was cancelled after the deposit was taken. Use this tool on source cancellation (spec R7.4); do not use createDepositCredit, which registers a new deposit, and note the cash disbursement itself is not performed here. Preconditions: the deposit credit must exist; a credit already REFUNDED is left unchanged, so the call is idempotent. Required inputs: depositCreditId (UUID) as a path parameter; the reason query parameter is optional and defaults to \"source cancelled\"; there is no request body. Emits an INVOICE_DEPOSIT_REFUND event and sets the credit\'s status to REFUNDED with a zero remaining balance. Returns 200 with the refunded credit, and 404 when no deposit credit exists for the supplied id. 
     * Refund a Deposit Credit Balance
     */
    async refundDepositCredit(requestParameters: RefundDepositCreditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DepositCreditResponse> {
        const response = await this.refundDepositCreditRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
