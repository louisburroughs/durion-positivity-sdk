/* tslint:disable */
/* eslint-disable */
/**
 * POS Invoice API
 * API documentation for invoice and billing rules management
 *
 * The version of the OpenAPI document: v1
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  PartyStandaloneRefundRequest,
  RefundPaymentResponse,
  StandaloneRefundRequest,
} from '../models/index';
import {
    PartyStandaloneRefundRequestFromJSON,
    PartyStandaloneRefundRequestToJSON,
    RefundPaymentResponseFromJSON,
    RefundPaymentResponseToJSON,
    StandaloneRefundRequestFromJSON,
    StandaloneRefundRequestToJSON,
} from '../models/index';

export interface CreateStandaloneInvoiceRefundRequest {
    invoiceId: string;
    standaloneRefundRequest: StandaloneRefundRequest;
}

export interface CreateStandalonePartyRefundRequest {
    partyStandaloneRefundRequest: PartyStandaloneRefundRequest;
}

/**
 * 
 */
export class StandaloneRefundApi extends runtime.BaseAPI {

    /**
     * Records a refund against an invoice whose original payment is not in this system — predecessor-system sales, pre-deploy invoices, vendor-paid warranty work — while the cash disbursement itself happens out of band (till, check, or vendor payment). Use this tool only when no captured payment intent exists to refund; do not use refundPayment, which reverses a CAPTURED gateway payment, and use createStandalonePartyRefund instead when no invoice is on file at all. Preconditions: the invoice must exist, the caller needs the finance-only ISSUE_MANUAL_REFUND authority, and cumulative refunds across payment-anchored and standalone records may not exceed the invoice total. Required inputs: amount (positive) and reason (a RefundReason such as CUSTOMER_RETURN); notes and externalReference are optional, and a retry replaying the same externalReference returns the existing record instead of double-recording. Emits an INVOICE_STANDALONE_REFUND event and stores a COMPLETED refund record anchored to the invoice; no gateway call is made. Returns 201 with the refund record, 404 when the invoice does not exist, and 422 when the amount exceeds the invoice\'s remaining refundable balance. 
     * Record Standalone Invoice Refund
     */
    async createStandaloneInvoiceRefundRaw(requestParameters: CreateStandaloneInvoiceRefundRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RefundPaymentResponse>> {
        if (requestParameters['invoiceId'] == null) {
            throw new runtime.RequiredError(
                'invoiceId',
                'Required parameter "invoiceId" was null or undefined when calling createStandaloneInvoiceRefund().'
            );
        }

        if (requestParameters['standaloneRefundRequest'] == null) {
            throw new runtime.RequiredError(
                'standaloneRefundRequest',
                'Required parameter "standaloneRefundRequest" was null or undefined when calling createStandaloneInvoiceRefund().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/invoices/{invoiceId}/refunds`.replace(`{${"invoiceId"}}`, encodeURIComponent(String(requestParameters['invoiceId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: StandaloneRefundRequestToJSON(requestParameters['standaloneRefundRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RefundPaymentResponseFromJSON(jsonValue));
    }

    /**
     * Records a refund against an invoice whose original payment is not in this system — predecessor-system sales, pre-deploy invoices, vendor-paid warranty work — while the cash disbursement itself happens out of band (till, check, or vendor payment). Use this tool only when no captured payment intent exists to refund; do not use refundPayment, which reverses a CAPTURED gateway payment, and use createStandalonePartyRefund instead when no invoice is on file at all. Preconditions: the invoice must exist, the caller needs the finance-only ISSUE_MANUAL_REFUND authority, and cumulative refunds across payment-anchored and standalone records may not exceed the invoice total. Required inputs: amount (positive) and reason (a RefundReason such as CUSTOMER_RETURN); notes and externalReference are optional, and a retry replaying the same externalReference returns the existing record instead of double-recording. Emits an INVOICE_STANDALONE_REFUND event and stores a COMPLETED refund record anchored to the invoice; no gateway call is made. Returns 201 with the refund record, 404 when the invoice does not exist, and 422 when the amount exceeds the invoice\'s remaining refundable balance. 
     * Record Standalone Invoice Refund
     */
    async createStandaloneInvoiceRefund(requestParameters: CreateStandaloneInvoiceRefundRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RefundPaymentResponse> {
        const response = await this.createStandaloneInvoiceRefundRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Records a refund anchored directly to a customer party when no invoice exists in this system, such as a vendor-paid warranty settlement on a predecessor-system sale; the cash disbursement itself happens out of band. Use this tool only when there is no invoice to anchor to; use createStandaloneInvoiceRefund instead when an invoice is on file, and refundPayment when the captured payment itself exists. Preconditions: the caller needs the finance-only ISSUE_MANUAL_REFUND authority; because there is no invoice, no refundable-amount cap applies. Required inputs: partyId (non-blank, max 64 characters), amount (positive) and reason (a RefundReason); notes and externalReference are optional, and a retry replaying the same externalReference among the party\'s purely party-anchored records returns the existing record. Emits an INVOICE_PARTY_STANDALONE_REFUND event and stores a COMPLETED refund record anchored to the party; no gateway call is made. Returns 201 with the refund record, and 400 when partyId is blank. 
     * Record Standalone Party Refund
     */
    async createStandalonePartyRefundRaw(requestParameters: CreateStandalonePartyRefundRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RefundPaymentResponse>> {
        if (requestParameters['partyStandaloneRefundRequest'] == null) {
            throw new runtime.RequiredError(
                'partyStandaloneRefundRequest',
                'Required parameter "partyStandaloneRefundRequest" was null or undefined when calling createStandalonePartyRefund().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/refunds`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PartyStandaloneRefundRequestToJSON(requestParameters['partyStandaloneRefundRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RefundPaymentResponseFromJSON(jsonValue));
    }

    /**
     * Records a refund anchored directly to a customer party when no invoice exists in this system, such as a vendor-paid warranty settlement on a predecessor-system sale; the cash disbursement itself happens out of band. Use this tool only when there is no invoice to anchor to; use createStandaloneInvoiceRefund instead when an invoice is on file, and refundPayment when the captured payment itself exists. Preconditions: the caller needs the finance-only ISSUE_MANUAL_REFUND authority; because there is no invoice, no refundable-amount cap applies. Required inputs: partyId (non-blank, max 64 characters), amount (positive) and reason (a RefundReason); notes and externalReference are optional, and a retry replaying the same externalReference among the party\'s purely party-anchored records returns the existing record. Emits an INVOICE_PARTY_STANDALONE_REFUND event and stores a COMPLETED refund record anchored to the party; no gateway call is made. Returns 201 with the refund record, and 400 when partyId is blank. 
     * Record Standalone Party Refund
     */
    async createStandalonePartyRefund(requestParameters: CreateStandalonePartyRefundRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RefundPaymentResponse> {
        const response = await this.createStandalonePartyRefundRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
