/* tslint:disable */
/* eslint-disable */
/**
 * POS Invoice API
 * API documentation for invoice and billing rules management
 *
 * The version of the OpenAPI document: v1
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  InvoiceLineSearchResult,
  PageInvoiceSearchResult,
  Pageable,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    InvoiceLineSearchResultFromJSON,
    InvoiceLineSearchResultToJSON,
    PageInvoiceSearchResultFromJSON,
    PageInvoiceSearchResultToJSON,
    PageableFromJSON,
    PageableToJSON,
} from '../models/index';

export interface SearchInvoiceLinesRequest {
    partyId: string;
    q?: string;
}

export interface SearchInvoicesRequest {
    pageable: Pageable;
    q?: string;
}

/**
 * 
 */
export class InvoiceSearchApi extends runtime.BaseAPI {

    /**
     * Returns invoice line items belonging to one customer party, flattened with the owning invoice\'s number, status and creation time, newest invoice first and bounded to the newest 200 lines; built for warranty-claim origin-line correlation. Use this tool to find the invoice line a warranty claim originates from; use searchInvoices instead for invoice-level free-text search. Preconditions: none — an unknown party simply returns an empty list. Required inputs: partyId (UUID) query parameter; q is an optional SKU or description term narrowing the lines. Emits an INVOICE_ITEM_SEARCH audit event; no state changes — this is a read-only projection. Returns 400 when partyId is missing or malformed. 
     * Search Invoice Lines by Party
     */
    async searchInvoiceLinesRaw(requestParameters: SearchInvoiceLinesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<InvoiceLineSearchResult>>> {
        if (requestParameters['partyId'] == null) {
            throw new runtime.RequiredError(
                'partyId',
                'Required parameter "partyId" was null or undefined when calling searchInvoiceLines().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['partyId'] != null) {
            queryParameters['partyId'] = requestParameters['partyId'];
        }

        if (requestParameters['q'] != null) {
            queryParameters['q'] = requestParameters['q'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["invoice:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/invoices/items/search`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(InvoiceLineSearchResultFromJSON));
    }

    /**
     * Returns invoice line items belonging to one customer party, flattened with the owning invoice\'s number, status and creation time, newest invoice first and bounded to the newest 200 lines; built for warranty-claim origin-line correlation. Use this tool to find the invoice line a warranty claim originates from; use searchInvoices instead for invoice-level free-text search. Preconditions: none — an unknown party simply returns an empty list. Required inputs: partyId (UUID) query parameter; q is an optional SKU or description term narrowing the lines. Emits an INVOICE_ITEM_SEARCH audit event; no state changes — this is a read-only projection. Returns 400 when partyId is missing or malformed. 
     * Search Invoice Lines by Party
     */
    async searchInvoiceLines(requestParameters: SearchInvoiceLinesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<InvoiceLineSearchResult>> {
        const response = await this.searchInvoiceLinesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Searches invoices by a free-text term matched against the invoice number, the customer name (resolved via the customer service), or the workorder number, returning a page of finder rows. Use this tool to locate an invoice when its id is unknown; use getInvoice instead once the invoiceId is known, and searchInvoiceLines for line-level warranty correlation. Preconditions: none — but a blank or missing q short-circuits to an empty page rather than listing all invoices. Required inputs: q (free-text term) plus optional page, size and sort parameters; size defaults to 25, is hard-capped at 50, and the default sort is createdAt descending. Emits an INVOICE_SEARCH audit event; no state changes — this is a read-only projection. Returns 400 when pagination parameters are malformed. 
     * Search Invoices by Free Text
     */
    async searchInvoicesRaw(requestParameters: SearchInvoicesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PageInvoiceSearchResult>> {
        if (requestParameters['pageable'] == null) {
            throw new runtime.RequiredError(
                'pageable',
                'Required parameter "pageable" was null or undefined when calling searchInvoices().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['q'] != null) {
            queryParameters['q'] = requestParameters['q'];
        }

        if (requestParameters['pageable'] != null) {
            queryParameters['pageable'] = requestParameters['pageable'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["invoice:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/invoices/search`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PageInvoiceSearchResultFromJSON(jsonValue));
    }

    /**
     * Searches invoices by a free-text term matched against the invoice number, the customer name (resolved via the customer service), or the workorder number, returning a page of finder rows. Use this tool to locate an invoice when its id is unknown; use getInvoice instead once the invoiceId is known, and searchInvoiceLines for line-level warranty correlation. Preconditions: none — but a blank or missing q short-circuits to an empty page rather than listing all invoices. Required inputs: q (free-text term) plus optional page, size and sort parameters; size defaults to 25, is hard-capped at 50, and the default sort is createdAt descending. Emits an INVOICE_SEARCH audit event; no state changes — this is a read-only projection. Returns 400 when pagination parameters are malformed. 
     * Search Invoices by Free Text
     */
    async searchInvoices(requestParameters: SearchInvoicesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PageInvoiceSearchResult> {
        const response = await this.searchInvoicesRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
