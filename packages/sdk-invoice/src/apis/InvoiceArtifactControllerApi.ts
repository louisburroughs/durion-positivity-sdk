/* tslint:disable */
/* eslint-disable */
/**
 * POS Invoice API
 * API documentation for invoice and billing rules management
 *
 * The version of the OpenAPI document: v1
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ArtifactDownloadToken,
  InvoiceArtifact,
} from '../models/index';
import {
    ArtifactDownloadTokenFromJSON,
    ArtifactDownloadTokenToJSON,
    InvoiceArtifactFromJSON,
    InvoiceArtifactToJSON,
} from '../models/index';

export interface CreateArtifactDownloadTokenRequest {
    invoiceId: string;
    artifactRefId: string;
}

export interface ListInvoiceArtifactsRequest {
    invoiceId: string;
}

/**
 * 
 */
export class InvoiceArtifactControllerApi extends runtime.BaseAPI {

    /**
     * Issues a short-lived signed token bound to one invoice artifact, so a browser can fetch the PDF through the public download link without an Authorization header. Use this tool after listInvoiceArtifacts has supplied an artifactRefId; do not use downloadInvoiceArtifact without a token — it rejects unsigned requests. Preconditions: the invoice must exist and the artifact reference must belong to that invoice (an invoice ref must match the path invoice, a receipt ref must be one of its receipts). Required inputs: invoiceId (UUID) and artifactRefId (opaque reference from listInvoiceArtifacts) as path parameters; there is no request body. No events are emitted and no record is stored; the token is stateless, signed, and expires after 300 seconds by default (invoice.artifacts.token-ttl-seconds). Returns 404 when the invoice does not exist or the artifact does not belong to it. 
     * Create Artifact Download Token
     */
    async createArtifactDownloadTokenRaw(requestParameters: CreateArtifactDownloadTokenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ArtifactDownloadToken>> {
        if (requestParameters['invoiceId'] == null) {
            throw new runtime.RequiredError(
                'invoiceId',
                'Required parameter "invoiceId" was null or undefined when calling createArtifactDownloadToken().'
            );
        }

        if (requestParameters['artifactRefId'] == null) {
            throw new runtime.RequiredError(
                'artifactRefId',
                'Required parameter "artifactRefId" was null or undefined when calling createArtifactDownloadToken().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/invoices/{invoiceId}/artifacts/{artifactRefId}/download-token`.replace(`{${"invoiceId"}}`, encodeURIComponent(String(requestParameters['invoiceId']))).replace(`{${"artifactRefId"}}`, encodeURIComponent(String(requestParameters['artifactRefId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ArtifactDownloadTokenFromJSON(jsonValue));
    }

    /**
     * Issues a short-lived signed token bound to one invoice artifact, so a browser can fetch the PDF through the public download link without an Authorization header. Use this tool after listInvoiceArtifacts has supplied an artifactRefId; do not use downloadInvoiceArtifact without a token — it rejects unsigned requests. Preconditions: the invoice must exist and the artifact reference must belong to that invoice (an invoice ref must match the path invoice, a receipt ref must be one of its receipts). Required inputs: invoiceId (UUID) and artifactRefId (opaque reference from listInvoiceArtifacts) as path parameters; there is no request body. No events are emitted and no record is stored; the token is stateless, signed, and expires after 300 seconds by default (invoice.artifacts.token-ttl-seconds). Returns 404 when the invoice does not exist or the artifact does not belong to it. 
     * Create Artifact Download Token
     */
    async createArtifactDownloadToken(requestParameters: CreateArtifactDownloadTokenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ArtifactDownloadToken> {
        const response = await this.createArtifactDownloadTokenRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the downloadable documents for an invoice — always the invoice document itself, plus one entry per generated receipt — as opaque URL-safe artifact references with suggested file names and application/pdf MIME types. Use this tool to discover artifactRefId values before minting a download token; do not use downloadInvoiceArtifact directly, which requires a signed token from createArtifactDownloadToken. Preconditions: the invoice must exist; receipts appear only after generateReceipt has created them. Required inputs: invoiceId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no invoice exists for the supplied id. 
     * List Downloadable Invoice Artifacts
     */
    async listInvoiceArtifactsRaw(requestParameters: ListInvoiceArtifactsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<InvoiceArtifact>>> {
        if (requestParameters['invoiceId'] == null) {
            throw new runtime.RequiredError(
                'invoiceId',
                'Required parameter "invoiceId" was null or undefined when calling listInvoiceArtifacts().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/invoices/{invoiceId}/artifacts`.replace(`{${"invoiceId"}}`, encodeURIComponent(String(requestParameters['invoiceId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(InvoiceArtifactFromJSON));
    }

    /**
     * Returns the downloadable documents for an invoice — always the invoice document itself, plus one entry per generated receipt — as opaque URL-safe artifact references with suggested file names and application/pdf MIME types. Use this tool to discover artifactRefId values before minting a download token; do not use downloadInvoiceArtifact directly, which requires a signed token from createArtifactDownloadToken. Preconditions: the invoice must exist; receipts appear only after generateReceipt has created them. Required inputs: invoiceId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no invoice exists for the supplied id. 
     * List Downloadable Invoice Artifacts
     */
    async listInvoiceArtifacts(requestParameters: ListInvoiceArtifactsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<InvoiceArtifact>> {
        const response = await this.listInvoiceArtifactsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
