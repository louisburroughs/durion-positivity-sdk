/* tslint:disable */
/* eslint-disable */
/**
 * POS Shop Manager Service API
 * Shop Manager service for managing shop operations and staff
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  BulkIngestRequestMechanicSkillBulkIngestRecord,
  BulkIngestResponse,
} from '../models/index';
import {
    BulkIngestRequestMechanicSkillBulkIngestRecordFromJSON,
    BulkIngestRequestMechanicSkillBulkIngestRecordToJSON,
    BulkIngestResponseFromJSON,
    BulkIngestResponseToJSON,
} from '../models/index';

export interface BulkIngestMechanicSkillsRequest {
    bulkIngestRequestMechanicSkillBulkIngestRecord: BulkIngestRequestMechanicSkillBulkIngestRecord;
}

/**
 * 
 */
export class MechanicSkillBulkIngestAPIApi extends runtime.BaseAPI {

    /**
     * Sets many mechanics\' skill sets at once, folding the rows of each mechanic into a single replacement. Use this tool when seeding a workshop\'s capabilities; use replaceMechanicSkills instead for a single mechanic. Preconditions: each person must already exist as a mechanic. Mechanics are projected from ACTIVE TECHNICIAN staffing assignments over Kafka, so run this after those assignments and allow the projection to catch up. Required inputs: jobId (UUID), locationId (UUID) and records, each with a personId, a skillCode and a proficiencyLevel from 1 to 5. A mechanic appears once per skill; the rows are grouped here. Emits a SHOP_MECHANIC_SKILLS_BULK_INGEST event, and routes each mechanic\'s set through the same HR-feed path the Kafka projection uses, so dedupe, stale-guard and audit apply as usual; re-running the same file is safe, since each mechanic\'s set is replaced rather than added to. Returns 200 with a per-record result, where every row of one mechanic shares that mechanic\'s outcome since they were applied together: MECHANIC_SKILL_INGEST_FAILED with the reason for rows the service refused, or INTERNAL_ERROR with a correlationId to quote for rows lost to a server-side fault. 
     * Set Mechanics\' Skill Sets in Bulk
     */
    async bulkIngestMechanicSkillsRaw(requestParameters: BulkIngestMechanicSkillsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkIngestResponse>> {
        if (requestParameters['bulkIngestRequestMechanicSkillBulkIngestRecord'] == null) {
            throw new runtime.RequiredError(
                'bulkIngestRequestMechanicSkillBulkIngestRecord',
                'Required parameter "bulkIngestRequestMechanicSkillBulkIngestRecord" was null or undefined when calling bulkIngestMechanicSkills().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["shop:schedule:edit"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/shop-manager/mechanics/bulk-ingest`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkIngestRequestMechanicSkillBulkIngestRecordToJSON(requestParameters['bulkIngestRequestMechanicSkillBulkIngestRecord']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkIngestResponseFromJSON(jsonValue));
    }

    /**
     * Sets many mechanics\' skill sets at once, folding the rows of each mechanic into a single replacement. Use this tool when seeding a workshop\'s capabilities; use replaceMechanicSkills instead for a single mechanic. Preconditions: each person must already exist as a mechanic. Mechanics are projected from ACTIVE TECHNICIAN staffing assignments over Kafka, so run this after those assignments and allow the projection to catch up. Required inputs: jobId (UUID), locationId (UUID) and records, each with a personId, a skillCode and a proficiencyLevel from 1 to 5. A mechanic appears once per skill; the rows are grouped here. Emits a SHOP_MECHANIC_SKILLS_BULK_INGEST event, and routes each mechanic\'s set through the same HR-feed path the Kafka projection uses, so dedupe, stale-guard and audit apply as usual; re-running the same file is safe, since each mechanic\'s set is replaced rather than added to. Returns 200 with a per-record result, where every row of one mechanic shares that mechanic\'s outcome since they were applied together: MECHANIC_SKILL_INGEST_FAILED with the reason for rows the service refused, or INTERNAL_ERROR with a correlationId to quote for rows lost to a server-side fault. 
     * Set Mechanics\' Skill Sets in Bulk
     */
    async bulkIngestMechanicSkills(requestParameters: BulkIngestMechanicSkillsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkIngestResponse> {
        const response = await this.bulkIngestMechanicSkillsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
