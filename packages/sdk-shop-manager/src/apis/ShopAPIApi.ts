/* tslint:disable */
/* eslint-disable */
/**
 * POS Shop Manager Service API
 * Shop Manager service for managing shop operations and staff
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  ShopResponse,
  ShopUpsertRequest,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    ShopResponseFromJSON,
    ShopResponseToJSON,
    ShopUpsertRequestFromJSON,
    ShopUpsertRequestToJSON,
} from '../models/index';

export interface UpsertShopRequest {
    locationId: string;
    shopUpsertRequest: ShopUpsertRequest;
}

/**
 * 
 */
export class ShopAPIApi extends runtime.BaseAPI {

    /**
     * Creates the shop record that makes a location schedulable, or replaces its configuration when one already exists. Use this tool to make a site bookable for the first time or to correct its name, address or scheduling timezone; use viewSchedule to read what is booked, which answers 404 for any location that has no shop record. Preconditions: none beyond the id — the shop carries the pos-location location id by convention, which is how every other read in this module resolves a request\'s locationId, and this operation does not verify that the location exists. Required inputs: locationId (UUID) as a path parameter and a body with a non-blank name; address is optional, and timezone is optional but must be a valid IANA zone id when supplied, because the schedule view computes the day window in it and falls back to UTC when it is unset. Emits a SHOPMGR_SHOP_UPSERT audit event and writes only the shop row; no other records are touched and no fact is published. Idempotent on locationId, so a reseed converges rather than duplicating. Returns 400 when the name is blank or the timezone is not a zone id, and 403 when the caller lacks shop:schedule:edit. 
     * Create or Replace a Location\'s Shop Configuration
     */
    async upsertShopRaw(requestParameters: UpsertShopRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ShopResponse>> {
        if (requestParameters['locationId'] == null) {
            throw new runtime.RequiredError(
                'locationId',
                'Required parameter "locationId" was null or undefined when calling upsertShop().'
            );
        }

        if (requestParameters['shopUpsertRequest'] == null) {
            throw new runtime.RequiredError(
                'shopUpsertRequest',
                'Required parameter "shopUpsertRequest" was null or undefined when calling upsertShop().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["shop:schedule:edit"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/shops/{locationId}`.replace(`{${"locationId"}}`, encodeURIComponent(String(requestParameters['locationId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: ShopUpsertRequestToJSON(requestParameters['shopUpsertRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ShopResponseFromJSON(jsonValue));
    }

    /**
     * Creates the shop record that makes a location schedulable, or replaces its configuration when one already exists. Use this tool to make a site bookable for the first time or to correct its name, address or scheduling timezone; use viewSchedule to read what is booked, which answers 404 for any location that has no shop record. Preconditions: none beyond the id — the shop carries the pos-location location id by convention, which is how every other read in this module resolves a request\'s locationId, and this operation does not verify that the location exists. Required inputs: locationId (UUID) as a path parameter and a body with a non-blank name; address is optional, and timezone is optional but must be a valid IANA zone id when supplied, because the schedule view computes the day window in it and falls back to UTC when it is unset. Emits a SHOPMGR_SHOP_UPSERT audit event and writes only the shop row; no other records are touched and no fact is published. Idempotent on locationId, so a reseed converges rather than duplicating. Returns 400 when the name is blank or the timezone is not a zone id, and 403 when the caller lacks shop:schedule:edit. 
     * Create or Replace a Location\'s Shop Configuration
     */
    async upsertShop(requestParameters: UpsertShopRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ShopResponse> {
        const response = await this.upsertShopRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
