/* tslint:disable */
/* eslint-disable */
/**
 * POS Shop Manager Service API
 * Shop Manager service for managing shop operations and staff
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  PagedModelMechanicRosterEntryResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    PagedModelMechanicRosterEntryResponseFromJSON,
    PagedModelMechanicRosterEntryResponseToJSON,
} from '../models/index';

export interface ListMechanicsRequest {
    status?: ListMechanicsStatusEnum;
    skillCode?: string;
    page?: number;
    size?: number;
    sort?: Array<string>;
}

/**
 * 
 */
export class MechanicRosterAPIApi extends runtime.BaseAPI {

    /**
     * Returns the eventually consistent mechanic read model synchronized from People/HR, one page at a time. Use this tool when building a shop-wide mechanic picker or capability report; use listLocationTechnicians instead when the roster must be scoped to a single shop location, and getTechnicianPerson for one technician\'s contact details. Preconditions: mechanics are projected from ACTIVE TECHNICIAN staffing assignments over Kafka, so a newly hired mechanic appears only once that projection has caught up. Required inputs: none, and there is no request body; optionally narrow with status and skillCode, both exact matches, where an omitted status defaults to ACTIVE, and page with the standard page, size and sort parameters, which default to a stable lastName, firstName, personId ordering. Emits a SHOPMGR_MECHANIC_ROSTER_LIST audit event; no state changes occur, and rows trail the People/HR authority by the event-propagation delay. Returns 403 when the caller lacks shop:technician:view, and an empty page rather than an error when no mechanic matches the filters. 
     * List mechanics
     */
    async listMechanicsRaw(requestParameters: ListMechanicsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagedModelMechanicRosterEntryResponse>> {
        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['skillCode'] != null) {
            queryParameters['skillCode'] = requestParameters['skillCode'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        if (requestParameters['sort'] != null) {
            queryParameters['sort'] = requestParameters['sort'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["shop:technician:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/shop-manager/mechanics`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagedModelMechanicRosterEntryResponseFromJSON(jsonValue));
    }

    /**
     * Returns the eventually consistent mechanic read model synchronized from People/HR, one page at a time. Use this tool when building a shop-wide mechanic picker or capability report; use listLocationTechnicians instead when the roster must be scoped to a single shop location, and getTechnicianPerson for one technician\'s contact details. Preconditions: mechanics are projected from ACTIVE TECHNICIAN staffing assignments over Kafka, so a newly hired mechanic appears only once that projection has caught up. Required inputs: none, and there is no request body; optionally narrow with status and skillCode, both exact matches, where an omitted status defaults to ACTIVE, and page with the standard page, size and sort parameters, which default to a stable lastName, firstName, personId ordering. Emits a SHOPMGR_MECHANIC_ROSTER_LIST audit event; no state changes occur, and rows trail the People/HR authority by the event-propagation delay. Returns 403 when the caller lacks shop:technician:view, and an empty page rather than an error when no mechanic matches the filters. 
     * List mechanics
     */
    async listMechanics(requestParameters: ListMechanicsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagedModelMechanicRosterEntryResponse> {
        const response = await this.listMechanicsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
  * @export
  * @enum {string}
  */
export enum ListMechanicsStatusEnum {
    Active = 'ACTIVE',
    Inactive = 'INACTIVE',
    OnLeave = 'ON_LEAVE'
}
