/* tslint:disable */
/* eslint-disable */
/**
 * POS Tax Service API
 * Tax calculation service with external API passthrough and test mode support
 *
 * The version of the OpenAPI document: 1.0
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ExemptionCertificateRequest,
  ExemptionCertificateResponse,
} from '../models/index';
import {
    ExemptionCertificateRequestFromJSON,
    ExemptionCertificateRequestToJSON,
    ExemptionCertificateResponseFromJSON,
    ExemptionCertificateResponseToJSON,
} from '../models/index';

export interface CreateExemptionCertificateRequest {
    exemptionCertificateRequest: ExemptionCertificateRequest;
}

export interface GetExemptionCertificateByIdRequest {
    id: string;
}

export interface ListExemptionCertificatesRequest {
    customerId?: string;
}

export interface UpdateExemptionCertificateRequest {
    id: string;
    exemptionCertificateRequest: ExemptionCertificateRequest;
}

/**
 * 
 */
export class TaxExemptionCertificatesApi extends runtime.BaseAPI {

    /**
     * Registers a tax exemption certificate for a customer so that later calculations can treat their line items as exempt. Use this tool when a customer supplies a new certificate; do not use it to correct an existing registration, which is updateExemptionCertificate. Preconditions: none are enforced against other services, so the customerId is accepted as an opaque identifier and is not verified against the customer registry. Required inputs: customerId, reasonCode (RESALE, GOVERNMENT, NONPROFIT, AGRICULTURAL or OTHER) and effectiveFrom; stateScope null means every state, expiresAt null means no expiry, and status defaults to ACTIVE. Emits a TAX_EXEMPTION_CERT_CREATE approval-preset audit event; no existing certificate is superseded, so overlapping certificates for the same customer can coexist. Returns 400 when customerId, reasonCode or effectiveFrom are missing or the reason code is not a recognised value. 
     * Create an exemption certificate
     */
    async createExemptionCertificateRaw(requestParameters: CreateExemptionCertificateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ExemptionCertificateResponse>> {
        if (requestParameters['exemptionCertificateRequest'] == null) {
            throw new runtime.RequiredError(
                'exemptionCertificateRequest',
                'Required parameter "exemptionCertificateRequest" was null or undefined when calling createExemptionCertificate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["tax:exemption:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/tax/exemption-certificates`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ExemptionCertificateRequestToJSON(requestParameters['exemptionCertificateRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ExemptionCertificateResponseFromJSON(jsonValue));
    }

    /**
     * Registers a tax exemption certificate for a customer so that later calculations can treat their line items as exempt. Use this tool when a customer supplies a new certificate; do not use it to correct an existing registration, which is updateExemptionCertificate. Preconditions: none are enforced against other services, so the customerId is accepted as an opaque identifier and is not verified against the customer registry. Required inputs: customerId, reasonCode (RESALE, GOVERNMENT, NONPROFIT, AGRICULTURAL or OTHER) and effectiveFrom; stateScope null means every state, expiresAt null means no expiry, and status defaults to ACTIVE. Emits a TAX_EXEMPTION_CERT_CREATE approval-preset audit event; no existing certificate is superseded, so overlapping certificates for the same customer can coexist. Returns 400 when customerId, reasonCode or effectiveFrom are missing or the reason code is not a recognised value. 
     * Create an exemption certificate
     */
    async createExemptionCertificate(requestParameters: CreateExemptionCertificateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ExemptionCertificateResponse> {
        const response = await this.createExemptionCertificateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns a single tax exemption certificate, including its scope, reason code and validity window. Use this tool when the certificate id is already known; use listExemptionCertificates instead when searching by customer. Preconditions: the certificate must exist in the registry. Required inputs: id (UUID) path parameter; there is no request body and no filtering. No events are emitted and no state changes; this is a read-only registry projection. Returns 404 when no certificate exists for the supplied id. 
     * Get an exemption certificate
     */
    async getExemptionCertificateByIdRaw(requestParameters: GetExemptionCertificateByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ExemptionCertificateResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getExemptionCertificateById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["tax:exemption:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/tax/exemption-certificates/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ExemptionCertificateResponseFromJSON(jsonValue));
    }

    /**
     * Returns a single tax exemption certificate, including its scope, reason code and validity window. Use this tool when the certificate id is already known; use listExemptionCertificates instead when searching by customer. Preconditions: the certificate must exist in the registry. Required inputs: id (UUID) path parameter; there is no request body and no filtering. No events are emitted and no state changes; this is a read-only registry projection. Returns 404 when no certificate exists for the supplied id. 
     * Get an exemption certificate
     */
    async getExemptionCertificateById(requestParameters: GetExemptionCertificateByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ExemptionCertificateResponse> {
        const response = await this.getExemptionCertificateByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the registered tax exemption certificates, newest effective date first, optionally narrowed to a single customer. Use this tool to discover a certificate id before calculating tax with an exemption; do not use it to test whether an exemption applies on a given date, which calculateTax resolves itself. Preconditions: none; an unknown or unmatched customerId yields an empty list rather than an error. Required inputs: none, and customerId is an optional query parameter matched exactly, not as a substring. No events are emitted and no state changes; this is a read-only registry projection. Returns 200 with an empty array when no certificate matches, so an empty result is not an error condition. 
     * List exemption certificates
     */
    async listExemptionCertificatesRaw(requestParameters: ListExemptionCertificatesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ExemptionCertificateResponse>>> {
        const queryParameters: any = {};

        if (requestParameters['customerId'] != null) {
            queryParameters['customerId'] = requestParameters['customerId'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["tax:exemption:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/tax/exemption-certificates`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ExemptionCertificateResponseFromJSON));
    }

    /**
     * Returns the registered tax exemption certificates, newest effective date first, optionally narrowed to a single customer. Use this tool to discover a certificate id before calculating tax with an exemption; do not use it to test whether an exemption applies on a given date, which calculateTax resolves itself. Preconditions: none; an unknown or unmatched customerId yields an empty list rather than an error. Required inputs: none, and customerId is an optional query parameter matched exactly, not as a substring. No events are emitted and no state changes; this is a read-only registry projection. Returns 200 with an empty array when no certificate matches, so an empty result is not an error condition. 
     * List exemption certificates
     */
    async listExemptionCertificates(requestParameters: ListExemptionCertificatesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ExemptionCertificateResponse>> {
        const response = await this.listExemptionCertificatesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Replaces the stored details of an exemption certificate, including its customer, scope, reason code and validity window. Use this tool to correct or expire an existing registration; do not use it to register a further certificate for the same customer, which is createExemptionCertificate. Preconditions: the certificate must exist; expiring a certificate is done by setting expiresAt or moving status to INACTIVE or REVOKED rather than by deleting it, because there is no delete endpoint. Required inputs: id (UUID) path parameter plus the full body, since every supplied field replaces the stored value; an omitted status leaves the current status unchanged. Emits a TAX_EXEMPTION_CERT_UPDATE approval-preset audit event; tax already calculated against the old values is not recalculated. Returns 404 when no certificate exists for the supplied id, and 400 when customerId, reasonCode or effectiveFrom are missing from the body. 
     * Update an exemption certificate
     */
    async updateExemptionCertificateRaw(requestParameters: UpdateExemptionCertificateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ExemptionCertificateResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updateExemptionCertificate().'
            );
        }

        if (requestParameters['exemptionCertificateRequest'] == null) {
            throw new runtime.RequiredError(
                'exemptionCertificateRequest',
                'Required parameter "exemptionCertificateRequest" was null or undefined when calling updateExemptionCertificate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["tax:exemption:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/tax/exemption-certificates/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: ExemptionCertificateRequestToJSON(requestParameters['exemptionCertificateRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ExemptionCertificateResponseFromJSON(jsonValue));
    }

    /**
     * Replaces the stored details of an exemption certificate, including its customer, scope, reason code and validity window. Use this tool to correct or expire an existing registration; do not use it to register a further certificate for the same customer, which is createExemptionCertificate. Preconditions: the certificate must exist; expiring a certificate is done by setting expiresAt or moving status to INACTIVE or REVOKED rather than by deleting it, because there is no delete endpoint. Required inputs: id (UUID) path parameter plus the full body, since every supplied field replaces the stored value; an omitted status leaves the current status unchanged. Emits a TAX_EXEMPTION_CERT_UPDATE approval-preset audit event; tax already calculated against the old values is not recalculated. Returns 404 when no certificate exists for the supplied id, and 400 when customerId, reasonCode or effectiveFrom are missing from the body. 
     * Update an exemption certificate
     */
    async updateExemptionCertificate(requestParameters: UpdateExemptionCertificateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ExemptionCertificateResponse> {
        const response = await this.updateExemptionCertificateRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
