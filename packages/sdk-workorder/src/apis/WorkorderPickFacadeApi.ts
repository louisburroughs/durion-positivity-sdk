/* tslint:disable */
/* eslint-disable */
/**
 * POS Workorder Service API
 * Workorder service for managing workorders and tasks
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  CompletePickTaskRequest,
  ConfirmPickLineRequest,
  ResolveScanRequest,
  ResolveScanResponse,
  WorkorderPickListResponse,
  WorkorderPickTaskResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    CompletePickTaskRequestFromJSON,
    CompletePickTaskRequestToJSON,
    ConfirmPickLineRequestFromJSON,
    ConfirmPickLineRequestToJSON,
    ResolveScanRequestFromJSON,
    ResolveScanRequestToJSON,
    ResolveScanResponseFromJSON,
    ResolveScanResponseToJSON,
    WorkorderPickListResponseFromJSON,
    WorkorderPickListResponseToJSON,
    WorkorderPickTaskResponseFromJSON,
    WorkorderPickTaskResponseToJSON,
} from '../models/index';

export interface CompletePickTaskOperationRequest {
    workorderId: string;
    pickTaskId: string;
    completePickTaskRequest?: CompletePickTaskRequest;
}

export interface ConfirmPickLineOperationRequest {
    workorderId: string;
    pickTaskId: string;
    pickLineId: string;
    confirmPickLineRequest: ConfirmPickLineRequest;
}

export interface GetPickTasksRequest {
    workorderId: string;
}

export interface GetWorkorderPickListRequest {
    workorderId: string;
}

export interface ResolvePickScanRequest {
    workorderId: string;
    pickTaskId: string;
    resolveScanRequest: ResolveScanRequest;
}

/**
 * 
 */
export class WorkorderPickFacadeApi extends runtime.BaseAPI {

    /**
     * Queues asynchronous completion of a pick task by confirming its full required quantity over the Kafka command feed per ADR-0044; a task with nothing left to pick is returned as-is. Use this tool to close out a pick task in one step; use confirmPickLine instead to record a partial picked quantity. Preconditions: the pick task must exist on the workorder\'s pick list replica. Required inputs: workorderId and pickTaskId (UUIDs) as path parameters; the body is optional and may carry a completion reason. Emits a WORKORDER_PICK_FACADE_COMPLETE_TASK event and publishes a pick-confirm command for the remaining quantity; callers must poll getPickTasks to observe the applied state. Returns 202 with status PENDING while the command is in flight, 200 with the current state when the task is already complete, 404 when the pick list or task is missing, 503 when the command feed is unavailable, and 403 LOCATION_SCOPE_DENIED when the caller\'s location scope does not cover the workorder\'s own site (ADR-0061 mechanism, #2204). 
     * Complete a Workorder Pick Task
     */
    async completePickTaskRaw(requestParameters: CompletePickTaskOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WorkorderPickTaskResponse>> {
        if (requestParameters['workorderId'] == null) {
            throw new runtime.RequiredError(
                'workorderId',
                'Required parameter "workorderId" was null or undefined when calling completePickTask().'
            );
        }

        if (requestParameters['pickTaskId'] == null) {
            throw new runtime.RequiredError(
                'pickTaskId',
                'Required parameter "pickTaskId" was null or undefined when calling completePickTask().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:pick_list:execute"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/workorders/{workorderId}/pick-tasks/{pickTaskId}:complete`.replace(`{${"workorderId"}}`, encodeURIComponent(String(requestParameters['workorderId']))).replace(`{${"pickTaskId"}}`, encodeURIComponent(String(requestParameters['pickTaskId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CompletePickTaskRequestToJSON(requestParameters['completePickTaskRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WorkorderPickTaskResponseFromJSON(jsonValue));
    }

    /**
     * Queues asynchronous completion of a pick task by confirming its full required quantity over the Kafka command feed per ADR-0044; a task with nothing left to pick is returned as-is. Use this tool to close out a pick task in one step; use confirmPickLine instead to record a partial picked quantity. Preconditions: the pick task must exist on the workorder\'s pick list replica. Required inputs: workorderId and pickTaskId (UUIDs) as path parameters; the body is optional and may carry a completion reason. Emits a WORKORDER_PICK_FACADE_COMPLETE_TASK event and publishes a pick-confirm command for the remaining quantity; callers must poll getPickTasks to observe the applied state. Returns 202 with status PENDING while the command is in flight, 200 with the current state when the task is already complete, 404 when the pick list or task is missing, 503 when the command feed is unavailable, and 403 LOCATION_SCOPE_DENIED when the caller\'s location scope does not cover the workorder\'s own site (ADR-0061 mechanism, #2204). 
     * Complete a Workorder Pick Task
     */
    async completePickTask(requestParameters: CompletePickTaskOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WorkorderPickTaskResponse> {
        const response = await this.completePickTaskRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Queues asynchronous confirmation of a picked quantity on one pick line over the Kafka command feed per ADR-0044; the response carries status PENDING, and the inventory pick-task-updated fact later updates the local replica. Use this tool after resolvePickScan validates the scan; do not use completePickTask, which confirms the full remaining required quantity in one step. Preconditions: the pick task must exist on the workorder\'s pick list replica, and pickLineId must equal pickTaskId in the current single-line model. Required inputs: workorderId, pickTaskId, and pickLineId (UUIDs) as path parameters, plus quantityPicked (integer) in the body. Emits a WORKORDER_PICK_FACADE_CONFIRM_LINE event and publishes a pick-confirm command; callers must poll getPickTasks to observe the applied quantity. Returns 202 with status PENDING when the confirmation is queued, 400 when pickLineId does not match pickTaskId, 404 when the pick list or task is missing, 503 when the command feed is unavailable, and 403 LOCATION_SCOPE_DENIED when the caller\'s location scope does not cover the workorder\'s own site (ADR-0061 mechanism, #2204). 
     * Confirm Pick Line Quantity
     */
    async confirmPickLineRaw(requestParameters: ConfirmPickLineOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WorkorderPickTaskResponse>> {
        if (requestParameters['workorderId'] == null) {
            throw new runtime.RequiredError(
                'workorderId',
                'Required parameter "workorderId" was null or undefined when calling confirmPickLine().'
            );
        }

        if (requestParameters['pickTaskId'] == null) {
            throw new runtime.RequiredError(
                'pickTaskId',
                'Required parameter "pickTaskId" was null or undefined when calling confirmPickLine().'
            );
        }

        if (requestParameters['pickLineId'] == null) {
            throw new runtime.RequiredError(
                'pickLineId',
                'Required parameter "pickLineId" was null or undefined when calling confirmPickLine().'
            );
        }

        if (requestParameters['confirmPickLineRequest'] == null) {
            throw new runtime.RequiredError(
                'confirmPickLineRequest',
                'Required parameter "confirmPickLineRequest" was null or undefined when calling confirmPickLine().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:pick_list:execute"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/workorders/{workorderId}/pick-tasks/{pickTaskId}/lines/{pickLineId}:confirm`.replace(`{${"workorderId"}}`, encodeURIComponent(String(requestParameters['workorderId']))).replace(`{${"pickTaskId"}}`, encodeURIComponent(String(requestParameters['pickTaskId']))).replace(`{${"pickLineId"}}`, encodeURIComponent(String(requestParameters['pickLineId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ConfirmPickLineRequestToJSON(requestParameters['confirmPickLineRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WorkorderPickTaskResponseFromJSON(jsonValue));
    }

    /**
     * Queues asynchronous confirmation of a picked quantity on one pick line over the Kafka command feed per ADR-0044; the response carries status PENDING, and the inventory pick-task-updated fact later updates the local replica. Use this tool after resolvePickScan validates the scan; do not use completePickTask, which confirms the full remaining required quantity in one step. Preconditions: the pick task must exist on the workorder\'s pick list replica, and pickLineId must equal pickTaskId in the current single-line model. Required inputs: workorderId, pickTaskId, and pickLineId (UUIDs) as path parameters, plus quantityPicked (integer) in the body. Emits a WORKORDER_PICK_FACADE_CONFIRM_LINE event and publishes a pick-confirm command; callers must poll getPickTasks to observe the applied quantity. Returns 202 with status PENDING when the confirmation is queued, 400 when pickLineId does not match pickTaskId, 404 when the pick list or task is missing, 503 when the command feed is unavailable, and 403 LOCATION_SCOPE_DENIED when the caller\'s location scope does not cover the workorder\'s own site (ADR-0061 mechanism, #2204). 
     * Confirm Pick Line Quantity
     */
    async confirmPickLine(requestParameters: ConfirmPickLineOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WorkorderPickTaskResponse> {
        const response = await this.confirmPickLineRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the pick tasks of the workorder\'s primary pick list in sort order, each with its SKU, location, required and picked quantities, and status. Use this tool to drive pick execution line by line; use getWorkorderPickList instead for the list header and getPickedItems for what has already been picked. Preconditions: none - promoting an estimate generates the pick list for the workorder\'s part lines, so tasks normally appear without any further call. Required inputs: workorderId (UUID) as a path parameter. No events are emitted and no state changes; this is a read-only replica projection. Returns 200 with an empty array when the workorder has no pick list - a labour-only job, or one whose generated list has not replicated yet - so \"nothing to pick\" never has to be handled as an error, 403 when the caller lacks inventory:pick_list:view, and 403 LOCATION_SCOPE_DENIED when the caller holds it but its location scope does not cover the workorder\'s own site (ADR-0061 mechanism, #2204). 
     * Get Pick Tasks for Workorder
     */
    async getPickTasksRaw(requestParameters: GetPickTasksRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<WorkorderPickTaskResponse>>> {
        if (requestParameters['workorderId'] == null) {
            throw new runtime.RequiredError(
                'workorderId',
                'Required parameter "workorderId" was null or undefined when calling getPickTasks().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:pick_list:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/workorders/{workorderId}/pick-list/tasks`.replace(`{${"workorderId"}}`, encodeURIComponent(String(requestParameters['workorderId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(WorkorderPickTaskResponseFromJSON));
    }

    /**
     * Returns the pick tasks of the workorder\'s primary pick list in sort order, each with its SKU, location, required and picked quantities, and status. Use this tool to drive pick execution line by line; use getWorkorderPickList instead for the list header and getPickedItems for what has already been picked. Preconditions: none - promoting an estimate generates the pick list for the workorder\'s part lines, so tasks normally appear without any further call. Required inputs: workorderId (UUID) as a path parameter. No events are emitted and no state changes; this is a read-only replica projection. Returns 200 with an empty array when the workorder has no pick list - a labour-only job, or one whose generated list has not replicated yet - so \"nothing to pick\" never has to be handled as an error, 403 when the caller lacks inventory:pick_list:view, and 403 LOCATION_SCOPE_DENIED when the caller holds it but its location scope does not cover the workorder\'s own site (ADR-0061 mechanism, #2204). 
     * Get Pick Tasks for Workorder
     */
    async getPickTasks(requestParameters: GetPickTasksRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<WorkorderPickTaskResponse>> {
        const response = await this.getPickTasksRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the workorder\'s primary pick list header from the local inventory replica so parts can be staged and fulfilled. Use this tool for the pick list summary; use getPickTasks instead for the individual pick lines that guide execution. The full sequence for a workorder\'s parts is promoteEstimate, which creates the workorder and asks pos-inventory to reserve each part line and generate this list, then getPickTasks, then resolvePickScan with confirmPickLine (or completePickTask), then consumeWorkorderPickedItems, which moves stock and raises the part line\'s quantityConsumed - and a part the site cannot cover is answered with a backorder id on that part line rather than a pick task. Preconditions: a pick list replica must already exist for the workorder, replicated from pos-inventory facts; a promotion-generated list arrives in DRAFT and is pickable as-is, while releasing it marks it READY_TO_PICK for the floor. Required inputs: workorderId (UUID) as a path parameter. No events are emitted and no state changes; this is a read-only replica projection. Returns 404 when no pick list exists for the workorder, and 403 LOCATION_SCOPE_DENIED when the caller holds inventory:pick_list:view but its location scope does not cover the workorder\'s own site (ADR-0061 mechanism, #2204). 
     * Get Pick List for Workorder
     */
    async getWorkorderPickListRaw(requestParameters: GetWorkorderPickListRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WorkorderPickListResponse>> {
        if (requestParameters['workorderId'] == null) {
            throw new runtime.RequiredError(
                'workorderId',
                'Required parameter "workorderId" was null or undefined when calling getWorkorderPickList().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:pick_list:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/workorders/{workorderId}/pick-list`.replace(`{${"workorderId"}}`, encodeURIComponent(String(requestParameters['workorderId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WorkorderPickListResponseFromJSON(jsonValue));
    }

    /**
     * Returns the workorder\'s primary pick list header from the local inventory replica so parts can be staged and fulfilled. Use this tool for the pick list summary; use getPickTasks instead for the individual pick lines that guide execution. The full sequence for a workorder\'s parts is promoteEstimate, which creates the workorder and asks pos-inventory to reserve each part line and generate this list, then getPickTasks, then resolvePickScan with confirmPickLine (or completePickTask), then consumeWorkorderPickedItems, which moves stock and raises the part line\'s quantityConsumed - and a part the site cannot cover is answered with a backorder id on that part line rather than a pick task. Preconditions: a pick list replica must already exist for the workorder, replicated from pos-inventory facts; a promotion-generated list arrives in DRAFT and is pickable as-is, while releasing it marks it READY_TO_PICK for the floor. Required inputs: workorderId (UUID) as a path parameter. No events are emitted and no state changes; this is a read-only replica projection. Returns 404 when no pick list exists for the workorder, and 403 LOCATION_SCOPE_DENIED when the caller holds inventory:pick_list:view but its location scope does not cover the workorder\'s own site (ADR-0061 mechanism, #2204). 
     * Get Pick List for Workorder
     */
    async getWorkorderPickList(requestParameters: GetWorkorderPickListRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WorkorderPickListResponse> {
        const response = await this.getWorkorderPickListRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Checks a scanned product and location against a pick task\'s expected product and location, returning matched plus a matchStatus of MATCHED, SKU_MISMATCH, LOCATION_MISMATCH, NO_MATCH, PRODUCT_CODE_UNAVAILABLE, or LOCATION_CODE_UNAVAILABLE (#2217). Use this tool to validate a barcode scan before confirmPickLine; it performs no confirmation itself, so do not treat a match as a recorded pick. Preconditions: the pick task must exist on the workorder\'s pick list replica. Required inputs: workorderId and pickTaskId (UUIDs) as path parameters, plus in the body exactly one of scannedSkuId (UUID) or scannedProductCode (the scanned EAN/UPC string), and exactly one of scannedLocationId (UUID) or scannedLocationCode (the scanned location name or barcode string). A code-based scan is compared against the task\'s replicated productCode / locationName / locationBarcode, trimmed and case-insensitively; PRODUCT_CODE_UNAVAILABLE or LOCATION_CODE_UNAVAILABLE means the task carries no replicated code to compare a scanned code against yet — \"cannot verify\", not \"wrong part\" — because resolving that further would require a synchronous pos-catalog/pos-location call, which ADR-0044 forbids here; comparison is scoped to this one task and EAN/UPC codes are unique per tenant (ADR-0053 §5), so there is no cross-task ambiguity to resolve. The response always carries expectedProductCode/expectedLocationCode/expectedLocationBarcode so the UI can tell the mechanic what was expected, and fills resolvedSkuId/resolvedLocationId with the task\'s own ids only once a code-based scan actually matches (an id-based scan is echoed back regardless of outcome). Emits a WORKORDER_PICK_FACADE_RESOLVE_SCAN audit event; no pick state changes — the check is purely evaluative. Returns 400 VALIDATION_FAILED (with fieldErrors naming productTargetValid and/or locationTargetValid) when neither or both of a target pair is supplied, 404 when the workorder has no pick list or the pick task is not on it, and 403 LOCATION_SCOPE_DENIED when the caller\'s location scope does not cover the workorder\'s own site (ADR-0061 mechanism, #2204). 
     * Resolve Scan Against Pick Task
     */
    async resolvePickScanRaw(requestParameters: ResolvePickScanRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ResolveScanResponse>> {
        if (requestParameters['workorderId'] == null) {
            throw new runtime.RequiredError(
                'workorderId',
                'Required parameter "workorderId" was null or undefined when calling resolvePickScan().'
            );
        }

        if (requestParameters['pickTaskId'] == null) {
            throw new runtime.RequiredError(
                'pickTaskId',
                'Required parameter "pickTaskId" was null or undefined when calling resolvePickScan().'
            );
        }

        if (requestParameters['resolveScanRequest'] == null) {
            throw new runtime.RequiredError(
                'resolveScanRequest',
                'Required parameter "resolveScanRequest" was null or undefined when calling resolvePickScan().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:pick_list:execute"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/workorders/{workorderId}/pick-tasks/{pickTaskId}:resolve-scan`.replace(`{${"workorderId"}}`, encodeURIComponent(String(requestParameters['workorderId']))).replace(`{${"pickTaskId"}}`, encodeURIComponent(String(requestParameters['pickTaskId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ResolveScanRequestToJSON(requestParameters['resolveScanRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ResolveScanResponseFromJSON(jsonValue));
    }

    /**
     * Checks a scanned product and location against a pick task\'s expected product and location, returning matched plus a matchStatus of MATCHED, SKU_MISMATCH, LOCATION_MISMATCH, NO_MATCH, PRODUCT_CODE_UNAVAILABLE, or LOCATION_CODE_UNAVAILABLE (#2217). Use this tool to validate a barcode scan before confirmPickLine; it performs no confirmation itself, so do not treat a match as a recorded pick. Preconditions: the pick task must exist on the workorder\'s pick list replica. Required inputs: workorderId and pickTaskId (UUIDs) as path parameters, plus in the body exactly one of scannedSkuId (UUID) or scannedProductCode (the scanned EAN/UPC string), and exactly one of scannedLocationId (UUID) or scannedLocationCode (the scanned location name or barcode string). A code-based scan is compared against the task\'s replicated productCode / locationName / locationBarcode, trimmed and case-insensitively; PRODUCT_CODE_UNAVAILABLE or LOCATION_CODE_UNAVAILABLE means the task carries no replicated code to compare a scanned code against yet — \"cannot verify\", not \"wrong part\" — because resolving that further would require a synchronous pos-catalog/pos-location call, which ADR-0044 forbids here; comparison is scoped to this one task and EAN/UPC codes are unique per tenant (ADR-0053 §5), so there is no cross-task ambiguity to resolve. The response always carries expectedProductCode/expectedLocationCode/expectedLocationBarcode so the UI can tell the mechanic what was expected, and fills resolvedSkuId/resolvedLocationId with the task\'s own ids only once a code-based scan actually matches (an id-based scan is echoed back regardless of outcome). Emits a WORKORDER_PICK_FACADE_RESOLVE_SCAN audit event; no pick state changes — the check is purely evaluative. Returns 400 VALIDATION_FAILED (with fieldErrors naming productTargetValid and/or locationTargetValid) when neither or both of a target pair is supplied, 404 when the workorder has no pick list or the pick task is not on it, and 403 LOCATION_SCOPE_DENIED when the caller\'s location scope does not cover the workorder\'s own site (ADR-0061 mechanism, #2204). 
     * Resolve Scan Against Pick Task
     */
    async resolvePickScan(requestParameters: ResolvePickScanRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ResolveScanResponse> {
        const response = await this.resolvePickScanRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
