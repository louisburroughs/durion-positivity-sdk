/* tslint:disable */
/* eslint-disable */
/**
 * POS Workorder Service API
 * Workorder service for managing workorders and tasks
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AddWorkorderNoteRequest,
  WorkorderNoteResponse,
} from '../models/index';
import {
    AddWorkorderNoteRequestFromJSON,
    AddWorkorderNoteRequestToJSON,
    WorkorderNoteResponseFromJSON,
    WorkorderNoteResponseToJSON,
} from '../models/index';

export interface AddWorkorderNoteOperationRequest {
    workorderId: string;
    addWorkorderNoteRequest: AddWorkorderNoteRequest;
}

export interface ListWorkorderNotesRequest {
    workorderId: string;
}

/**
 * 
 */
export class WorkorderNoteAPIApi extends runtime.BaseAPI {

    /**
     * Records a free-text note about the customer against a workorder, such as something the customer said while the job was open, and publishes it so it lands on the customer\'s CRM timeline. Use this tool for a note about the CUSTOMER; do not use it for notes about the work itself or about a decision on it — completion notes and approval notes are fields on the workorder and the change request respectively. Preconditions: the workorder must exist; the author is taken from the authenticated caller rather than the request body. Required inputs: workorderId (UUID) as a path parameter and noteText (max 2000 characters); noteType is an optional free-text classification such as CUSTOMER_REQUEST. Emits a WORKORDER_NOTE_ADD event and publishes the workorder.note.added.v1 fact, which pos-customer projects onto the party\'s timeline. Returns 201 with the saved note, and 404 when the workorder does not exist. 
     * Record a Note About the Customer
     */
    async addWorkorderNoteRaw(requestParameters: AddWorkorderNoteOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WorkorderNoteResponse>> {
        if (requestParameters['workorderId'] == null) {
            throw new runtime.RequiredError(
                'workorderId',
                'Required parameter "workorderId" was null or undefined when calling addWorkorderNote().'
            );
        }

        if (requestParameters['addWorkorderNoteRequest'] == null) {
            throw new runtime.RequiredError(
                'addWorkorderNoteRequest',
                'Required parameter "addWorkorderNoteRequest" was null or undefined when calling addWorkorderNote().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["workorder:note:add"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/workorders/{workorderId}/notes`.replace(`{${"workorderId"}}`, encodeURIComponent(String(requestParameters['workorderId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AddWorkorderNoteRequestToJSON(requestParameters['addWorkorderNoteRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WorkorderNoteResponseFromJSON(jsonValue));
    }

    /**
     * Records a free-text note about the customer against a workorder, such as something the customer said while the job was open, and publishes it so it lands on the customer\'s CRM timeline. Use this tool for a note about the CUSTOMER; do not use it for notes about the work itself or about a decision on it — completion notes and approval notes are fields on the workorder and the change request respectively. Preconditions: the workorder must exist; the author is taken from the authenticated caller rather than the request body. Required inputs: workorderId (UUID) as a path parameter and noteText (max 2000 characters); noteType is an optional free-text classification such as CUSTOMER_REQUEST. Emits a WORKORDER_NOTE_ADD event and publishes the workorder.note.added.v1 fact, which pos-customer projects onto the party\'s timeline. Returns 201 with the saved note, and 404 when the workorder does not exist. 
     * Record a Note About the Customer
     */
    async addWorkorderNote(requestParameters: AddWorkorderNoteOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WorkorderNoteResponse> {
        const response = await this.addWorkorderNoteRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the notes recorded about the customer on one workorder, most recent first. Use this tool to read one workorder\'s own notes; read the customer\'s CRM interaction timeline instead when the question spans every workorder for that customer. Preconditions: the workorder must exist. Required inputs: workorderId (UUID) as a path parameter. Emits a WORKORDER_NOTE_LIST audit event; no domain events are published and no state changes, since this is a read-only query. Returns 200 with the notes (an empty list when none were recorded), and 404 when the workorder does not exist. 
     * List a Workorder\'s Customer Notes
     */
    async listWorkorderNotesRaw(requestParameters: ListWorkorderNotesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<WorkorderNoteResponse>>> {
        if (requestParameters['workorderId'] == null) {
            throw new runtime.RequiredError(
                'workorderId',
                'Required parameter "workorderId" was null or undefined when calling listWorkorderNotes().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["workorder:note:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/workorders/{workorderId}/notes`.replace(`{${"workorderId"}}`, encodeURIComponent(String(requestParameters['workorderId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(WorkorderNoteResponseFromJSON));
    }

    /**
     * Returns the notes recorded about the customer on one workorder, most recent first. Use this tool to read one workorder\'s own notes; read the customer\'s CRM interaction timeline instead when the question spans every workorder for that customer. Preconditions: the workorder must exist. Required inputs: workorderId (UUID) as a path parameter. Emits a WORKORDER_NOTE_LIST audit event; no domain events are published and no state changes, since this is a read-only query. Returns 200 with the notes (an empty list when none were recorded), and 404 when the workorder does not exist. 
     * List a Workorder\'s Customer Notes
     */
    async listWorkorderNotes(requestParameters: ListWorkorderNotesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<WorkorderNoteResponse>> {
        const response = await this.listWorkorderNotesRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
