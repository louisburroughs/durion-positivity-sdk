/* tslint:disable */
/* eslint-disable */
/**
 * POS Workorder Service API
 * Workorder service for managing workorders and tasks
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  FleetAuthorizationResolutionRequest,
  FleetAuthorizationView,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    FleetAuthorizationResolutionRequestFromJSON,
    FleetAuthorizationResolutionRequestToJSON,
    FleetAuthorizationViewFromJSON,
    FleetAuthorizationViewToJSON,
} from '../models/index';

export interface GetWorkorderFleetAuthorizationRequest {
    workorderId: string;
}

export interface RequestWorkorderFleetAuthorizationRequest {
    workorderId: string;
}

export interface ResolveWorkorderFleetAuthorizationRequest {
    workorderId: string;
    fleetAuthorizationResolutionRequest: FleetAuthorizationResolutionRequest;
}

/**
 * 
 */
export class WorkorderFleetAuthorizationApi extends runtime.BaseAPI {

    /**
     * Returns the fleet payment authorization recorded for a workorder, including the vendor\'s own reason where it gave one. Use this tool to check why a fleet-covered workorder cannot start, or whether a re-request has been answered; do not use it to decide whether work may start, because startWorkorder enforces that itself and will refuse a start this endpoint would not have blocked. Preconditions: none. Required inputs: workorderId path parameter. Emits a WORKORDER_FLEET_AUTHORIZATION_READ event. Returns 200 with the authorization, and 404 when this workorder\'s payer requires none or none has ever been requested. 
     * Read A Workorder\'s Fleet Payment Authorization
     */
    async getWorkorderFleetAuthorizationRaw(requestParameters: GetWorkorderFleetAuthorizationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FleetAuthorizationView>> {
        if (requestParameters['workorderId'] == null) {
            throw new runtime.RequiredError(
                'workorderId',
                'Required parameter "workorderId" was null or undefined when calling getWorkorderFleetAuthorization().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["workorder:fleet_auth:request"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/workorders/{workorderId}/fleet-authorization`.replace(`{${"workorderId"}}`, encodeURIComponent(String(requestParameters['workorderId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FleetAuthorizationViewFromJSON(jsonValue));
    }

    /**
     * Returns the fleet payment authorization recorded for a workorder, including the vendor\'s own reason where it gave one. Use this tool to check why a fleet-covered workorder cannot start, or whether a re-request has been answered; do not use it to decide whether work may start, because startWorkorder enforces that itself and will refuse a start this endpoint would not have blocked. Preconditions: none. Required inputs: workorderId path parameter. Emits a WORKORDER_FLEET_AUTHORIZATION_READ event. Returns 200 with the authorization, and 404 when this workorder\'s payer requires none or none has ever been requested. 
     * Read A Workorder\'s Fleet Payment Authorization
     */
    async getWorkorderFleetAuthorization(requestParameters: GetWorkorderFleetAuthorizationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FleetAuthorizationView> {
        const response = await this.getWorkorderFleetAuthorizationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Asks this workorder\'s payer\'s fleet program to authorize paying for it. The same endpoint that fires automatically on approval; calling it again is how an advisor retries after fixing whatever blocked the first attempt, such as a missing vehicle identity. Use this tool to retry a stuck authorization; do not call it while one is already PENDING or GRANTED, because it will not re-ask in either case and asking a fleet twice for the same workorder risks opening a second authorization at the vendor. Preconditions: this workorder\'s payer must require fleet authorization; there is nothing to request otherwise. Required inputs: workorderId path parameter. Emits a WORKORDER_FLEET_AUTHORIZATION_REQUEST event, and where a vehicle can be identified, a workorder.fleetauth.requested command for pos-supplier to act on. Returns 200 with the authorization, whose status is PENDING when the vendor was asked, or MANUAL_REVIEW when no vehicle could be identified so nobody was asked; and 404 when this workorder\'s payer does not require fleet authorization. 
     * Request Or Re-Request Fleet Payment Authorization
     */
    async requestWorkorderFleetAuthorizationRaw(requestParameters: RequestWorkorderFleetAuthorizationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FleetAuthorizationView>> {
        if (requestParameters['workorderId'] == null) {
            throw new runtime.RequiredError(
                'workorderId',
                'Required parameter "workorderId" was null or undefined when calling requestWorkorderFleetAuthorization().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["workorder:fleet_auth:request"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/workorders/{workorderId}/fleet-authorization/requests`.replace(`{${"workorderId"}}`, encodeURIComponent(String(requestParameters['workorderId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FleetAuthorizationViewFromJSON(jsonValue));
    }

    /**
     * Asks this workorder\'s payer\'s fleet program to authorize paying for it. The same endpoint that fires automatically on approval; calling it again is how an advisor retries after fixing whatever blocked the first attempt, such as a missing vehicle identity. Use this tool to retry a stuck authorization; do not call it while one is already PENDING or GRANTED, because it will not re-ask in either case and asking a fleet twice for the same workorder risks opening a second authorization at the vendor. Preconditions: this workorder\'s payer must require fleet authorization; there is nothing to request otherwise. Required inputs: workorderId path parameter. Emits a WORKORDER_FLEET_AUTHORIZATION_REQUEST event, and where a vehicle can be identified, a workorder.fleetauth.requested command for pos-supplier to act on. Returns 200 with the authorization, whose status is PENDING when the vendor was asked, or MANUAL_REVIEW when no vehicle could be identified so nobody was asked; and 404 when this workorder\'s payer does not require fleet authorization. 
     * Request Or Re-Request Fleet Payment Authorization
     */
    async requestWorkorderFleetAuthorization(requestParameters: RequestWorkorderFleetAuthorizationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FleetAuthorizationView> {
        const response = await this.requestWorkorderFleetAuthorizationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Records what an advisor decided to do about a fleet payment authorization that did not come back granted: revise the scope and try again, or cancel and find another payer. Use this tool after a fleet program refuses payment or no answer could be obtained; do not use it to select a different payer directly, because that needs a payer model this endpoint does not have -- cancel here and record the new payer through whatever assigns one. Preconditions: an authorization must be recorded for this workorder and must not already be GRANTED. Required inputs: workorderId path parameter; resolution (SCOPE_REVISED or CANCELLED) and reason in the request body. Emits a WORKORDER_FLEET_AUTHORIZATION_RESOLVE event. Returns 200 with the authorization, 404 when none is recorded for this workorder, and 409 when the recorded authorization is already GRANTED and needs no resolution. 
     * Resolve A Refused Fleet Payment Authorization
     */
    async resolveWorkorderFleetAuthorizationRaw(requestParameters: ResolveWorkorderFleetAuthorizationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FleetAuthorizationView>> {
        if (requestParameters['workorderId'] == null) {
            throw new runtime.RequiredError(
                'workorderId',
                'Required parameter "workorderId" was null or undefined when calling resolveWorkorderFleetAuthorization().'
            );
        }

        if (requestParameters['fleetAuthorizationResolutionRequest'] == null) {
            throw new runtime.RequiredError(
                'fleetAuthorizationResolutionRequest',
                'Required parameter "fleetAuthorizationResolutionRequest" was null or undefined when calling resolveWorkorderFleetAuthorization().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["workorder:fleet_auth:resolve"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/workorders/{workorderId}/fleet-authorization/resolution`.replace(`{${"workorderId"}}`, encodeURIComponent(String(requestParameters['workorderId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: FleetAuthorizationResolutionRequestToJSON(requestParameters['fleetAuthorizationResolutionRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FleetAuthorizationViewFromJSON(jsonValue));
    }

    /**
     * Records what an advisor decided to do about a fleet payment authorization that did not come back granted: revise the scope and try again, or cancel and find another payer. Use this tool after a fleet program refuses payment or no answer could be obtained; do not use it to select a different payer directly, because that needs a payer model this endpoint does not have -- cancel here and record the new payer through whatever assigns one. Preconditions: an authorization must be recorded for this workorder and must not already be GRANTED. Required inputs: workorderId path parameter; resolution (SCOPE_REVISED or CANCELLED) and reason in the request body. Emits a WORKORDER_FLEET_AUTHORIZATION_RESOLVE event. Returns 200 with the authorization, 404 when none is recorded for this workorder, and 409 when the recorded authorization is already GRANTED and needs no resolution. 
     * Resolve A Refused Fleet Payment Authorization
     */
    async resolveWorkorderFleetAuthorization(requestParameters: ResolveWorkorderFleetAuthorizationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FleetAuthorizationView> {
        const response = await this.resolveWorkorderFleetAuthorizationRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
