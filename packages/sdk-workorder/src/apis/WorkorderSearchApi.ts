/* tslint:disable */
/* eslint-disable */
/**
 * POS Workorder Service API
 * Workorder service for managing workorders and tasks
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  PageWorkorderSearchResult,
  Pageable,
  WorkorderNumberRef,
  WorkorderNumberResolveRequest,
} from '../models/index';
import {
    PageWorkorderSearchResultFromJSON,
    PageWorkorderSearchResultToJSON,
    PageableFromJSON,
    PageableToJSON,
    WorkorderNumberRefFromJSON,
    WorkorderNumberRefToJSON,
    WorkorderNumberResolveRequestFromJSON,
    WorkorderNumberResolveRequestToJSON,
} from '../models/index';

export interface ResolveWorkorderNumbersRequest {
    workorderNumberResolveRequest: WorkorderNumberResolveRequest;
}

export interface SearchWorkordersRequest {
    pageable: Pageable;
    q?: string;
    customerId?: string;
    vehicleId?: string;
}

/**
 * 
 */
export class WorkorderSearchApi extends runtime.BaseAPI {

    /**
     * Batch-resolves workorder ids to their human-readable workorder numbers for sibling services that store only the id. Use this tool when known workorder ids need display numbers; do not use searchWorkorders, which is for discovering workorders by text query rather than resolving known ids. Preconditions: none — nulls and duplicates in the id list are silently dropped, and ids with no matching workorder are omitted from the response rather than erroring. Required inputs: a body with workorderIds, a non-empty list of UUIDs. Emits a WORKORDER_NUMBER_RESOLVE audit event; no workorder state changes — this is a read-only projection. Returns 200 with one pairing per found workorder, and 400 when workorderIds is missing or empty. 
     * Resolve Workorder Ids to Numbers
     */
    async resolveWorkorderNumbersRaw(requestParameters: ResolveWorkorderNumbersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<WorkorderNumberRef>>> {
        if (requestParameters['workorderNumberResolveRequest'] == null) {
            throw new runtime.RequiredError(
                'workorderNumberResolveRequest',
                'Required parameter "workorderNumberResolveRequest" was null or undefined when calling resolveWorkorderNumbers().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["workorder:workorder:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/workorders/numbers:resolve`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: WorkorderNumberResolveRequestToJSON(requestParameters['workorderNumberResolveRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(WorkorderNumberRefFromJSON));
    }

    /**
     * Batch-resolves workorder ids to their human-readable workorder numbers for sibling services that store only the id. Use this tool when known workorder ids need display numbers; do not use searchWorkorders, which is for discovering workorders by text query rather than resolving known ids. Preconditions: none — nulls and duplicates in the id list are silently dropped, and ids with no matching workorder are omitted from the response rather than erroring. Required inputs: a body with workorderIds, a non-empty list of UUIDs. Emits a WORKORDER_NUMBER_RESOLVE audit event; no workorder state changes — this is a read-only projection. Returns 200 with one pairing per found workorder, and 400 when workorderIds is missing or empty. 
     * Resolve Workorder Ids to Numbers
     */
    async resolveWorkorderNumbers(requestParameters: ResolveWorkorderNumbersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<WorkorderNumberRef>> {
        const response = await this.resolveWorkorderNumbersRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Searches workorders by free-text query against customer display names or a literal workorder id, optionally narrowed by exact customerId and vehicleId filters, returning a page of rows enriched with customer name, vehicle label, and VIN. Use this tool when finding workorders by customer or id fragments; use listWorkorders instead for an unfiltered listing, and resolveWorkorderNumbers to map known ids to human numbers. Preconditions: customer-name matching depends on the local customer replica; at most 10 name-matched customers are considered per query. Required inputs: none are mandatory — q defaults to an empty string and is treated as a workorder id when it parses as a UUID; page size defaults to 25. Emits a WORKORDER_SEARCH audit event; no workorder state changes — this is a read-only projection. Returns 200 with an empty page when nothing matches; no 404 is produced for empty results. 
     * Search Workorders With Filters
     */
    async searchWorkordersRaw(requestParameters: SearchWorkordersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PageWorkorderSearchResult>> {
        if (requestParameters['pageable'] == null) {
            throw new runtime.RequiredError(
                'pageable',
                'Required parameter "pageable" was null or undefined when calling searchWorkorders().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['q'] != null) {
            queryParameters['q'] = requestParameters['q'];
        }

        if (requestParameters['customerId'] != null) {
            queryParameters['customerId'] = requestParameters['customerId'];
        }

        if (requestParameters['vehicleId'] != null) {
            queryParameters['vehicleId'] = requestParameters['vehicleId'];
        }

        if (requestParameters['pageable'] != null) {
            queryParameters['pageable'] = requestParameters['pageable'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["workorder:workorder:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/workorders/search`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PageWorkorderSearchResultFromJSON(jsonValue));
    }

    /**
     * Searches workorders by free-text query against customer display names or a literal workorder id, optionally narrowed by exact customerId and vehicleId filters, returning a page of rows enriched with customer name, vehicle label, and VIN. Use this tool when finding workorders by customer or id fragments; use listWorkorders instead for an unfiltered listing, and resolveWorkorderNumbers to map known ids to human numbers. Preconditions: customer-name matching depends on the local customer replica; at most 10 name-matched customers are considered per query. Required inputs: none are mandatory — q defaults to an empty string and is treated as a workorder id when it parses as a UUID; page size defaults to 25. Emits a WORKORDER_SEARCH audit event; no workorder state changes — this is a read-only projection. Returns 200 with an empty page when nothing matches; no 404 is produced for empty results. 
     * Search Workorders With Filters
     */
    async searchWorkorders(requestParameters: SearchWorkordersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PageWorkorderSearchResult> {
        const response = await this.searchWorkordersRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
