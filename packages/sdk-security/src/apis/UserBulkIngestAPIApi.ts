/* tslint:disable */
/* eslint-disable */
/**
 * POS Security Service API
 * API documentation for POS Security Service
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  BulkIngestRequestUserBulkIngestRecord,
  BulkIngestResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    BulkIngestRequestUserBulkIngestRecordFromJSON,
    BulkIngestRequestUserBulkIngestRecordToJSON,
    BulkIngestResponseFromJSON,
    BulkIngestResponseToJSON,
} from '../models/index';

export interface BulkIngestUsersRequest {
    bulkIngestRequestUserBulkIngestRecord: BulkIngestRequestUserBulkIngestRecord;
}

/**
 * 
 */
export class UserBulkIngestAPIApi extends runtime.BaseAPI {

    /**
     * Provisions many user accounts at once, each with its roles attached and a password generated inside the service. Use this tool when standing up an environment\'s accounts; use createUser instead when the account needs a known password. Preconditions: every role named must already exist. Required inputs: jobId (UUID), locationId (UUID) and records, each with a username and at least one role. There is deliberately no password field: a bulk file is stored, and a password in it would exist at rest for as long as the upload does. Emits a SECURITY_USER_BULK_INGEST event and a user-created event per row; the generated passwords are returned to no one, so these accounts have no usable login until someone goes through the password reset path. Re-running the same file is safe: an existing username is reported as already provisioned rather than as a failure. Returns 200 with a per-record result; check each result\'s errorCode rather than the status alone, which is USER_INGEST_FAILED with the reason for a row the service refused, or INTERNAL_ERROR with a correlationId to quote for a row lost to a server-side fault. 
     * Provision User Accounts in Bulk
     */
    async bulkIngestUsersRaw(requestParameters: BulkIngestUsersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkIngestResponse>> {
        if (requestParameters['bulkIngestRequestUserBulkIngestRecord'] == null) {
            throw new runtime.RequiredError(
                'bulkIngestRequestUserBulkIngestRecord',
                'Required parameter "bulkIngestRequestUserBulkIngestRecord" was null or undefined when calling bulkIngestUsers().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["security:user:create"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/users/bulk-ingest`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkIngestRequestUserBulkIngestRecordToJSON(requestParameters['bulkIngestRequestUserBulkIngestRecord']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkIngestResponseFromJSON(jsonValue));
    }

    /**
     * Provisions many user accounts at once, each with its roles attached and a password generated inside the service. Use this tool when standing up an environment\'s accounts; use createUser instead when the account needs a known password. Preconditions: every role named must already exist. Required inputs: jobId (UUID), locationId (UUID) and records, each with a username and at least one role. There is deliberately no password field: a bulk file is stored, and a password in it would exist at rest for as long as the upload does. Emits a SECURITY_USER_BULK_INGEST event and a user-created event per row; the generated passwords are returned to no one, so these accounts have no usable login until someone goes through the password reset path. Re-running the same file is safe: an existing username is reported as already provisioned rather than as a failure. Returns 200 with a per-record result; check each result\'s errorCode rather than the status alone, which is USER_INGEST_FAILED with the reason for a row the service refused, or INTERNAL_ERROR with a correlationId to quote for a row lost to a server-side fault. 
     * Provision User Accounts in Bulk
     */
    async bulkIngestUsers(requestParameters: BulkIngestUsersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkIngestResponse> {
        const response = await this.bulkIngestUsersRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
