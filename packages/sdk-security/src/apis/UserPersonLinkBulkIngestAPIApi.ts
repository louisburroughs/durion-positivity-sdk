/* tslint:disable */
/* eslint-disable */
/**
 * POS Security Service API
 * API documentation for POS Security Service
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  BulkIngestRequestUserPersonLinkBulkIngestRecord,
  BulkIngestResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    BulkIngestRequestUserPersonLinkBulkIngestRecordFromJSON,
    BulkIngestRequestUserPersonLinkBulkIngestRecordToJSON,
    BulkIngestResponseFromJSON,
    BulkIngestResponseToJSON,
} from '../models/index';

export interface BulkIngestUserPersonLinksRequest {
    bulkIngestRequestUserPersonLinkBulkIngestRecord: BulkIngestRequestUserPersonLinkBulkIngestRecord;
}

/**
 * 
 */
export class UserPersonLinkBulkIngestAPIApi extends runtime.BaseAPI {

    /**
     * Queues many user-to-person links at once, resolving each row\'s username to an account. Use this tool when connecting seeded accounts to their staff records; use linkUserPerson instead for a single account. Preconditions: each username must name an existing account. The person id is not validated here — pos-people-contact rejects an unknown person when it processes the command, which surfaces later rather than as a row failure. Required inputs: jobId (UUID), locationId (UUID) and records, each with a username and a personId. Emits a SECURITY_USER_PERSON_LINK_BULK_INGEST event and queues a link command per row; linking is asynchronous, so a successful row means the link was accepted rather than applied and the accounts should be read back to confirm the projection landed. Re-running the same file is safe: the consumer upserts by username. Returns 200 with a per-record result; check each result\'s errorCode rather than the status alone, which is USER_PERSON_LINK_INGEST_FAILED with the reason for a row the service refused, or INTERNAL_ERROR with a correlationId to quote for a row lost to a server-side fault. 
     * Link User Accounts to Persons in Bulk
     */
    async bulkIngestUserPersonLinksRaw(requestParameters: BulkIngestUserPersonLinksRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkIngestResponse>> {
        if (requestParameters['bulkIngestRequestUserPersonLinkBulkIngestRecord'] == null) {
            throw new runtime.RequiredError(
                'bulkIngestRequestUserPersonLinkBulkIngestRecord',
                'Required parameter "bulkIngestRequestUserPersonLinkBulkIngestRecord" was null or undefined when calling bulkIngestUserPersonLinks().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["security:user:edit"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/users/person-link/bulk-ingest`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkIngestRequestUserPersonLinkBulkIngestRecordToJSON(requestParameters['bulkIngestRequestUserPersonLinkBulkIngestRecord']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkIngestResponseFromJSON(jsonValue));
    }

    /**
     * Queues many user-to-person links at once, resolving each row\'s username to an account. Use this tool when connecting seeded accounts to their staff records; use linkUserPerson instead for a single account. Preconditions: each username must name an existing account. The person id is not validated here — pos-people-contact rejects an unknown person when it processes the command, which surfaces later rather than as a row failure. Required inputs: jobId (UUID), locationId (UUID) and records, each with a username and a personId. Emits a SECURITY_USER_PERSON_LINK_BULK_INGEST event and queues a link command per row; linking is asynchronous, so a successful row means the link was accepted rather than applied and the accounts should be read back to confirm the projection landed. Re-running the same file is safe: the consumer upserts by username. Returns 200 with a per-record result; check each result\'s errorCode rather than the status alone, which is USER_PERSON_LINK_INGEST_FAILED with the reason for a row the service refused, or INTERNAL_ERROR with a correlationId to quote for a row lost to a server-side fault. 
     * Link User Accounts to Persons in Bulk
     */
    async bulkIngestUserPersonLinks(requestParameters: BulkIngestUserPersonLinksRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkIngestResponse> {
        const response = await this.bulkIngestUserPersonLinksRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
