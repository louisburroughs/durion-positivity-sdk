/* tslint:disable */
/* eslint-disable */
/**
 * POS Security Service API
 * API documentation for POS Security Service
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  BulkIngestRequestRoleBulkIngestRecord,
  BulkIngestResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    BulkIngestRequestRoleBulkIngestRecordFromJSON,
    BulkIngestRequestRoleBulkIngestRecordToJSON,
    BulkIngestResponseFromJSON,
    BulkIngestResponseToJSON,
} from '../models/index';

export interface BulkIngestRolesRequest {
    bulkIngestRequestRoleBulkIngestRecord: BulkIngestRequestRoleBulkIngestRecord;
}

/**
 * 
 */
export class RoleBulkIngestAPIApi extends runtime.BaseAPI {

    /**
     * Creates roles from a bulk-load batch, each with an optional description and optional MCP persona metadata; every role starts with no permissions and no user assignments. Use this tool to provision a set of roles from a load file; do not use it to grant permissions, which is bulkIngestRolePermissions, and do not use createRole, which creates one role at a time. Preconditions: the caller must hold security:role:create. Required inputs: jobId, locationId, and records; each record needs a non-blank name. A role that already exists counts as a success — provisioning is expected to be re-runnable against an environment that is already partly seeded. Persona slots must describe the role rather than instruct the assistant: single line, within the length cap, and free of imperative control verbs. Emits a SECURITY_ROLE_BULK_INGEST event. Returns 200 in all cases; inspect each record\'s errorCode, which is ROLE_INGEST_FAILED with the reason for a row the service refused, or INTERNAL_ERROR with a correlationId to quote for a row lost to a server-side fault. 
     * Provision Roles in Bulk
     */
    async bulkIngestRolesRaw(requestParameters: BulkIngestRolesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkIngestResponse>> {
        if (requestParameters['bulkIngestRequestRoleBulkIngestRecord'] == null) {
            throw new runtime.RequiredError(
                'bulkIngestRequestRoleBulkIngestRecord',
                'Required parameter "bulkIngestRequestRoleBulkIngestRecord" was null or undefined when calling bulkIngestRoles().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["security:role:create"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/roles/bulk-ingest`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkIngestRequestRoleBulkIngestRecordToJSON(requestParameters['bulkIngestRequestRoleBulkIngestRecord']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkIngestResponseFromJSON(jsonValue));
    }

    /**
     * Creates roles from a bulk-load batch, each with an optional description and optional MCP persona metadata; every role starts with no permissions and no user assignments. Use this tool to provision a set of roles from a load file; do not use it to grant permissions, which is bulkIngestRolePermissions, and do not use createRole, which creates one role at a time. Preconditions: the caller must hold security:role:create. Required inputs: jobId, locationId, and records; each record needs a non-blank name. A role that already exists counts as a success — provisioning is expected to be re-runnable against an environment that is already partly seeded. Persona slots must describe the role rather than instruct the assistant: single line, within the length cap, and free of imperative control verbs. Emits a SECURITY_ROLE_BULK_INGEST event. Returns 200 in all cases; inspect each record\'s errorCode, which is ROLE_INGEST_FAILED with the reason for a row the service refused, or INTERNAL_ERROR with a correlationId to quote for a row lost to a server-side fault. 
     * Provision Roles in Bulk
     */
    async bulkIngestRoles(requestParameters: BulkIngestRolesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkIngestResponse> {
        const response = await this.bulkIngestRolesRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
