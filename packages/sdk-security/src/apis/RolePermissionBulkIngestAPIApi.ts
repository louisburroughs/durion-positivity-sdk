/* tslint:disable */
/* eslint-disable */
/**
 * POS Security Service API
 * API documentation for POS Security Service
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  BulkIngestRequestRolePermissionBulkIngestRecord,
  BulkIngestResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    BulkIngestRequestRolePermissionBulkIngestRecordFromJSON,
    BulkIngestRequestRolePermissionBulkIngestRecordToJSON,
    BulkIngestResponseFromJSON,
    BulkIngestResponseToJSON,
} from '../models/index';

export interface BulkIngestRolePermissionsRequest {
    bulkIngestRequestRolePermissionBulkIngestRecord: BulkIngestRequestRolePermissionBulkIngestRecord;
}

/**
 * 
 */
export class RolePermissionBulkIngestAPIApi extends runtime.BaseAPI {

    /**
     * Grants named permissions to existing roles from a bulk-load batch. Use this tool to apply a role-to-permission grant file; do not use it to create roles, which is bulkIngestRoles, and note that both the role and each permission must already exist — permissions are registered by their owning module at startup. Preconditions: the caller must hold security:role:edit. Required inputs: jobId, locationId, and records; each record needs a roleName and at least one permission name. Re-granting a permission a role already holds is a success and does not rewrite who granted it originally, so a grant file is safe to re-apply. Emits a SECURITY_ROLE_PERMISSION_BULK_INGEST event. Returns 200 in all cases; a row naming an unknown role fails with ROLE_PERMISSION_ROLE_UNKNOWN while the rest of the batch continues. 
     * Grant Permissions to Roles in Bulk
     */
    async bulkIngestRolePermissionsRaw(requestParameters: BulkIngestRolePermissionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkIngestResponse>> {
        if (requestParameters['bulkIngestRequestRolePermissionBulkIngestRecord'] == null) {
            throw new runtime.RequiredError(
                'bulkIngestRequestRolePermissionBulkIngestRecord',
                'Required parameter "bulkIngestRequestRolePermissionBulkIngestRecord" was null or undefined when calling bulkIngestRolePermissions().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["security:role:edit"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/roles/permissions/bulk-ingest`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkIngestRequestRolePermissionBulkIngestRecordToJSON(requestParameters['bulkIngestRequestRolePermissionBulkIngestRecord']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkIngestResponseFromJSON(jsonValue));
    }

    /**
     * Grants named permissions to existing roles from a bulk-load batch. Use this tool to apply a role-to-permission grant file; do not use it to create roles, which is bulkIngestRoles, and note that both the role and each permission must already exist — permissions are registered by their owning module at startup. Preconditions: the caller must hold security:role:edit. Required inputs: jobId, locationId, and records; each record needs a roleName and at least one permission name. Re-granting a permission a role already holds is a success and does not rewrite who granted it originally, so a grant file is safe to re-apply. Emits a SECURITY_ROLE_PERMISSION_BULK_INGEST event. Returns 200 in all cases; a row naming an unknown role fails with ROLE_PERMISSION_ROLE_UNKNOWN while the rest of the batch continues. 
     * Grant Permissions to Roles in Bulk
     */
    async bulkIngestRolePermissions(requestParameters: BulkIngestRolePermissionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkIngestResponse> {
        const response = await this.bulkIngestRolePermissionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
