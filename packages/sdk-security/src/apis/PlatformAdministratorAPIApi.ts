/* tslint:disable */
/* eslint-disable */
/**
 * POS Security Service API
 * API documentation for POS Security Service
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ActivationTokenResponse,
  ApiError,
} from '../models/index';
import {
    ActivationTokenResponseFromJSON,
    ActivationTokenResponseToJSON,
    ApiErrorFromJSON,
    ApiErrorToJSON,
} from '../models/index';

export interface MintAdministratorActivationTokenRequest {
    tenantId: string;
    userId: string;
}

/**
 * 
 */
export class PlatformAdministratorAPIApi extends runtime.BaseAPI {

    /**
     * Mints a one-time activation token for the named user of the named tenant and returns it once, together with its expiry. Any earlier token still open for that user is closed. The operator hands the token to the administrator out of band; the administrator exchanges it, unauthenticated, at activateAccount (POST /v1/auth/activate) for the account\'s first password. Use this tool after tenant provisioning (tenant.created → tenant.provisioned) leaves the first ADMIN unable to sign in, or whenever that administrator has lost an unexchanged token; do not use updateUser to set a tenant\'s first password from the platform tenant. Preconditions: the caller must hold platform:tenant:provision and be bound to the platform tenant; the user must exist in the named tenant and still be awaiting activation (credentials expired by provisioning, never signed in). Required inputs: tenantId and userId as path parameters; there is no request body. Emits a SECURITY_PLATFORM_ADMINISTRATOR_ACTIVATION_TOKEN_MINT event and an audit event on the user; only the token\'s SHA-256 is stored, so a lost token is replaced by minting again. Returns 201 with the token and expiresAt (72 hours); 403 with PLATFORM_TENANT_REQUIRED when the caller is bound to a tenant other than the platform tenant; 404 with USER_NOT_FOUND when the user is not in that tenant; 409 with USER_NOT_AWAITING_ACTIVATION when the user has already been activated or signed in, so a live account\'s password is never overwritten. 
     * Mint a First-Administrator Activation Token
     */
    async mintAdministratorActivationTokenRaw(requestParameters: MintAdministratorActivationTokenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ActivationTokenResponse>> {
        if (requestParameters['tenantId'] == null) {
            throw new runtime.RequiredError(
                'tenantId',
                'Required parameter "tenantId" was null or undefined when calling mintAdministratorActivationToken().'
            );
        }

        if (requestParameters['userId'] == null) {
            throw new runtime.RequiredError(
                'userId',
                'Required parameter "userId" was null or undefined when calling mintAdministratorActivationToken().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["platform:tenant:provision"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/platform/tenants/{tenantId}/administrators/{userId}/activation-token`.replace(`{${"tenantId"}}`, encodeURIComponent(String(requestParameters['tenantId']))).replace(`{${"userId"}}`, encodeURIComponent(String(requestParameters['userId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ActivationTokenResponseFromJSON(jsonValue));
    }

    /**
     * Mints a one-time activation token for the named user of the named tenant and returns it once, together with its expiry. Any earlier token still open for that user is closed. The operator hands the token to the administrator out of band; the administrator exchanges it, unauthenticated, at activateAccount (POST /v1/auth/activate) for the account\'s first password. Use this tool after tenant provisioning (tenant.created → tenant.provisioned) leaves the first ADMIN unable to sign in, or whenever that administrator has lost an unexchanged token; do not use updateUser to set a tenant\'s first password from the platform tenant. Preconditions: the caller must hold platform:tenant:provision and be bound to the platform tenant; the user must exist in the named tenant and still be awaiting activation (credentials expired by provisioning, never signed in). Required inputs: tenantId and userId as path parameters; there is no request body. Emits a SECURITY_PLATFORM_ADMINISTRATOR_ACTIVATION_TOKEN_MINT event and an audit event on the user; only the token\'s SHA-256 is stored, so a lost token is replaced by minting again. Returns 201 with the token and expiresAt (72 hours); 403 with PLATFORM_TENANT_REQUIRED when the caller is bound to a tenant other than the platform tenant; 404 with USER_NOT_FOUND when the user is not in that tenant; 409 with USER_NOT_AWAITING_ACTIVATION when the user has already been activated or signed in, so a live account\'s password is never overwritten. 
     * Mint a First-Administrator Activation Token
     */
    async mintAdministratorActivationToken(requestParameters: MintAdministratorActivationTokenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ActivationTokenResponse> {
        const response = await this.mintAdministratorActivationTokenRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
