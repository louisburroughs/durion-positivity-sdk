/* tslint:disable */
/* eslint-disable */
/**
 * POS People Contact Service API
 * Identity, contact, and user-link authority service (ADR-0044 Phase 3)
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  PersonRoleAssignmentRequest,
  ProblemDetail,
  RoleDto,
  UserRoleDto,
} from '../models/index';
import {
    PersonRoleAssignmentRequestFromJSON,
    PersonRoleAssignmentRequestToJSON,
    ProblemDetailFromJSON,
    ProblemDetailToJSON,
    RoleDtoFromJSON,
    RoleDtoToJSON,
    UserRoleDtoFromJSON,
    UserRoleDtoToJSON,
} from '../models/index';

export interface AssignRoleToPersonRequest {
    personUuid: string;
    personRoleAssignmentRequest: PersonRoleAssignmentRequest;
}

export interface ListAssignableRolesRequest {
    personUuid: string;
}

export interface ListRoleAssignmentsRequest {
    personUuid: string;
    includeHistory?: boolean;
    endDate?: Date;
}

export interface RevokePersonRoleAssignmentRequest {
    personUuid: string;
    roleCode: string;
    endDate?: Date;
}

/**
 * 
 */
export class PeopleAccessControlApi extends runtime.BaseAPI {

    /**
     * Assigns a role to a person by delegating to pos-security through the person\'s linked user account, optionally scoped to one location and bounded by a date window. Use this tool to grant access; do not use revokePersonRoleAssignment, which ends an assignment that already exists. Preconditions: the person must have an active user-person link resolving to a pos-security user, and the roleCode must exist in pos-security. Required inputs: personUuid (UUID) as a path parameter and roleCode in the body; locationId (UUID) scopes the role to one location when supplied, and optional startDate/endDate bound the assignment, with endDate required to be on or after startDate. Emits a PEOPLE_CONTACT_ACCESS_ASSIGNMENT_CREATE event; the assignment record itself is created and owned by pos-security. Returns 201 with the created assignment, 400 when roleCode is blank or endDate precedes startDate, and 404 when the person\'s user link, the security user, or the role cannot be resolved. 
     * Assign a Role to a Person
     */
    async assignRoleToPersonRaw(requestParameters: AssignRoleToPersonRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UserRoleDto>> {
        if (requestParameters['personUuid'] == null) {
            throw new runtime.RequiredError(
                'personUuid',
                'Required parameter "personUuid" was null or undefined when calling assignRoleToPerson().'
            );
        }

        if (requestParameters['personRoleAssignmentRequest'] == null) {
            throw new runtime.RequiredError(
                'personRoleAssignmentRequest',
                'Required parameter "personRoleAssignmentRequest" was null or undefined when calling assignRoleToPerson().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people-contact:role:assign"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/people/{personUuid}/access/assignments`.replace(`{${"personUuid"}}`, encodeURIComponent(String(requestParameters['personUuid']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PersonRoleAssignmentRequestToJSON(requestParameters['personRoleAssignmentRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UserRoleDtoFromJSON(jsonValue));
    }

    /**
     * Assigns a role to a person by delegating to pos-security through the person\'s linked user account, optionally scoped to one location and bounded by a date window. Use this tool to grant access; do not use revokePersonRoleAssignment, which ends an assignment that already exists. Preconditions: the person must have an active user-person link resolving to a pos-security user, and the roleCode must exist in pos-security. Required inputs: personUuid (UUID) as a path parameter and roleCode in the body; locationId (UUID) scopes the role to one location when supplied, and optional startDate/endDate bound the assignment, with endDate required to be on or after startDate. Emits a PEOPLE_CONTACT_ACCESS_ASSIGNMENT_CREATE event; the assignment record itself is created and owned by pos-security. Returns 201 with the created assignment, 400 when roleCode is blank or endDate precedes startDate, and 404 when the person\'s user link, the security user, or the role cannot be resolved. 
     * Assign a Role to a Person
     */
    async assignRoleToPerson(requestParameters: AssignRoleToPersonRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UserRoleDto> {
        const response = await this.assignRoleToPersonRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the role catalog a person could be assigned, combining LOCATION-scoped and GLOBAL-scoped roles fetched live from pos-security. Use this tool to populate a role picker before calling assignRoleToPerson; do not use listRoleAssignments, which returns the roles the person already holds. Preconditions: the person record must exist; the roles themselves are owned by pos-security, and no user-person link is needed for this listing. Required inputs: personUuid (UUID) as a path parameter; there is no request body and no filtering. Emits a PEOPLE_CONTACT_ACCESS_ROLES_LIST audit event; no state changes. Returns 404 when the person does not exist, and propagates the pos-security status code when the role listing call fails there. 
     * List Roles Assignable to Person
     */
    async listAssignableRolesRaw(requestParameters: ListAssignableRolesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<RoleDto>>> {
        if (requestParameters['personUuid'] == null) {
            throw new runtime.RequiredError(
                'personUuid',
                'Required parameter "personUuid" was null or undefined when calling listAssignableRoles().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people-contact:role:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/people/{personUuid}/access/roles`.replace(`{${"personUuid"}}`, encodeURIComponent(String(requestParameters['personUuid']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(RoleDtoFromJSON));
    }

    /**
     * Lists the role catalog a person could be assigned, combining LOCATION-scoped and GLOBAL-scoped roles fetched live from pos-security. Use this tool to populate a role picker before calling assignRoleToPerson; do not use listRoleAssignments, which returns the roles the person already holds. Preconditions: the person record must exist; the roles themselves are owned by pos-security, and no user-person link is needed for this listing. Required inputs: personUuid (UUID) as a path parameter; there is no request body and no filtering. Emits a PEOPLE_CONTACT_ACCESS_ROLES_LIST audit event; no state changes. Returns 404 when the person does not exist, and propagates the pos-security status code when the role listing call fails there. 
     * List Roles Assignable to Person
     */
    async listAssignableRoles(requestParameters: ListAssignableRolesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<RoleDto>> {
        const response = await this.listAssignableRolesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the role assignments a person holds in pos-security, resolved through the person\'s active user-person link. Use this tool to inspect the access a person already has; do not use listAssignableRoles, which returns the catalog of roles available for assignment. Preconditions: the person must have an active user-person link, and the linked username must resolve to a pos-security user. Required inputs: personUuid (UUID) as a path parameter; includeHistory defaults to false and adds ended assignments when true, and endDate (ISO date-time) optionally evaluates assignments as of that moment. Emits a PEOPLE_CONTACT_ACCESS_ASSIGNMENTS_LIST audit event; no state changes. Returns 404 when the person has no user link or the linked username has no security user. 
     * List a Person\'s Role Assignments
     */
    async listRoleAssignmentsRaw(requestParameters: ListRoleAssignmentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<UserRoleDto>>> {
        if (requestParameters['personUuid'] == null) {
            throw new runtime.RequiredError(
                'personUuid',
                'Required parameter "personUuid" was null or undefined when calling listRoleAssignments().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['includeHistory'] != null) {
            queryParameters['includeHistory'] = requestParameters['includeHistory'];
        }

        if (requestParameters['endDate'] != null) {
            queryParameters['endDate'] = (requestParameters['endDate'] as any).toISOString();
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people-contact:role:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/people/{personUuid}/access/assignments`.replace(`{${"personUuid"}}`, encodeURIComponent(String(requestParameters['personUuid']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(UserRoleDtoFromJSON));
    }

    /**
     * Lists the role assignments a person holds in pos-security, resolved through the person\'s active user-person link. Use this tool to inspect the access a person already has; do not use listAssignableRoles, which returns the catalog of roles available for assignment. Preconditions: the person must have an active user-person link, and the linked username must resolve to a pos-security user. Required inputs: personUuid (UUID) as a path parameter; includeHistory defaults to false and adds ended assignments when true, and endDate (ISO date-time) optionally evaluates assignments as of that moment. Emits a PEOPLE_CONTACT_ACCESS_ASSIGNMENTS_LIST audit event; no state changes. Returns 404 when the person has no user link or the linked username has no security user. 
     * List a Person\'s Role Assignments
     */
    async listRoleAssignments(requestParameters: ListRoleAssignmentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<UserRoleDto>> {
        const response = await this.listRoleAssignmentsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Revokes one of a person\'s role assignments by delegating to pos-security through the person\'s linked user account. Use this tool to end access granted with assignRoleToPerson; do not use unlinkUserFromPerson, which severs the whole user-person link rather than a single role. Preconditions: the person must have an active user-person link resolving to a pos-security user, and an assignment for the roleCode must exist there. Required inputs: personUuid (UUID) and roleCode as path parameters; endDate (ISO date-time) optionally ends the assignment at that moment instead of immediately. Emits a PEOPLE_CONTACT_ACCESS_ASSIGNMENT_REVOKE event; the assignment state change is recorded in pos-security. Returns 204 on success, and 404 when the person has no user link, the security user cannot be resolved, or pos-security has no matching assignment. 
     * Revoke Role Assignment from Person
     */
    async revokePersonRoleAssignmentRaw(requestParameters: RevokePersonRoleAssignmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['personUuid'] == null) {
            throw new runtime.RequiredError(
                'personUuid',
                'Required parameter "personUuid" was null or undefined when calling revokePersonRoleAssignment().'
            );
        }

        if (requestParameters['roleCode'] == null) {
            throw new runtime.RequiredError(
                'roleCode',
                'Required parameter "roleCode" was null or undefined when calling revokePersonRoleAssignment().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['endDate'] != null) {
            queryParameters['endDate'] = (requestParameters['endDate'] as any).toISOString();
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people-contact:role:revoke"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/people/{personUuid}/access/assignments/{roleCode}`.replace(`{${"personUuid"}}`, encodeURIComponent(String(requestParameters['personUuid']))).replace(`{${"roleCode"}}`, encodeURIComponent(String(requestParameters['roleCode']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Revokes one of a person\'s role assignments by delegating to pos-security through the person\'s linked user account. Use this tool to end access granted with assignRoleToPerson; do not use unlinkUserFromPerson, which severs the whole user-person link rather than a single role. Preconditions: the person must have an active user-person link resolving to a pos-security user, and an assignment for the roleCode must exist there. Required inputs: personUuid (UUID) and roleCode as path parameters; endDate (ISO date-time) optionally ends the assignment at that moment instead of immediately. Emits a PEOPLE_CONTACT_ACCESS_ASSIGNMENT_REVOKE event; the assignment state change is recorded in pos-security. Returns 204 on success, and 404 when the person has no user link, the security user cannot be resolved, or pos-security has no matching assignment. 
     * Revoke Role Assignment from Person
     */
    async revokePersonRoleAssignment(requestParameters: RevokePersonRoleAssignmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.revokePersonRoleAssignmentRaw(requestParameters, initOverrides);
    }

}
