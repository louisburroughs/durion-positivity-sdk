/* tslint:disable */
/* eslint-disable */
/**
 * POS People Contact Service API
 * Identity, contact, and user-link authority service (ADR-0044 Phase 3)
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CreateUserLinkRequest,
  LinkUserToPersonRequest,
  PersonResponse,
  UserPersonLinkResponse,
} from '../models/index';
import {
    CreateUserLinkRequestFromJSON,
    CreateUserLinkRequestToJSON,
    LinkUserToPersonRequestFromJSON,
    LinkUserToPersonRequestToJSON,
    PersonResponseFromJSON,
    PersonResponseToJSON,
    UserPersonLinkResponseFromJSON,
    UserPersonLinkResponseToJSON,
} from '../models/index';

export interface CreateUserPersonLinkRequest {
    createUserLinkRequest: CreateUserLinkRequest;
}

export interface GetLinksByPersonIdRequest {
    personId: string;
}

export interface GetPersonByUsernameRequest {
    username: string;
}

export interface LinkUserToPersonOperationRequest {
    linkUserToPersonRequest: LinkUserToPersonRequest;
}

export interface ListUsernamesForPersonRequest {
    personId: string;
}

export interface UnlinkUserFromPersonRequest {
    username: string;
}

/**
 * 
 */
export class UserPersonLinkingAPIApi extends runtime.BaseAPI {

    /**
     * Creates a user-person link from the minimal administrative payload of username and personId only; the link is stored with linkType PRIMARY. Use this tool for administrative linking when linkType and notes are not needed; do not use linkUserToPerson, which accepts the same pair plus an explicit linkType and notes. Preconditions: the person must exist, and the username must not already be linked to a different person; one username maps to at most one person. Required inputs: username and personId (UUID); both are mandatory and there are no other fields. Emits a PEOPLE_CONTACT_USER_LINK_CREATE event and queues a link-updated fact on the transactional outbox. Returns 201 when the link is created, 200 when the identical link already exists (the call is idempotent), 404 when the person does not exist, and 409 when the username is already linked to a different person. 
     * Create User Person Link as Admin
     */
    async createUserPersonLinkRaw(requestParameters: CreateUserPersonLinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UserPersonLinkResponse>> {
        if (requestParameters['createUserLinkRequest'] == null) {
            throw new runtime.RequiredError(
                'createUserLinkRequest',
                'Required parameter "createUserLinkRequest" was null or undefined when calling createUserPersonLink().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people-contact:userLink:write"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/people/user-links`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateUserLinkRequestToJSON(requestParameters['createUserLinkRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UserPersonLinkResponseFromJSON(jsonValue));
    }

    /**
     * Creates a user-person link from the minimal administrative payload of username and personId only; the link is stored with linkType PRIMARY. Use this tool for administrative linking when linkType and notes are not needed; do not use linkUserToPerson, which accepts the same pair plus an explicit linkType and notes. Preconditions: the person must exist, and the username must not already be linked to a different person; one username maps to at most one person. Required inputs: username and personId (UUID); both are mandatory and there are no other fields. Emits a PEOPLE_CONTACT_USER_LINK_CREATE event and queues a link-updated fact on the transactional outbox. Returns 201 when the link is created, 200 when the identical link already exists (the call is idempotent), 404 when the person does not exist, and 409 when the username is already linked to a different person. 
     * Create User Person Link as Admin
     */
    async createUserPersonLink(requestParameters: CreateUserPersonLinkRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UserPersonLinkResponse> {
        const response = await this.createUserPersonLinkRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the full user-person link records for a person, including linkType, notes, creator and creation time. Use this tool when link metadata is needed; use listUsernamesForPerson instead when only the usernames matter, and getPersonByUsername to go from a username to its person. Preconditions: the person record must exist; a person with no links yields an empty list. Required inputs: personId (UUID) as a path parameter; there is no request body. Emits a PEOPLE_CONTACT_USER_LINK_GET audit event; no state changes. Returns 404 when the person does not exist. 
     * Get User Links for Person
     */
    async getLinksByPersonIdRaw(requestParameters: GetLinksByPersonIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UserPersonLinkResponse>> {
        if (requestParameters['personId'] == null) {
            throw new runtime.RequiredError(
                'personId',
                'Required parameter "personId" was null or undefined when calling getLinksByPersonId().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people-contact:userLink:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/people/user-links/{personId}`.replace(`{${"personId"}}`, encodeURIComponent(String(requestParameters['personId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UserPersonLinkResponseFromJSON(jsonValue));
    }

    /**
     * Returns the full user-person link records for a person, including linkType, notes, creator and creation time. Use this tool when link metadata is needed; use listUsernamesForPerson instead when only the usernames matter, and getPersonByUsername to go from a username to its person. Preconditions: the person record must exist; a person with no links yields an empty list. Required inputs: personId (UUID) as a path parameter; there is no request body. Emits a PEOPLE_CONTACT_USER_LINK_GET audit event; no state changes. Returns 404 when the person does not exist. 
     * Get User Links for Person
     */
    async getLinksByPersonId(requestParameters: GetLinksByPersonIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UserPersonLinkResponse> {
        const response = await this.getLinksByPersonIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the person record linked to a username, including emails, work phone numbers and the username itself. Use this tool to translate a login identity into its person record; use getCurrentPerson instead when the target is the authenticated caller, and getLinksByPersonId to go from a person to its links. Preconditions: a user-person link must exist for the username, and the linked person record must still exist. Required inputs: username as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no link exists for the username or the linked person record is missing. 
     * Get Person Linked to Username
     */
    async getPersonByUsernameRaw(requestParameters: GetPersonByUsernameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PersonResponse>> {
        if (requestParameters['username'] == null) {
            throw new runtime.RequiredError(
                'username',
                'Required parameter "username" was null or undefined when calling getPersonByUsername().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people-contact:userLink:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/people/users/{username}/person`.replace(`{${"username"}}`, encodeURIComponent(String(requestParameters['username']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PersonResponseFromJSON(jsonValue));
    }

    /**
     * Returns the person record linked to a username, including emails, work phone numbers and the username itself. Use this tool to translate a login identity into its person record; use getCurrentPerson instead when the target is the authenticated caller, and getLinksByPersonId to go from a person to its links. Preconditions: a user-person link must exist for the username, and the linked person record must still exist. Required inputs: username as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no link exists for the username or the linked person record is missing. 
     * Get Person Linked to Username
     */
    async getPersonByUsername(requestParameters: GetPersonByUsernameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PersonResponse> {
        const response = await this.getPersonByUsernameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Links a security user account to a person record, optionally recording a linkType and notes; the link is what lets a login act as that person. Use this tool when associating a user with a person, for example during onboarding; do not use createUserPersonLink, which is the minimal admin variant without linkType or notes, and do not use createPerson, which creates the identity record itself. Preconditions: the person must exist, and the username must not already be linked to a different person; one username maps to at most one person. Required inputs: username and personId (UUID); linkType defaults to PRIMARY and notes is optional. Emits a PEOPLE_CONTACT_USER_LINK_CREATE event and queues a link-updated fact on the transactional outbox. Returns 201 when the link is created, 200 when the identical link already exists (the call is idempotent), 404 when the person does not exist, and 409 when the username is already linked to a different person. 
     * Link User to Person Record
     */
    async linkUserToPersonRaw(requestParameters: LinkUserToPersonOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UserPersonLinkResponse>> {
        if (requestParameters['linkUserToPersonRequest'] == null) {
            throw new runtime.RequiredError(
                'linkUserToPersonRequest',
                'Required parameter "linkUserToPersonRequest" was null or undefined when calling linkUserToPerson().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people-contact:userLink:write"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/people/users/link`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LinkUserToPersonRequestToJSON(requestParameters['linkUserToPersonRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UserPersonLinkResponseFromJSON(jsonValue));
    }

    /**
     * Links a security user account to a person record, optionally recording a linkType and notes; the link is what lets a login act as that person. Use this tool when associating a user with a person, for example during onboarding; do not use createUserPersonLink, which is the minimal admin variant without linkType or notes, and do not use createPerson, which creates the identity record itself. Preconditions: the person must exist, and the username must not already be linked to a different person; one username maps to at most one person. Required inputs: username and personId (UUID); linkType defaults to PRIMARY and notes is optional. Emits a PEOPLE_CONTACT_USER_LINK_CREATE event and queues a link-updated fact on the transactional outbox. Returns 201 when the link is created, 200 when the identical link already exists (the call is idempotent), 404 when the person does not exist, and 409 when the username is already linked to a different person. 
     * Link User to Person Record
     */
    async linkUserToPerson(requestParameters: LinkUserToPersonOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UserPersonLinkResponse> {
        const response = await this.linkUserToPersonRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists every username linked to a person record as a flat array of strings. Use this tool to see which logins can act as a person; use getLinksByPersonId instead when the full link records with linkType, notes and audit fields are needed. Preconditions: the person record must exist; a person with no links yields an empty list. Required inputs: personId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when the person does not exist. 
     * List Usernames Linked to Person
     */
    async listUsernamesForPersonRaw(requestParameters: ListUsernamesForPersonRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<string>>> {
        if (requestParameters['personId'] == null) {
            throw new runtime.RequiredError(
                'personId',
                'Required parameter "personId" was null or undefined when calling listUsernamesForPerson().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people-contact:userLink:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/people/{personId}/users`.replace(`{${"personId"}}`, encodeURIComponent(String(requestParameters['personId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Lists every username linked to a person record as a flat array of strings. Use this tool to see which logins can act as a person; use getLinksByPersonId instead when the full link records with linkType, notes and audit fields are needed. Preconditions: the person record must exist; a person with no links yields an empty list. Required inputs: personId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when the person does not exist. 
     * List Usernames Linked to Person
     */
    async listUsernamesForPerson(requestParameters: ListUsernamesForPersonRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<string>> {
        const response = await this.listUsernamesForPersonRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes the link between a security user and its person record, leaving both the user account and the person untouched. Use this tool to sever a login from an identity, including before deletePerson on a person that still has linked users; do not use revokePersonRoleAssignment, which removes a single role rather than the whole link. Preconditions: a user-person link must exist for the username. Required inputs: username as a path parameter; there is no request body. Emits a PEOPLE_CONTACT_USER_PERSON_LINK_DELETE event and queues a link-removed fact on the transactional outbox. Returns 204 on success, and 404 when no link exists for the username. 
     * Unlink User from Person Record
     */
    async unlinkUserFromPersonRaw(requestParameters: UnlinkUserFromPersonRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['username'] == null) {
            throw new runtime.RequiredError(
                'username',
                'Required parameter "username" was null or undefined when calling unlinkUserFromPerson().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people-contact:userLink:write"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/people/users/{username}/link`.replace(`{${"username"}}`, encodeURIComponent(String(requestParameters['username']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes the link between a security user and its person record, leaving both the user account and the person untouched. Use this tool to sever a login from an identity, including before deletePerson on a person that still has linked users; do not use revokePersonRoleAssignment, which removes a single role rather than the whole link. Preconditions: a user-person link must exist for the username. Required inputs: username as a path parameter; there is no request body. Emits a PEOPLE_CONTACT_USER_PERSON_LINK_DELETE event and queues a link-removed fact on the transactional outbox. Returns 204 on success, and 404 when no link exists for the username. 
     * Unlink User from Person Record
     */
    async unlinkUserFromPerson(requestParameters: UnlinkUserFromPersonRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.unlinkUserFromPersonRaw(requestParameters, initOverrides);
    }

}
