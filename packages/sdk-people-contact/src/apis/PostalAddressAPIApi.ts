/* tslint:disable */
/* eslint-disable */
/**
 * POS People Contact Service API
 * Identity, contact, and user-link authority service (ADR-0044 Phase 3)
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  PostalAddressDto,
  ProblemDetail,
} from '../models/index';
import {
    PostalAddressDtoFromJSON,
    PostalAddressDtoToJSON,
    ProblemDetailFromJSON,
    ProblemDetailToJSON,
} from '../models/index';

export interface DeleteOrganizationPostalAddressRequest {
    organizationId: string;
}

export interface DeletePersonPostalAddressRequest {
    personId: string;
}

export interface GetOrganizationPostalAddressRequest {
    organizationId: string;
}

export interface GetPersonPostalAddressRequest {
    personId: string;
}

export interface PutOrganizationPostalAddressRequest {
    organizationId: string;
    postalAddressDto: PostalAddressDto;
}

export interface PutPersonPostalAddressRequest {
    personId: string;
    postalAddressDto: PostalAddressDto;
}

/**
 * 
 */
export class PostalAddressAPIApi extends runtime.BaseAPI {

    /**
     * Removes the postal address on file for a CRM organization party; the operation is idempotent and succeeds when none exists. Use this tool to clear an organization\'s mailing address; use deletePersonPostalAddress instead for person parties. Preconditions: none; a missing address is treated as already deleted. Required inputs: organizationId (UUID) as a path parameter; there is no request body. Emits a PEOPLE_CONTACT_ORG_ADDRESS_DELETE event, and an organization-address-removed fact is published only when an address actually existed. Returns 204 whether or not an address was on file. 
     * Delete an Organization\'s Postal Address
     */
    async deleteOrganizationPostalAddressRaw(requestParameters: DeleteOrganizationPostalAddressRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling deleteOrganizationPostalAddress().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people-contact:organization:edit"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/organizations/{organizationId}/postal-address`.replace(`{${"organizationId"}}`, encodeURIComponent(String(requestParameters['organizationId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes the postal address on file for a CRM organization party; the operation is idempotent and succeeds when none exists. Use this tool to clear an organization\'s mailing address; use deletePersonPostalAddress instead for person parties. Preconditions: none; a missing address is treated as already deleted. Required inputs: organizationId (UUID) as a path parameter; there is no request body. Emits a PEOPLE_CONTACT_ORG_ADDRESS_DELETE event, and an organization-address-removed fact is published only when an address actually existed. Returns 204 whether or not an address was on file. 
     * Delete an Organization\'s Postal Address
     */
    async deleteOrganizationPostalAddress(requestParameters: DeleteOrganizationPostalAddressRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteOrganizationPostalAddressRaw(requestParameters, initOverrides);
    }

    /**
     * Removes the postal address on file for a person; the operation is idempotent and succeeds when none exists. Use this tool to clear a person\'s mailing address; do not use deletePerson, which removes the entire identity record. Preconditions: none; a missing address is treated as already deleted. Required inputs: personId (UUID) as a path parameter; there is no request body. Emits a PEOPLE_CONTACT_PERSON_ADDRESS_DELETE event, and queues a person.updated identity fact only when an address actually existed. Returns 204 whether or not an address was on file. 
     * Delete a Person\'s Postal Address
     */
    async deletePersonPostalAddressRaw(requestParameters: DeletePersonPostalAddressRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['personId'] == null) {
            throw new runtime.RequiredError(
                'personId',
                'Required parameter "personId" was null or undefined when calling deletePersonPostalAddress().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people-contact:person:edit"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/people/{personId}/postal-address`.replace(`{${"personId"}}`, encodeURIComponent(String(requestParameters['personId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes the postal address on file for a person; the operation is idempotent and succeeds when none exists. Use this tool to clear a person\'s mailing address; do not use deletePerson, which removes the entire identity record. Preconditions: none; a missing address is treated as already deleted. Required inputs: personId (UUID) as a path parameter; there is no request body. Emits a PEOPLE_CONTACT_PERSON_ADDRESS_DELETE event, and queues a person.updated identity fact only when an address actually existed. Returns 204 whether or not an address was on file. 
     * Delete a Person\'s Postal Address
     */
    async deletePersonPostalAddress(requestParameters: DeletePersonPostalAddressRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deletePersonPostalAddressRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the structured postal address on file for a CRM organization party; the organization id is an external pos-customer party reference stored verbatim. Use this tool when reading an organization\'s mailing address; use getPersonPostalAddress instead for person parties. Preconditions: an address must already have been stored for the organization with putOrganizationPostalAddress. Required inputs: organizationId (the pos-customer commercial party UUID) as a path parameter; there is no request body. Emits a PEOPLE_CONTACT_ORG_ADDRESS_GET audit event; no state changes. Returns 404 when no address is on file for the organization. 
     * Get an Organization\'s Postal Address
     */
    async getOrganizationPostalAddressRaw(requestParameters: GetOrganizationPostalAddressRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PostalAddressDto>> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling getOrganizationPostalAddress().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people-contact:organization:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/organizations/{organizationId}/postal-address`.replace(`{${"organizationId"}}`, encodeURIComponent(String(requestParameters['organizationId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PostalAddressDtoFromJSON(jsonValue));
    }

    /**
     * Returns the structured postal address on file for a CRM organization party; the organization id is an external pos-customer party reference stored verbatim. Use this tool when reading an organization\'s mailing address; use getPersonPostalAddress instead for person parties. Preconditions: an address must already have been stored for the organization with putOrganizationPostalAddress. Required inputs: organizationId (the pos-customer commercial party UUID) as a path parameter; there is no request body. Emits a PEOPLE_CONTACT_ORG_ADDRESS_GET audit event; no state changes. Returns 404 when no address is on file for the organization. 
     * Get an Organization\'s Postal Address
     */
    async getOrganizationPostalAddress(requestParameters: GetOrganizationPostalAddressRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PostalAddressDto> {
        const response = await this.getOrganizationPostalAddressRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the single structured postal address on file for a person; pos-people-contact is the postal-address authority for person parties (FI-4). Use this tool when reading a person\'s mailing address; use getOrganizationPostalAddress instead for CRM organization parties. Preconditions: an address must already have been stored for the person with putPersonPostalAddress. Required inputs: personId (UUID) as a path parameter; there is no request body. Emits a PEOPLE_CONTACT_PERSON_ADDRESS_GET audit event; no state changes. Returns 404 when no address is on file for the person. 
     * Get a Person\'s Postal Address
     */
    async getPersonPostalAddressRaw(requestParameters: GetPersonPostalAddressRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PostalAddressDto>> {
        if (requestParameters['personId'] == null) {
            throw new runtime.RequiredError(
                'personId',
                'Required parameter "personId" was null or undefined when calling getPersonPostalAddress().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people-contact:person:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/people/{personId}/postal-address`.replace(`{${"personId"}}`, encodeURIComponent(String(requestParameters['personId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PostalAddressDtoFromJSON(jsonValue));
    }

    /**
     * Returns the single structured postal address on file for a person; pos-people-contact is the postal-address authority for person parties (FI-4). Use this tool when reading a person\'s mailing address; use getOrganizationPostalAddress instead for CRM organization parties. Preconditions: an address must already have been stored for the person with putPersonPostalAddress. Required inputs: personId (UUID) as a path parameter; there is no request body. Emits a PEOPLE_CONTACT_PERSON_ADDRESS_GET audit event; no state changes. Returns 404 when no address is on file for the person. 
     * Get a Person\'s Postal Address
     */
    async getPersonPostalAddress(requestParameters: GetPersonPostalAddressRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PostalAddressDto> {
        const response = await this.getPersonPostalAddressRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates or replaces the single structured postal address for a CRM organization party. Use this tool for organization mailing addresses; use putPersonPostalAddress instead for person parties. Preconditions: none in this module; the organization id is an external pos-customer party reference stored verbatim without an existence check. Required inputs: organizationId (UUID) as a path parameter, plus line1 and an ISO 3166-1 alpha-2 countryCode in the body; line2, city, region and postalCode are optional free text. Emits a PEOPLE_CONTACT_ORG_ADDRESS_PUT event and an organization-address-updated fact for downstream consumers. Returns 200 with the stored address, and 422 when line1 is blank or countryCode is not a valid ISO 3166-1 alpha-2 code. 
     * Create or Replace Organization Postal Address
     */
    async putOrganizationPostalAddressRaw(requestParameters: PutOrganizationPostalAddressRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PostalAddressDto>> {
        if (requestParameters['organizationId'] == null) {
            throw new runtime.RequiredError(
                'organizationId',
                'Required parameter "organizationId" was null or undefined when calling putOrganizationPostalAddress().'
            );
        }

        if (requestParameters['postalAddressDto'] == null) {
            throw new runtime.RequiredError(
                'postalAddressDto',
                'Required parameter "postalAddressDto" was null or undefined when calling putOrganizationPostalAddress().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people-contact:organization:edit"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/organizations/{organizationId}/postal-address`.replace(`{${"organizationId"}}`, encodeURIComponent(String(requestParameters['organizationId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: PostalAddressDtoToJSON(requestParameters['postalAddressDto']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PostalAddressDtoFromJSON(jsonValue));
    }

    /**
     * Creates or replaces the single structured postal address for a CRM organization party. Use this tool for organization mailing addresses; use putPersonPostalAddress instead for person parties. Preconditions: none in this module; the organization id is an external pos-customer party reference stored verbatim without an existence check. Required inputs: organizationId (UUID) as a path parameter, plus line1 and an ISO 3166-1 alpha-2 countryCode in the body; line2, city, region and postalCode are optional free text. Emits a PEOPLE_CONTACT_ORG_ADDRESS_PUT event and an organization-address-updated fact for downstream consumers. Returns 200 with the stored address, and 422 when line1 is blank or countryCode is not a valid ISO 3166-1 alpha-2 code. 
     * Create or Replace Organization Postal Address
     */
    async putOrganizationPostalAddress(requestParameters: PutOrganizationPostalAddressRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PostalAddressDto> {
        const response = await this.putOrganizationPostalAddressRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates or replaces the single structured postal address for a person; the shape is country-agnostic, with free-text region and an optional postalCode. Use this tool for person mailing addresses; use putOrganizationPostalAddress instead for CRM organization parties, and do not use replaceContactPoints, which manages email and phone channels. Preconditions: the person record must exist; any previously stored address is overwritten in place. Required inputs: line1 and an ISO 3166-1 alpha-2 countryCode (stored upper-case); line2, city, region and postalCode are optional free text, trimmed, with blanks stored as null. Emits a PEOPLE_CONTACT_PERSON_ADDRESS_PUT event and queues a person.updated identity fact carrying the new address. Returns 200 with the stored address, 404 when the person does not exist, and 422 when line1 is blank or countryCode is not a valid ISO 3166-1 alpha-2 code. 
     * Create or Replace Person Postal Address
     */
    async putPersonPostalAddressRaw(requestParameters: PutPersonPostalAddressRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PostalAddressDto>> {
        if (requestParameters['personId'] == null) {
            throw new runtime.RequiredError(
                'personId',
                'Required parameter "personId" was null or undefined when calling putPersonPostalAddress().'
            );
        }

        if (requestParameters['postalAddressDto'] == null) {
            throw new runtime.RequiredError(
                'postalAddressDto',
                'Required parameter "postalAddressDto" was null or undefined when calling putPersonPostalAddress().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["people-contact:person:edit"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/people/{personId}/postal-address`.replace(`{${"personId"}}`, encodeURIComponent(String(requestParameters['personId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: PostalAddressDtoToJSON(requestParameters['postalAddressDto']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PostalAddressDtoFromJSON(jsonValue));
    }

    /**
     * Creates or replaces the single structured postal address for a person; the shape is country-agnostic, with free-text region and an optional postalCode. Use this tool for person mailing addresses; use putOrganizationPostalAddress instead for CRM organization parties, and do not use replaceContactPoints, which manages email and phone channels. Preconditions: the person record must exist; any previously stored address is overwritten in place. Required inputs: line1 and an ISO 3166-1 alpha-2 countryCode (stored upper-case); line2, city, region and postalCode are optional free text, trimmed, with blanks stored as null. Emits a PEOPLE_CONTACT_PERSON_ADDRESS_PUT event and queues a person.updated identity fact carrying the new address. Returns 200 with the stored address, 404 when the person does not exist, and 422 when line1 is blank or countryCode is not a valid ISO 3166-1 alpha-2 code. 
     * Create or Replace Person Postal Address
     */
    async putPersonPostalAddress(requestParameters: PutPersonPostalAddressRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PostalAddressDto> {
        const response = await this.putPersonPostalAddressRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
