/* tslint:disable */
/* eslint-disable */
/**
 * OpenAPI definition
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  SystemPromptRequest,
  SystemPromptResponse,
} from '../models/index';
import {
    SystemPromptRequestFromJSON,
    SystemPromptRequestToJSON,
    SystemPromptResponseFromJSON,
    SystemPromptResponseToJSON,
} from '../models/index';

export interface CreateSystemPromptRequest {
    systemPromptRequest: SystemPromptRequest;
}

export interface DeleteSystemPromptRequest {
    id: string;
}

export interface GetSystemPromptRequest {
    id: string;
}

export interface UpdateSystemPromptRequest {
    id: string;
    systemPromptRequest: SystemPromptRequest;
}

/**
 * 
 */
export class SystemPromptsApi extends runtime.BaseAPI {

    /**
     * Creates a reusable MCP system prompt that agent orchestration can assign to assistant agents. Use this tool to add a new named prompt; do not use updateSystemPrompt, which edits a prompt that already exists. Preconditions: no prompt may already use the requested name, which is unique across prompts. Required inputs: name (unique, human-readable) and content (the prompt text); both are mandatory and non-blank. Emits a MCP_SYSTEM_PROMPT_CREATE event and invalidates any cached agents built from a prompt of that name so they rebuild with the new content. Returns 201 with the stored prompt, and 409 when a prompt with the same name already exists. 
     * Create an MCP System Prompt
     */
    async createSystemPromptRaw(requestParameters: CreateSystemPromptRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SystemPromptResponse>> {
        if (requestParameters['systemPromptRequest'] == null) {
            throw new runtime.RequiredError(
                'systemPromptRequest',
                'Required parameter "systemPromptRequest" was null or undefined when calling createSystemPrompt().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        const response = await this.request({
            path: `/v1/prompts`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SystemPromptRequestToJSON(requestParameters['systemPromptRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SystemPromptResponseFromJSON(jsonValue));
    }

    /**
     * Creates a reusable MCP system prompt that agent orchestration can assign to assistant agents. Use this tool to add a new named prompt; do not use updateSystemPrompt, which edits a prompt that already exists. Preconditions: no prompt may already use the requested name, which is unique across prompts. Required inputs: name (unique, human-readable) and content (the prompt text); both are mandatory and non-blank. Emits a MCP_SYSTEM_PROMPT_CREATE event and invalidates any cached agents built from a prompt of that name so they rebuild with the new content. Returns 201 with the stored prompt, and 409 when a prompt with the same name already exists. 
     * Create an MCP System Prompt
     */
    async createSystemPrompt(requestParameters: CreateSystemPromptRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SystemPromptResponse> {
        const response = await this.createSystemPromptRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Deletes an MCP system prompt so it can no longer be assigned to agent sessions. Use this tool to retire a prompt; do not use updateSystemPrompt, which keeps the prompt and changes its fields. Preconditions: none; deleting an id that does not exist is a no-op. Required inputs: id (UUID) as a path parameter; there is no request body. Emits a MCP_SYSTEM_PROMPT_DELETE event and, when the prompt existed, invalidates cached agents that were built from it. Returns 204 whether or not the prompt existed, making the call idempotent. 
     * Delete an MCP System Prompt
     */
    async deleteSystemPromptRaw(requestParameters: DeleteSystemPromptRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteSystemPrompt().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const response = await this.request({
            path: `/v1/prompts/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes an MCP system prompt so it can no longer be assigned to agent sessions. Use this tool to retire a prompt; do not use updateSystemPrompt, which keeps the prompt and changes its fields. Preconditions: none; deleting an id that does not exist is a no-op. Required inputs: id (UUID) as a path parameter; there is no request body. Emits a MCP_SYSTEM_PROMPT_DELETE event and, when the prompt existed, invalidates cached agents that were built from it. Returns 204 whether or not the prompt existed, making the call idempotent. 
     * Delete an MCP System Prompt
     */
    async deleteSystemPrompt(requestParameters: DeleteSystemPromptRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteSystemPromptRaw(requestParameters, initOverrides);
    }

    /**
     * Returns a single MCP system prompt by its identifier, including its full content. Use this tool when the prompt id (UUID) is already known; use listSystemPrompts instead to discover ids or find a prompt by name. Preconditions: a system prompt with the given id must exist. Required inputs: id (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no system prompt exists for the supplied id. 
     * Get an MCP System Prompt
     */
    async getSystemPromptRaw(requestParameters: GetSystemPromptRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SystemPromptResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getSystemPrompt().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const response = await this.request({
            path: `/v1/prompts/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SystemPromptResponseFromJSON(jsonValue));
    }

    /**
     * Returns a single MCP system prompt by its identifier, including its full content. Use this tool when the prompt id (UUID) is already known; use listSystemPrompts instead to discover ids or find a prompt by name. Preconditions: a system prompt with the given id must exist. Required inputs: id (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no system prompt exists for the supplied id. 
     * Get an MCP System Prompt
     */
    async getSystemPrompt(requestParameters: GetSystemPromptRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SystemPromptResponse> {
        const response = await this.getSystemPromptRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every reusable MCP system prompt with its name and content. Use this tool to browse prompts or to find a prompt id by name; use getSystemPrompt instead when the id is already known. Preconditions: none; an empty list simply means no prompts have been created yet. Required inputs: none; there are no filters, no paging and no request body. No events are emitted and no state changes; this is a read-only projection. Returns 200 with the full list of prompts. 
     * List All MCP System Prompts
     */
    async listSystemPromptsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<SystemPromptResponse>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const response = await this.request({
            path: `/v1/prompts`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(SystemPromptResponseFromJSON));
    }

    /**
     * Returns every reusable MCP system prompt with its name and content. Use this tool to browse prompts or to find a prompt id by name; use getSystemPrompt instead when the id is already known. Preconditions: none; an empty list simply means no prompts have been created yet. Required inputs: none; there are no filters, no paging and no request body. No events are emitted and no state changes; this is a read-only projection. Returns 200 with the full list of prompts. 
     * List All MCP System Prompts
     */
    async listSystemPrompts(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<SystemPromptResponse>> {
        const response = await this.listSystemPromptsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Replaces the name and content of an existing MCP system prompt. Use this tool to edit prompt text or rename a prompt; do not use createSystemPrompt, which adds a new prompt alongside the existing ones. Preconditions: the prompt must exist, and when the name is being changed no other prompt may already use the new name. Required inputs: id (UUID) as a path parameter plus name and content in the body; both body fields are mandatory and fully replace the stored values. Emits a MCP_SYSTEM_PROMPT_UPDATE event and invalidates cached agents built from the prompt so subsequent chats use the new content. Returns 200 with the updated prompt, 404 when no prompt exists for the id, and 409 when the new name is already used by another prompt. 
     * Update an MCP System Prompt
     */
    async updateSystemPromptRaw(requestParameters: UpdateSystemPromptRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SystemPromptResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updateSystemPrompt().'
            );
        }

        if (requestParameters['systemPromptRequest'] == null) {
            throw new runtime.RequiredError(
                'systemPromptRequest',
                'Required parameter "systemPromptRequest" was null or undefined when calling updateSystemPrompt().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        const response = await this.request({
            path: `/v1/prompts/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: SystemPromptRequestToJSON(requestParameters['systemPromptRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SystemPromptResponseFromJSON(jsonValue));
    }

    /**
     * Replaces the name and content of an existing MCP system prompt. Use this tool to edit prompt text or rename a prompt; do not use createSystemPrompt, which adds a new prompt alongside the existing ones. Preconditions: the prompt must exist, and when the name is being changed no other prompt may already use the new name. Required inputs: id (UUID) as a path parameter plus name and content in the body; both body fields are mandatory and fully replace the stored values. Emits a MCP_SYSTEM_PROMPT_UPDATE event and invalidates cached agents built from the prompt so subsequent chats use the new content. Returns 200 with the updated prompt, 404 when no prompt exists for the id, and 409 when the new name is already used by another prompt. 
     * Update an MCP System Prompt
     */
    async updateSystemPrompt(requestParameters: UpdateSystemPromptRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SystemPromptResponse> {
        const response = await this.updateSystemPromptRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
