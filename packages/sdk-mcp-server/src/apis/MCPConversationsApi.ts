/* tslint:disable */
/* eslint-disable */
/**
 * OpenAPI definition
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  AppendMessageRequest,
  ConversationDetail,
  ConversationMessage,
  ConversationPolicy,
  ConversationSummary,
  CreateConversationRequest,
  MessageFeedbackRequest,
  UpdateConversationRequest,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    AppendMessageRequestFromJSON,
    AppendMessageRequestToJSON,
    ConversationDetailFromJSON,
    ConversationDetailToJSON,
    ConversationMessageFromJSON,
    ConversationMessageToJSON,
    ConversationPolicyFromJSON,
    ConversationPolicyToJSON,
    ConversationSummaryFromJSON,
    ConversationSummaryToJSON,
    CreateConversationRequestFromJSON,
    CreateConversationRequestToJSON,
    MessageFeedbackRequestFromJSON,
    MessageFeedbackRequestToJSON,
    UpdateConversationRequestFromJSON,
    UpdateConversationRequestToJSON,
} from '../models/index';

export interface AppendMcpConversationMessageRequest {
    id: string;
    appendMessageRequest: AppendMessageRequest;
}

export interface ClearMcpMessageFeedbackRequest {
    id: string;
    messageId: string;
}

export interface CreateMcpConversationRequest {
    createConversationRequest: CreateConversationRequest;
}

export interface DeleteMcpConversationRequest {
    id: string;
}

export interface GetMcpConversationRequest {
    id: string;
}

export interface SetMcpMessageFeedbackRequest {
    id: string;
    messageId: string;
    messageFeedbackRequest: MessageFeedbackRequest;
}

export interface UpdateMcpConversationRequest {
    id: string;
    updateConversationRequest: UpdateConversationRequest;
}

/**
 * 
 */
export class MCPConversationsApi extends runtime.BaseAPI {

    /**
     * Appends a client-authored turn directly to a conversation, for a one-time import of previously browser-only history or for a non-chat caller. Use this tool only when the caller itself authored the turn; do not call it for a turn executeMcpChat already returned, which persists its own user and assistant messages, because re-appending them here duplicates the conversation. Preconditions: the conversation must exist and belong to the caller. Required inputs: id (conversation id) as a path parameter, plus role (user or assistant) and blocks (at most 64 entries, at most 256 KB serialized) in the body, with an optional raw-text content fallback. Emits a MCP_CONVERSATION_MESSAGE_APPEND event. Returns 201 with the stored message, and 404 (never 403) when the id does not exist or belongs to another subject. 
     * Append a Message to a Conversation
     */
    async appendMcpConversationMessageRaw(requestParameters: AppendMcpConversationMessageRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ConversationMessage>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling appendMcpConversationMessage().'
            );
        }

        if (requestParameters['appendMessageRequest'] == null) {
            throw new runtime.RequiredError(
                'appendMessageRequest',
                'Required parameter "appendMessageRequest" was null or undefined when calling appendMcpConversationMessage().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        const response = await this.request({
            path: `/v1/mcp/conversations/{id}/messages`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AppendMessageRequestToJSON(requestParameters['appendMessageRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ConversationMessageFromJSON(jsonValue));
    }

    /**
     * Appends a client-authored turn directly to a conversation, for a one-time import of previously browser-only history or for a non-chat caller. Use this tool only when the caller itself authored the turn; do not call it for a turn executeMcpChat already returned, which persists its own user and assistant messages, because re-appending them here duplicates the conversation. Preconditions: the conversation must exist and belong to the caller. Required inputs: id (conversation id) as a path parameter, plus role (user or assistant) and blocks (at most 64 entries, at most 256 KB serialized) in the body, with an optional raw-text content fallback. Emits a MCP_CONVERSATION_MESSAGE_APPEND event. Returns 201 with the stored message, and 404 (never 403) when the id does not exist or belongs to another subject. 
     * Append a Message to a Conversation
     */
    async appendMcpConversationMessage(requestParameters: AppendMcpConversationMessageRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ConversationMessage> {
        const response = await this.appendMcpConversationMessageRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Deletes every conversation the caller owns, in one call. Use this tool for a deliberate clear-all-history action; do not use it to remove a single conversation, which deleteMcpConversation does. Preconditions: none; a caller with zero conversations still succeeds. Required inputs: none; there is no request body, and the owner comes from the authenticated principal. Emits a MCP_CONVERSATION_CLEAR_ALL event. Returns 204 whether or not the caller had any conversations, making the call idempotent. 
     * Clear All My Conversations
     */
    async clearMcpConversationsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const response = await this.request({
            path: `/v1/mcp/conversations`,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes every conversation the caller owns, in one call. Use this tool for a deliberate clear-all-history action; do not use it to remove a single conversation, which deleteMcpConversation does. Preconditions: none; a caller with zero conversations still succeeds. Required inputs: none; there is no request body, and the owner comes from the authenticated principal. Emits a MCP_CONVERSATION_CLEAR_ALL event. Returns 204 whether or not the caller had any conversations, making the call idempotent. 
     * Clear All My Conversations
     */
    async clearMcpConversations(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.clearMcpConversationsRaw(initOverrides);
    }

    /**
     * Withdraws the caller\'s rating of one assistant answer. Use this tool to retract a rating set by setMcpMessageFeedback; do not use it to change one, since setMcpMessageFeedback replaces a rating in full. Preconditions: the conversation must exist and belong to the caller, and messageId must address an assistant answer produced by a chat turn within it; a client-appended assistant message cannot be rated, so there is nothing to withdraw and the call answers 404 rather than a silent 204. Required inputs: id (conversation id) and messageId as path parameters; there is no request body. Emits a MCP_MESSAGE_FEEDBACK_CLEAR event; the call is idempotent and answers 204 even when the message was never rated. Returns 404 (never 403) when the message does not exist, is in another conversation, is not owned by the caller, belongs to another tenant, has role user, was appended directly rather than produced by a chat turn, or was purged. 
     * Withdraw a Rating
     */
    async clearMcpMessageFeedbackRaw(requestParameters: ClearMcpMessageFeedbackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling clearMcpMessageFeedback().'
            );
        }

        if (requestParameters['messageId'] == null) {
            throw new runtime.RequiredError(
                'messageId',
                'Required parameter "messageId" was null or undefined when calling clearMcpMessageFeedback().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const response = await this.request({
            path: `/v1/mcp/conversations/{id}/messages/{messageId}/feedback`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))).replace(`{${"messageId"}}`, encodeURIComponent(String(requestParameters['messageId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Withdraws the caller\'s rating of one assistant answer. Use this tool to retract a rating set by setMcpMessageFeedback; do not use it to change one, since setMcpMessageFeedback replaces a rating in full. Preconditions: the conversation must exist and belong to the caller, and messageId must address an assistant answer produced by a chat turn within it; a client-appended assistant message cannot be rated, so there is nothing to withdraw and the call answers 404 rather than a silent 204. Required inputs: id (conversation id) and messageId as path parameters; there is no request body. Emits a MCP_MESSAGE_FEEDBACK_CLEAR event; the call is idempotent and answers 204 even when the message was never rated. Returns 404 (never 403) when the message does not exist, is in another conversation, is not owned by the caller, belongs to another tenant, has role user, was appended directly rather than produced by a chat turn, or was purged. 
     * Withdraw a Rating
     */
    async clearMcpMessageFeedback(requestParameters: ClearMcpMessageFeedbackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.clearMcpMessageFeedbackRaw(requestParameters, initOverrides);
    }

    /**
     * Starts a new, empty conversation owned by the caller, optionally with an initial title. Use this tool to start a conversation the caller can then append turns to; do not use it to continue an existing one, since executeMcpChat with an existing conversationId does that. Preconditions: none. Required inputs: none; title is optional (1..120 characters after trimming) and, when omitted, is derived from the first message later appended to the conversation. Emits a MCP_CONVERSATION_CREATE event. Returns 201 with the created conversation (empty message list), and 400 when title falls outside 1..120 characters after trimming. 
     * Start a New Conversation
     */
    async createMcpConversationRaw(requestParameters: CreateMcpConversationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ConversationDetail>> {
        if (requestParameters['createConversationRequest'] == null) {
            throw new runtime.RequiredError(
                'createConversationRequest',
                'Required parameter "createConversationRequest" was null or undefined when calling createMcpConversation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        const response = await this.request({
            path: `/v1/mcp/conversations`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateConversationRequestToJSON(requestParameters['createConversationRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ConversationDetailFromJSON(jsonValue));
    }

    /**
     * Starts a new, empty conversation owned by the caller, optionally with an initial title. Use this tool to start a conversation the caller can then append turns to; do not use it to continue an existing one, since executeMcpChat with an existing conversationId does that. Preconditions: none. Required inputs: none; title is optional (1..120 characters after trimming) and, when omitted, is derived from the first message later appended to the conversation. Emits a MCP_CONVERSATION_CREATE event. Returns 201 with the created conversation (empty message list), and 400 when title falls outside 1..120 characters after trimming. 
     * Start a New Conversation
     */
    async createMcpConversation(requestParameters: CreateMcpConversationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ConversationDetail> {
        const response = await this.createMcpConversationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Deletes one conversation and every message in it. Use this tool to remove a single conversation the caller owns; do not use clearMcpConversations, which deletes every conversation the caller has. Preconditions: the conversation must exist and belong to the caller. Required inputs: id (conversation id) as a path parameter; there is no request body. Emits a MCP_CONVERSATION_DELETE event. Returns 204 on success, and 404 (never 403) when the id does not exist or belongs to another subject. 
     * Delete a Conversation
     */
    async deleteMcpConversationRaw(requestParameters: DeleteMcpConversationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteMcpConversation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const response = await this.request({
            path: `/v1/mcp/conversations/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes one conversation and every message in it. Use this tool to remove a single conversation the caller owns; do not use clearMcpConversations, which deletes every conversation the caller has. Preconditions: the conversation must exist and belong to the caller. Required inputs: id (conversation id) as a path parameter; there is no request body. Emits a MCP_CONVERSATION_DELETE event. Returns 204 on success, and 404 (never 403) when the id does not exist or belongs to another subject. 
     * Delete a Conversation
     */
    async deleteMcpConversation(requestParameters: DeleteMcpConversationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteMcpConversationRaw(requestParameters, initOverrides);
    }

    /**
     * Returns a single conversation, including its full message history, so a reopened conversation renders identically to when it was live. Use this tool when a conversation id is already known; do not use listMcpConversations for this, since its summaries carry no messages. Preconditions: the conversation must exist and belong to the caller. Required inputs: id (conversation id) as a path parameter; there is no request body. Emits a MCP_CONVERSATION_VIEW event. Returns 404 (never 403) when the id does not exist or belongs to another subject, the two being indistinguishable on purpose. 
     * Get a Conversation
     */
    async getMcpConversationRaw(requestParameters: GetMcpConversationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ConversationDetail>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getMcpConversation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const response = await this.request({
            path: `/v1/mcp/conversations/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ConversationDetailFromJSON(jsonValue));
    }

    /**
     * Returns a single conversation, including its full message history, so a reopened conversation renders identically to when it was live. Use this tool when a conversation id is already known; do not use listMcpConversations for this, since its summaries carry no messages. Preconditions: the conversation must exist and belong to the caller. Required inputs: id (conversation id) as a path parameter; there is no request body. Emits a MCP_CONVERSATION_VIEW event. Returns 404 (never 403) when the id does not exist or belongs to another subject, the two being indistinguishable on purpose. 
     * Get a Conversation
     */
    async getMcpConversation(requestParameters: GetMcpConversationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ConversationDetail> {
        const response = await this.getMcpConversationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the server-wide conversation retention policy, so the history rail\'s footer can state it truthfully instead of the old per-browser wording. Use this tool to render retention wording in a client; do not infer the window from listMcpConversations, whose row count says nothing about when a conversation is purged. Preconditions: none. Required inputs: none; the policy is server-wide rather than per-caller. No events are emitted; this is a read-only projection of configuration, under which unpinned conversations idle beyond retentionDays are purged while pinned conversations are exempt when pinnedExempt is true. Returns 200 with the retention policy. 
     * Get Conversation Retention Policy
     */
    async getMcpConversationPolicyRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ConversationPolicy>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const response = await this.request({
            path: `/v1/mcp/conversations/policy`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ConversationPolicyFromJSON(jsonValue));
    }

    /**
     * Returns the server-wide conversation retention policy, so the history rail\'s footer can state it truthfully instead of the old per-browser wording. Use this tool to render retention wording in a client; do not infer the window from listMcpConversations, whose row count says nothing about when a conversation is purged. Preconditions: none. Required inputs: none; the policy is server-wide rather than per-caller. No events are emitted; this is a read-only projection of configuration, under which unpinned conversations idle beyond retentionDays are purged while pinned conversations are exempt when pinnedExempt is true. Returns 200 with the retention policy. 
     * Get Conversation Retention Policy
     */
    async getMcpConversationPolicy(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ConversationPolicy> {
        const response = await this.getMcpConversationPolicyRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller\'s own conversation history, pinned first then most-recently-updated first, capped at 200 rows (no pagination). Use this tool to populate the assistant\'s history rail; do not use it to read a conversation\'s turns, which getMcpConversation returns. Preconditions: none; an empty list means the caller has no conversations yet. Required inputs: none; tenant and owner come from the authenticated principal, never from a request parameter. Emits a MCP_CONVERSATION_LIST event; unpinned conversations idle beyond the server\'s retention window are purged and pinned conversations are exempt, as reported by getMcpConversationPolicy. Returns 200 with the caller\'s conversation summaries. 
     * List My Conversations
     */
    async listMcpConversationsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ConversationSummary>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const response = await this.request({
            path: `/v1/mcp/conversations`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ConversationSummaryFromJSON));
    }

    /**
     * Returns the caller\'s own conversation history, pinned first then most-recently-updated first, capped at 200 rows (no pagination). Use this tool to populate the assistant\'s history rail; do not use it to read a conversation\'s turns, which getMcpConversation returns. Preconditions: none; an empty list means the caller has no conversations yet. Required inputs: none; tenant and owner come from the authenticated principal, never from a request parameter. Emits a MCP_CONVERSATION_LIST event; unpinned conversations idle beyond the server\'s retention window are purged and pinned conversations are exempt, as reported by getMcpConversationPolicy. Returns 200 with the caller\'s conversation summaries. 
     * List My Conversations
     */
    async listMcpConversations(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ConversationSummary>> {
        const response = await this.listMcpConversationsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Rates one assistant answer helpful or not helpful, with an optional reason and free-text comment. Use this tool to record a reader\'s verdict on an answer; a repeat call replaces the earlier rating in full, so an omitted reason or comment clears the stored value rather than leaving it untouched. Preconditions: the conversation must exist and belong to the caller, and messageId must address an assistant answer produced by a chat turn within it, since a client-appended assistant message cannot be rated. Required inputs: id (conversation id) and messageId as path parameters, messageId being either the messageId executeMcpChat returned or a ConversationMessage id from this conversation\'s history, plus rating in the body, with reason and comment optional. Emits a MCP_MESSAGE_FEEDBACK_SET event. Returns 204 on success, and 404 (never 403) with the same body when the message does not exist, is in another conversation, is not owned by the caller, belongs to another tenant, has role user, was appended directly rather than produced by a chat turn, or was purged. 
     * Rate an Assistant Answer
     */
    async setMcpMessageFeedbackRaw(requestParameters: SetMcpMessageFeedbackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling setMcpMessageFeedback().'
            );
        }

        if (requestParameters['messageId'] == null) {
            throw new runtime.RequiredError(
                'messageId',
                'Required parameter "messageId" was null or undefined when calling setMcpMessageFeedback().'
            );
        }

        if (requestParameters['messageFeedbackRequest'] == null) {
            throw new runtime.RequiredError(
                'messageFeedbackRequest',
                'Required parameter "messageFeedbackRequest" was null or undefined when calling setMcpMessageFeedback().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        const response = await this.request({
            path: `/v1/mcp/conversations/{id}/messages/{messageId}/feedback`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))).replace(`{${"messageId"}}`, encodeURIComponent(String(requestParameters['messageId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: MessageFeedbackRequestToJSON(requestParameters['messageFeedbackRequest']),
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Rates one assistant answer helpful or not helpful, with an optional reason and free-text comment. Use this tool to record a reader\'s verdict on an answer; a repeat call replaces the earlier rating in full, so an omitted reason or comment clears the stored value rather than leaving it untouched. Preconditions: the conversation must exist and belong to the caller, and messageId must address an assistant answer produced by a chat turn within it, since a client-appended assistant message cannot be rated. Required inputs: id (conversation id) and messageId as path parameters, messageId being either the messageId executeMcpChat returned or a ConversationMessage id from this conversation\'s history, plus rating in the body, with reason and comment optional. Emits a MCP_MESSAGE_FEEDBACK_SET event. Returns 204 on success, and 404 (never 403) with the same body when the message does not exist, is in another conversation, is not owned by the caller, belongs to another tenant, has role user, was appended directly rather than produced by a chat turn, or was purged. 
     * Rate an Assistant Answer
     */
    async setMcpMessageFeedback(requestParameters: SetMcpMessageFeedbackRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.setMcpMessageFeedbackRaw(requestParameters, initOverrides);
    }

    /**
     * Applies a partial update to a conversation\'s title and pinned state; either field may be omitted. Use this tool to rename or pin a conversation the caller owns; do not use it to add turns, which appendMcpConversationMessage does. Preconditions: the conversation must exist and belong to the caller. Required inputs: id (conversation id) as a path parameter, plus title (1..120 characters after trimming) and pinned in the body, either of which may be omitted; a body with neither set is a no-op 200, not a 400. Emits a MCP_CONVERSATION_UPDATE event. Returns 404 (never 403) when the id does not exist or belongs to another subject. 
     * Rename or Pin a Conversation
     */
    async updateMcpConversationRaw(requestParameters: UpdateMcpConversationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ConversationDetail>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updateMcpConversation().'
            );
        }

        if (requestParameters['updateConversationRequest'] == null) {
            throw new runtime.RequiredError(
                'updateConversationRequest',
                'Required parameter "updateConversationRequest" was null or undefined when calling updateMcpConversation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        const response = await this.request({
            path: `/v1/mcp/conversations/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: UpdateConversationRequestToJSON(requestParameters['updateConversationRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ConversationDetailFromJSON(jsonValue));
    }

    /**
     * Applies a partial update to a conversation\'s title and pinned state; either field may be omitted. Use this tool to rename or pin a conversation the caller owns; do not use it to add turns, which appendMcpConversationMessage does. Preconditions: the conversation must exist and belong to the caller. Required inputs: id (conversation id) as a path parameter, plus title (1..120 characters after trimming) and pinned in the body, either of which may be omitted; a body with neither set is a no-op 200, not a 400. Emits a MCP_CONVERSATION_UPDATE event. Returns 404 (never 403) when the id does not exist or belongs to another subject. 
     * Rename or Pin a Conversation
     */
    async updateMcpConversation(requestParameters: UpdateMcpConversationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ConversationDetail> {
        const response = await this.updateMcpConversationRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
