/* tslint:disable */
/* eslint-disable */
/**
 * OpenAPI definition
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ConfirmWritePlanRequest,
  NltiRequestDTO,
  NltiResponseV1,
  WorkflowStateResponse,
  WorkflowStateUpdateRequest,
  WritePlanResponseV1,
} from '../models/index';
import {
    ConfirmWritePlanRequestFromJSON,
    ConfirmWritePlanRequestToJSON,
    NltiRequestDTOFromJSON,
    NltiRequestDTOToJSON,
    NltiResponseV1FromJSON,
    NltiResponseV1ToJSON,
    WorkflowStateResponseFromJSON,
    WorkflowStateResponseToJSON,
    WorkflowStateUpdateRequestFromJSON,
    WorkflowStateUpdateRequestToJSON,
    WritePlanResponseV1FromJSON,
    WritePlanResponseV1ToJSON,
} from '../models/index';

export interface CancelWritePlanRequest {
    requestId: string;
}

export interface ConfirmWritePlanOperationRequest {
    requestId: string;
    authorization?: string;
    confirmWritePlanRequest?: ConfirmWritePlanRequest;
}

export interface SetSessionWorkflowStateRequest {
    sessionId: string;
    workflowStateUpdateRequest: WorkflowStateUpdateRequest;
}

export interface SubmitNltiRequestRequest {
    nltiRequestDTO: NltiRequestDTO;
    xCorrelationId?: string;
}

/**
 * 
 */
export class NLTIApi extends runtime.BaseAPI {

    /**
     * Cancels the pending write plan attached to an NLTI request so it can never be executed. Use this tool when the user declines a previewed plan; do not use confirmWritePlan, which executes the plan instead of discarding it. Preconditions: the plan and its request must exist and belong to the authenticated subject; a plan that is already CANCELLED or EXPIRED is returned unchanged, making the call idempotent. Required inputs: requestId (UUID) as a path parameter; there is no request body. Emits a NLTI_WRITE_PLAN_CANCEL event and appends a CONFIRMATION audit-ledger entry with outcome cancelled. Returns 200 with the plan (idempotently for already-inactive plans), 404 when no plan or request exists for the id, 403 when the plan is not owned by the caller, and 409 when the plan is COMPLETE, EXECUTING or ERROR and can no longer be cancelled. 
     * Cancel a Previewed Write Plan
     */
    async cancelWritePlanRaw(requestParameters: CancelWritePlanRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WritePlanResponseV1>> {
        if (requestParameters['requestId'] == null) {
            throw new runtime.RequiredError(
                'requestId',
                'Required parameter "requestId" was null or undefined when calling cancelWritePlan().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const response = await this.request({
            path: `/v1/nlt/requests/{requestId}/cancel`.replace(`{${"requestId"}}`, encodeURIComponent(String(requestParameters['requestId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WritePlanResponseV1FromJSON(jsonValue));
    }

    /**
     * Cancels the pending write plan attached to an NLTI request so it can never be executed. Use this tool when the user declines a previewed plan; do not use confirmWritePlan, which executes the plan instead of discarding it. Preconditions: the plan and its request must exist and belong to the authenticated subject; a plan that is already CANCELLED or EXPIRED is returned unchanged, making the call idempotent. Required inputs: requestId (UUID) as a path parameter; there is no request body. Emits a NLTI_WRITE_PLAN_CANCEL event and appends a CONFIRMATION audit-ledger entry with outcome cancelled. Returns 200 with the plan (idempotently for already-inactive plans), 404 when no plan or request exists for the id, 403 when the plan is not owned by the caller, and 409 when the plan is COMPLETE, EXECUTING or ERROR and can no longer be cancelled. 
     * Cancel a Previewed Write Plan
     */
    async cancelWritePlan(requestParameters: CancelWritePlanRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WritePlanResponseV1> {
        const response = await this.cancelWritePlanRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Executes the previewed write plan attached to an NLTI request, invoking the plan\'s target tool with the exact persisted arguments rather than a re-parse of the user\'s text. Use this tool after submitNltiRequest returned a PENDING_CONFIRMATION write-plan preview and the user approved it; do not use cancelWritePlan, which discards the plan without executing it. Preconditions: the plan and its request must exist and belong to the authenticated subject; the plan must not be expired, cancelled or already executing; the caller must still hold the target tool\'s permission at execution time; and for MEDIUM or higher risk plans the source-entity versions captured at plan time must be unchanged. Required inputs: requestId (UUID) as a path parameter; the body is optional and may carry idempotencyKey, which must match the plan\'s key when present. Emits a NLTI_WRITE_PLAN_CONFIRM event, appends CONFIRMATION and EXECUTION audit-ledger entries, and executes the target tool; re-confirming a COMPLETE plan returns the prior outcome without re-executing. Returns 200 on execution or idempotent replay, 404 when no plan or request exists for the id, 403 when the plan is not owned by the caller or the tool permission is missing, 409 when the plan is cancelled, executing, previously failed, stale, or the idempotency key mismatches, 410 when the plan has expired, and 502 when the target tool call itself fails. 
     * Confirm a Previewed Write Plan
     */
    async confirmWritePlanRaw(requestParameters: ConfirmWritePlanOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WritePlanResponseV1>> {
        if (requestParameters['requestId'] == null) {
            throw new runtime.RequiredError(
                'requestId',
                'Required parameter "requestId" was null or undefined when calling confirmWritePlan().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['authorization'] != null) {
            headerParameters['Authorization'] = String(requestParameters['authorization']);
        }

        const response = await this.request({
            path: `/v1/nlt/requests/{requestId}/confirm`.replace(`{${"requestId"}}`, encodeURIComponent(String(requestParameters['requestId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ConfirmWritePlanRequestToJSON(requestParameters['confirmWritePlanRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WritePlanResponseV1FromJSON(jsonValue));
    }

    /**
     * Executes the previewed write plan attached to an NLTI request, invoking the plan\'s target tool with the exact persisted arguments rather than a re-parse of the user\'s text. Use this tool after submitNltiRequest returned a PENDING_CONFIRMATION write-plan preview and the user approved it; do not use cancelWritePlan, which discards the plan without executing it. Preconditions: the plan and its request must exist and belong to the authenticated subject; the plan must not be expired, cancelled or already executing; the caller must still hold the target tool\'s permission at execution time; and for MEDIUM or higher risk plans the source-entity versions captured at plan time must be unchanged. Required inputs: requestId (UUID) as a path parameter; the body is optional and may carry idempotencyKey, which must match the plan\'s key when present. Emits a NLTI_WRITE_PLAN_CONFIRM event, appends CONFIRMATION and EXECUTION audit-ledger entries, and executes the target tool; re-confirming a COMPLETE plan returns the prior outcome without re-executing. Returns 200 on execution or idempotent replay, 404 when no plan or request exists for the id, 403 when the plan is not owned by the caller or the tool permission is missing, 409 when the plan is cancelled, executing, previously failed, stale, or the idempotency key mismatches, 410 when the plan has expired, and 502 when the target tool call itself fails. 
     * Confirm a Previewed Write Plan
     */
    async confirmWritePlan(requestParameters: ConfirmWritePlanOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WritePlanResponseV1> {
        const response = await this.confirmWritePlanRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Advances the operational workflow state persisted on the caller\'s NLTI session so subsequent chat turns receive the workflow-gated tool set for that state. Use this tool when a conversation enters or leaves an operational flow such as purchase-order creation; do not use submitNltiRequest, which submits a new prompt and never changes the workflow state directly. Preconditions: the session must exist and be owned by the authenticated subject; an unknown session and a session owned by someone else are both rejected as ownership violations. Required inputs: sessionId (UUID) as a path parameter and workflowState, one of IDLE, CREATING_PO, RECEIVING_ASN, INVENTORY_RECON or PROCESSING_RETURN; new sessions default to IDLE. Emits a NLTI_SESSION_WORKFLOW_STATE_SET event and updates only the session\'s workflow state. Returns 200 with the persisted state, and 403 when the session does not exist or is not owned by the caller. 
     * Set the Session Workflow State
     */
    async setSessionWorkflowStateRaw(requestParameters: SetSessionWorkflowStateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WorkflowStateResponse>> {
        if (requestParameters['sessionId'] == null) {
            throw new runtime.RequiredError(
                'sessionId',
                'Required parameter "sessionId" was null or undefined when calling setSessionWorkflowState().'
            );
        }

        if (requestParameters['workflowStateUpdateRequest'] == null) {
            throw new runtime.RequiredError(
                'workflowStateUpdateRequest',
                'Required parameter "workflowStateUpdateRequest" was null or undefined when calling setSessionWorkflowState().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        const response = await this.request({
            path: `/v1/nlt/sessions/{sessionId}/workflow-state`.replace(`{${"sessionId"}}`, encodeURIComponent(String(requestParameters['sessionId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: WorkflowStateUpdateRequestToJSON(requestParameters['workflowStateUpdateRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WorkflowStateResponseFromJSON(jsonValue));
    }

    /**
     * Advances the operational workflow state persisted on the caller\'s NLTI session so subsequent chat turns receive the workflow-gated tool set for that state. Use this tool when a conversation enters or leaves an operational flow such as purchase-order creation; do not use submitNltiRequest, which submits a new prompt and never changes the workflow state directly. Preconditions: the session must exist and be owned by the authenticated subject; an unknown session and a session owned by someone else are both rejected as ownership violations. Required inputs: sessionId (UUID) as a path parameter and workflowState, one of IDLE, CREATING_PO, RECEIVING_ASN, INVENTORY_RECON or PROCESSING_RETURN; new sessions default to IDLE. Emits a NLTI_SESSION_WORKFLOW_STATE_SET event and updates only the session\'s workflow state. Returns 200 with the persisted state, and 403 when the session does not exist or is not owned by the caller. 
     * Set the Session Workflow State
     */
    async setSessionWorkflowState(requestParameters: SetSessionWorkflowStateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WorkflowStateResponse> {
        const response = await this.setSessionWorkflowStateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Submits a natural-language prompt for asynchronous task interpretation, classifying the intent as QUERY or ACTION; an ACTION prompt yields a persisted write-plan preview that must be explicitly confirmed and is never executed directly. Use this tool to start or continue a natural-language task conversation; do not use confirmWritePlan, which executes a plan this operation has already previewed. Preconditions: a supplied sessionId must belong to the calling subject, and a session idle for more than 24 hours is silently replaced with a new one. Required inputs: prompt (non-blank text); sessionId (UUID) is optional and a new session is created when it is absent; clientContext is an optional key-value map; an optional X-Correlation-Id header is reused and echoed back, otherwise a correlation id is generated. Emits a NLTI_REQUEST_SUBMIT event and persists the request with a SHA-256 prompt hash rather than the raw prompt text. Returns 202 with status ACCEPTED or an attached write-plan preview, 403 when the supplied sessionId is owned by another subject, and 429 when the per-session rate limit (default 100 requests) is exceeded. 
     * Submit a Natural Language Task Request
     */
    async submitNltiRequestRaw(requestParameters: SubmitNltiRequestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<NltiResponseV1>> {
        if (requestParameters['nltiRequestDTO'] == null) {
            throw new runtime.RequiredError(
                'nltiRequestDTO',
                'Required parameter "nltiRequestDTO" was null or undefined when calling submitNltiRequest().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xCorrelationId'] != null) {
            headerParameters['X-Correlation-Id'] = String(requestParameters['xCorrelationId']);
        }

        const response = await this.request({
            path: `/v1/nlt/requests`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: NltiRequestDTOToJSON(requestParameters['nltiRequestDTO']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => NltiResponseV1FromJSON(jsonValue));
    }

    /**
     * Submits a natural-language prompt for asynchronous task interpretation, classifying the intent as QUERY or ACTION; an ACTION prompt yields a persisted write-plan preview that must be explicitly confirmed and is never executed directly. Use this tool to start or continue a natural-language task conversation; do not use confirmWritePlan, which executes a plan this operation has already previewed. Preconditions: a supplied sessionId must belong to the calling subject, and a session idle for more than 24 hours is silently replaced with a new one. Required inputs: prompt (non-blank text); sessionId (UUID) is optional and a new session is created when it is absent; clientContext is an optional key-value map; an optional X-Correlation-Id header is reused and echoed back, otherwise a correlation id is generated. Emits a NLTI_REQUEST_SUBMIT event and persists the request with a SHA-256 prompt hash rather than the raw prompt text. Returns 202 with status ACCEPTED or an attached write-plan preview, 403 when the supplied sessionId is owned by another subject, and 429 when the per-session rate limit (default 100 requests) is exceeded. 
     * Submit a Natural Language Task Request
     */
    async submitNltiRequest(requestParameters: SubmitNltiRequestRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<NltiResponseV1> {
        const response = await this.submitNltiRequestRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
