/* tslint:disable */
/* eslint-disable */
/**
 * OpenAPI definition
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  PageAuditEventResponse,
} from '../models/index';
import {
    PageAuditEventResponseFromJSON,
    PageAuditEventResponseToJSON,
} from '../models/index';

export interface SearchNltiAuditEventsRequest {
    correlationId?: string;
    from?: Date;
    to?: Date;
    eventType?: string;
    page?: number;
    size?: number;
    sort?: Array<string>;
}

/**
 * 
 */
export class NLTIAuditApi extends runtime.BaseAPI {

    /**
     * Returns a page of NLTI audit ledger entries recording request, intent, plan, confirmation and execution events. Use this tool to trace what the NLTI pipeline did for a conversation or time window; filter by correlationId rather than paging the whole ledger when tracing a single request. Preconditions: none; the ledger is append-only and entries exist only for activity that has already happened. Required inputs: all filters are optional — correlationId (UUID) takes precedence, then from and to (ISO-8601 instants, applied only when both are present), then eventType (one of REQUEST, INTENT, PLAN, CONFIRMATION, EXECUTION_STEP, EXECUTION_COMPLETE, EXECUTION_FAILED); results default to page size 20 sorted by timestamp. Emits a NLTI_AUDIT_QUERY event; the ledger itself is never modified by this call. Returns 200 with a page of audit events, and 400 with code INVALID_EVENT_TYPE when eventType is not one of the supported values. 
     * Query the NLTI Audit Ledger
     */
    async searchNltiAuditEventsRaw(requestParameters: SearchNltiAuditEventsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PageAuditEventResponse>> {
        const queryParameters: any = {};

        if (requestParameters['correlationId'] != null) {
            queryParameters['correlationId'] = requestParameters['correlationId'];
        }

        if (requestParameters['from'] != null) {
            queryParameters['from'] = (requestParameters['from'] as any).toISOString();
        }

        if (requestParameters['to'] != null) {
            queryParameters['to'] = (requestParameters['to'] as any).toISOString();
        }

        if (requestParameters['eventType'] != null) {
            queryParameters['eventType'] = requestParameters['eventType'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        if (requestParameters['sort'] != null) {
            queryParameters['sort'] = requestParameters['sort'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        const response = await this.request({
            path: `/v1/nlt/audit`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PageAuditEventResponseFromJSON(jsonValue));
    }

    /**
     * Returns a page of NLTI audit ledger entries recording request, intent, plan, confirmation and execution events. Use this tool to trace what the NLTI pipeline did for a conversation or time window; filter by correlationId rather than paging the whole ledger when tracing a single request. Preconditions: none; the ledger is append-only and entries exist only for activity that has already happened. Required inputs: all filters are optional — correlationId (UUID) takes precedence, then from and to (ISO-8601 instants, applied only when both are present), then eventType (one of REQUEST, INTENT, PLAN, CONFIRMATION, EXECUTION_STEP, EXECUTION_COMPLETE, EXECUTION_FAILED); results default to page size 20 sorted by timestamp. Emits a NLTI_AUDIT_QUERY event; the ledger itself is never modified by this call. Returns 200 with a page of audit events, and 400 with code INVALID_EVENT_TYPE when eventType is not one of the supported values. 
     * Query the NLTI Audit Ledger
     */
    async searchNltiAuditEvents(requestParameters: SearchNltiAuditEventsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PageAuditEventResponse> {
        const response = await this.searchNltiAuditEventsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
