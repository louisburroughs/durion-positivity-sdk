/* tslint:disable */
/* eslint-disable */
/**
 * OpenAPI definition
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  TranscriptionResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    TranscriptionResponseFromJSON,
    TranscriptionResponseToJSON,
} from '../models/index';

export interface TranscribeMcpAudioRequest {
    audio: Blob;
    language?: string;
}

/**
 * 
 */
export class MCPTranscriptionApi extends runtime.BaseAPI {

    /**
     * Transcribes one short audio clip to text for the assistant composer, for browsers where the in-browser SpeechRecognition API is unavailable. Use this tool only as that fallback; do not use it for long-form or file-based transcription, which this endpoint rejects rather than queueing for later polling. Preconditions: the clip is at most 5 MiB and at most 60 seconds long, both limits inclusive, and its content type, ignoring parameters such as codecs=opus, is one of audio/webm, audio/ogg, audio/mp4. Required inputs: audio, the recorded clip, as a required multipart file part, and language, an optional BCP-47 tag such as en-US; when language is omitted the request\'s resolved Accept-Language locale is used, falling back to provider auto-detection when that locale is undetermined. Emits an MCP_TRANSCRIPTION_EXECUTE event that carries no audio; the clip is transcribe-and-discard, held in memory for this request only, forwarded to the configured speech-to-text provider, and never persisted or logged. A hosted provider applies its own retention policy to the copy it received, while a self-hosted provider, being infrastructure under our own control, keeps nothing beyond serving this one request. Returns 200 with the transcript synchronously, 413 when the clip is over 5 MiB or longer than 60 seconds, 415 when the audio part is missing or its content type is outside the allowed set, 422 when the clip yields no intelligible text, and 503 when the provider is unconfigured or unreachable. 
     * Transcribe an Audio Clip
     */
    async transcribeMcpAudioRaw(requestParameters: TranscribeMcpAudioRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TranscriptionResponse>> {
        if (requestParameters['audio'] == null) {
            throw new runtime.RequiredError(
                'audio',
                'Required parameter "audio" was null or undefined when calling transcribeMcpAudio().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const consumes: runtime.Consume[] = [
            { contentType: 'multipart/form-data' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        // use FormData to transmit files using content-type "multipart/form-data"
        useForm = canConsumeForm;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['audio'] != null) {
            formParams.append('audio', requestParameters['audio'] as any);
        }

        if (requestParameters['language'] != null) {
            formParams.append('language', requestParameters['language'] as any);
        }

        const response = await this.request({
            path: `/v1/mcp/transcriptions`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TranscriptionResponseFromJSON(jsonValue));
    }

    /**
     * Transcribes one short audio clip to text for the assistant composer, for browsers where the in-browser SpeechRecognition API is unavailable. Use this tool only as that fallback; do not use it for long-form or file-based transcription, which this endpoint rejects rather than queueing for later polling. Preconditions: the clip is at most 5 MiB and at most 60 seconds long, both limits inclusive, and its content type, ignoring parameters such as codecs=opus, is one of audio/webm, audio/ogg, audio/mp4. Required inputs: audio, the recorded clip, as a required multipart file part, and language, an optional BCP-47 tag such as en-US; when language is omitted the request\'s resolved Accept-Language locale is used, falling back to provider auto-detection when that locale is undetermined. Emits an MCP_TRANSCRIPTION_EXECUTE event that carries no audio; the clip is transcribe-and-discard, held in memory for this request only, forwarded to the configured speech-to-text provider, and never persisted or logged. A hosted provider applies its own retention policy to the copy it received, while a self-hosted provider, being infrastructure under our own control, keeps nothing beyond serving this one request. Returns 200 with the transcript synchronously, 413 when the clip is over 5 MiB or longer than 60 seconds, 415 when the audio part is missing or its content type is outside the allowed set, 422 when the clip yields no intelligible text, and 503 when the provider is unconfigured or unreachable. 
     * Transcribe an Audio Clip
     */
    async transcribeMcpAudio(requestParameters: TranscribeMcpAudioRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TranscriptionResponse> {
        const response = await this.transcribeMcpAudioRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
