/* tslint:disable */
/* eslint-disable */
/**
 * OpenAPI definition
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  DocumentIngestionJobResponse,
  DocumentIngestionRequest,
} from '../models/index';
import {
    DocumentIngestionJobResponseFromJSON,
    DocumentIngestionJobResponseToJSON,
    DocumentIngestionRequestFromJSON,
    DocumentIngestionRequestToJSON,
} from '../models/index';

export interface GetDocumentIngestionJobRequest {
    jobId: string;
}

export interface IngestDocumentRequest {
    documentIngestionRequest: DocumentIngestionRequest;
}

/**
 * 
 */
export class DocumentIngestionApi extends runtime.BaseAPI {

    /**
     * Returns the current status of an asynchronous RAG document ingestion job, including timestamps, chunk count and any failure message. Use this tool to poll a job created by ingestDocument; do not use ingestDocument again to check progress, since that queues a second ingestion instead. Preconditions: the job must exist for the supplied jobId returned by ingestDocument. Required inputs: jobId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 200 with the job in state PENDING, RUNNING, SUCCEEDED or FAILED, and 404 when no job exists for the id. 
     * Get Document Ingestion Job Status
     */
    async getDocumentIngestionJobRaw(requestParameters: GetDocumentIngestionJobRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DocumentIngestionJobResponse>> {
        if (requestParameters['jobId'] == null) {
            throw new runtime.RequiredError(
                'jobId',
                'Required parameter "jobId" was null or undefined when calling getDocumentIngestionJob().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const response = await this.request({
            path: `/v1/mcp/documents/jobs/{jobId}`.replace(`{${"jobId"}}`, encodeURIComponent(String(requestParameters['jobId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DocumentIngestionJobResponseFromJSON(jsonValue));
    }

    /**
     * Returns the current status of an asynchronous RAG document ingestion job, including timestamps, chunk count and any failure message. Use this tool to poll a job created by ingestDocument; do not use ingestDocument again to check progress, since that queues a second ingestion instead. Preconditions: the job must exist for the supplied jobId returned by ingestDocument. Required inputs: jobId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 200 with the job in state PENDING, RUNNING, SUCCEEDED or FAILED, and 404 when no job exists for the id. 
     * Get Document Ingestion Job Status
     */
    async getDocumentIngestionJob(requestParameters: GetDocumentIngestionJobRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DocumentIngestionJobResponse> {
        const response = await this.getDocumentIngestionJobRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Queues a text document for asynchronous ingestion into the RAG vector store and returns a trackable job. Use this tool to add reference content that chat agents can retrieve; do not use getDocumentIngestionJob, which only reads the status of a job that was already queued. Preconditions: none beyond the mcp:document:ingest authority; the job is accepted and persisted before any embedding work happens. Required inputs: content (non-blank raw text); metadata is an optional key-value map whose document_id entry names the document — when absent a random identifier is generated. Emits a MCP_DOCUMENT_INGEST event and creates a PENDING job that is processed in the background through RUNNING to SUCCEEDED or FAILED. Returns 202 with the job and a Location header pointing at the job-status resource; 400 when the supplied metadata cannot be serialized back to JSON; embedding failures happening after the job is accepted are reported on the job\'s errorMessage, not on this call. 
     * Queue a Document for RAG Ingestion
     */
    async ingestDocumentRaw(requestParameters: IngestDocumentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DocumentIngestionJobResponse>> {
        if (requestParameters['documentIngestionRequest'] == null) {
            throw new runtime.RequiredError(
                'documentIngestionRequest',
                'Required parameter "documentIngestionRequest" was null or undefined when calling ingestDocument().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        const response = await this.request({
            path: `/v1/mcp/documents`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: DocumentIngestionRequestToJSON(requestParameters['documentIngestionRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DocumentIngestionJobResponseFromJSON(jsonValue));
    }

    /**
     * Queues a text document for asynchronous ingestion into the RAG vector store and returns a trackable job. Use this tool to add reference content that chat agents can retrieve; do not use getDocumentIngestionJob, which only reads the status of a job that was already queued. Preconditions: none beyond the mcp:document:ingest authority; the job is accepted and persisted before any embedding work happens. Required inputs: content (non-blank raw text); metadata is an optional key-value map whose document_id entry names the document — when absent a random identifier is generated. Emits a MCP_DOCUMENT_INGEST event and creates a PENDING job that is processed in the background through RUNNING to SUCCEEDED or FAILED. Returns 202 with the job and a Location header pointing at the job-status resource; 400 when the supplied metadata cannot be serialized back to JSON; embedding failures happening after the job is accepted are reported on the job\'s errorMessage, not on this call. 
     * Queue a Document for RAG Ingestion
     */
    async ingestDocument(requestParameters: IngestDocumentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DocumentIngestionJobResponse> {
        const response = await this.ingestDocumentRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
