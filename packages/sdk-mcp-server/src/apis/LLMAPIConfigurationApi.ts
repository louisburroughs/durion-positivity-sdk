/* tslint:disable */
/* eslint-disable */
/**
 * OpenAPI definition
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  LlmApiConfigRequest,
  LlmApiConfigResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    LlmApiConfigRequestFromJSON,
    LlmApiConfigRequestToJSON,
    LlmApiConfigResponseFromJSON,
    LlmApiConfigResponseToJSON,
} from '../models/index';

export interface CreateLlmApiConfigRequest {
    llmApiConfigRequest: LlmApiConfigRequest;
}

export interface DeleteLlmApiConfigRequest {
    id: string;
}

export interface GetLlmApiConfigRequest {
    id: string;
}

export interface UpdateLlmApiConfigRequest {
    id: string;
    llmApiConfigRequest: LlmApiConfigRequest;
}

/**
 * 
 */
export class LLMAPIConfigurationApi extends runtime.BaseAPI {

    /**
     * Creates a new LLM provider API configuration that the MCP server can use to invoke an external model. Use this tool to register a new provider endpoint and credential; do not use updateLlmApiConfig, which modifies a configuration that already exists. Preconditions: no configuration may already use the requested apiId, which is unique across configurations. Required inputs: apiId (stable string identifier), model, baseUrl and apiKey; headers is an optional map of extra HTTP headers and defaults to empty. Emits a MCP_LLM_API_CREATE event and stores the credential for later provider calls. Returns 201 with the stored configuration, and 409 LLM_API_ID_CONFLICT in the ApiError envelope when another configuration already uses the requested apiId. 
     * Create an LLM API Configuration
     */
    async createLlmApiConfigRaw(requestParameters: CreateLlmApiConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LlmApiConfigResponse>> {
        if (requestParameters['llmApiConfigRequest'] == null) {
            throw new runtime.RequiredError(
                'llmApiConfigRequest',
                'Required parameter "llmApiConfigRequest" was null or undefined when calling createLlmApiConfig().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        const response = await this.request({
            path: `/v1/llm-apis`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LlmApiConfigRequestToJSON(requestParameters['llmApiConfigRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LlmApiConfigResponseFromJSON(jsonValue));
    }

    /**
     * Creates a new LLM provider API configuration that the MCP server can use to invoke an external model. Use this tool to register a new provider endpoint and credential; do not use updateLlmApiConfig, which modifies a configuration that already exists. Preconditions: no configuration may already use the requested apiId, which is unique across configurations. Required inputs: apiId (stable string identifier), model, baseUrl and apiKey; headers is an optional map of extra HTTP headers and defaults to empty. Emits a MCP_LLM_API_CREATE event and stores the credential for later provider calls. Returns 201 with the stored configuration, and 409 LLM_API_ID_CONFLICT in the ApiError envelope when another configuration already uses the requested apiId. 
     * Create an LLM API Configuration
     */
    async createLlmApiConfig(requestParameters: CreateLlmApiConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LlmApiConfigResponse> {
        const response = await this.createLlmApiConfigRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Deletes an LLM provider API configuration so the MCP server can no longer use it to invoke that provider. Use this tool to retire a provider configuration; do not use updateLlmApiConfig, which keeps the configuration and changes its fields. Preconditions: none; deleting an id that does not exist is a no-op. Required inputs: id (UUID) as a path parameter; there is no request body. Emits a MCP_LLM_API_DELETE event. Returns 204 whether or not the configuration existed, making the call idempotent. 
     * Delete an LLM API Configuration
     */
    async deleteLlmApiConfigRaw(requestParameters: DeleteLlmApiConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteLlmApiConfig().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const response = await this.request({
            path: `/v1/llm-apis/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes an LLM provider API configuration so the MCP server can no longer use it to invoke that provider. Use this tool to retire a provider configuration; do not use updateLlmApiConfig, which keeps the configuration and changes its fields. Preconditions: none; deleting an id that does not exist is a no-op. Required inputs: id (UUID) as a path parameter; there is no request body. Emits a MCP_LLM_API_DELETE event. Returns 204 whether or not the configuration existed, making the call idempotent. 
     * Delete an LLM API Configuration
     */
    async deleteLlmApiConfig(requestParameters: DeleteLlmApiConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteLlmApiConfigRaw(requestParameters, initOverrides);
    }

    /**
     * Returns a single LLM provider API configuration by its identifier. Use this tool when the configuration id (UUID) is already known; use listLlmApiConfigs instead to discover ids. Preconditions: a configuration with the given id must exist. Required inputs: id (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 200 when the configuration exists, and 404 LLM_API_NOT_FOUND in the ApiError envelope when the id is unknown. 
     * Get an LLM API Configuration
     */
    async getLlmApiConfigRaw(requestParameters: GetLlmApiConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LlmApiConfigResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getLlmApiConfig().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const response = await this.request({
            path: `/v1/llm-apis/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LlmApiConfigResponseFromJSON(jsonValue));
    }

    /**
     * Returns a single LLM provider API configuration by its identifier. Use this tool when the configuration id (UUID) is already known; use listLlmApiConfigs instead to discover ids. Preconditions: a configuration with the given id must exist. Required inputs: id (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 200 when the configuration exists, and 404 LLM_API_NOT_FOUND in the ApiError envelope when the id is unknown. 
     * Get an LLM API Configuration
     */
    async getLlmApiConfig(requestParameters: GetLlmApiConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LlmApiConfigResponse> {
        const response = await this.getLlmApiConfigRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every configured LLM provider API definition available to the MCP server, including provider base URL and model. Use this tool to enumerate provider configurations or to find a configuration id; use getLlmApiConfig instead when the id is already known. Preconditions: none; an empty list simply means no provider has been configured yet. Required inputs: none; there are no filters, no paging and no request body. No events are emitted and no state changes; this is a read-only projection. Returns 200 with the full list of configurations. 
     * List All LLM API Configurations
     */
    async listLlmApiConfigsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<LlmApiConfigResponse>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const response = await this.request({
            path: `/v1/llm-apis`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(LlmApiConfigResponseFromJSON));
    }

    /**
     * Returns every configured LLM provider API definition available to the MCP server, including provider base URL and model. Use this tool to enumerate provider configurations or to find a configuration id; use getLlmApiConfig instead when the id is already known. Preconditions: none; an empty list simply means no provider has been configured yet. Required inputs: none; there are no filters, no paging and no request body. No events are emitted and no state changes; this is a read-only projection. Returns 200 with the full list of configurations. 
     * List All LLM API Configurations
     */
    async listLlmApiConfigs(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<LlmApiConfigResponse>> {
        const response = await this.listLlmApiConfigsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Replaces every editable field of an existing LLM provider API configuration identified by id. Use this tool to change a provider\'s model, base URL, credential or headers; do not use createLlmApiConfig, which registers an additional configuration. Preconditions: the configuration must exist, and when apiId is being changed the new apiId must not already be used by another configuration. Required inputs: id (UUID) as a path parameter plus a full body with apiId, model, baseUrl and apiKey; headers defaults to empty when omitted, so a partial body is not merged. Emits a MCP_LLM_API_UPDATE event. Returns 200 with the updated configuration, 404 LLM_API_NOT_FOUND when the id is unknown, and 409 LLM_API_ID_CONFLICT when another configuration already uses the requested apiId — both in the ApiError envelope. 
     * Update an LLM API Configuration
     */
    async updateLlmApiConfigRaw(requestParameters: UpdateLlmApiConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LlmApiConfigResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updateLlmApiConfig().'
            );
        }

        if (requestParameters['llmApiConfigRequest'] == null) {
            throw new runtime.RequiredError(
                'llmApiConfigRequest',
                'Required parameter "llmApiConfigRequest" was null or undefined when calling updateLlmApiConfig().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        const response = await this.request({
            path: `/v1/llm-apis/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: LlmApiConfigRequestToJSON(requestParameters['llmApiConfigRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LlmApiConfigResponseFromJSON(jsonValue));
    }

    /**
     * Replaces every editable field of an existing LLM provider API configuration identified by id. Use this tool to change a provider\'s model, base URL, credential or headers; do not use createLlmApiConfig, which registers an additional configuration. Preconditions: the configuration must exist, and when apiId is being changed the new apiId must not already be used by another configuration. Required inputs: id (UUID) as a path parameter plus a full body with apiId, model, baseUrl and apiKey; headers defaults to empty when omitted, so a partial body is not merged. Emits a MCP_LLM_API_UPDATE event. Returns 200 with the updated configuration, 404 LLM_API_NOT_FOUND when the id is unknown, and 409 LLM_API_ID_CONFLICT when another configuration already uses the requested apiId — both in the ApiError envelope. 
     * Update an LLM API Configuration
     */
    async updateLlmApiConfig(requestParameters: UpdateLlmApiConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LlmApiConfigResponse> {
        const response = await this.updateLlmApiConfigRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
