/* tslint:disable */
/* eslint-disable */
/**
 * POS Accounting Service API
 * Accounting service for invoice, journal entry, and GL account management
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  SettlementLineResponse,
  SettlementManualMatchRequest,
  SettlementWriteOffRequest,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    SettlementLineResponseFromJSON,
    SettlementLineResponseToJSON,
    SettlementManualMatchRequestFromJSON,
    SettlementManualMatchRequestToJSON,
    SettlementWriteOffRequestFromJSON,
    SettlementWriteOffRequestToJSON,
} from '../models/index';

export interface ListSettlementLinesRequest {
    settlementId: string;
    unmatchedOnly?: boolean;
}

export interface MatchSettlementLineRequest {
    lineId: string;
    settlementManualMatchRequest: SettlementManualMatchRequest;
}

export interface WriteOffSettlementLineRequest {
    lineId: string;
    settlementWriteOffRequest: SettlementWriteOffRequest;
}

/**
 * 
 */
export class SettlementReconciliationApi extends runtime.BaseAPI {

    /**
     * Lists the lines of one processor settlement (payout), optionally filtered to only the UNMATCHED lines awaiting review; matched lines are already posted, while unmatched lines park their gross in the settlement suspense account until resolved. Use this tool to triage a settlement before matchSettlementLine or writeOffSettlementLine; do not use those mutation tools just to inspect state. Preconditions: none; an unknown settlementId returns an empty list rather than an error. Required inputs: settlementId (the provider payout id string) as a path parameter; unmatchedOnly defaults to false. Emits an ACCOUNTING_SETTLEMENT_LINES_LIST audit event; no state changes. Returns 200 with an empty list when the settlement is unknown or fully filtered out. 
     * List Settlement Lines
     */
    async listSettlementLinesRaw(requestParameters: ListSettlementLinesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<SettlementLineResponse>>> {
        if (requestParameters['settlementId'] == null) {
            throw new runtime.RequiredError(
                'settlementId',
                'Required parameter "settlementId" was null or undefined when calling listSettlementLines().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['unmatchedOnly'] != null) {
            queryParameters['unmatchedOnly'] = requestParameters['unmatchedOnly'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:reconciliation:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/settlements/{settlementId}/lines`.replace(`{${"settlementId"}}`, encodeURIComponent(String(requestParameters['settlementId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(SettlementLineResponseFromJSON));
    }

    /**
     * Lists the lines of one processor settlement (payout), optionally filtered to only the UNMATCHED lines awaiting review; matched lines are already posted, while unmatched lines park their gross in the settlement suspense account until resolved. Use this tool to triage a settlement before matchSettlementLine or writeOffSettlementLine; do not use those mutation tools just to inspect state. Preconditions: none; an unknown settlementId returns an empty list rather than an error. Required inputs: settlementId (the provider payout id string) as a path parameter; unmatchedOnly defaults to false. Emits an ACCOUNTING_SETTLEMENT_LINES_LIST audit event; no state changes. Returns 200 with an empty list when the settlement is unknown or fully filtered out. 
     * List Settlement Lines
     */
    async listSettlementLines(requestParameters: ListSettlementLinesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<SettlementLineResponse>> {
        const response = await this.listSettlementLinesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Manually matches an UNMATCHED settlement line to an AR receivable payment, posting a reclass entry (debit Settlement Suspense, credit Undeposited Funds) that clears the line\'s gross out of suspense. Use this tool when the automatic gross match could not identify the payment; do not use writeOffSettlementLine, which is only for small unidentifiable residuals, and note that AP vendor matching is not supported in v1. Preconditions: the line must exist and be UNMATCHED, its parent settlement must already be POSTED, and the receivable payment must exist. Required inputs: lineId (UUID) as a path parameter and receivablePaymentId (UUID) in the body. Emits an ACCOUNTING_SETTLEMENT_LINE_MATCH event and posts the reclass journal entry. Returns 404 SETTLEMENT_LINE_NOT_FOUND or RECEIVABLE_PAYMENT_NOT_FOUND when either record is missing, 409 SETTLEMENT_LINE_NOT_UNMATCHED or SETTLEMENT_NOT_POSTED for state conflicts, and 422 PERIOD_CLOSED or PERIOD_HARD_LOCKED when the reclass entry is dated into a locked period. 
     * Manually Match Settlement Line
     */
    async matchSettlementLineRaw(requestParameters: MatchSettlementLineRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SettlementLineResponse>> {
        if (requestParameters['lineId'] == null) {
            throw new runtime.RequiredError(
                'lineId',
                'Required parameter "lineId" was null or undefined when calling matchSettlementLine().'
            );
        }

        if (requestParameters['settlementManualMatchRequest'] == null) {
            throw new runtime.RequiredError(
                'settlementManualMatchRequest',
                'Required parameter "settlementManualMatchRequest" was null or undefined when calling matchSettlementLine().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:reconciliation:adjust"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/settlements/lines/{lineId}/match`.replace(`{${"lineId"}}`, encodeURIComponent(String(requestParameters['lineId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SettlementManualMatchRequestToJSON(requestParameters['settlementManualMatchRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SettlementLineResponseFromJSON(jsonValue));
    }

    /**
     * Manually matches an UNMATCHED settlement line to an AR receivable payment, posting a reclass entry (debit Settlement Suspense, credit Undeposited Funds) that clears the line\'s gross out of suspense. Use this tool when the automatic gross match could not identify the payment; do not use writeOffSettlementLine, which is only for small unidentifiable residuals, and note that AP vendor matching is not supported in v1. Preconditions: the line must exist and be UNMATCHED, its parent settlement must already be POSTED, and the receivable payment must exist. Required inputs: lineId (UUID) as a path parameter and receivablePaymentId (UUID) in the body. Emits an ACCOUNTING_SETTLEMENT_LINE_MATCH event and posts the reclass journal entry. Returns 404 SETTLEMENT_LINE_NOT_FOUND or RECEIVABLE_PAYMENT_NOT_FOUND when either record is missing, 409 SETTLEMENT_LINE_NOT_UNMATCHED or SETTLEMENT_NOT_POSTED for state conflicts, and 422 PERIOD_CLOSED or PERIOD_HARD_LOCKED when the reclass entry is dated into a locked period. 
     * Manually Match Settlement Line
     */
    async matchSettlementLine(requestParameters: MatchSettlementLineRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SettlementLineResponse> {
        const response = await this.matchSettlementLineRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Writes off an UNMATCHED settlement line whose gross is at or below the configured write-off threshold (default 25.00), posting a reversible adjustment entry (debit Settlement Suspense, credit Settlement Adjustment) rather than a silent status flip. Use this tool only for genuinely unidentifiable small residuals; do not use it above the threshold, where matchSettlementLine or escalation is the only path. Preconditions: the line must exist and be UNMATCHED, its parent settlement must be POSTED, and the line\'s absolute gross must not exceed the threshold. Required inputs: lineId (UUID) as a path parameter and a non-blank audited reason (max 500 chars). Emits an ACCOUNTING_SETTLEMENT_LINE_WRITE_OFF event and posts the adjustment entry through the accounting-period gate. Returns 422 WRITE_OFF_THRESHOLD_EXCEEDED when the absolute gross exceeds the threshold or PERIOD_CLOSED / PERIOD_HARD_LOCKED for locked periods, 409 SETTLEMENT_LINE_NOT_UNMATCHED or SETTLEMENT_NOT_POSTED for state conflicts, and 404 SETTLEMENT_LINE_NOT_FOUND when the line is missing. 
     * Write Off Small Settlement Line
     */
    async writeOffSettlementLineRaw(requestParameters: WriteOffSettlementLineRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SettlementLineResponse>> {
        if (requestParameters['lineId'] == null) {
            throw new runtime.RequiredError(
                'lineId',
                'Required parameter "lineId" was null or undefined when calling writeOffSettlementLine().'
            );
        }

        if (requestParameters['settlementWriteOffRequest'] == null) {
            throw new runtime.RequiredError(
                'settlementWriteOffRequest',
                'Required parameter "settlementWriteOffRequest" was null or undefined when calling writeOffSettlementLine().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:reconciliation:adjust"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/settlements/lines/{lineId}/write-off`.replace(`{${"lineId"}}`, encodeURIComponent(String(requestParameters['lineId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SettlementWriteOffRequestToJSON(requestParameters['settlementWriteOffRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SettlementLineResponseFromJSON(jsonValue));
    }

    /**
     * Writes off an UNMATCHED settlement line whose gross is at or below the configured write-off threshold (default 25.00), posting a reversible adjustment entry (debit Settlement Suspense, credit Settlement Adjustment) rather than a silent status flip. Use this tool only for genuinely unidentifiable small residuals; do not use it above the threshold, where matchSettlementLine or escalation is the only path. Preconditions: the line must exist and be UNMATCHED, its parent settlement must be POSTED, and the line\'s absolute gross must not exceed the threshold. Required inputs: lineId (UUID) as a path parameter and a non-blank audited reason (max 500 chars). Emits an ACCOUNTING_SETTLEMENT_LINE_WRITE_OFF event and posts the adjustment entry through the accounting-period gate. Returns 422 WRITE_OFF_THRESHOLD_EXCEEDED when the absolute gross exceeds the threshold or PERIOD_CLOSED / PERIOD_HARD_LOCKED for locked periods, 409 SETTLEMENT_LINE_NOT_UNMATCHED or SETTLEMENT_NOT_POSTED for state conflicts, and 404 SETTLEMENT_LINE_NOT_FOUND when the line is missing. 
     * Write Off Small Settlement Line
     */
    async writeOffSettlementLine(requestParameters: WriteOffSettlementLineRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SettlementLineResponse> {
        const response = await this.writeOffSettlementLineRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
