/* tslint:disable */
/* eslint-disable */
/**
 * POS Accounting Service API
 * Accounting service for invoice, journal entry, and GL account management
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AccountingPeriodReopenRequest,
  AccountingPeriodResponse,
  ApiError,
  HardLockDateResponse,
  HardLockDateUpdateRequest,
} from '../models/index';
import {
    AccountingPeriodReopenRequestFromJSON,
    AccountingPeriodReopenRequestToJSON,
    AccountingPeriodResponseFromJSON,
    AccountingPeriodResponseToJSON,
    ApiErrorFromJSON,
    ApiErrorToJSON,
    HardLockDateResponseFromJSON,
    HardLockDateResponseToJSON,
    HardLockDateUpdateRequestFromJSON,
    HardLockDateUpdateRequestToJSON,
} from '../models/index';

export interface CloseAccountingPeriodRequest {
    periodCode: string;
}

export interface ReopenAccountingPeriodRequest {
    periodCode: string;
    accountingPeriodReopenRequest: AccountingPeriodReopenRequest;
}

export interface SetAccountingHardLockDateRequest {
    hardLockDateUpdateRequest: HardLockDateUpdateRequest;
}

/**
 * 
 */
export class AccountingPeriodsApi extends runtime.BaseAPI {

    /**
     * Closes an OPEN accounting period (OPEN to CLOSED), after which posting paths reject entries dated inside it with PERIOD_CLOSED unless a permissioned override is supplied. Use this tool during month-end close after all journal entries for the month are posted; do not use reopenAccountingPeriod, which reverses this transition for late adjustments. Preconditions: the period must not already be CLOSED, and no DRAFT journal entries may be dated inside the period; a valid YYYY-MM code with no row whose month has already started is auto-provisioned and then closed. Required inputs: periodCode (YYYY-MM) as a path parameter; there is no request body. Emits an ACCOUNTING_PERIOD_CLOSE event and audit-logs the close with the acting user. Returns 409 PERIOD_ALREADY_CLOSED when the period is already closed, 404 PERIOD_NOT_FOUND when no row exists and the month has not started, and 422 PERIOD_HAS_DRAFT_ENTRIES listing the blocking draftJournalEntryIds in fieldErrors; post or delete those entries before retrying. 
     * Close Accounting Period
     */
    async closeAccountingPeriodRaw(requestParameters: CloseAccountingPeriodRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccountingPeriodResponse>> {
        if (requestParameters['periodCode'] == null) {
            throw new runtime.RequiredError(
                'periodCode',
                'Required parameter "periodCode" was null or undefined when calling closeAccountingPeriod().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:period:close"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/periods/{periodCode}/close`.replace(`{${"periodCode"}}`, encodeURIComponent(String(requestParameters['periodCode']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccountingPeriodResponseFromJSON(jsonValue));
    }

    /**
     * Closes an OPEN accounting period (OPEN to CLOSED), after which posting paths reject entries dated inside it with PERIOD_CLOSED unless a permissioned override is supplied. Use this tool during month-end close after all journal entries for the month are posted; do not use reopenAccountingPeriod, which reverses this transition for late adjustments. Preconditions: the period must not already be CLOSED, and no DRAFT journal entries may be dated inside the period; a valid YYYY-MM code with no row whose month has already started is auto-provisioned and then closed. Required inputs: periodCode (YYYY-MM) as a path parameter; there is no request body. Emits an ACCOUNTING_PERIOD_CLOSE event and audit-logs the close with the acting user. Returns 409 PERIOD_ALREADY_CLOSED when the period is already closed, 404 PERIOD_NOT_FOUND when no row exists and the month has not started, and 422 PERIOD_HAS_DRAFT_ENTRIES listing the blocking draftJournalEntryIds in fieldErrors; post or delete those entries before retrying. 
     * Close Accounting Period
     */
    async closeAccountingPeriod(requestParameters: CloseAccountingPeriodRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccountingPeriodResponse> {
        const response = await this.closeAccountingPeriodRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the org-level hard-lock date; journal entries dated strictly before this date are permanently rejected with PERIOD_HARD_LOCKED and no override path, unlike a CLOSED period which accounting:period:override plus a justification can bypass. Use this tool to check the current lock boundary before posting backdated entries; do not use setAccountingHardLockDate, which moves the lock, unless a change is intended. Preconditions: none; the lock may be unset. Required inputs: none; there are no parameters and no request body. Emits an ACCOUNTING_PERIOD_HARD_LOCK_VIEW audit event; no state changes. Returns 200 with hardLockDate null when no hard lock has been configured yet. 
     * Get Accounting Hard-Lock Date
     */
    async getAccountingHardLockDateRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<HardLockDateResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:period:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/periods/hard-lock`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => HardLockDateResponseFromJSON(jsonValue));
    }

    /**
     * Returns the org-level hard-lock date; journal entries dated strictly before this date are permanently rejected with PERIOD_HARD_LOCKED and no override path, unlike a CLOSED period which accounting:period:override plus a justification can bypass. Use this tool to check the current lock boundary before posting backdated entries; do not use setAccountingHardLockDate, which moves the lock, unless a change is intended. Preconditions: none; the lock may be unset. Required inputs: none; there are no parameters and no request body. Emits an ACCOUNTING_PERIOD_HARD_LOCK_VIEW audit event; no state changes. Returns 200 with hardLockDate null when no hard lock has been configured yet. 
     * Get Accounting Hard-Lock Date
     */
    async getAccountingHardLockDate(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<HardLockDateResponse> {
        const response = await this.getAccountingHardLockDateRaw(initOverrides);
        return await response.value();
    }

    /**
     * Lists all known accounting periods with their OPEN or CLOSED status, most recent first (descending period code). Use this tool to review period statuses before closing or reopening a period; do not use closeAccountingPeriod or reopenAccountingPeriod, which perform the state transitions. Preconditions: none; periods are auto-provisioned on first posting, so months never posted into may be absent, and an absent month counts as OPEN for posting purposes. Required inputs: none; there are no parameters and no request body. Emits an ACCOUNTING_PERIOD_LIST audit event; nothing is created or changed by this call. Returns 200 with an empty list when no period has ever been provisioned. 
     * List Accounting Periods
     */
    async listAccountingPeriodsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<AccountingPeriodResponse>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:period:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/periods`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(AccountingPeriodResponseFromJSON));
    }

    /**
     * Lists all known accounting periods with their OPEN or CLOSED status, most recent first (descending period code). Use this tool to review period statuses before closing or reopening a period; do not use closeAccountingPeriod or reopenAccountingPeriod, which perform the state transitions. Preconditions: none; periods are auto-provisioned on first posting, so months never posted into may be absent, and an absent month counts as OPEN for posting purposes. Required inputs: none; there are no parameters and no request body. Emits an ACCOUNTING_PERIOD_LIST audit event; nothing is created or changed by this call. Returns 200 with an empty list when no period has ever been provisioned. 
     * List Accounting Periods
     */
    async listAccountingPeriods(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<AccountingPeriodResponse>> {
        const response = await this.listAccountingPeriodsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Reopens a CLOSED accounting period (CLOSED to OPEN), allowing entries to be posted into that month again. Use this tool only when late adjustments must be posted into an already-closed month; use closeAccountingPeriod instead to close it again afterwards, and prefer a permissioned closed-period override on postJournalEntry for a one-off posting. Preconditions: a period row must exist for the code and be CLOSED. Required inputs: periodCode (YYYY-MM) as a path parameter and a non-blank justification (max 500 characters), recorded on the period and in the audit trail with the acting user. Emits an ACCOUNTING_PERIOD_REOPEN event. Returns 409 PERIOD_ALREADY_OPEN when the period is not closed, 404 PERIOD_NOT_FOUND when no period row exists for the code, and 400 when the justification is missing or blank. 
     * Reopen Accounting Period
     */
    async reopenAccountingPeriodRaw(requestParameters: ReopenAccountingPeriodRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccountingPeriodResponse>> {
        if (requestParameters['periodCode'] == null) {
            throw new runtime.RequiredError(
                'periodCode',
                'Required parameter "periodCode" was null or undefined when calling reopenAccountingPeriod().'
            );
        }

        if (requestParameters['accountingPeriodReopenRequest'] == null) {
            throw new runtime.RequiredError(
                'accountingPeriodReopenRequest',
                'Required parameter "accountingPeriodReopenRequest" was null or undefined when calling reopenAccountingPeriod().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:period:reopen"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/periods/{periodCode}/reopen`.replace(`{${"periodCode"}}`, encodeURIComponent(String(requestParameters['periodCode']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AccountingPeriodReopenRequestToJSON(requestParameters['accountingPeriodReopenRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccountingPeriodResponseFromJSON(jsonValue));
    }

    /**
     * Reopens a CLOSED accounting period (CLOSED to OPEN), allowing entries to be posted into that month again. Use this tool only when late adjustments must be posted into an already-closed month; use closeAccountingPeriod instead to close it again afterwards, and prefer a permissioned closed-period override on postJournalEntry for a one-off posting. Preconditions: a period row must exist for the code and be CLOSED. Required inputs: periodCode (YYYY-MM) as a path parameter and a non-blank justification (max 500 characters), recorded on the period and in the audit trail with the acting user. Emits an ACCOUNTING_PERIOD_REOPEN event. Returns 409 PERIOD_ALREADY_OPEN when the period is not closed, 404 PERIOD_NOT_FOUND when no period row exists for the code, and 400 when the justification is missing or blank. 
     * Reopen Accounting Period
     */
    async reopenAccountingPeriod(requestParameters: ReopenAccountingPeriodRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccountingPeriodResponse> {
        const response = await this.reopenAccountingPeriodRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Sets the org-level hard-lock date; from then on, journal entries dated strictly before this date are permanently rejected with PERIOD_HARD_LOCKED and no override path, not even accounting:period:override, which only covers CLOSED periods. Use this tool after statutory filings or audits to make history immutable; use closeAccountingPeriod for the reversible month-end close instead. Preconditions: the new date must be on or after the currently stored hard-lock date; the lock is monotonic-forward-only and effectively irreversible, so set it deliberately. Required inputs: hardLockDate (ISO date) and a non-blank justification (max 500 characters), recorded in the audit trail with the acting user. Emits an ACCOUNTING_PERIOD_HARD_LOCK_SET event and returns the stored hard-lock date. Returns 422 HARD_LOCK_DATE_REGRESSION when the date would move backward, and 400 when the date or justification is missing or blank. 
     * Set Accounting Hard-Lock Date
     */
    async setAccountingHardLockDateRaw(requestParameters: SetAccountingHardLockDateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<HardLockDateResponse>> {
        if (requestParameters['hardLockDateUpdateRequest'] == null) {
            throw new runtime.RequiredError(
                'hardLockDateUpdateRequest',
                'Required parameter "hardLockDateUpdateRequest" was null or undefined when calling setAccountingHardLockDate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:period:hard_lock"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/periods/hard-lock`,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: HardLockDateUpdateRequestToJSON(requestParameters['hardLockDateUpdateRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => HardLockDateResponseFromJSON(jsonValue));
    }

    /**
     * Sets the org-level hard-lock date; from then on, journal entries dated strictly before this date are permanently rejected with PERIOD_HARD_LOCKED and no override path, not even accounting:period:override, which only covers CLOSED periods. Use this tool after statutory filings or audits to make history immutable; use closeAccountingPeriod for the reversible month-end close instead. Preconditions: the new date must be on or after the currently stored hard-lock date; the lock is monotonic-forward-only and effectively irreversible, so set it deliberately. Required inputs: hardLockDate (ISO date) and a non-blank justification (max 500 characters), recorded in the audit trail with the acting user. Emits an ACCOUNTING_PERIOD_HARD_LOCK_SET event and returns the stored hard-lock date. Returns 422 HARD_LOCK_DATE_REGRESSION when the date would move backward, and 400 when the date or justification is missing or blank. 
     * Set Accounting Hard-Lock Date
     */
    async setAccountingHardLockDate(requestParameters: SetAccountingHardLockDateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<HardLockDateResponse> {
        const response = await this.setAccountingHardLockDateRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
