/* tslint:disable */
/* eslint-disable */
/**
 * POS Accounting Service API
 * Accounting service for invoice, journal entry, and GL account management
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  InvoiceRevenueReconcileRequest,
  InvoiceRevenueReconcileResponse,
} from '../models/index';
import {
    InvoiceRevenueReconcileRequestFromJSON,
    InvoiceRevenueReconcileRequestToJSON,
    InvoiceRevenueReconcileResponseFromJSON,
    InvoiceRevenueReconcileResponseToJSON,
} from '../models/index';

export interface ReconcileInvoiceRevenueRequest {
    invoiceRevenueReconcileRequest?: InvoiceRevenueReconcileRequest;
}

/**
 * 
 */
export class AccountingGLApi extends runtime.BaseAPI {

    /**
     * Posts revenue to the general ledger for every finalized invoice in the accounting replica that has no open revenue posting yet, using the same entries the live invoice event path creates (Dr Accounts Receivable / Cr Service Revenue / Cr Sales Tax Payable, dated at the invoice\'s finalization). Use this tool to backfill invoices finalized before revenue posting existed, after a consumer outage, or when the income statement disagrees with invoice totals; do not use retryAccountingEvent, which re-runs a single failed ingestion event, or an outbox replay, which cannot reach invoices that never produced an event. Preconditions: caller holds accounting:gl:reconcile. Idempotent: an invoice already on the ledger is reported as ALREADY_POSTED and left alone; a second run posts nothing new. Required inputs: none. Optional body: finalizedFrom / finalizedTo (half-open instant window), invoiceIds (overrides the window), dryRun (report without posting), limit (oldest first). Emits an ACCOUNTING_INVOICE_REVENUE_RECONCILE event and returns 200 with per-invoice outcomes (POSTED, WOULD_POST, ALREADY_POSTED, SKIPPED with reason, FAILED with error) and totals. 
     * Reconcile Invoice Revenue Postings
     */
    async reconcileInvoiceRevenueRaw(requestParameters: ReconcileInvoiceRevenueRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InvoiceRevenueReconcileResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:gl:reconcile"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/invoice-revenue/reconcile`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: InvoiceRevenueReconcileRequestToJSON(requestParameters['invoiceRevenueReconcileRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InvoiceRevenueReconcileResponseFromJSON(jsonValue));
    }

    /**
     * Posts revenue to the general ledger for every finalized invoice in the accounting replica that has no open revenue posting yet, using the same entries the live invoice event path creates (Dr Accounts Receivable / Cr Service Revenue / Cr Sales Tax Payable, dated at the invoice\'s finalization). Use this tool to backfill invoices finalized before revenue posting existed, after a consumer outage, or when the income statement disagrees with invoice totals; do not use retryAccountingEvent, which re-runs a single failed ingestion event, or an outbox replay, which cannot reach invoices that never produced an event. Preconditions: caller holds accounting:gl:reconcile. Idempotent: an invoice already on the ledger is reported as ALREADY_POSTED and left alone; a second run posts nothing new. Required inputs: none. Optional body: finalizedFrom / finalizedTo (half-open instant window), invoiceIds (overrides the window), dryRun (report without posting), limit (oldest first). Emits an ACCOUNTING_INVOICE_REVENUE_RECONCILE event and returns 200 with per-invoice outcomes (POSTED, WOULD_POST, ALREADY_POSTED, SKIPPED with reason, FAILED with error) and totals. 
     * Reconcile Invoice Revenue Postings
     */
    async reconcileInvoiceRevenue(requestParameters: ReconcileInvoiceRevenueRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InvoiceRevenueReconcileResponse> {
        const response = await this.reconcileInvoiceRevenueRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
