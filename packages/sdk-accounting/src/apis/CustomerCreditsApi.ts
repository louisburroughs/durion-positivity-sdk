/* tslint:disable */
/* eslint-disable */
/**
 * POS Accounting Service API
 * Accounting service for invoice, journal entry, and GL account management
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  CustomerCreditApplicationRequest,
  CustomerCreditRefundRequest,
  CustomerCreditResponse,
  CustomerCreditTransactionResponse,
  PageCustomerCreditResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    CustomerCreditApplicationRequestFromJSON,
    CustomerCreditApplicationRequestToJSON,
    CustomerCreditRefundRequestFromJSON,
    CustomerCreditRefundRequestToJSON,
    CustomerCreditResponseFromJSON,
    CustomerCreditResponseToJSON,
    CustomerCreditTransactionResponseFromJSON,
    CustomerCreditTransactionResponseToJSON,
    PageCustomerCreditResponseFromJSON,
    PageCustomerCreditResponseToJSON,
} from '../models/index';

export interface ApplyCustomerCreditRequest {
    creditId: string;
    customerCreditApplicationRequest: CustomerCreditApplicationRequest;
}

export interface GetCustomerCreditRequest {
    creditId: string;
}

export interface ListCustomerCreditsRequest {
    customerId?: string;
    status?: ListCustomerCreditsStatusEnum;
    page?: number;
    size?: number;
    sort?: Array<string>;
}

export interface RefundCustomerCreditRequest {
    creditId: string;
    customerCreditRefundRequest: CustomerCreditRefundRequest;
}

/**
 * 
 */
export class CustomerCreditsApi extends runtime.BaseAPI {

    /**
     * Draws an open customer credit down against an outstanding invoice, posting debit Customer Credit Liability, credit Accounts Receivable for the applied amount. Use this tool to consume standing credit against an invoice; do not use refundCustomerCredit, which pays the credit back as cash, and do not use createCreditMemo, which issues new credit against an invoice. Preconditions: the credit must have sufficient open amount, the invoice must be AR-eligible and belong to the same customer, the amount must not exceed the invoice balance, and the accounting period must be open. Required inputs: creditId (UUID) as a path parameter, requestId (max 100 chars, the idempotency key), invoiceId (UUID) and amount (min 0.01). Emits an ACCOUNTING_CUSTOMER_CREDIT_APPLY event; replaying the same requestId returns the original application instead of double-applying. Returns 404 when the credit or invoice is not found, 409 for insufficient open credit, an ineligible or foreign invoice, an amount over the invoice balance, a closed period, or a requestId reused for a different operation, and 400 when the amount is missing or non-positive. 
     * Apply Customer Credit To Invoice
     */
    async applyCustomerCreditRaw(requestParameters: ApplyCustomerCreditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CustomerCreditTransactionResponse>> {
        if (requestParameters['creditId'] == null) {
            throw new runtime.RequiredError(
                'creditId',
                'Required parameter "creditId" was null or undefined when calling applyCustomerCredit().'
            );
        }

        if (requestParameters['customerCreditApplicationRequest'] == null) {
            throw new runtime.RequiredError(
                'customerCreditApplicationRequest',
                'Required parameter "customerCreditApplicationRequest" was null or undefined when calling applyCustomerCredit().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:customer-credit:apply"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/customer-credits/{creditId}/applications`.replace(`{${"creditId"}}`, encodeURIComponent(String(requestParameters['creditId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CustomerCreditApplicationRequestToJSON(requestParameters['customerCreditApplicationRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CustomerCreditTransactionResponseFromJSON(jsonValue));
    }

    /**
     * Draws an open customer credit down against an outstanding invoice, posting debit Customer Credit Liability, credit Accounts Receivable for the applied amount. Use this tool to consume standing credit against an invoice; do not use refundCustomerCredit, which pays the credit back as cash, and do not use createCreditMemo, which issues new credit against an invoice. Preconditions: the credit must have sufficient open amount, the invoice must be AR-eligible and belong to the same customer, the amount must not exceed the invoice balance, and the accounting period must be open. Required inputs: creditId (UUID) as a path parameter, requestId (max 100 chars, the idempotency key), invoiceId (UUID) and amount (min 0.01). Emits an ACCOUNTING_CUSTOMER_CREDIT_APPLY event; replaying the same requestId returns the original application instead of double-applying. Returns 404 when the credit or invoice is not found, 409 for insufficient open credit, an ineligible or foreign invoice, an amount over the invoice balance, a closed period, or a requestId reused for a different operation, and 400 when the amount is missing or non-positive. 
     * Apply Customer Credit To Invoice
     */
    async applyCustomerCredit(requestParameters: ApplyCustomerCreditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CustomerCreditTransactionResponse> {
        const response = await this.applyCustomerCreditRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one AR customer credit with its applied and refunded totals and the remaining open amount. Use this tool when the credit id is already known; use listCustomerCredits instead when searching by customer or consumption state. Preconditions: the customer credit must exist. Required inputs: creditId (UUID) as a path parameter; there is no request body. Emits an ACCOUNTING_CUSTOMER_CREDIT_GET audit event; no state changes. Returns 404 when no customer credit exists for the supplied id. 
     * Get Customer Credit
     */
    async getCustomerCreditRaw(requestParameters: GetCustomerCreditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CustomerCreditResponse>> {
        if (requestParameters['creditId'] == null) {
            throw new runtime.RequiredError(
                'creditId',
                'Required parameter "creditId" was null or undefined when calling getCustomerCredit().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:customer-credit:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/customer-credits/{creditId}`.replace(`{${"creditId"}}`, encodeURIComponent(String(requestParameters['creditId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CustomerCreditResponseFromJSON(jsonValue));
    }

    /**
     * Returns one AR customer credit with its applied and refunded totals and the remaining open amount. Use this tool when the credit id is already known; use listCustomerCredits instead when searching by customer or consumption state. Preconditions: the customer credit must exist. Required inputs: creditId (UUID) as a path parameter; there is no request body. Emits an ACCOUNTING_CUSTOMER_CREDIT_GET audit event; no state changes. Returns 404 when no customer credit exists for the supplied id. 
     * Get Customer Credit
     */
    async getCustomerCredit(requestParameters: GetCustomerCreditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CustomerCreditResponse> {
        const response = await this.getCustomerCreditRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists AR customer credits with their remaining open amounts as a paginated projection, optionally filtered by customer and consumption state. Use this tool to find open credit before applying or refunding; do not use getCustomerCredit, which fetches one credit by its known id. Preconditions: none beyond the caller holding accounting:customer-credit:view. Required inputs: none; customerId and status are optional filters, with standard page, size and sort parameters. Emits an ACCOUNTING_CUSTOMER_CREDIT_LIST audit event; no state changes. Returns 200 with an empty page when nothing matches the filters. 
     * List Customer Credits
     */
    async listCustomerCreditsRaw(requestParameters: ListCustomerCreditsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PageCustomerCreditResponse>> {
        const queryParameters: any = {};

        if (requestParameters['customerId'] != null) {
            queryParameters['customerId'] = requestParameters['customerId'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        if (requestParameters['sort'] != null) {
            queryParameters['sort'] = requestParameters['sort'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:customer-credit:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/customer-credits`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PageCustomerCreditResponseFromJSON(jsonValue));
    }

    /**
     * Lists AR customer credits with their remaining open amounts as a paginated projection, optionally filtered by customer and consumption state. Use this tool to find open credit before applying or refunding; do not use getCustomerCredit, which fetches one credit by its known id. Preconditions: none beyond the caller holding accounting:customer-credit:view. Required inputs: none; customerId and status are optional filters, with standard page, size and sort parameters. Emits an ACCOUNTING_CUSTOMER_CREDIT_LIST audit event; no state changes. Returns 200 with an empty page when nothing matches the filters. 
     * List Customer Credits
     */
    async listCustomerCredits(requestParameters: ListCustomerCreditsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PageCustomerCreditResponse> {
        const response = await this.listCustomerCreditsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Refunds an open customer credit as cash back to the customer, posting debit Customer Credit Liability, credit Undeposited Funds for the refunded amount. Use this tool to pay standing credit out; do not use applyCustomerCredit, which consumes the credit against an invoice instead. Preconditions: the credit must have sufficient open amount and the accounting period must be open. Required inputs: creditId (UUID) as a path parameter, requestId (max 100 chars, the idempotency key) and amount (min 0.01); note (max 500 chars) is optional. Emits an ACCOUNTING_CUSTOMER_CREDIT_REFUND event; replaying the same requestId returns the original refund instead of double-paying. Returns 404 when the credit is not found, 409 for insufficient open credit, a closed period, or a requestId reused for a different operation, and 400 when the amount is missing or non-positive. 
     * Refund Customer Credit
     */
    async refundCustomerCreditRaw(requestParameters: RefundCustomerCreditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CustomerCreditTransactionResponse>> {
        if (requestParameters['creditId'] == null) {
            throw new runtime.RequiredError(
                'creditId',
                'Required parameter "creditId" was null or undefined when calling refundCustomerCredit().'
            );
        }

        if (requestParameters['customerCreditRefundRequest'] == null) {
            throw new runtime.RequiredError(
                'customerCreditRefundRequest',
                'Required parameter "customerCreditRefundRequest" was null or undefined when calling refundCustomerCredit().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:customer-credit:refund"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/customer-credits/{creditId}/refunds`.replace(`{${"creditId"}}`, encodeURIComponent(String(requestParameters['creditId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CustomerCreditRefundRequestToJSON(requestParameters['customerCreditRefundRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CustomerCreditTransactionResponseFromJSON(jsonValue));
    }

    /**
     * Refunds an open customer credit as cash back to the customer, posting debit Customer Credit Liability, credit Undeposited Funds for the refunded amount. Use this tool to pay standing credit out; do not use applyCustomerCredit, which consumes the credit against an invoice instead. Preconditions: the credit must have sufficient open amount and the accounting period must be open. Required inputs: creditId (UUID) as a path parameter, requestId (max 100 chars, the idempotency key) and amount (min 0.01); note (max 500 chars) is optional. Emits an ACCOUNTING_CUSTOMER_CREDIT_REFUND event; replaying the same requestId returns the original refund instead of double-paying. Returns 404 when the credit is not found, 409 for insufficient open credit, a closed period, or a requestId reused for a different operation, and 400 when the amount is missing or non-positive. 
     * Refund Customer Credit
     */
    async refundCustomerCredit(requestParameters: RefundCustomerCreditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CustomerCreditTransactionResponse> {
        const response = await this.refundCustomerCreditRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
  * @export
  * @enum {string}
  */
export enum ListCustomerCreditsStatusEnum {
    Available = 'AVAILABLE',
    PartiallyConsumed = 'PARTIALLY_CONSUMED',
    Consumed = 'CONSUMED'
}
