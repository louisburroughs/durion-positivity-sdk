/* tslint:disable */
/* eslint-disable */
/**
 * POS Accounting Service API
 * Accounting service for invoice, journal entry, and GL account management
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  LaborOverheadCostReport,
} from '../models/index';
import {
    LaborOverheadCostReportFromJSON,
    LaborOverheadCostReportToJSON,
} from '../models/index';

export interface GenerateLaborOverheadReportRequest {
    locationId: string;
    fiscalYear: number;
    asOfMonth?: number;
}

/**
 * 
 */
export class LocationCostReportingApi extends runtime.BaseAPI {

    /**
     * Generates the read-only Location Labor and Overhead Cost Report: the canonical cost-line matrix of 12 monthly amounts plus YTD with section subtotals, for one location and fiscal year, derived entirely from posted GL data. Use this tool for location-level labor and overhead cost review; do not use generateIncomeStatement, which is the org-wide statement without the location dimension. Preconditions: none; a location with no posted activity yields zeroed lines. Required inputs: locationId (the accounting locationId dimension, e.g. LOC-107) and fiscalYear (four-digit year); asOfMonth (1-12) is optional and defaults to 12, bounding the YTD column. Emits a REPORT_LABOR_OVERHEAD_GENERATE audit event; no state changes. Returns 400 when fiscalYear is not a four-digit year or asOfMonth is outside 1 to 12. 
     * Generate Labor Overhead Cost Report
     */
    async generateLaborOverheadReportRaw(requestParameters: GenerateLaborOverheadReportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LaborOverheadCostReport>> {
        if (requestParameters['locationId'] == null) {
            throw new runtime.RequiredError(
                'locationId',
                'Required parameter "locationId" was null or undefined when calling generateLaborOverheadReport().'
            );
        }

        if (requestParameters['fiscalYear'] == null) {
            throw new runtime.RequiredError(
                'fiscalYear',
                'Required parameter "fiscalYear" was null or undefined when calling generateLaborOverheadReport().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['locationId'] != null) {
            queryParameters['locationId'] = requestParameters['locationId'];
        }

        if (requestParameters['fiscalYear'] != null) {
            queryParameters['fiscalYear'] = requestParameters['fiscalYear'];
        }

        if (requestParameters['asOfMonth'] != null) {
            queryParameters['asOfMonth'] = requestParameters['asOfMonth'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["reporting:view:financial-statements"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/reports/location/labor-overhead`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LaborOverheadCostReportFromJSON(jsonValue));
    }

    /**
     * Generates the read-only Location Labor and Overhead Cost Report: the canonical cost-line matrix of 12 monthly amounts plus YTD with section subtotals, for one location and fiscal year, derived entirely from posted GL data. Use this tool for location-level labor and overhead cost review; do not use generateIncomeStatement, which is the org-wide statement without the location dimension. Preconditions: none; a location with no posted activity yields zeroed lines. Required inputs: locationId (the accounting locationId dimension, e.g. LOC-107) and fiscalYear (four-digit year); asOfMonth (1-12) is optional and defaults to 12, bounding the YTD column. Emits a REPORT_LABOR_OVERHEAD_GENERATE audit event; no state changes. Returns 400 when fiscalYear is not a four-digit year or asOfMonth is outside 1 to 12. 
     * Generate Labor Overhead Cost Report
     */
    async generateLaborOverheadReport(requestParameters: GenerateLaborOverheadReportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LaborOverheadCostReport> {
        const response = await this.generateLaborOverheadReportRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
