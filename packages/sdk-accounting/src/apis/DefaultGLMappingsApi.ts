/* tslint:disable */
/* eslint-disable */
/**
 * POS Accounting Service API
 * Accounting service for invoice, journal entry, and GL account management
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  DefaultGLMappingListResponse,
  DefaultGLMappingRequest,
  DefaultGLMappingResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    DefaultGLMappingListResponseFromJSON,
    DefaultGLMappingListResponseToJSON,
    DefaultGLMappingRequestFromJSON,
    DefaultGLMappingRequestToJSON,
    DefaultGLMappingResponseFromJSON,
    DefaultGLMappingResponseToJSON,
} from '../models/index';

export interface CreateDefaultMappingRequest {
    defaultGLMappingRequest: DefaultGLMappingRequest;
}

export interface DeactivateDefaultMappingRequest {
    id: string;
}

export interface GetDefaultMappingRequest {
    id: string;
}

export interface ListDefaultMappingsRequest {
    page?: number;
    size?: number;
}

export interface ResolveDefaultMappingRequest {
    eventType: string;
    organizationId?: string;
}

export interface SearchDefaultMappingsRequest {
    eventType?: string;
    organizationId?: string;
}

export interface UpdateDefaultMappingRequest {
    id: string;
    defaultGLMappingRequest: DefaultGLMappingRequest;
}

/**
 * 
 */
export class DefaultGLMappingsApi extends runtime.BaseAPI {

    /**
     * Creates a fallback debit and credit account pair for an event type, used by the posting engine when no explicit posting rule matches; resolution order is explicit rule, then org-scoped default, then global default, then UNMAPPED_EVENT_TYPE failure. Use this tool to guarantee an event type always posts somewhere; do not use createGLMapping, which maps a source-system external code to a single account. Preconditions: the debit and credit GL accounts must exist. Required inputs: eventType (max 100 chars), debitAccountId (UUID) and creditAccountId (UUID); organizationId is optional (null makes the default global) and active defaults to true. Emits an ACCOUNTING_DEFAULT_MAPPING_CREATE event. Returns 400 for an invalid request, 404 GL_ACCOUNT_NOT_FOUND when a referenced GL account does not exist, and 422 GL_ACCOUNT_NOT_ACTIVE when it exists but is not active on the current date. 
     * Create Default GL Mapping
     */
    async createDefaultMappingRaw(requestParameters: CreateDefaultMappingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DefaultGLMappingResponse>> {
        if (requestParameters['defaultGLMappingRequest'] == null) {
            throw new runtime.RequiredError(
                'defaultGLMappingRequest',
                'Required parameter "defaultGLMappingRequest" was null or undefined when calling createDefaultMapping().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:default-mapping:create"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/default-mappings`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: DefaultGLMappingRequestToJSON(requestParameters['defaultGLMappingRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DefaultGLMappingResponseFromJSON(jsonValue));
    }

    /**
     * Creates a fallback debit and credit account pair for an event type, used by the posting engine when no explicit posting rule matches; resolution order is explicit rule, then org-scoped default, then global default, then UNMAPPED_EVENT_TYPE failure. Use this tool to guarantee an event type always posts somewhere; do not use createGLMapping, which maps a source-system external code to a single account. Preconditions: the debit and credit GL accounts must exist. Required inputs: eventType (max 100 chars), debitAccountId (UUID) and creditAccountId (UUID); organizationId is optional (null makes the default global) and active defaults to true. Emits an ACCOUNTING_DEFAULT_MAPPING_CREATE event. Returns 400 for an invalid request, 404 GL_ACCOUNT_NOT_FOUND when a referenced GL account does not exist, and 422 GL_ACCOUNT_NOT_ACTIVE when it exists but is not active on the current date. 
     * Create Default GL Mapping
     */
    async createDefaultMapping(requestParameters: CreateDefaultMappingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DefaultGLMappingResponse> {
        const response = await this.createDefaultMappingRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Soft deletes a default GL mapping by marking it inactive; the row is retained for history and stops participating in fallback resolution. Use this tool to retire a fallback pair; do not use updateDefaultMapping with active=false only when other fields must change at the same time, and note there is no reactivation endpoint other than updateDefaultMapping. Preconditions: the default mapping must exist. Required inputs: id (UUID) as a path parameter; there is no request body. Emits an ACCOUNTING_DEFAULT_MAPPING_DELETE event. Returns 204 with no body on success, and 404 DEFAULT_GL_MAPPING_NOT_FOUND when no default mapping exists for the supplied id. 
     * Deactivate Default GL Mapping
     */
    async deactivateDefaultMappingRaw(requestParameters: DeactivateDefaultMappingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deactivateDefaultMapping().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:default-mapping:delete"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/default-mappings/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Soft deletes a default GL mapping by marking it inactive; the row is retained for history and stops participating in fallback resolution. Use this tool to retire a fallback pair; do not use updateDefaultMapping with active=false only when other fields must change at the same time, and note there is no reactivation endpoint other than updateDefaultMapping. Preconditions: the default mapping must exist. Required inputs: id (UUID) as a path parameter; there is no request body. Emits an ACCOUNTING_DEFAULT_MAPPING_DELETE event. Returns 204 with no body on success, and 404 DEFAULT_GL_MAPPING_NOT_FOUND when no default mapping exists for the supplied id. 
     * Deactivate Default GL Mapping
     */
    async deactivateDefaultMapping(requestParameters: DeactivateDefaultMappingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deactivateDefaultMappingRaw(requestParameters, initOverrides);
    }

    /**
     * Returns one default GL mapping with its event type, debit and credit accounts, scope and active flag. Use this tool when the mapping id is already known; use searchDefaultMappings or listDefaultMappings instead when hunting by event type or organization. Preconditions: the default mapping must exist. Required inputs: id (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 DEFAULT_GL_MAPPING_NOT_FOUND when no default mapping exists for the supplied id. 
     * Get Default GL Mapping
     */
    async getDefaultMappingRaw(requestParameters: GetDefaultMappingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DefaultGLMappingResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getDefaultMapping().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:default-mapping:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/default-mappings/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DefaultGLMappingResponseFromJSON(jsonValue));
    }

    /**
     * Returns one default GL mapping with its event type, debit and credit accounts, scope and active flag. Use this tool when the mapping id is already known; use searchDefaultMappings or listDefaultMappings instead when hunting by event type or organization. Preconditions: the default mapping must exist. Required inputs: id (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 DEFAULT_GL_MAPPING_NOT_FOUND when no default mapping exists for the supplied id. 
     * Get Default GL Mapping
     */
    async getDefaultMapping(requestParameters: GetDefaultMappingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DefaultGLMappingResponse> {
        const response = await this.getDefaultMappingRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists all default GL mappings as a paginated projection, including inactive ones. Use this tool to browse the fallback catalog; use searchDefaultMappings instead to filter by event type or organization, or resolveDefaultMapping to find the one that would apply to an event. Preconditions: none beyond the caller holding accounting:default-mapping:view. Required inputs: none; page defaults to 0 and size to 20. Emits an ACCOUNTING_DEFAULT_MAPPING_LIST audit event; no state changes. Returns 200 with an empty page when no default mappings exist. 
     * List Default GL Mappings
     */
    async listDefaultMappingsRaw(requestParameters: ListDefaultMappingsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DefaultGLMappingListResponse>> {
        const queryParameters: any = {};

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:default-mapping:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/default-mappings`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DefaultGLMappingListResponseFromJSON(jsonValue));
    }

    /**
     * Lists all default GL mappings as a paginated projection, including inactive ones. Use this tool to browse the fallback catalog; use searchDefaultMappings instead to filter by event type or organization, or resolveDefaultMapping to find the one that would apply to an event. Preconditions: none beyond the caller holding accounting:default-mapping:view. Required inputs: none; page defaults to 0 and size to 20. Emits an ACCOUNTING_DEFAULT_MAPPING_LIST audit event; no state changes. Returns 200 with an empty page when no default mappings exist. 
     * List Default GL Mappings
     */
    async listDefaultMappings(requestParameters: ListDefaultMappingsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DefaultGLMappingListResponse> {
        const response = await this.listDefaultMappingsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns all global default GL mappings, meaning those with no organizationId, which act as the last fallback before an event type fails as UNMAPPED_EVENT_TYPE. Use this tool to audit the global safety net; use searchDefaultMappings instead when org-scoped mappings are also of interest. Preconditions: none beyond the caller holding accounting:default-mapping:view. Required inputs: none; there are no parameters and no request body. No events are emitted and no state changes; this is a read-only projection. Returns 200 with an empty list when no global defaults are configured. 
     * List Global Default Mappings
     */
    async listGlobalDefaultMappingsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<DefaultGLMappingResponse>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:default-mapping:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/default-mappings/global`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(DefaultGLMappingResponseFromJSON));
    }

    /**
     * Returns all global default GL mappings, meaning those with no organizationId, which act as the last fallback before an event type fails as UNMAPPED_EVENT_TYPE. Use this tool to audit the global safety net; use searchDefaultMappings instead when org-scoped mappings are also of interest. Preconditions: none beyond the caller holding accounting:default-mapping:view. Required inputs: none; there are no parameters and no request body. No events are emitted and no state changes; this is a read-only projection. Returns 200 with an empty list when no global defaults are configured. 
     * List Global Default Mappings
     */
    async listGlobalDefaultMappings(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<DefaultGLMappingResponse>> {
        const response = await this.listGlobalDefaultMappingsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Finds the most specific active default mapping for an event type, preferring an org-scoped mapping over the global one. Use this tool to see which fallback the posting engine would use for an event; do not use searchDefaultMappings, which lists matches without applying resolution priority. Preconditions: an active default mapping must exist for the event type at org or global scope. Required inputs: eventType as a query parameter; organizationId (UUID) is optional and widens resolution to that org\'s mappings first. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no active default mapping resolves for the event type. 
     * Resolve Default Mapping For Event
     */
    async resolveDefaultMappingRaw(requestParameters: ResolveDefaultMappingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DefaultGLMappingResponse>> {
        if (requestParameters['eventType'] == null) {
            throw new runtime.RequiredError(
                'eventType',
                'Required parameter "eventType" was null or undefined when calling resolveDefaultMapping().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['eventType'] != null) {
            queryParameters['eventType'] = requestParameters['eventType'];
        }

        if (requestParameters['organizationId'] != null) {
            queryParameters['organizationId'] = requestParameters['organizationId'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:default-mapping:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/default-mappings/resolve`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DefaultGLMappingResponseFromJSON(jsonValue));
    }

    /**
     * Finds the most specific active default mapping for an event type, preferring an org-scoped mapping over the global one. Use this tool to see which fallback the posting engine would use for an event; do not use searchDefaultMappings, which lists matches without applying resolution priority. Preconditions: an active default mapping must exist for the event type at org or global scope. Required inputs: eventType as a query parameter; organizationId (UUID) is optional and widens resolution to that org\'s mappings first. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no active default mapping resolves for the event type. 
     * Resolve Default Mapping For Event
     */
    async resolveDefaultMapping(requestParameters: ResolveDefaultMappingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DefaultGLMappingResponse> {
        const response = await this.resolveDefaultMappingRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Finds default GL mappings filtered by event type or organization; when both filters are omitted it returns the global defaults only. Use this tool to filter the fallback catalog; do not use resolveDefaultMapping, which applies the org-then-global resolution priority to pick the single winning mapping. Preconditions: none; eventType takes precedence when both filters are supplied. Required inputs: none; eventType and organizationId are optional query parameters. No events are emitted and no state changes; this is a read-only projection. Returns 200 with an empty list when nothing matches the filter. 
     * Search Default GL Mappings
     */
    async searchDefaultMappingsRaw(requestParameters: SearchDefaultMappingsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<DefaultGLMappingResponse>>> {
        const queryParameters: any = {};

        if (requestParameters['eventType'] != null) {
            queryParameters['eventType'] = requestParameters['eventType'];
        }

        if (requestParameters['organizationId'] != null) {
            queryParameters['organizationId'] = requestParameters['organizationId'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:default-mapping:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/default-mappings/search`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(DefaultGLMappingResponseFromJSON));
    }

    /**
     * Finds default GL mappings filtered by event type or organization; when both filters are omitted it returns the global defaults only. Use this tool to filter the fallback catalog; do not use resolveDefaultMapping, which applies the org-then-global resolution priority to pick the single winning mapping. Preconditions: none; eventType takes precedence when both filters are supplied. Required inputs: none; eventType and organizationId are optional query parameters. No events are emitted and no state changes; this is a read-only projection. Returns 200 with an empty list when nothing matches the filter. 
     * Search Default GL Mappings
     */
    async searchDefaultMappings(requestParameters: SearchDefaultMappingsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<DefaultGLMappingResponse>> {
        const response = await this.searchDefaultMappingsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Updates an existing default GL mapping\'s event type, accounts, scope, description or active flag. Use this tool to repoint or rescope an existing fallback; do not use createDefaultMapping, which adds a new one, or deactivateDefaultMapping, which soft deletes. Preconditions: the default mapping must exist. Required inputs: id (UUID) as a path parameter plus the full replacement body with eventType, debitAccountId and creditAccountId; organizationId null means global scope. Emits an ACCOUNTING_DEFAULT_MAPPING_UPDATE event; future postings resolve against the updated pair while already-posted entries are untouched. Returns 404 DEFAULT_GL_MAPPING_NOT_FOUND when no default mapping exists for the supplied id, 404 GL_ACCOUNT_NOT_FOUND when a referenced GL account does not exist, and 422 GL_ACCOUNT_NOT_ACTIVE when it exists but is not active on the current date. 
     * Update Default GL Mapping
     */
    async updateDefaultMappingRaw(requestParameters: UpdateDefaultMappingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DefaultGLMappingResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updateDefaultMapping().'
            );
        }

        if (requestParameters['defaultGLMappingRequest'] == null) {
            throw new runtime.RequiredError(
                'defaultGLMappingRequest',
                'Required parameter "defaultGLMappingRequest" was null or undefined when calling updateDefaultMapping().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["accounting:default-mapping:edit"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/accounting/default-mappings/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: DefaultGLMappingRequestToJSON(requestParameters['defaultGLMappingRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DefaultGLMappingResponseFromJSON(jsonValue));
    }

    /**
     * Updates an existing default GL mapping\'s event type, accounts, scope, description or active flag. Use this tool to repoint or rescope an existing fallback; do not use createDefaultMapping, which adds a new one, or deactivateDefaultMapping, which soft deletes. Preconditions: the default mapping must exist. Required inputs: id (UUID) as a path parameter plus the full replacement body with eventType, debitAccountId and creditAccountId; organizationId null means global scope. Emits an ACCOUNTING_DEFAULT_MAPPING_UPDATE event; future postings resolve against the updated pair while already-posted entries are untouched. Returns 404 DEFAULT_GL_MAPPING_NOT_FOUND when no default mapping exists for the supplied id, 404 GL_ACCOUNT_NOT_FOUND when a referenced GL account does not exist, and 422 GL_ACCOUNT_NOT_ACTIVE when it exists but is not active on the current date. 
     * Update Default GL Mapping
     */
    async updateDefaultMapping(requestParameters: UpdateDefaultMappingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DefaultGLMappingResponse> {
        const response = await this.updateDefaultMappingRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
