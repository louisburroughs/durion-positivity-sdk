/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Customer API
 * API documentation for the Customer service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AddSuppressionRequest,
  PagedResponse,
  SuppressionEntryResponse,
} from '../models/index';
import {
    AddSuppressionRequestFromJSON,
    AddSuppressionRequestToJSON,
    PagedResponseFromJSON,
    PagedResponseToJSON,
    SuppressionEntryResponseFromJSON,
    SuppressionEntryResponseToJSON,
} from '../models/index';

export interface AddSuppressionEntryRequest {
    addSuppressionRequest: AddSuppressionRequest;
}

export interface CheckAddressSuppressionRequest {
    channel: CheckAddressSuppressionChannelEnum;
    address: string;
}

export interface ListSuppressionEntriesRequest {
    channel?: ListSuppressionEntriesChannelEnum;
    page?: number;
    size?: number;
}

export interface RemoveSuppressionEntryRequest {
    suppressionId: string;
}

/**
 * 
 */
export class CRMSuppressionApi extends runtime.BaseAPI {

    /**
     * Hard-blocks an address from marketing on one channel, storing only a normalized hash and a masked hint of the address. Use this tool for bounce feeds, spam complaints, and legal do-not-contact requests; do not use updateMarketingConsent, which records a party\'s choice and can be reversed by the party, whereas suppression outranks consent entirely. Preconditions: none; the call is idempotent, and re-adding an already-suppressed address returns the existing entry without touching the audit trail. Required inputs: channel (EMAIL or SMS), address (raw, max 320 characters), and reason (HARD_BOUNCE, SPAM_COMPLAINT, LEGAL_DNC, MANUAL, or UNSUBSCRIBE_LINK); partyId is optional and source defaults to CSR. Emits a CRM_SUPPRESSION_ADD event and publishes a suppression-changed fact when a new entry is actually created. Returns 400 when channel, address, or reason is missing, and 201 with the existing entry when the address was already suppressed. 
     * Suppress Marketing Address
     */
    async addSuppressionEntryRaw(requestParameters: AddSuppressionEntryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SuppressionEntryResponse>> {
        if (requestParameters['addSuppressionRequest'] == null) {
            throw new runtime.RequiredError(
                'addSuppressionRequest',
                'Required parameter "addSuppressionRequest" was null or undefined when calling addSuppressionEntry().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:suppression:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/suppression`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AddSuppressionRequestToJSON(requestParameters['addSuppressionRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SuppressionEntryResponseFromJSON(jsonValue));
    }

    /**
     * Hard-blocks an address from marketing on one channel, storing only a normalized hash and a masked hint of the address. Use this tool for bounce feeds, spam complaints, and legal do-not-contact requests; do not use updateMarketingConsent, which records a party\'s choice and can be reversed by the party, whereas suppression outranks consent entirely. Preconditions: none; the call is idempotent, and re-adding an already-suppressed address returns the existing entry without touching the audit trail. Required inputs: channel (EMAIL or SMS), address (raw, max 320 characters), and reason (HARD_BOUNCE, SPAM_COMPLAINT, LEGAL_DNC, MANUAL, or UNSUBSCRIBE_LINK); partyId is optional and source defaults to CSR. Emits a CRM_SUPPRESSION_ADD event and publishes a suppression-changed fact when a new entry is actually created. Returns 400 when channel, address, or reason is missing, and 201 with the existing entry when the address was already suppressed. 
     * Suppress Marketing Address
     */
    async addSuppressionEntry(requestParameters: AddSuppressionEntryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SuppressionEntryResponse> {
        const response = await this.addSuppressionEntryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Reports whether a raw address is currently hard-blocked on a channel by normalizing and hashing it and looking the hash up in the suppression list. Use this tool from send pipelines immediately before dispatch; use resolveMarketingEligibility instead for the full party-level decision that also folds in consent and the account gate. Preconditions: none; suppression outranks consent, so a true result must block the send regardless of opt-in state. Required inputs: channel (EMAIL or SMS) and address (the raw address to test) as required query parameters; the raw address is used only for hashing and never stored. Emits a CRM_SUPPRESSION_CHECK audit event; no state changes occur. Returns 200 with a bare boolean body, true when the address is suppressed on that channel and false otherwise. 
     * Check Address Suppression
     */
    async checkAddressSuppressionRaw(requestParameters: CheckAddressSuppressionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<boolean>> {
        if (requestParameters['channel'] == null) {
            throw new runtime.RequiredError(
                'channel',
                'Required parameter "channel" was null or undefined when calling checkAddressSuppression().'
            );
        }

        if (requestParameters['address'] == null) {
            throw new runtime.RequiredError(
                'address',
                'Required parameter "address" was null or undefined when calling checkAddressSuppression().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['channel'] != null) {
            queryParameters['channel'] = requestParameters['channel'];
        }

        if (requestParameters['address'] != null) {
            queryParameters['address'] = requestParameters['address'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:suppression:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/suppression/check`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<boolean>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Reports whether a raw address is currently hard-blocked on a channel by normalizing and hashing it and looking the hash up in the suppression list. Use this tool from send pipelines immediately before dispatch; use resolveMarketingEligibility instead for the full party-level decision that also folds in consent and the account gate. Preconditions: none; suppression outranks consent, so a true result must block the send regardless of opt-in state. Required inputs: channel (EMAIL or SMS) and address (the raw address to test) as required query parameters; the raw address is used only for hashing and never stored. Emits a CRM_SUPPRESSION_CHECK audit event; no state changes occur. Returns 200 with a bare boolean body, true when the address is suppressed on that channel and false otherwise. 
     * Check Address Suppression
     */
    async checkAddressSuppression(requestParameters: CheckAddressSuppressionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<boolean> {
        const response = await this.checkAddressSuppressionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns hard-suppressed marketing addresses newest first, showing only a masked address hint and the normalized hash — raw addresses are never stored or returned. Use this tool when reviewing the suppression list; use checkAddressSuppression instead to test one specific address before a send. Preconditions: none; an empty page is returned when nothing matches. Required inputs: none; channel optionally filters on EMAIL or SMS, page defaults to 0, and size defaults to 50 clamped between 1 and 200. Emits a CRM_SUPPRESSION_LIST audit event; no state changes occur. Returns 200 with an empty page rather than an error when the list is empty. 
     * List Suppression Entries
     */
    async listSuppressionEntriesRaw(requestParameters: ListSuppressionEntriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagedResponse>> {
        const queryParameters: any = {};

        if (requestParameters['channel'] != null) {
            queryParameters['channel'] = requestParameters['channel'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:suppression:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/suppression`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagedResponseFromJSON(jsonValue));
    }

    /**
     * Returns hard-suppressed marketing addresses newest first, showing only a masked address hint and the normalized hash — raw addresses are never stored or returned. Use this tool when reviewing the suppression list; use checkAddressSuppression instead to test one specific address before a send. Preconditions: none; an empty page is returned when nothing matches. Required inputs: none; channel optionally filters on EMAIL or SMS, page defaults to 0, and size defaults to 50 clamped between 1 and 200. Emits a CRM_SUPPRESSION_LIST audit event; no state changes occur. Returns 200 with an empty page rather than an error when the list is empty. 
     * List Suppression Entries
     */
    async listSuppressionEntries(requestParameters: ListSuppressionEntriesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagedResponse> {
        const response = await this.listSuppressionEntriesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes one suppression entry, allowing marketing to reach the address again subject to normal consent rules. Use this tool when an address was suppressed in error or a legal block is lifted; do not use updateMarketingConsent for this, which cannot override a suppression entry. Preconditions: the suppression entry must exist. Required inputs: suppressionId (UUID) as a path parameter; there is no request body. Emits a CRM_SUPPRESSION_REMOVE event and publishes a suppression-changed fact so consumers stop blocking the address. Returns 404 when no suppression entry exists for the supplied suppressionId. 
     * Lift Address Suppression
     */
    async removeSuppressionEntryRaw(requestParameters: RemoveSuppressionEntryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['suppressionId'] == null) {
            throw new runtime.RequiredError(
                'suppressionId',
                'Required parameter "suppressionId" was null or undefined when calling removeSuppressionEntry().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:suppression:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/suppression/{suppressionId}`.replace(`{${"suppressionId"}}`, encodeURIComponent(String(requestParameters['suppressionId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one suppression entry, allowing marketing to reach the address again subject to normal consent rules. Use this tool when an address was suppressed in error or a legal block is lifted; do not use updateMarketingConsent for this, which cannot override a suppression entry. Preconditions: the suppression entry must exist. Required inputs: suppressionId (UUID) as a path parameter; there is no request body. Emits a CRM_SUPPRESSION_REMOVE event and publishes a suppression-changed fact so consumers stop blocking the address. Returns 404 when no suppression entry exists for the supplied suppressionId. 
     * Lift Address Suppression
     */
    async removeSuppressionEntry(requestParameters: RemoveSuppressionEntryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.removeSuppressionEntryRaw(requestParameters, initOverrides);
    }

}

/**
  * @export
  * @enum {string}
  */
export enum CheckAddressSuppressionChannelEnum {
    Email = 'EMAIL',
    Sms = 'SMS'
}
/**
  * @export
  * @enum {string}
  */
export enum ListSuppressionEntriesChannelEnum {
    Email = 'EMAIL',
    Sms = 'SMS'
}
