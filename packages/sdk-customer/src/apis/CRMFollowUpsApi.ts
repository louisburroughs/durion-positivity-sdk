/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Customer API
 * API documentation for the Customer service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CloseFollowUpTaskRequest,
  CreateFollowUpTaskRequest,
  FollowUpTaskResponse,
  PagedResponse,
} from '../models/index';
import {
    CloseFollowUpTaskRequestFromJSON,
    CloseFollowUpTaskRequestToJSON,
    CreateFollowUpTaskRequestFromJSON,
    CreateFollowUpTaskRequestToJSON,
    FollowUpTaskResponseFromJSON,
    FollowUpTaskResponseToJSON,
    PagedResponseFromJSON,
    PagedResponseToJSON,
} from '../models/index';

export interface AssignFollowUpTaskRequest {
    taskId: string;
    assignedTo?: string;
}

export interface CompleteFollowUpTaskRequest {
    taskId: string;
    closeFollowUpTaskRequest: CloseFollowUpTaskRequest;
}

export interface CreateFollowUpTaskOperationRequest {
    partyId: string;
    createFollowUpTaskRequest: CreateFollowUpTaskRequest;
}

export interface DismissFollowUpTaskRequest {
    taskId: string;
    closeFollowUpTaskRequest: CloseFollowUpTaskRequest;
}

export interface GetFollowUpQueueRequest {
    status?: GetFollowUpQueueStatusEnum;
    assignedTo?: string;
    type?: GetFollowUpQueueTypeEnum;
    page?: number;
    size?: number;
}

export interface GetFollowUpTaskRequest {
    taskId: string;
}

export interface ListPartyFollowUpsRequest {
    partyId: string;
    status?: ListPartyFollowUpsStatusEnum;
    page?: number;
    size?: number;
}

/**
 * 
 */
export class CRMFollowUpsApi extends runtime.BaseAPI {

    /**
     * Assigns an open follow-up task to a CSR, or returns it to the shared queue when assignedTo is omitted. Use this tool when routing queue work to a specific person; do not use completeFollowUpTask or dismissFollowUpTask, which close the task rather than route it. Preconditions: the task must exist and still be OPEN; closed tasks cannot be modified and a new follow-up must be raised instead. Required inputs: taskId (UUID) as a path parameter; assignedTo is an optional query parameter whose omission unassigns the task, and there is no request body. Emits a CRM_FOLLOWUP_ASSIGN event; only the task\'s assignee changes. Returns 404 when the task does not exist, and 422 when the task is already DONE or DISMISSED. 
     * Assign Follow-Up Task
     */
    async assignFollowUpTaskRaw(requestParameters: AssignFollowUpTaskRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FollowUpTaskResponse>> {
        if (requestParameters['taskId'] == null) {
            throw new runtime.RequiredError(
                'taskId',
                'Required parameter "taskId" was null or undefined when calling assignFollowUpTask().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['assignedTo'] != null) {
            queryParameters['assignedTo'] = requestParameters['assignedTo'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:followup:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/follow-ups/{taskId}/assignee`.replace(`{${"taskId"}}`, encodeURIComponent(String(requestParameters['taskId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FollowUpTaskResponseFromJSON(jsonValue));
    }

    /**
     * Assigns an open follow-up task to a CSR, or returns it to the shared queue when assignedTo is omitted. Use this tool when routing queue work to a specific person; do not use completeFollowUpTask or dismissFollowUpTask, which close the task rather than route it. Preconditions: the task must exist and still be OPEN; closed tasks cannot be modified and a new follow-up must be raised instead. Required inputs: taskId (UUID) as a path parameter; assignedTo is an optional query parameter whose omission unassigns the task, and there is no request body. Emits a CRM_FOLLOWUP_ASSIGN event; only the task\'s assignee changes. Returns 404 when the task does not exist, and 422 when the task is already DONE or DISMISSED. 
     * Assign Follow-Up Task
     */
    async assignFollowUpTask(requestParameters: AssignFollowUpTaskRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FollowUpTaskResponse> {
        const response = await this.assignFollowUpTaskRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Closes an open follow-up task as DONE, recording the outcome and any workorder or appointment that resulted from the touch. Use this tool when the follow-up was actually worked; use dismissFollowUpTask instead when the task is being deliberately abandoned without contact. Preconditions: the task must exist and still be OPEN; a closed task cannot be reopened, so a new follow-up must be raised to try again. Required inputs: taskId (UUID) as a path parameter and outcome (non-blank, max 500) in the body; bookedWorkorderId and bookedAppointmentId are optional booking references. Emits a CRM_FOLLOWUP_COMPLETE event and stamps closedAt and closedBy on the task. Returns 404 when the task does not exist, 422 when it is already DONE or DISMISSED, and 400 when outcome is blank. 
     * Complete Follow-Up Task
     */
    async completeFollowUpTaskRaw(requestParameters: CompleteFollowUpTaskRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FollowUpTaskResponse>> {
        if (requestParameters['taskId'] == null) {
            throw new runtime.RequiredError(
                'taskId',
                'Required parameter "taskId" was null or undefined when calling completeFollowUpTask().'
            );
        }

        if (requestParameters['closeFollowUpTaskRequest'] == null) {
            throw new runtime.RequiredError(
                'closeFollowUpTaskRequest',
                'Required parameter "closeFollowUpTaskRequest" was null or undefined when calling completeFollowUpTask().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:followup:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/follow-ups/{taskId}/complete`.replace(`{${"taskId"}}`, encodeURIComponent(String(requestParameters['taskId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CloseFollowUpTaskRequestToJSON(requestParameters['closeFollowUpTaskRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FollowUpTaskResponseFromJSON(jsonValue));
    }

    /**
     * Closes an open follow-up task as DONE, recording the outcome and any workorder or appointment that resulted from the touch. Use this tool when the follow-up was actually worked; use dismissFollowUpTask instead when the task is being deliberately abandoned without contact. Preconditions: the task must exist and still be OPEN; a closed task cannot be reopened, so a new follow-up must be raised to try again. Required inputs: taskId (UUID) as a path parameter and outcome (non-blank, max 500) in the body; bookedWorkorderId and bookedAppointmentId are optional booking references. Emits a CRM_FOLLOWUP_COMPLETE event and stamps closedAt and closedBy on the task. Returns 404 when the task does not exist, 422 when it is already DONE or DISMISSED, and 400 when outcome is blank. 
     * Complete Follow-Up Task
     */
    async completeFollowUpTask(requestParameters: CompleteFollowUpTaskRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FollowUpTaskResponse> {
        const response = await this.completeFollowUpTaskRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Raises a new OPEN follow-up task against a party, recording the creating user from the security context. Use this tool when a customer needs a future touch such as a declined-service or service-due follow-up; do not use completeFollowUpTask or dismissFollowUpTask, which close an existing task. Preconditions: the party must exist as either a commercial or person party. Required inputs: partyId (UUID) as a path parameter and type in the body (DECLINED_SERVICE_FOLLOWUP, SERVICE_DUE_REMINDER, FLEET_CHECKIN, CAMPAIGN_RESPONSE, or GENERAL); vehicleId, dueDate, assignedTo, reason, sourceWorkorderId, and notes are optional, and an omitted assignedTo leaves the task in the shared queue. Emits a CRM_FOLLOWUP_CREATE event and persists the task with status OPEN. Returns 404 when no party exists for the supplied partyId, and 400 when type is missing. 
     * Raise Follow-Up Task
     */
    async createFollowUpTaskRaw(requestParameters: CreateFollowUpTaskOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FollowUpTaskResponse>> {
        if (requestParameters['partyId'] == null) {
            throw new runtime.RequiredError(
                'partyId',
                'Required parameter "partyId" was null or undefined when calling createFollowUpTask().'
            );
        }

        if (requestParameters['createFollowUpTaskRequest'] == null) {
            throw new runtime.RequiredError(
                'createFollowUpTaskRequest',
                'Required parameter "createFollowUpTaskRequest" was null or undefined when calling createFollowUpTask().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:followup:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/parties/{partyId}/follow-ups`.replace(`{${"partyId"}}`, encodeURIComponent(String(requestParameters['partyId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateFollowUpTaskRequestToJSON(requestParameters['createFollowUpTaskRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FollowUpTaskResponseFromJSON(jsonValue));
    }

    /**
     * Raises a new OPEN follow-up task against a party, recording the creating user from the security context. Use this tool when a customer needs a future touch such as a declined-service or service-due follow-up; do not use completeFollowUpTask or dismissFollowUpTask, which close an existing task. Preconditions: the party must exist as either a commercial or person party. Required inputs: partyId (UUID) as a path parameter and type in the body (DECLINED_SERVICE_FOLLOWUP, SERVICE_DUE_REMINDER, FLEET_CHECKIN, CAMPAIGN_RESPONSE, or GENERAL); vehicleId, dueDate, assignedTo, reason, sourceWorkorderId, and notes are optional, and an omitted assignedTo leaves the task in the shared queue. Emits a CRM_FOLLOWUP_CREATE event and persists the task with status OPEN. Returns 404 when no party exists for the supplied partyId, and 400 when type is missing. 
     * Raise Follow-Up Task
     */
    async createFollowUpTask(requestParameters: CreateFollowUpTaskOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FollowUpTaskResponse> {
        const response = await this.createFollowUpTaskRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Closes an open follow-up task as DISMISSED, recording why it is deliberately not being pursued. Use this tool when a follow-up should be abandoned, for example because the customer already returned; use completeFollowUpTask instead when the touch was actually made. Preconditions: the task must exist and still be OPEN; dismissal is final and a new task must be raised to revisit the customer. Required inputs: taskId (UUID) as a path parameter and outcome (non-blank, max 500) in the body explaining the dismissal; booking references are accepted but normally absent. Emits a CRM_FOLLOWUP_DISMISS event and stamps closedAt and closedBy on the task. Returns 404 when the task does not exist, 422 when it is already DONE or DISMISSED, and 400 when outcome is blank. 
     * Dismiss Follow-Up Task
     */
    async dismissFollowUpTaskRaw(requestParameters: DismissFollowUpTaskRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FollowUpTaskResponse>> {
        if (requestParameters['taskId'] == null) {
            throw new runtime.RequiredError(
                'taskId',
                'Required parameter "taskId" was null or undefined when calling dismissFollowUpTask().'
            );
        }

        if (requestParameters['closeFollowUpTaskRequest'] == null) {
            throw new runtime.RequiredError(
                'closeFollowUpTaskRequest',
                'Required parameter "closeFollowUpTaskRequest" was null or undefined when calling dismissFollowUpTask().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:followup:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/follow-ups/{taskId}/dismiss`.replace(`{${"taskId"}}`, encodeURIComponent(String(requestParameters['taskId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CloseFollowUpTaskRequestToJSON(requestParameters['closeFollowUpTaskRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FollowUpTaskResponseFromJSON(jsonValue));
    }

    /**
     * Closes an open follow-up task as DISMISSED, recording why it is deliberately not being pursued. Use this tool when a follow-up should be abandoned, for example because the customer already returned; use completeFollowUpTask instead when the touch was actually made. Preconditions: the task must exist and still be OPEN; dismissal is final and a new task must be raised to revisit the customer. Required inputs: taskId (UUID) as a path parameter and outcome (non-blank, max 500) in the body explaining the dismissal; booking references are accepted but normally absent. Emits a CRM_FOLLOWUP_DISMISS event and stamps closedAt and closedBy on the task. Returns 404 when the task does not exist, 422 when it is already DONE or DISMISSED, and 400 when outcome is blank. 
     * Dismiss Follow-Up Task
     */
    async dismissFollowUpTask(requestParameters: DismissFollowUpTaskRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FollowUpTaskResponse> {
        const response = await this.dismissFollowUpTaskRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the CSR follow-up work queue across all parties, ordered by due date, optionally filtered by status, assignee, and task type. Use this tool when working the shared queue of outstanding follow-ups; use listPartyFollowUps instead to see tasks raised against one specific party. Preconditions: none; an empty page is returned when no task matches the filters. Required inputs: none; status filters on OPEN, DONE, or DISMISSED, type filters on DECLINED_SERVICE_FOLLOWUP, SERVICE_DUE_REMINDER, FLEET_CHECKIN, CAMPAIGN_RESPONSE, or GENERAL, page defaults to 0, and size defaults to 50 clamped between 1 and 200. Emits a CRM_FOLLOWUP_QUEUE audit event; no state changes occur. Returns 200 with an empty page rather than an error when the queue is clear. 
     * Get Follow-Up Work Queue
     */
    async getFollowUpQueueRaw(requestParameters: GetFollowUpQueueRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagedResponse>> {
        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['assignedTo'] != null) {
            queryParameters['assignedTo'] = requestParameters['assignedTo'];
        }

        if (requestParameters['type'] != null) {
            queryParameters['type'] = requestParameters['type'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:followup:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/follow-ups`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagedResponseFromJSON(jsonValue));
    }

    /**
     * Returns the CSR follow-up work queue across all parties, ordered by due date, optionally filtered by status, assignee, and task type. Use this tool when working the shared queue of outstanding follow-ups; use listPartyFollowUps instead to see tasks raised against one specific party. Preconditions: none; an empty page is returned when no task matches the filters. Required inputs: none; status filters on OPEN, DONE, or DISMISSED, type filters on DECLINED_SERVICE_FOLLOWUP, SERVICE_DUE_REMINDER, FLEET_CHECKIN, CAMPAIGN_RESPONSE, or GENERAL, page defaults to 0, and size defaults to 50 clamped between 1 and 200. Emits a CRM_FOLLOWUP_QUEUE audit event; no state changes occur. Returns 200 with an empty page rather than an error when the queue is clear. 
     * Get Follow-Up Work Queue
     */
    async getFollowUpQueue(requestParameters: GetFollowUpQueueRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagedResponse> {
        const response = await this.getFollowUpQueueRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one follow-up task with its party, vehicle, type, due date, assignee, status, and any recorded outcome and booking references. Use this tool when the task id is already known; use getFollowUpQueue instead to discover tasks by status, assignee, or type. Preconditions: the follow-up task must exist. Required inputs: taskId (UUID) as a path parameter; there is no request body. Emits a CRM_FOLLOWUP_GET audit event; no state changes occur. Returns 404 when no follow-up task exists for the supplied taskId. 
     * Get Follow-Up Task
     */
    async getFollowUpTaskRaw(requestParameters: GetFollowUpTaskRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FollowUpTaskResponse>> {
        if (requestParameters['taskId'] == null) {
            throw new runtime.RequiredError(
                'taskId',
                'Required parameter "taskId" was null or undefined when calling getFollowUpTask().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:followup:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/follow-ups/{taskId}`.replace(`{${"taskId"}}`, encodeURIComponent(String(requestParameters['taskId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FollowUpTaskResponseFromJSON(jsonValue));
    }

    /**
     * Returns one follow-up task with its party, vehicle, type, due date, assignee, status, and any recorded outcome and booking references. Use this tool when the task id is already known; use getFollowUpQueue instead to discover tasks by status, assignee, or type. Preconditions: the follow-up task must exist. Required inputs: taskId (UUID) as a path parameter; there is no request body. Emits a CRM_FOLLOWUP_GET audit event; no state changes occur. Returns 404 when no follow-up task exists for the supplied taskId. 
     * Get Follow-Up Task
     */
    async getFollowUpTask(requestParameters: GetFollowUpTaskRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FollowUpTaskResponse> {
        const response = await this.getFollowUpTaskRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the follow-up tasks raised against one party, ordered by due date then creation time, optionally filtered by status. Use this tool when reviewing outstanding work for a specific customer; use getFollowUpQueue instead for the cross-party CSR work queue. Preconditions: none; an unknown partyId yields an empty page rather than an error. Required inputs: partyId (UUID) as a path parameter; status optionally filters on OPEN, DONE, or DISMISSED, page defaults to 0, and size defaults to 50 clamped between 1 and 200. Emits a CRM_FOLLOWUP_LIST audit event; no state changes occur. Returns 200 with an empty page rather than an error when the party has no follow-ups. 
     * List Party Follow-Up Tasks
     */
    async listPartyFollowUpsRaw(requestParameters: ListPartyFollowUpsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagedResponse>> {
        if (requestParameters['partyId'] == null) {
            throw new runtime.RequiredError(
                'partyId',
                'Required parameter "partyId" was null or undefined when calling listPartyFollowUps().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:followup:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/parties/{partyId}/follow-ups`.replace(`{${"partyId"}}`, encodeURIComponent(String(requestParameters['partyId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagedResponseFromJSON(jsonValue));
    }

    /**
     * Returns the follow-up tasks raised against one party, ordered by due date then creation time, optionally filtered by status. Use this tool when reviewing outstanding work for a specific customer; use getFollowUpQueue instead for the cross-party CSR work queue. Preconditions: none; an unknown partyId yields an empty page rather than an error. Required inputs: partyId (UUID) as a path parameter; status optionally filters on OPEN, DONE, or DISMISSED, page defaults to 0, and size defaults to 50 clamped between 1 and 200. Emits a CRM_FOLLOWUP_LIST audit event; no state changes occur. Returns 200 with an empty page rather than an error when the party has no follow-ups. 
     * List Party Follow-Up Tasks
     */
    async listPartyFollowUps(requestParameters: ListPartyFollowUpsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagedResponse> {
        const response = await this.listPartyFollowUpsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
  * @export
  * @enum {string}
  */
export enum GetFollowUpQueueStatusEnum {
    Open = 'OPEN',
    Done = 'DONE',
    Dismissed = 'DISMISSED'
}
/**
  * @export
  * @enum {string}
  */
export enum GetFollowUpQueueTypeEnum {
    DeclinedServiceFollowup = 'DECLINED_SERVICE_FOLLOWUP',
    ServiceDueReminder = 'SERVICE_DUE_REMINDER',
    FleetCheckin = 'FLEET_CHECKIN',
    CampaignResponse = 'CAMPAIGN_RESPONSE',
    General = 'GENERAL'
}
/**
  * @export
  * @enum {string}
  */
export enum ListPartyFollowUpsStatusEnum {
    Open = 'OPEN',
    Done = 'DONE',
    Dismissed = 'DISMISSED'
}
