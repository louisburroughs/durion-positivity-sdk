/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Customer API
 * API documentation for the Customer service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  SegmentAttributeResponse,
  SegmentMembersRequest,
  SegmentResolutionResponse,
  SegmentResponse,
  UpsertSegmentRequest,
} from '../models/index';
import {
    SegmentAttributeResponseFromJSON,
    SegmentAttributeResponseToJSON,
    SegmentMembersRequestFromJSON,
    SegmentMembersRequestToJSON,
    SegmentResolutionResponseFromJSON,
    SegmentResolutionResponseToJSON,
    SegmentResponseFromJSON,
    SegmentResponseToJSON,
    UpsertSegmentRequestFromJSON,
    UpsertSegmentRequestToJSON,
} from '../models/index';

export interface AddSegmentMembersRequest {
    segmentId: string;
    segmentMembersRequest: SegmentMembersRequest;
}

export interface CreateSegmentRequest {
    upsertSegmentRequest: UpsertSegmentRequest;
}

export interface DeleteSegmentRequest {
    segmentId: string;
}

export interface GetSegmentRequest {
    segmentId: string;
}

export interface ListSegmentsRequest {
    audienceType?: ListSegmentsAudienceTypeEnum;
}

export interface RemoveSegmentMemberRequest {
    segmentId: string;
    partyId: string;
}

export interface ResolveSegmentRequest {
    segmentId: string;
    channel?: ResolveSegmentChannelEnum;
    sampleSize?: number;
}

export interface UpdateSegmentRequest {
    segmentId: string;
    upsertSegmentRequest: UpsertSegmentRequest;
}

/**
 * 
 */
export class CRMSegmentsApi extends runtime.BaseAPI {

    /**
     * Pins parties onto a STATIC segment\'s member list, recording who added them and when; parties already in the segment are silently skipped. Use this tool when curating a hand-picked audience; do not use it on a DYNAMIC segment, whose membership is computed from its predicate by resolveSegment. Preconditions: the segment must exist and be STATIC. Required inputs: segmentId (UUID) as a path parameter and partyIds, a non-empty list of up to 5000 UUIDs; duplicates in the list are collapsed. Emits a CRM_SEGMENT_MEMBERS_ADD event; only new membership rows are written. Returns 404 when the segment does not exist, and 422 when the segment is DYNAMIC. 
     * Pin Segment Members
     */
    async addSegmentMembersRaw(requestParameters: AddSegmentMembersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SegmentResponse>> {
        if (requestParameters['segmentId'] == null) {
            throw new runtime.RequiredError(
                'segmentId',
                'Required parameter "segmentId" was null or undefined when calling addSegmentMembers().'
            );
        }

        if (requestParameters['segmentMembersRequest'] == null) {
            throw new runtime.RequiredError(
                'segmentMembersRequest',
                'Required parameter "segmentMembersRequest" was null or undefined when calling addSegmentMembers().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:segment:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/segments/{segmentId}/members`.replace(`{${"segmentId"}}`, encodeURIComponent(String(requestParameters['segmentId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SegmentMembersRequestToJSON(requestParameters['segmentMembersRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SegmentResponseFromJSON(jsonValue));
    }

    /**
     * Pins parties onto a STATIC segment\'s member list, recording who added them and when; parties already in the segment are silently skipped. Use this tool when curating a hand-picked audience; do not use it on a DYNAMIC segment, whose membership is computed from its predicate by resolveSegment. Preconditions: the segment must exist and be STATIC. Required inputs: segmentId (UUID) as a path parameter and partyIds, a non-empty list of up to 5000 UUIDs; duplicates in the list are collapsed. Emits a CRM_SEGMENT_MEMBERS_ADD event; only new membership rows are written. Returns 404 when the segment does not exist, and 422 when the segment is DYNAMIC. 
     * Pin Segment Members
     */
    async addSegmentMembers(requestParameters: AddSegmentMembersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SegmentResponse> {
        const response = await this.addSegmentMembersRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a saved audience segment, either STATIC with an explicitly pinned member list or DYNAMIC with a predicate validated against the attribute catalog. Use this tool when defining a new audience; do not use updateSegment, which modifies an existing segment and cannot change its type or audience type. Preconditions: no existing segment may already use the same name case-insensitively; a DYNAMIC segment must carry a predicate and a STATIC one must not. Required inputs: name (max 150), audienceType (COMMERCIAL or INDIVIDUAL), and type (STATIC or DYNAMIC); description, predicate, and active are optional. Emits a CRM_SEGMENT_CREATE event and publishes the segment definition to consumers. Returns 409 when the name is already taken, and 422 when the predicate is missing on a DYNAMIC segment, present on a STATIC one, or references attributes outside the catalog. 
     * Create Audience Segment
     */
    async createSegmentRaw(requestParameters: CreateSegmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SegmentResponse>> {
        if (requestParameters['upsertSegmentRequest'] == null) {
            throw new runtime.RequiredError(
                'upsertSegmentRequest',
                'Required parameter "upsertSegmentRequest" was null or undefined when calling createSegment().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:segment:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/segments`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: UpsertSegmentRequestToJSON(requestParameters['upsertSegmentRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SegmentResponseFromJSON(jsonValue));
    }

    /**
     * Creates a saved audience segment, either STATIC with an explicitly pinned member list or DYNAMIC with a predicate validated against the attribute catalog. Use this tool when defining a new audience; do not use updateSegment, which modifies an existing segment and cannot change its type or audience type. Preconditions: no existing segment may already use the same name case-insensitively; a DYNAMIC segment must carry a predicate and a STATIC one must not. Required inputs: name (max 150), audienceType (COMMERCIAL or INDIVIDUAL), and type (STATIC or DYNAMIC); description, predicate, and active are optional. Emits a CRM_SEGMENT_CREATE event and publishes the segment definition to consumers. Returns 409 when the name is already taken, and 422 when the predicate is missing on a DYNAMIC segment, present on a STATIC one, or references attributes outside the catalog. 
     * Create Audience Segment
     */
    async createSegment(requestParameters: CreateSegmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SegmentResponse> {
        const response = await this.createSegmentRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Deletes a segment and all of its pinned membership rows permanently. Use this tool when a segment is no longer wanted at all; use updateSegment with active false instead to retire a segment while keeping its definition. Preconditions: the segment must exist; deletion is not reversible. Required inputs: segmentId (UUID) as a path parameter; there is no request body. Emits a CRM_SEGMENT_DELETE event and publishes a deletion notice so consumers drop the segment. Returns 404 when no segment exists for the supplied segmentId. 
     * Delete Audience Segment
     */
    async deleteSegmentRaw(requestParameters: DeleteSegmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['segmentId'] == null) {
            throw new runtime.RequiredError(
                'segmentId',
                'Required parameter "segmentId" was null or undefined when calling deleteSegment().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:segment:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/segments/{segmentId}`.replace(`{${"segmentId"}}`, encodeURIComponent(String(requestParameters['segmentId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes a segment and all of its pinned membership rows permanently. Use this tool when a segment is no longer wanted at all; use updateSegment with active false instead to retire a segment while keeping its definition. Preconditions: the segment must exist; deletion is not reversible. Required inputs: segmentId (UUID) as a path parameter; there is no request body. Emits a CRM_SEGMENT_DELETE event and publishes a deletion notice so consumers drop the segment. Returns 404 when no segment exists for the supplied segmentId. 
     * Delete Audience Segment
     */
    async deleteSegment(requestParameters: DeleteSegmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteSegmentRaw(requestParameters, initOverrides);
    }

    /**
     * Returns one segment\'s definition, including its type, audience type, predicate for DYNAMIC segments, active flag, and member count. Use this tool when the segment id is already known; use listSegments instead to browse segments, and resolveSegment to compute who currently matches. Preconditions: the segment must exist. Required inputs: segmentId (UUID) as a path parameter; there is no request body. Emits a CRM_SEGMENT_GET audit event; no state changes occur. Returns 404 when no segment exists for the supplied segmentId. 
     * Get Segment Definition
     */
    async getSegmentRaw(requestParameters: GetSegmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SegmentResponse>> {
        if (requestParameters['segmentId'] == null) {
            throw new runtime.RequiredError(
                'segmentId',
                'Required parameter "segmentId" was null or undefined when calling getSegment().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:segment:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/segments/{segmentId}`.replace(`{${"segmentId"}}`, encodeURIComponent(String(requestParameters['segmentId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SegmentResponseFromJSON(jsonValue));
    }

    /**
     * Returns one segment\'s definition, including its type, audience type, predicate for DYNAMIC segments, active flag, and member count. Use this tool when the segment id is already known; use listSegments instead to browse segments, and resolveSegment to compute who currently matches. Preconditions: the segment must exist. Required inputs: segmentId (UUID) as a path parameter; there is no request body. Emits a CRM_SEGMENT_GET audit event; no state changes occur. Returns 404 when no segment exists for the supplied segmentId. 
     * Get Segment Definition
     */
    async getSegment(requestParameters: GetSegmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SegmentResponse> {
        const response = await this.getSegmentRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the whitelist of attributes a dynamic segment predicate may reference, each with its operand kind, allowed operators, and description. Use this tool before createSegment or updateSegment to build a valid DYNAMIC predicate; do not use listSegments, which lists saved segments rather than the predicate vocabulary. Preconditions: none; the catalog is fixed in code per audience type. Required inputs: none; there are no parameters and no request body. Emits a CRM_SEGMENT_ATTRIBUTES audit event; no state changes occur. Returns 200 with the full catalog in every successful call; there are no business error responses. 
     * List Segment Attribute Catalog
     */
    async listSegmentAttributesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<SegmentAttributeResponse>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:segment:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/segments/attributes`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(SegmentAttributeResponseFromJSON));
    }

    /**
     * Returns the whitelist of attributes a dynamic segment predicate may reference, each with its operand kind, allowed operators, and description. Use this tool before createSegment or updateSegment to build a valid DYNAMIC predicate; do not use listSegments, which lists saved segments rather than the predicate vocabulary. Preconditions: none; the catalog is fixed in code per audience type. Required inputs: none; there are no parameters and no request body. Emits a CRM_SEGMENT_ATTRIBUTES audit event; no state changes occur. Returns 200 with the full catalog in every successful call; there are no business error responses. 
     * List Segment Attribute Catalog
     */
    async listSegmentAttributes(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<SegmentAttributeResponse>> {
        const response = await this.listSegmentAttributesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns all saved audience segments with their type, audience type, active flag, and member counts, optionally filtered by audience type. Use this tool when browsing or picking a segment; use getSegment instead when the segment id is already known. Preconditions: none; an empty list is returned when no segment matches. Required inputs: none; audienceType optionally filters on COMMERCIAL or INDIVIDUAL, and there is no request body. Emits a CRM_SEGMENT_LIST audit event; no state changes occur. Returns 200 with an empty list rather than an error when no segments exist. 
     * List Audience Segments
     */
    async listSegmentsRaw(requestParameters: ListSegmentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<SegmentResponse>>> {
        const queryParameters: any = {};

        if (requestParameters['audienceType'] != null) {
            queryParameters['audienceType'] = requestParameters['audienceType'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:segment:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/segments`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(SegmentResponseFromJSON));
    }

    /**
     * Returns all saved audience segments with their type, audience type, active flag, and member counts, optionally filtered by audience type. Use this tool when browsing or picking a segment; use getSegment instead when the segment id is already known. Preconditions: none; an empty list is returned when no segment matches. Required inputs: none; audienceType optionally filters on COMMERCIAL or INDIVIDUAL, and there is no request body. Emits a CRM_SEGMENT_LIST audit event; no state changes occur. Returns 200 with an empty list rather than an error when no segments exist. 
     * List Audience Segments
     */
    async listSegments(requestParameters: ListSegmentsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<SegmentResponse>> {
        const response = await this.listSegmentsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes one party from a static segment\'s pinned member list. Use this tool when a hand-picked party should leave the audience; do not use deleteSegment, which removes the whole segment and every member with it. Preconditions: none are enforced; removing a party that is not a member, or naming an unknown segment, is a silent no-op. Required inputs: segmentId and partyId (UUIDs) as path parameters; there is no request body. Emits a CRM_SEGMENT_MEMBER_REMOVE event; at most one membership row is deleted. Returns 204 in every authorized call, including when nothing was actually removed. 
     * Unpin Segment Member
     */
    async removeSegmentMemberRaw(requestParameters: RemoveSegmentMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['segmentId'] == null) {
            throw new runtime.RequiredError(
                'segmentId',
                'Required parameter "segmentId" was null or undefined when calling removeSegmentMember().'
            );
        }

        if (requestParameters['partyId'] == null) {
            throw new runtime.RequiredError(
                'partyId',
                'Required parameter "partyId" was null or undefined when calling removeSegmentMember().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:segment:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/segments/{segmentId}/members/{partyId}`.replace(`{${"segmentId"}}`, encodeURIComponent(String(requestParameters['segmentId']))).replace(`{${"partyId"}}`, encodeURIComponent(String(requestParameters['partyId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one party from a static segment\'s pinned member list. Use this tool when a hand-picked party should leave the audience; do not use deleteSegment, which removes the whole segment and every member with it. Preconditions: none are enforced; removing a party that is not a member, or naming an unknown segment, is a silent no-op. Required inputs: segmentId and partyId (UUIDs) as path parameters; there is no request body. Emits a CRM_SEGMENT_MEMBER_REMOVE event; at most one membership row is deleted. Returns 204 in every authorized call, including when nothing was actually removed. 
     * Unpin Segment Member
     */
    async removeSegmentMember(requestParameters: RemoveSegmentMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.removeSegmentMemberRaw(requestParameters, initOverrides);
    }

    /**
     * Resolves a segment to its current match count, an optional per-channel eligible count that folds in consent and suppression, and a masked sample of members; the full recipient list is never returned, so the preview cannot become a bulk PII export. Use this tool when previewing an audience before a campaign; use getSegment instead for the stored definition without computing membership. Preconditions: the segment must exist; eligible counts are computed only when a channel is supplied. Required inputs: segmentId (UUID) as a path parameter; channel (EMAIL or SMS) is optional, and sampleSize defaults to 10 and is clamped to the server\'s maximum. Emits a CRM_SEGMENT_RESOLVE audit event; no state changes occur. Returns 404 when no segment exists for the supplied segmentId, and 200 with a truncated flag when the match list was capped during resolution. 
     * Resolve Segment Audience
     */
    async resolveSegmentRaw(requestParameters: ResolveSegmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SegmentResolutionResponse>> {
        if (requestParameters['segmentId'] == null) {
            throw new runtime.RequiredError(
                'segmentId',
                'Required parameter "segmentId" was null or undefined when calling resolveSegment().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['channel'] != null) {
            queryParameters['channel'] = requestParameters['channel'];
        }

        if (requestParameters['sampleSize'] != null) {
            queryParameters['sampleSize'] = requestParameters['sampleSize'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:segment:resolve"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/segments/{segmentId}/resolve`.replace(`{${"segmentId"}}`, encodeURIComponent(String(requestParameters['segmentId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SegmentResolutionResponseFromJSON(jsonValue));
    }

    /**
     * Resolves a segment to its current match count, an optional per-channel eligible count that folds in consent and suppression, and a masked sample of members; the full recipient list is never returned, so the preview cannot become a bulk PII export. Use this tool when previewing an audience before a campaign; use getSegment instead for the stored definition without computing membership. Preconditions: the segment must exist; eligible counts are computed only when a channel is supplied. Required inputs: segmentId (UUID) as a path parameter; channel (EMAIL or SMS) is optional, and sampleSize defaults to 10 and is clamped to the server\'s maximum. Emits a CRM_SEGMENT_RESOLVE audit event; no state changes occur. Returns 404 when no segment exists for the supplied segmentId, and 200 with a truncated flag when the match list was capped during resolution. 
     * Resolve Segment Audience
     */
    async resolveSegment(requestParameters: ResolveSegmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SegmentResolutionResponse> {
        const response = await this.resolveSegmentRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Updates a segment\'s name, description, predicate, or active flag; type and audienceType are immutable after creation because switching kinds would strand pinned members or a predicate. Use this tool when refining an existing segment; use createSegment instead when a different type or audience type is needed. Preconditions: the segment must exist, the new name must not collide with another segment case-insensitively, and the submitted type and audienceType must equal the stored values. Required inputs: segmentId (UUID) as a path parameter plus the full upsert body with name, audienceType, and type restated; predicate rules follow the segment\'s type and active is optional. Emits a CRM_SEGMENT_UPDATE event and republishes the segment definition. Returns 404 when the segment does not exist, 409 when the name is taken, and 422 when type or audienceType differ from the stored segment or the predicate is invalid. 
     * Update Audience Segment
     */
    async updateSegmentRaw(requestParameters: UpdateSegmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SegmentResponse>> {
        if (requestParameters['segmentId'] == null) {
            throw new runtime.RequiredError(
                'segmentId',
                'Required parameter "segmentId" was null or undefined when calling updateSegment().'
            );
        }

        if (requestParameters['upsertSegmentRequest'] == null) {
            throw new runtime.RequiredError(
                'upsertSegmentRequest',
                'Required parameter "upsertSegmentRequest" was null or undefined when calling updateSegment().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:segment:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/segments/{segmentId}`.replace(`{${"segmentId"}}`, encodeURIComponent(String(requestParameters['segmentId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: UpsertSegmentRequestToJSON(requestParameters['upsertSegmentRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SegmentResponseFromJSON(jsonValue));
    }

    /**
     * Updates a segment\'s name, description, predicate, or active flag; type and audienceType are immutable after creation because switching kinds would strand pinned members or a predicate. Use this tool when refining an existing segment; use createSegment instead when a different type or audience type is needed. Preconditions: the segment must exist, the new name must not collide with another segment case-insensitively, and the submitted type and audienceType must equal the stored values. Required inputs: segmentId (UUID) as a path parameter plus the full upsert body with name, audienceType, and type restated; predicate rules follow the segment\'s type and active is optional. Emits a CRM_SEGMENT_UPDATE event and republishes the segment definition. Returns 404 when the segment does not exist, 409 when the name is taken, and 422 when type or audienceType differ from the stored segment or the predicate is invalid. 
     * Update Audience Segment
     */
    async updateSegment(requestParameters: UpdateSegmentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SegmentResponse> {
        const response = await this.updateSegmentRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
  * @export
  * @enum {string}
  */
export enum ListSegmentsAudienceTypeEnum {
    Commercial = 'COMMERCIAL',
    Individual = 'INDIVIDUAL'
}
/**
  * @export
  * @enum {string}
  */
export enum ResolveSegmentChannelEnum {
    Email = 'EMAIL',
    Sms = 'SMS'
}
