/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Customer API
 * API documentation for the Customer service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  InquiryResponse,
  PagedResponse,
  SubmitInquiryRequest,
} from '../models/index';
import {
    InquiryResponseFromJSON,
    InquiryResponseToJSON,
    PagedResponseFromJSON,
    PagedResponseToJSON,
    SubmitInquiryRequestFromJSON,
    SubmitInquiryRequestToJSON,
} from '../models/index';

export interface AssignInquiryRequest {
    inquiryId: string;
    assignedTo?: string;
}

export interface CaptureInquiryRequest {
    submitInquiryRequest: SubmitInquiryRequest;
}

export interface ConvertInquiryRequest {
    inquiryId: string;
    existingPartyId?: string;
}

export interface GetInquiryRequest {
    inquiryId: string;
}

export interface ListInquiriesRequest {
    status?: ListInquiriesStatusEnum;
    page?: number;
    size?: number;
}

export interface UpdateInquiryStatusRequest {
    inquiryId: string;
    status: UpdateInquiryStatusStatusEnum;
    resolutionNote?: string;
}

/**
 * 
 */
export class CRMInquiriesApi extends runtime.BaseAPI {

    /**
     * Assigns an inquiry to a staff member for follow-through, or returns it to the shared queue when assignedTo is omitted. Use this tool when routing a triaged inquiry to an owner; do not use updateInquiryStatus, which moves the lifecycle state rather than ownership. Preconditions: the inquiry must exist; assignment is allowed in any status. Required inputs: inquiryId (UUID) as a path parameter; assignedTo is an optional query parameter whose omission unassigns the inquiry, and there is no request body. Emits a CRM_INQUIRY_ASSIGN event; only the inquiry\'s assignee changes. Returns 404 when no inquiry exists for the supplied inquiryId. 
     * Assign Inquiry
     */
    async assignInquiryRaw(requestParameters: AssignInquiryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InquiryResponse>> {
        if (requestParameters['inquiryId'] == null) {
            throw new runtime.RequiredError(
                'inquiryId',
                'Required parameter "inquiryId" was null or undefined when calling assignInquiry().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['assignedTo'] != null) {
            queryParameters['assignedTo'] = requestParameters['assignedTo'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:inquiry:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/inquiries/{inquiryId}/assignee`.replace(`{${"inquiryId"}}`, encodeURIComponent(String(requestParameters['inquiryId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InquiryResponseFromJSON(jsonValue));
    }

    /**
     * Assigns an inquiry to a staff member for follow-through, or returns it to the shared queue when assignedTo is omitted. Use this tool when routing a triaged inquiry to an owner; do not use updateInquiryStatus, which moves the lifecycle state rather than ownership. Preconditions: the inquiry must exist; assignment is allowed in any status. Required inputs: inquiryId (UUID) as a path parameter; assignedTo is an optional query parameter whose omission unassigns the inquiry, and there is no request body. Emits a CRM_INQUIRY_ASSIGN event; only the inquiry\'s assignee changes. Returns 404 when no inquiry exists for the supplied inquiryId. 
     * Assign Inquiry
     */
    async assignInquiry(requestParameters: AssignInquiryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InquiryResponse> {
        const response = await this.assignInquiryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Records an inbound inquiry taken by staff over phone, walk-in, email, or referral, creating it in NEW status and deliberately unassigned so it lands in the shared queue. Use this tool for staff-entered inquiries on the authenticated API; do not use submitPublicInquiry, which is the anonymous rate-limited public web-form path. Preconditions: none beyond authorization; no party needs to exist yet. Required inputs: channel (WEB_FORM, PHONE, WALK_IN, EMAIL, REFERRAL, or CAMPAIGN), audienceType (COMMERCIAL or INDIVIDUAL), contactName, and at least one of email or phone; organizationName, vehicleOfInterest, serviceOfInterest, message, and campaignCode are optional. Emits a CRM_INQUIRY_CAPTURE event and persists the inquiry. Returns 400 when required fields are missing or when neither email nor phone is supplied, since an unreachable enquirer cannot be actioned. 
     * Capture Staff-Taken Inquiry
     */
    async captureInquiryRaw(requestParameters: CaptureInquiryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InquiryResponse>> {
        if (requestParameters['submitInquiryRequest'] == null) {
            throw new runtime.RequiredError(
                'submitInquiryRequest',
                'Required parameter "submitInquiryRequest" was null or undefined when calling captureInquiry().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:inquiry:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/inquiries`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SubmitInquiryRequestToJSON(requestParameters['submitInquiryRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InquiryResponseFromJSON(jsonValue));
    }

    /**
     * Records an inbound inquiry taken by staff over phone, walk-in, email, or referral, creating it in NEW status and deliberately unassigned so it lands in the shared queue. Use this tool for staff-entered inquiries on the authenticated API; do not use submitPublicInquiry, which is the anonymous rate-limited public web-form path. Preconditions: none beyond authorization; no party needs to exist yet. Required inputs: channel (WEB_FORM, PHONE, WALK_IN, EMAIL, REFERRAL, or CAMPAIGN), audienceType (COMMERCIAL or INDIVIDUAL), contactName, and at least one of email or phone; organizationName, vehicleOfInterest, serviceOfInterest, message, and campaignCode are optional. Emits a CRM_INQUIRY_CAPTURE event and persists the inquiry. Returns 400 when required fields are missing or when neither email nor phone is supplied, since an unreachable enquirer cannot be actioned. 
     * Capture Staff-Taken Inquiry
     */
    async captureInquiry(requestParameters: CaptureInquiryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InquiryResponse> {
        const response = await this.captureInquiryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Converts an inquiry into CRM state: a commercial inquiry with no existingPartyId creates a new commercial party in lifecycle stage PROSPECT, while supplying existingPartyId links the inquiry to that party instead; either way the inquiry becomes CONVERTED. Use this tool when an enquirer becomes a prospect or is recognized as an existing customer; do not use updateInquiryStatus, which rejects CONVERTED as a direct status change. Preconditions: the inquiry must exist in an open status (NEW or CONTACTED); an INDIVIDUAL-audience inquiry can only be converted by linking an existing party, because creating a person party requires a verified identity in pos-people-contact. Required inputs: inquiryId (UUID) as a path parameter; existingPartyId (UUID) is an optional query parameter naming a known party to link, and there is no request body. Emits a CRM_INQUIRY_CONVERT event; a new PROSPECT commercial party may be created and the inquiry is stamped with the resulting partyId. Returns 404 when the inquiry or the supplied existingPartyId cannot be found, and 422 when the inquiry is already CONVERTED or CLOSED, or when an individual inquiry has no party to link. 
     * Convert Inquiry To Party
     */
    async convertInquiryRaw(requestParameters: ConvertInquiryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InquiryResponse>> {
        if (requestParameters['inquiryId'] == null) {
            throw new runtime.RequiredError(
                'inquiryId',
                'Required parameter "inquiryId" was null or undefined when calling convertInquiry().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['existingPartyId'] != null) {
            queryParameters['existingPartyId'] = requestParameters['existingPartyId'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:inquiry:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/inquiries/{inquiryId}/convert`.replace(`{${"inquiryId"}}`, encodeURIComponent(String(requestParameters['inquiryId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InquiryResponseFromJSON(jsonValue));
    }

    /**
     * Converts an inquiry into CRM state: a commercial inquiry with no existingPartyId creates a new commercial party in lifecycle stage PROSPECT, while supplying existingPartyId links the inquiry to that party instead; either way the inquiry becomes CONVERTED. Use this tool when an enquirer becomes a prospect or is recognized as an existing customer; do not use updateInquiryStatus, which rejects CONVERTED as a direct status change. Preconditions: the inquiry must exist in an open status (NEW or CONTACTED); an INDIVIDUAL-audience inquiry can only be converted by linking an existing party, because creating a person party requires a verified identity in pos-people-contact. Required inputs: inquiryId (UUID) as a path parameter; existingPartyId (UUID) is an optional query parameter naming a known party to link, and there is no request body. Emits a CRM_INQUIRY_CONVERT event; a new PROSPECT commercial party may be created and the inquiry is stamped with the resulting partyId. Returns 404 when the inquiry or the supplied existingPartyId cannot be found, and 422 when the inquiry is already CONVERTED or CLOSED, or when an individual inquiry has no party to link. 
     * Convert Inquiry To Party
     */
    async convertInquiry(requestParameters: ConvertInquiryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InquiryResponse> {
        const response = await this.convertInquiryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one inbound inquiry with its channel, audience type, contact details, interest fields, status, assignee, and any linked party. Use this tool when the inquiry id is already known; use listInquiries instead to find inquiries by status. Preconditions: the inquiry must exist. Required inputs: inquiryId (UUID) as a path parameter; there is no request body. Emits a CRM_INQUIRY_GET audit event; no state changes occur. Returns 404 when no inquiry exists for the supplied inquiryId. 
     * Get Inquiry
     */
    async getInquiryRaw(requestParameters: GetInquiryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InquiryResponse>> {
        if (requestParameters['inquiryId'] == null) {
            throw new runtime.RequiredError(
                'inquiryId',
                'Required parameter "inquiryId" was null or undefined when calling getInquiry().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:inquiry:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/inquiries/{inquiryId}`.replace(`{${"inquiryId"}}`, encodeURIComponent(String(requestParameters['inquiryId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InquiryResponseFromJSON(jsonValue));
    }

    /**
     * Returns one inbound inquiry with its channel, audience type, contact details, interest fields, status, assignee, and any linked party. Use this tool when the inquiry id is already known; use listInquiries instead to find inquiries by status. Preconditions: the inquiry must exist. Required inputs: inquiryId (UUID) as a path parameter; there is no request body. Emits a CRM_INQUIRY_GET audit event; no state changes occur. Returns 404 when no inquiry exists for the supplied inquiryId. 
     * Get Inquiry
     */
    async getInquiry(requestParameters: GetInquiryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InquiryResponse> {
        const response = await this.getInquiryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns inbound service and fleet-quote inquiries newest first, optionally filtered by lifecycle status. Use this tool when triaging the inquiry queue; use getInquiry instead when the inquiry id is already known. Preconditions: none; an empty page is returned when nothing matches. Required inputs: none; status optionally filters on NEW, CONTACTED, CONVERTED, or CLOSED, page defaults to 0, and size defaults to 50 clamped between 1 and 200. Emits a CRM_INQUIRY_LIST audit event; no state changes occur. Returns 200 with an empty page rather than an error when no inquiry matches. 
     * List Inbound Inquiries
     */
    async listInquiriesRaw(requestParameters: ListInquiriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagedResponse>> {
        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:inquiry:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/inquiries`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagedResponseFromJSON(jsonValue));
    }

    /**
     * Returns inbound service and fleet-quote inquiries newest first, optionally filtered by lifecycle status. Use this tool when triaging the inquiry queue; use getInquiry instead when the inquiry id is already known. Preconditions: none; an empty page is returned when nothing matches. Required inputs: none; status optionally filters on NEW, CONTACTED, CONVERTED, or CLOSED, page defaults to 0, and size defaults to 50 clamped between 1 and 200. Emits a CRM_INQUIRY_LIST audit event; no state changes occur. Returns 200 with an empty page rather than an error when no inquiry matches. 
     * List Inbound Inquiries
     */
    async listInquiries(requestParameters: ListInquiriesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagedResponse> {
        const response = await this.listInquiriesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Moves an inquiry through its lifecycle to CONTACTED or CLOSED, optionally recording a resolution note. Use this tool for ordinary status progression; do not use it to reach CONVERTED, which is only reachable through convertInquiry because conversion creates or links a party. Preconditions: the inquiry must exist and the transition must be legal — NEW may move to CONTACTED or CLOSED, CONTACTED may move to CLOSED, and CONVERTED and CLOSED are terminal. Required inputs: inquiryId (UUID) as a path parameter and status (CONTACTED or CLOSED) as a required query parameter; resolutionNote is optional and there is no request body. Emits a CRM_INQUIRY_STATUS_UPDATE event; only the inquiry record changes. Returns 404 when the inquiry does not exist, and 422 when CONVERTED is requested directly or the transition is illegal from the current status. 
     * Update Inquiry Status
     */
    async updateInquiryStatusRaw(requestParameters: UpdateInquiryStatusRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InquiryResponse>> {
        if (requestParameters['inquiryId'] == null) {
            throw new runtime.RequiredError(
                'inquiryId',
                'Required parameter "inquiryId" was null or undefined when calling updateInquiryStatus().'
            );
        }

        if (requestParameters['status'] == null) {
            throw new runtime.RequiredError(
                'status',
                'Required parameter "status" was null or undefined when calling updateInquiryStatus().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['resolutionNote'] != null) {
            queryParameters['resolutionNote'] = requestParameters['resolutionNote'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:inquiry:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/inquiries/{inquiryId}/status`.replace(`{${"inquiryId"}}`, encodeURIComponent(String(requestParameters['inquiryId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InquiryResponseFromJSON(jsonValue));
    }

    /**
     * Moves an inquiry through its lifecycle to CONTACTED or CLOSED, optionally recording a resolution note. Use this tool for ordinary status progression; do not use it to reach CONVERTED, which is only reachable through convertInquiry because conversion creates or links a party. Preconditions: the inquiry must exist and the transition must be legal — NEW may move to CONTACTED or CLOSED, CONTACTED may move to CLOSED, and CONVERTED and CLOSED are terminal. Required inputs: inquiryId (UUID) as a path parameter and status (CONTACTED or CLOSED) as a required query parameter; resolutionNote is optional and there is no request body. Emits a CRM_INQUIRY_STATUS_UPDATE event; only the inquiry record changes. Returns 404 when the inquiry does not exist, and 422 when CONVERTED is requested directly or the transition is illegal from the current status. 
     * Update Inquiry Status
     */
    async updateInquiryStatus(requestParameters: UpdateInquiryStatusRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InquiryResponse> {
        const response = await this.updateInquiryStatusRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
  * @export
  * @enum {string}
  */
export enum ListInquiriesStatusEnum {
    New = 'NEW',
    Contacted = 'CONTACTED',
    Converted = 'CONVERTED',
    Closed = 'CLOSED'
}
/**
  * @export
  * @enum {string}
  */
export enum UpdateInquiryStatusStatusEnum {
    New = 'NEW',
    Contacted = 'CONTACTED',
    Converted = 'CONVERTED',
    Closed = 'CLOSED'
}
