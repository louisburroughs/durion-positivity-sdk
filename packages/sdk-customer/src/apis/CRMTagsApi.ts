/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Customer API
 * API documentation for the Customer service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AssignPartyTagRequest,
  PartyTagAssignmentResponse,
  PartyTagResponse,
  UpsertPartyTagRequest,
} from '../models/index';
import {
    AssignPartyTagRequestFromJSON,
    AssignPartyTagRequestToJSON,
    PartyTagAssignmentResponseFromJSON,
    PartyTagAssignmentResponseToJSON,
    PartyTagResponseFromJSON,
    PartyTagResponseToJSON,
    UpsertPartyTagRequestFromJSON,
    UpsertPartyTagRequestToJSON,
} from '../models/index';

export interface AssignTagToPartyRequest {
    partyId: string;
    assignPartyTagRequest: AssignPartyTagRequest;
}

export interface CreateTagRequest {
    upsertPartyTagRequest: UpsertPartyTagRequest;
}

export interface DeleteTagRequest {
    tagId: string;
}

export interface GetTagRequest {
    tagId: string;
}

export interface ListPartyTagsRequest {
    partyId: string;
}

export interface ListTagsRequest {
    includeInactive?: boolean;
}

export interface RemoveTagFromPartyRequest {
    partyId: string;
    tagId: string;
}

export interface UpdateTagRequest {
    tagId: string;
    upsertPartyTagRequest: UpsertPartyTagRequest;
}

/**
 * 
 */
export class CRMTagsApi extends runtime.BaseAPI {

    /**
     * Attaches a catalog tag to a party, recording the assigning user, timestamp, and source. Use this tool when labeling a party; do not use createTag, which adds a new tag to the catalog without attaching it to anyone. Preconditions: the tag must exist and, for a new assignment, be active; the call is idempotent, and re-assigning an already-attached tag returns the existing assignment even when the tag has since been retired. Required inputs: partyId (UUID) as a path parameter and tagId (UUID) in the body; source defaults to MANUAL and accepts MANUAL, CAMPAIGN, IMPORT, or RULE. Emits a CRM_PARTY_TAG_ASSIGN event and publishes a party-tag-changed fact when a new assignment is created. Returns 404 when the tag does not exist, and 422 when the tag is inactive and not already assigned. 
     * Assign Tag To Party
     */
    async assignTagToPartyRaw(requestParameters: AssignTagToPartyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PartyTagAssignmentResponse>> {
        if (requestParameters['partyId'] == null) {
            throw new runtime.RequiredError(
                'partyId',
                'Required parameter "partyId" was null or undefined when calling assignTagToParty().'
            );
        }

        if (requestParameters['assignPartyTagRequest'] == null) {
            throw new runtime.RequiredError(
                'assignPartyTagRequest',
                'Required parameter "assignPartyTagRequest" was null or undefined when calling assignTagToParty().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:tag:assign"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/parties/{partyId}/tags`.replace(`{${"partyId"}}`, encodeURIComponent(String(requestParameters['partyId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AssignPartyTagRequestToJSON(requestParameters['assignPartyTagRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PartyTagAssignmentResponseFromJSON(jsonValue));
    }

    /**
     * Attaches a catalog tag to a party, recording the assigning user, timestamp, and source. Use this tool when labeling a party; do not use createTag, which adds a new tag to the catalog without attaching it to anyone. Preconditions: the tag must exist and, for a new assignment, be active; the call is idempotent, and re-assigning an already-attached tag returns the existing assignment even when the tag has since been retired. Required inputs: partyId (UUID) as a path parameter and tagId (UUID) in the body; source defaults to MANUAL and accepts MANUAL, CAMPAIGN, IMPORT, or RULE. Emits a CRM_PARTY_TAG_ASSIGN event and publishes a party-tag-changed fact when a new assignment is created. Returns 404 when the tag does not exist, and 422 when the tag is inactive and not already assigned. 
     * Assign Tag To Party
     */
    async assignTagToParty(requestParameters: AssignTagToPartyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PartyTagAssignmentResponse> {
        const response = await this.assignTagToPartyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Adds a new tag to the CRM tag catalog for later assignment to parties. Use this tool when a new label is needed; do not use assignTagToParty, which attaches an existing catalog tag to a party. Preconditions: no existing tag may already use the same name case-insensitively. Required inputs: name (non-blank, max 100); category, color, and active (default true) are optional. Emits a CRM_TAG_CREATE event; the tag becomes immediately assignable when active. Returns 409 when a tag with the same name already exists, and 400 when name is blank. 
     * Create Catalog Tag
     */
    async createTagRaw(requestParameters: CreateTagRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PartyTagResponse>> {
        if (requestParameters['upsertPartyTagRequest'] == null) {
            throw new runtime.RequiredError(
                'upsertPartyTagRequest',
                'Required parameter "upsertPartyTagRequest" was null or undefined when calling createTag().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:tag:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/tags`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: UpsertPartyTagRequestToJSON(requestParameters['upsertPartyTagRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PartyTagResponseFromJSON(jsonValue));
    }

    /**
     * Adds a new tag to the CRM tag catalog for later assignment to parties. Use this tool when a new label is needed; do not use assignTagToParty, which attaches an existing catalog tag to a party. Preconditions: no existing tag may already use the same name case-insensitively. Required inputs: name (non-blank, max 100); category, color, and active (default true) are optional. Emits a CRM_TAG_CREATE event; the tag becomes immediately assignable when active. Returns 409 when a tag with the same name already exists, and 400 when name is blank. 
     * Create Catalog Tag
     */
    async createTag(requestParameters: CreateTagRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PartyTagResponse> {
        const response = await this.createTagRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes a tag from the catalog together with every assignment of it on every party. Use this tool only when the tag and its history should disappear entirely; use updateTag with active false instead to retire a tag while keeping existing assignments. Preconditions: the tag must exist; deletion is not reversible. Required inputs: tagId (UUID) as a path parameter; there is no request body. Emits a CRM_TAG_DELETE event; all assignment rows for the tag are removed. Returns 404 when no tag exists for the supplied tagId. 
     * Delete Catalog Tag
     */
    async deleteTagRaw(requestParameters: DeleteTagRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['tagId'] == null) {
            throw new runtime.RequiredError(
                'tagId',
                'Required parameter "tagId" was null or undefined when calling deleteTag().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:tag:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/tags/{tagId}`.replace(`{${"tagId"}}`, encodeURIComponent(String(requestParameters['tagId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes a tag from the catalog together with every assignment of it on every party. Use this tool only when the tag and its history should disappear entirely; use updateTag with active false instead to retire a tag while keeping existing assignments. Preconditions: the tag must exist; deletion is not reversible. Required inputs: tagId (UUID) as a path parameter; there is no request body. Emits a CRM_TAG_DELETE event; all assignment rows for the tag are removed. Returns 404 when no tag exists for the supplied tagId. 
     * Delete Catalog Tag
     */
    async deleteTag(requestParameters: DeleteTagRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteTagRaw(requestParameters, initOverrides);
    }

    /**
     * Returns one catalog tag with its name, category, color, active flag, and assignment count. Use this tool when the tag id is already known; use listTags instead to browse the catalog. Preconditions: the tag must exist in the catalog. Required inputs: tagId (UUID) as a path parameter; there is no request body. Emits a CRM_TAG_GET audit event; no state changes occur. Returns 404 when no tag exists for the supplied tagId. 
     * Get Tag By Id
     */
    async getTagRaw(requestParameters: GetTagRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PartyTagResponse>> {
        if (requestParameters['tagId'] == null) {
            throw new runtime.RequiredError(
                'tagId',
                'Required parameter "tagId" was null or undefined when calling getTag().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:tag:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/tags/{tagId}`.replace(`{${"tagId"}}`, encodeURIComponent(String(requestParameters['tagId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PartyTagResponseFromJSON(jsonValue));
    }

    /**
     * Returns one catalog tag with its name, category, color, active flag, and assignment count. Use this tool when the tag id is already known; use listTags instead to browse the catalog. Preconditions: the tag must exist in the catalog. Required inputs: tagId (UUID) as a path parameter; there is no request body. Emits a CRM_TAG_GET audit event; no state changes occur. Returns 404 when no tag exists for the supplied tagId. 
     * Get Tag By Id
     */
    async getTag(requestParameters: GetTagRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PartyTagResponse> {
        const response = await this.getTagRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the tags currently attached to one party, with who assigned each tag, when, and through which source. Use this tool when reviewing a party\'s labels; use listTags instead for the full catalog of assignable tags. Preconditions: none; an unknown partyId yields an empty list rather than an error. Required inputs: partyId (UUID) as a path parameter; there is no request body. Emits a CRM_PARTY_TAG_LIST audit event; no state changes occur. Returns 200 with an empty list rather than an error when the party has no tags. 
     * List Party Tag Assignments
     */
    async listPartyTagsRaw(requestParameters: ListPartyTagsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<PartyTagAssignmentResponse>>> {
        if (requestParameters['partyId'] == null) {
            throw new runtime.RequiredError(
                'partyId',
                'Required parameter "partyId" was null or undefined when calling listPartyTags().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:tag:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/parties/{partyId}/tags`.replace(`{${"partyId"}}`, encodeURIComponent(String(requestParameters['partyId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(PartyTagAssignmentResponseFromJSON));
    }

    /**
     * Returns the tags currently attached to one party, with who assigned each tag, when, and through which source. Use this tool when reviewing a party\'s labels; use listTags instead for the full catalog of assignable tags. Preconditions: none; an unknown partyId yields an empty list rather than an error. Required inputs: partyId (UUID) as a path parameter; there is no request body. Emits a CRM_PARTY_TAG_LIST audit event; no state changes occur. Returns 200 with an empty list rather than an error when the party has no tags. 
     * List Party Tag Assignments
     */
    async listPartyTags(requestParameters: ListPartyTagsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<PartyTagAssignmentResponse>> {
        const response = await this.listPartyTagsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the CRM tag catalog with each tag\'s category, color, active flag, and assignment count. Use this tool when browsing available tags before assigning one; use listPartyTags instead to see which tags a specific party carries. Preconditions: none; inactive tags are omitted unless explicitly requested. Required inputs: none; includeInactive defaults to false, and there is no request body. Emits a CRM_TAG_LIST audit event; no state changes occur. Returns 200 with an empty list rather than an error when the catalog is empty. 
     * List Tag Catalog
     */
    async listTagsRaw(requestParameters: ListTagsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<PartyTagResponse>>> {
        const queryParameters: any = {};

        if (requestParameters['includeInactive'] != null) {
            queryParameters['includeInactive'] = requestParameters['includeInactive'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:tag:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/tags`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(PartyTagResponseFromJSON));
    }

    /**
     * Returns the CRM tag catalog with each tag\'s category, color, active flag, and assignment count. Use this tool when browsing available tags before assigning one; use listPartyTags instead to see which tags a specific party carries. Preconditions: none; inactive tags are omitted unless explicitly requested. Required inputs: none; includeInactive defaults to false, and there is no request body. Emits a CRM_TAG_LIST audit event; no state changes occur. Returns 200 with an empty list rather than an error when the catalog is empty. 
     * List Tag Catalog
     */
    async listTags(requestParameters: ListTagsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<PartyTagResponse>> {
        const response = await this.listTagsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Detaches a tag from a party while leaving the tag itself in the catalog. Use this tool when a label no longer applies to a party; use deleteTag instead to remove the tag from the catalog and every party at once. Preconditions: none; removing a tag that is not assigned is an idempotent no-op. Required inputs: partyId and tagId (UUIDs) as path parameters; there is no request body. Emits a CRM_PARTY_TAG_REMOVE event and publishes a party-tag-changed fact when an assignment was actually removed. Returns 204 in every authorized call, including when nothing was assigned. 
     * Remove Tag From Party
     */
    async removeTagFromPartyRaw(requestParameters: RemoveTagFromPartyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['partyId'] == null) {
            throw new runtime.RequiredError(
                'partyId',
                'Required parameter "partyId" was null or undefined when calling removeTagFromParty().'
            );
        }

        if (requestParameters['tagId'] == null) {
            throw new runtime.RequiredError(
                'tagId',
                'Required parameter "tagId" was null or undefined when calling removeTagFromParty().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:tag:assign"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/parties/{partyId}/tags/{tagId}`.replace(`{${"partyId"}}`, encodeURIComponent(String(requestParameters['partyId']))).replace(`{${"tagId"}}`, encodeURIComponent(String(requestParameters['tagId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Detaches a tag from a party while leaving the tag itself in the catalog. Use this tool when a label no longer applies to a party; use deleteTag instead to remove the tag from the catalog and every party at once. Preconditions: none; removing a tag that is not assigned is an idempotent no-op. Required inputs: partyId and tagId (UUIDs) as path parameters; there is no request body. Emits a CRM_PARTY_TAG_REMOVE event and publishes a party-tag-changed fact when an assignment was actually removed. Returns 204 in every authorized call, including when nothing was assigned. 
     * Remove Tag From Party
     */
    async removeTagFromParty(requestParameters: RemoveTagFromPartyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.removeTagFromPartyRaw(requestParameters, initOverrides);
    }

    /**
     * Renames, recolours, recategorizes, or retires a catalog tag; setting active false stops new assignments while keeping existing ones. Use this tool when changing how a tag looks or retiring it; use deleteTag instead to remove the tag and every assignment of it. Preconditions: the tag must exist, and the new name must not collide with another tag case-insensitively. Required inputs: tagId (UUID) as a path parameter and name in the body; category, color, and active are optional. Emits a CRM_TAG_UPDATE event; existing assignments are untouched. Returns 404 when the tag does not exist, and 409 when the new name is already taken. 
     * Update Catalog Tag
     */
    async updateTagRaw(requestParameters: UpdateTagRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PartyTagResponse>> {
        if (requestParameters['tagId'] == null) {
            throw new runtime.RequiredError(
                'tagId',
                'Required parameter "tagId" was null or undefined when calling updateTag().'
            );
        }

        if (requestParameters['upsertPartyTagRequest'] == null) {
            throw new runtime.RequiredError(
                'upsertPartyTagRequest',
                'Required parameter "upsertPartyTagRequest" was null or undefined when calling updateTag().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:tag:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/tags/{tagId}`.replace(`{${"tagId"}}`, encodeURIComponent(String(requestParameters['tagId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: UpsertPartyTagRequestToJSON(requestParameters['upsertPartyTagRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PartyTagResponseFromJSON(jsonValue));
    }

    /**
     * Renames, recolours, recategorizes, or retires a catalog tag; setting active false stops new assignments while keeping existing ones. Use this tool when changing how a tag looks or retiring it; use deleteTag instead to remove the tag and every assignment of it. Preconditions: the tag must exist, and the new name must not collide with another tag case-insensitively. Required inputs: tagId (UUID) as a path parameter and name in the body; category, color, and active are optional. Emits a CRM_TAG_UPDATE event; existing assignments are untouched. Returns 404 when the tag does not exist, and 409 when the new name is already taken. 
     * Update Catalog Tag
     */
    async updateTag(requestParameters: UpdateTagRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PartyTagResponse> {
        const response = await this.updateTagRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
