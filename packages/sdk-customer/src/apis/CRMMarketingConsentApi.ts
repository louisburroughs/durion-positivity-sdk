/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Customer API
 * API documentation for the Customer service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  MarketingConsentDecision,
  MarketingConsentSummaryResponse,
  PagedResponse,
  UpdateMarketingConsentRequest,
} from '../models/index';
import {
    MarketingConsentDecisionFromJSON,
    MarketingConsentDecisionToJSON,
    MarketingConsentSummaryResponseFromJSON,
    MarketingConsentSummaryResponseToJSON,
    PagedResponseFromJSON,
    PagedResponseToJSON,
    UpdateMarketingConsentRequestFromJSON,
    UpdateMarketingConsentRequestToJSON,
} from '../models/index';

export interface GetConsentHistoryRequest {
    partyId: string;
    page?: number;
    size?: number;
}

export interface GetMarketingConsentRequest {
    partyId: string;
}

export interface ResolveMarketingEligibilityRequest {
    partyId: string;
    channel: ResolveMarketingEligibilityChannelEnum;
}

export interface SetAccountMarketingGateRequest {
    partyId: string;
    optOut: boolean;
}

export interface UpdateMarketingConsentOperationRequest {
    partyId: string;
    updateMarketingConsentRequest: UpdateMarketingConsentRequest;
}

/**
 * 
 */
export class CRMMarketingConsentApi extends runtime.BaseAPI {

    /**
     * Returns the append-only marketing-consent audit trail for a party, newest first, with one entry per actual consent change recording the channel, old and new values, reason, source, and acting user. Use this tool when auditing how a party\'s consent reached its current state; use getMarketingConsent instead for the current consent summary and eligibility. Preconditions: none; a party with no recorded consent changes yields an empty page rather than an error. Required inputs: partyId (UUID) as a path parameter; page defaults to 0 and size defaults to 50 with a clamp between 1 and 200. No events are emitted by the consent trail itself beyond the CRM_CONSENT_HISTORY_GET audit event this read emits; no state changes occur. Returns 200 with an empty page rather than an error when no history exists. 
     * Get Consent Audit History
     */
    async getConsentHistoryRaw(requestParameters: GetConsentHistoryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagedResponse>> {
        if (requestParameters['partyId'] == null) {
            throw new runtime.RequiredError(
                'partyId',
                'Required parameter "partyId" was null or undefined when calling getConsentHistory().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:consent:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/parties/{partyId}/consent-history`.replace(`{${"partyId"}}`, encodeURIComponent(String(requestParameters['partyId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagedResponseFromJSON(jsonValue));
    }

    /**
     * Returns the append-only marketing-consent audit trail for a party, newest first, with one entry per actual consent change recording the channel, old and new values, reason, source, and acting user. Use this tool when auditing how a party\'s consent reached its current state; use getMarketingConsent instead for the current consent summary and eligibility. Preconditions: none; a party with no recorded consent changes yields an empty page rather than an error. Required inputs: partyId (UUID) as a path parameter; page defaults to 0 and size defaults to 50 with a clamp between 1 and 200. No events are emitted by the consent trail itself beyond the CRM_CONSENT_HISTORY_GET audit event this read emits; no state changes occur. Returns 200 with an empty page rather than an error when no history exists. 
     * Get Consent Audit History
     */
    async getConsentHistory(requestParameters: GetConsentHistoryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagedResponse> {
        const response = await this.getConsentHistoryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns a party\'s per-channel marketing consent (EMAIL and SMS, each OPT_IN, OPT_OUT, or UNSET) together with the account gate, quiet hours, monthly send cap, and the resolved send eligibility per channel. Use this tool when reviewing a party\'s full consent state; use resolveMarketingEligibility instead when only a single allow or deny decision for one channel is needed. Preconditions: the party must exist as either a commercial or person party. Required inputs: partyId (UUID) as a path parameter; there is no request body. Emits a CRM_MARKETING_CONSENT_GET audit event; no state changes occur. Returns 404 when no party exists for the supplied partyId. 
     * Get Marketing Consent Summary
     */
    async getMarketingConsentRaw(requestParameters: GetMarketingConsentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MarketingConsentSummaryResponse>> {
        if (requestParameters['partyId'] == null) {
            throw new runtime.RequiredError(
                'partyId',
                'Required parameter "partyId" was null or undefined when calling getMarketingConsent().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:consent:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/parties/{partyId}/marketing-consent`.replace(`{${"partyId"}}`, encodeURIComponent(String(requestParameters['partyId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MarketingConsentSummaryResponseFromJSON(jsonValue));
    }

    /**
     * Returns a party\'s per-channel marketing consent (EMAIL and SMS, each OPT_IN, OPT_OUT, or UNSET) together with the account gate, quiet hours, monthly send cap, and the resolved send eligibility per channel. Use this tool when reviewing a party\'s full consent state; use resolveMarketingEligibility instead when only a single allow or deny decision for one channel is needed. Preconditions: the party must exist as either a commercial or person party. Required inputs: partyId (UUID) as a path parameter; there is no request body. Emits a CRM_MARKETING_CONSENT_GET audit event; no state changes occur. Returns 404 when no party exists for the supplied partyId. 
     * Get Marketing Consent Summary
     */
    async getMarketingConsent(requestParameters: GetMarketingConsentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MarketingConsentSummaryResponse> {
        const response = await this.getMarketingConsentRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Resolves a single allow-or-deny marketing send decision for one party and channel by folding together the account gate, the governing party\'s consent, and the suppression list, where suppression is a hard block layered on top of an opt-in. Use this tool immediately before sending marketing to a party; use getMarketingConsent instead when the full consent summary rather than one decision is needed. Preconditions: none; unknown parties resolve to a deny decision rather than an error. Required inputs: partyId (UUID) as a path parameter and channel (EMAIL or SMS) as a required query parameter. Emits a CRM_MARKETING_ELIGIBILITY_GET audit event; no state changes occur. Returns 200 with allowed false and a reason code such as SUPPRESSED when the send must not happen, and 400 when channel is not EMAIL or SMS. 
     * Resolve Marketing Send Eligibility
     */
    async resolveMarketingEligibilityRaw(requestParameters: ResolveMarketingEligibilityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MarketingConsentDecision>> {
        if (requestParameters['partyId'] == null) {
            throw new runtime.RequiredError(
                'partyId',
                'Required parameter "partyId" was null or undefined when calling resolveMarketingEligibility().'
            );
        }

        if (requestParameters['channel'] == null) {
            throw new runtime.RequiredError(
                'channel',
                'Required parameter "channel" was null or undefined when calling resolveMarketingEligibility().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['channel'] != null) {
            queryParameters['channel'] = requestParameters['channel'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:consent:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/parties/{partyId}/marketing-eligibility`.replace(`{${"partyId"}}`, encodeURIComponent(String(requestParameters['partyId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MarketingConsentDecisionFromJSON(jsonValue));
    }

    /**
     * Resolves a single allow-or-deny marketing send decision for one party and channel by folding together the account gate, the governing party\'s consent, and the suppression list, where suppression is a hard block layered on top of an opt-in. Use this tool immediately before sending marketing to a party; use getMarketingConsent instead when the full consent summary rather than one decision is needed. Preconditions: none; unknown parties resolve to a deny decision rather than an error. Required inputs: partyId (UUID) as a path parameter and channel (EMAIL or SMS) as a required query parameter. Emits a CRM_MARKETING_ELIGIBILITY_GET audit event; no state changes occur. Returns 200 with allowed false and a reason code such as SUPPRESSED when the send must not happen, and 400 when channel is not EMAIL or SMS. 
     * Resolve Marketing Send Eligibility
     */
    async resolveMarketingEligibility(requestParameters: ResolveMarketingEligibilityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MarketingConsentDecision> {
        const response = await this.resolveMarketingEligibilityRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Sets or clears the commercial-account marketing hard gate; while set, no marketing reaches the account on any channel regardless of individual contact consent. Use this tool when an entire commercial account must be excluded from marketing; do not use updateMarketingConsent, which changes one party\'s per-channel consent and cannot override the gate. Preconditions: the party must exist as a commercial party; person parties have no account gate, and setting the gate to its current value is an idempotent no-op. Required inputs: partyId (UUID) as a path parameter and optOut (boolean) as a query parameter, where true engages the gate and false clears it; there is no request body. Emits a CRM_MARKETING_ACCOUNT_GATE_SET event, and when the gate actually flips it republishes the resolved eligibility decisions for both channels. Returns 404 when no commercial party exists for the supplied partyId. 
     * Set Account Marketing Gate
     */
    async setAccountMarketingGateRaw(requestParameters: SetAccountMarketingGateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MarketingConsentSummaryResponse>> {
        if (requestParameters['partyId'] == null) {
            throw new runtime.RequiredError(
                'partyId',
                'Required parameter "partyId" was null or undefined when calling setAccountMarketingGate().'
            );
        }

        if (requestParameters['optOut'] == null) {
            throw new runtime.RequiredError(
                'optOut',
                'Required parameter "optOut" was null or undefined when calling setAccountMarketingGate().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['optOut'] != null) {
            queryParameters['optOut'] = requestParameters['optOut'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:consent:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/parties/{partyId}/marketing-consent/account-gate`.replace(`{${"partyId"}}`, encodeURIComponent(String(requestParameters['partyId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MarketingConsentSummaryResponseFromJSON(jsonValue));
    }

    /**
     * Sets or clears the commercial-account marketing hard gate; while set, no marketing reaches the account on any channel regardless of individual contact consent. Use this tool when an entire commercial account must be excluded from marketing; do not use updateMarketingConsent, which changes one party\'s per-channel consent and cannot override the gate. Preconditions: the party must exist as a commercial party; person parties have no account gate, and setting the gate to its current value is an idempotent no-op. Required inputs: partyId (UUID) as a path parameter and optOut (boolean) as a query parameter, where true engages the gate and false clears it; there is no request body. Emits a CRM_MARKETING_ACCOUNT_GATE_SET event, and when the gate actually flips it republishes the resolved eligibility decisions for both channels. Returns 404 when no commercial party exists for the supplied partyId. 
     * Set Account Marketing Gate
     */
    async setAccountMarketingGate(requestParameters: SetAccountMarketingGateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MarketingConsentSummaryResponse> {
        const response = await this.setAccountMarketingGateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Updates a party\'s per-channel marketing consent; only channels present in the request change, and each actual change appends one immutable consent-audit entry attributed to the calling user. Use this tool when a party grants or withdraws marketing consent; do not use setAccountMarketingGate, which is the account-level hard gate overriding all contact consent, and do not use upsertCommunicationPreferences, which stores transactional channel preferences rather than marketing consent. Preconditions: the party must exist as either a commercial or person party; re-submitting an unchanged value is an idempotent no-op that writes no audit row. Required inputs: partyId (UUID) as a path parameter; marketingEmailConsent and marketingSmsConsent take OPT_IN, OPT_OUT, or UNSET, source defaults to CSR, and optOutReason, quietHoursStart, quietHoursEnd, and maxMarketingSendsPerMonth are optional. Emits a CRM_MARKETING_CONSENT_UPDATE event and republishes the resolved eligibility decision for both channels so downstream consumers never hold a stale allow. Returns 404 when no party exists for the supplied partyId. 
     * Update Marketing Consent
     */
    async updateMarketingConsentRaw(requestParameters: UpdateMarketingConsentOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MarketingConsentSummaryResponse>> {
        if (requestParameters['partyId'] == null) {
            throw new runtime.RequiredError(
                'partyId',
                'Required parameter "partyId" was null or undefined when calling updateMarketingConsent().'
            );
        }

        if (requestParameters['updateMarketingConsentRequest'] == null) {
            throw new runtime.RequiredError(
                'updateMarketingConsentRequest',
                'Required parameter "updateMarketingConsentRequest" was null or undefined when calling updateMarketingConsent().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:consent:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/parties/{partyId}/marketing-consent`.replace(`{${"partyId"}}`, encodeURIComponent(String(requestParameters['partyId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: UpdateMarketingConsentRequestToJSON(requestParameters['updateMarketingConsentRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MarketingConsentSummaryResponseFromJSON(jsonValue));
    }

    /**
     * Updates a party\'s per-channel marketing consent; only channels present in the request change, and each actual change appends one immutable consent-audit entry attributed to the calling user. Use this tool when a party grants or withdraws marketing consent; do not use setAccountMarketingGate, which is the account-level hard gate overriding all contact consent, and do not use upsertCommunicationPreferences, which stores transactional channel preferences rather than marketing consent. Preconditions: the party must exist as either a commercial or person party; re-submitting an unchanged value is an idempotent no-op that writes no audit row. Required inputs: partyId (UUID) as a path parameter; marketingEmailConsent and marketingSmsConsent take OPT_IN, OPT_OUT, or UNSET, source defaults to CSR, and optOutReason, quietHoursStart, quietHoursEnd, and maxMarketingSendsPerMonth are optional. Emits a CRM_MARKETING_CONSENT_UPDATE event and republishes the resolved eligibility decision for both channels so downstream consumers never hold a stale allow. Returns 404 when no party exists for the supplied partyId. 
     * Update Marketing Consent
     */
    async updateMarketingConsent(requestParameters: UpdateMarketingConsentOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MarketingConsentSummaryResponse> {
        const response = await this.updateMarketingConsentRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
  * @export
  * @enum {string}
  */
export enum ResolveMarketingEligibilityChannelEnum {
    Email = 'EMAIL',
    Sms = 'SMS'
}
