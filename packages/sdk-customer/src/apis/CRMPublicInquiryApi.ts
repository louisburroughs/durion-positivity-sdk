/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Customer API
 * API documentation for the Customer service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  InquiryAcceptedResponse,
  SubmitInquiryRequest,
} from '../models/index';
import {
    InquiryAcceptedResponseFromJSON,
    InquiryAcceptedResponseToJSON,
    SubmitInquiryRequestFromJSON,
    SubmitInquiryRequestToJSON,
} from '../models/index';

export interface SubmitPublicInquiryRequest {
    submitInquiryRequest: SubmitInquiryRequest;
}

/**
 * 
 */
export class CRMPublicInquiryApi extends runtime.BaseAPI {

    /**
     * Accepts an anonymous service or fleet-quote inquiry from the public web form, rate-limited per hashed submitter fingerprint, and returns only a reference id and NEW status so the endpoint cannot be used to enumerate existing customers. Use this tool only for the unauthenticated public form when the deployment has enabled it via pos.customer.inquiry.public.enabled; do not use captureInquiry here, which is the authenticated staff path and is not rate-limited. Preconditions: the public-inquiry feature flag must be enabled and the submitter must be under the per-source rate ceiling; the raw source address is never stored, only a truncated SHA-256 fingerprint. Required inputs: channel, audienceType (COMMERCIAL or INDIVIDUAL), contactName, and at least one of email or phone; message and interest fields are optional. Emits a CRM_PUBLIC_INQUIRY_SUBMIT event and persists the inquiry in NEW status, unassigned in the shared triage queue. Returns 429 when the submitter has exceeded the rate limit, and 400 when required fields are missing or neither email nor phone is supplied. 
     * Submit Public Inquiry
     */
    async submitPublicInquiryRaw(requestParameters: SubmitPublicInquiryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InquiryAcceptedResponse>> {
        if (requestParameters['submitInquiryRequest'] == null) {
            throw new runtime.RequiredError(
                'submitInquiryRequest',
                'Required parameter "submitInquiryRequest" was null or undefined when calling submitPublicInquiry().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        const response = await this.request({
            path: `/v1/crm/public/inquiries`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SubmitInquiryRequestToJSON(requestParameters['submitInquiryRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InquiryAcceptedResponseFromJSON(jsonValue));
    }

    /**
     * Accepts an anonymous service or fleet-quote inquiry from the public web form, rate-limited per hashed submitter fingerprint, and returns only a reference id and NEW status so the endpoint cannot be used to enumerate existing customers. Use this tool only for the unauthenticated public form when the deployment has enabled it via pos.customer.inquiry.public.enabled; do not use captureInquiry here, which is the authenticated staff path and is not rate-limited. Preconditions: the public-inquiry feature flag must be enabled and the submitter must be under the per-source rate ceiling; the raw source address is never stored, only a truncated SHA-256 fingerprint. Required inputs: channel, audienceType (COMMERCIAL or INDIVIDUAL), contactName, and at least one of email or phone; message and interest fields are optional. Emits a CRM_PUBLIC_INQUIRY_SUBMIT event and persists the inquiry in NEW status, unassigned in the shared triage queue. Returns 429 when the submitter has exceeded the rate limit, and 400 when required fields are missing or neither email nor phone is supplied. 
     * Submit Public Inquiry
     */
    async submitPublicInquiry(requestParameters: SubmitPublicInquiryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InquiryAcceptedResponse> {
        const response = await this.submitPublicInquiryRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
