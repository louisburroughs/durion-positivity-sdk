/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Customer API
 * API documentation for the Customer service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CustomerInteractionResponse,
  PagedResponse,
  RecordInteractionRequest,
} from '../models/index';
import {
    CustomerInteractionResponseFromJSON,
    CustomerInteractionResponseToJSON,
    PagedResponseFromJSON,
    PagedResponseToJSON,
    RecordInteractionRequestFromJSON,
    RecordInteractionRequestToJSON,
} from '../models/index';

export interface ListPartyInteractionsRequest {
    partyId: string;
    type?: ListPartyInteractionsTypeEnum;
    page?: number;
    size?: number;
}

export interface RecordInteractionOperationRequest {
    partyId: string;
    recordInteractionRequest: RecordInteractionRequest;
}

/**
 * 
 */
export class CRMInteractionsApi extends runtime.BaseAPI {

    /**
     * Returns a party\'s interaction timeline newest first, unifying campaign sends, CSR touches, and workorder notes, with interaction bodies passed through redaction before they are returned. Use this tool when reviewing a party\'s touch history; do not use recordInteraction, which appends a new entry to the timeline instead of reading it. Preconditions: none; an unknown partyId simply yields an empty page rather than an error. Required inputs: partyId (UUID) as a path parameter; type optionally filters to one of CAMPAIGN_SEND, EMAIL, SMS, CALL, FOLLOW_UP, NOTE, or WORKORDER_NOTE, page defaults to 0, and size defaults to 50 with a clamp between 1 and 200. Emits a CRM_INTERACTION_LIST audit event; no state changes occur. Returns 200 with an empty page rather than an error when the party has no interactions. 
     * List Party Interaction Timeline
     */
    async listPartyInteractionsRaw(requestParameters: ListPartyInteractionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagedResponse>> {
        if (requestParameters['partyId'] == null) {
            throw new runtime.RequiredError(
                'partyId',
                'Required parameter "partyId" was null or undefined when calling listPartyInteractions().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['type'] != null) {
            queryParameters['type'] = requestParameters['type'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:interaction:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/parties/{partyId}/interactions`.replace(`{${"partyId"}}`, encodeURIComponent(String(requestParameters['partyId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagedResponseFromJSON(jsonValue));
    }

    /**
     * Returns a party\'s interaction timeline newest first, unifying campaign sends, CSR touches, and workorder notes, with interaction bodies passed through redaction before they are returned. Use this tool when reviewing a party\'s touch history; do not use recordInteraction, which appends a new entry to the timeline instead of reading it. Preconditions: none; an unknown partyId simply yields an empty page rather than an error. Required inputs: partyId (UUID) as a path parameter; type optionally filters to one of CAMPAIGN_SEND, EMAIL, SMS, CALL, FOLLOW_UP, NOTE, or WORKORDER_NOTE, page defaults to 0, and size defaults to 50 with a clamp between 1 and 200. Emits a CRM_INTERACTION_LIST audit event; no state changes occur. Returns 200 with an empty page rather than an error when the party has no interactions. 
     * List Party Interaction Timeline
     */
    async listPartyInteractions(requestParameters: ListPartyInteractionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagedResponse> {
        const response = await this.listPartyInteractionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Records a CSR-initiated touch such as a call, email, or note on a party\'s interaction timeline, stamping the acting username from the security context. Use this tool when logging a manual customer touch; do not use listPartyInteractions, which reads the timeline, and note that campaign and workorder interactions are ingested from events rather than through this endpoint. Preconditions: none are checked against the party; the interaction is stored against the supplied partyId as-is. Required inputs: partyId (UUID) as a path parameter and type (CAMPAIGN_SEND, EMAIL, SMS, CALL, FOLLOW_UP, NOTE, or WORKORDER_NOTE) in the body; direction defaults to OUTBOUND, occurredAt defaults to now, and channel accepts EMAIL or SMS. Emits a CRM_INTERACTION_RECORD event and persists the interaction row. Returns 400 when type is missing or subject, summary, or body exceed their length limits. 
     * Record Party Interaction
     */
    async recordInteractionRaw(requestParameters: RecordInteractionOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CustomerInteractionResponse>> {
        if (requestParameters['partyId'] == null) {
            throw new runtime.RequiredError(
                'partyId',
                'Required parameter "partyId" was null or undefined when calling recordInteraction().'
            );
        }

        if (requestParameters['recordInteractionRequest'] == null) {
            throw new runtime.RequiredError(
                'recordInteractionRequest',
                'Required parameter "recordInteractionRequest" was null or undefined when calling recordInteraction().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["crm:interaction:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/crm/parties/{partyId}/interactions`.replace(`{${"partyId"}}`, encodeURIComponent(String(requestParameters['partyId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RecordInteractionRequestToJSON(requestParameters['recordInteractionRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CustomerInteractionResponseFromJSON(jsonValue));
    }

    /**
     * Records a CSR-initiated touch such as a call, email, or note on a party\'s interaction timeline, stamping the acting username from the security context. Use this tool when logging a manual customer touch; do not use listPartyInteractions, which reads the timeline, and note that campaign and workorder interactions are ingested from events rather than through this endpoint. Preconditions: none are checked against the party; the interaction is stored against the supplied partyId as-is. Required inputs: partyId (UUID) as a path parameter and type (CAMPAIGN_SEND, EMAIL, SMS, CALL, FOLLOW_UP, NOTE, or WORKORDER_NOTE) in the body; direction defaults to OUTBOUND, occurredAt defaults to now, and channel accepts EMAIL or SMS. Emits a CRM_INTERACTION_RECORD event and persists the interaction row. Returns 400 when type is missing or subject, summary, or body exceed their length limits. 
     * Record Party Interaction
     */
    async recordInteraction(requestParameters: RecordInteractionOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CustomerInteractionResponse> {
        const response = await this.recordInteractionRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
  * @export
  * @enum {string}
  */
export enum ListPartyInteractionsTypeEnum {
    CampaignSend = 'CAMPAIGN_SEND',
    Email = 'EMAIL',
    Sms = 'SMS',
    Call = 'CALL',
    FollowUp = 'FOLLOW_UP',
    Note = 'NOTE',
    WorkorderNote = 'WORKORDER_NOTE'
}
