/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Inventory API
 * API documentation for the Inventory service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  CreateCycleCountToleranceRequest,
  CycleCountToleranceResponse,
  UpdateCycleCountToleranceRequest,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    CreateCycleCountToleranceRequestFromJSON,
    CreateCycleCountToleranceRequestToJSON,
    CycleCountToleranceResponseFromJSON,
    CycleCountToleranceResponseToJSON,
    UpdateCycleCountToleranceRequestFromJSON,
    UpdateCycleCountToleranceRequestToJSON,
} from '../models/index';

export interface CreateCycleCountToleranceOperationRequest {
    createCycleCountToleranceRequest: CreateCycleCountToleranceRequest;
}

export interface DeleteCycleCountToleranceRequest {
    toleranceId: string;
}

export interface GetCycleCountToleranceRequest {
    toleranceId: string;
}

export interface UpdateCycleCountToleranceOperationRequest {
    toleranceId: string;
    updateCycleCountToleranceRequest: UpdateCycleCountToleranceRequest;
}

/**
 * 
 */
export class CycleCountTolerancesApi extends runtime.BaseAPI {

    /**
     * Creates a tolerance configuration scoped by product, storage location, both, or neither (the global default), as an absolute quantity bound, a percentage bound, or both. Use this tool to configure how much variance a bulk product or location may show before a cycle count is flagged for investigation; do not use this for approval thresholds, which are a separate configuration governing who must sign off on an adjustment once one is created. Preconditions: no other active tolerance may already occupy the exact same scope. Required inputs: createdBy; productId, storageLocation, absoluteTolerance and percentageTolerance are all optional. Emits an INVENTORY_CYCLE_COUNT_TOLERANCE_CREATE event. Returns 400 when an active tolerance already exists for this scope. 
     * Create a cycle-count tolerance configuration
     */
    async createCycleCountToleranceRaw(requestParameters: CreateCycleCountToleranceOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CycleCountToleranceResponse>> {
        if (requestParameters['createCycleCountToleranceRequest'] == null) {
            throw new runtime.RequiredError(
                'createCycleCountToleranceRequest',
                'Required parameter "createCycleCountToleranceRequest" was null or undefined when calling createCycleCountTolerance().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:cycle_count_tolerance:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/cycleCountTolerances`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateCycleCountToleranceRequestToJSON(requestParameters['createCycleCountToleranceRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CycleCountToleranceResponseFromJSON(jsonValue));
    }

    /**
     * Creates a tolerance configuration scoped by product, storage location, both, or neither (the global default), as an absolute quantity bound, a percentage bound, or both. Use this tool to configure how much variance a bulk product or location may show before a cycle count is flagged for investigation; do not use this for approval thresholds, which are a separate configuration governing who must sign off on an adjustment once one is created. Preconditions: no other active tolerance may already occupy the exact same scope. Required inputs: createdBy; productId, storageLocation, absoluteTolerance and percentageTolerance are all optional. Emits an INVENTORY_CYCLE_COUNT_TOLERANCE_CREATE event. Returns 400 when an active tolerance already exists for this scope. 
     * Create a cycle-count tolerance configuration
     */
    async createCycleCountTolerance(requestParameters: CreateCycleCountToleranceOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CycleCountToleranceResponse> {
        const response = await this.createCycleCountToleranceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Permanently removes a tolerance configuration. Use this tool to retire a tolerance no longer wanted; do not use updateCycleCountTolerance with active=false instead unless the row should be kept around for its audit trail — delete removes it outright. Preconditions: the tolerance must exist. Required inputs: toleranceId (UUID) path parameter; there is no request body. Emits an INVENTORY_CYCLE_COUNT_TOLERANCE_DELETE event. Returns 404 when no tolerance exists for the supplied id. 
     * Delete a cycle-count tolerance configuration
     */
    async deleteCycleCountToleranceRaw(requestParameters: DeleteCycleCountToleranceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['toleranceId'] == null) {
            throw new runtime.RequiredError(
                'toleranceId',
                'Required parameter "toleranceId" was null or undefined when calling deleteCycleCountTolerance().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:cycle_count_tolerance:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/cycleCountTolerances/{toleranceId}`.replace(`{${"toleranceId"}}`, encodeURIComponent(String(requestParameters['toleranceId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Permanently removes a tolerance configuration. Use this tool to retire a tolerance no longer wanted; do not use updateCycleCountTolerance with active=false instead unless the row should be kept around for its audit trail — delete removes it outright. Preconditions: the tolerance must exist. Required inputs: toleranceId (UUID) path parameter; there is no request body. Emits an INVENTORY_CYCLE_COUNT_TOLERANCE_DELETE event. Returns 404 when no tolerance exists for the supplied id. 
     * Delete a cycle-count tolerance configuration
     */
    async deleteCycleCountTolerance(requestParameters: DeleteCycleCountToleranceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteCycleCountToleranceRaw(requestParameters, initOverrides);
    }

    /**
     * Returns one tolerance configuration by id. Use this tool when the toleranceId is already known; use listCycleCountTolerances instead to browse all configured tolerances. Preconditions: the tolerance must exist. Required inputs: toleranceId (UUID) path parameter; there is no request body. No events are emitted; this is a read-only projection. Returns 404 when no tolerance exists for the supplied id. 
     * Get a cycle-count tolerance configuration
     */
    async getCycleCountToleranceRaw(requestParameters: GetCycleCountToleranceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CycleCountToleranceResponse>> {
        if (requestParameters['toleranceId'] == null) {
            throw new runtime.RequiredError(
                'toleranceId',
                'Required parameter "toleranceId" was null or undefined when calling getCycleCountTolerance().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:cycle_count:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/cycleCountTolerances/{toleranceId}`.replace(`{${"toleranceId"}}`, encodeURIComponent(String(requestParameters['toleranceId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CycleCountToleranceResponseFromJSON(jsonValue));
    }

    /**
     * Returns one tolerance configuration by id. Use this tool when the toleranceId is already known; use listCycleCountTolerances instead to browse all configured tolerances. Preconditions: the tolerance must exist. Required inputs: toleranceId (UUID) path parameter; there is no request body. No events are emitted; this is a read-only projection. Returns 404 when no tolerance exists for the supplied id. 
     * Get a cycle-count tolerance configuration
     */
    async getCycleCountTolerance(requestParameters: GetCycleCountToleranceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CycleCountToleranceResponse> {
        const response = await this.getCycleCountToleranceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every tolerance configuration, active and inactive, newest first. Use this tool to browse or audit configured tolerances; use getCycleCountTolerance instead when the toleranceId is already known. Preconditions: none. Required inputs: none; there is no paging or filtering. No events are emitted; this is a read-only projection. Returns 200 with an empty array when none are configured. 
     * List all cycle-count tolerance configurations
     */
    async listCycleCountTolerancesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<CycleCountToleranceResponse>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:cycle_count:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/cycleCountTolerances`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(CycleCountToleranceResponseFromJSON));
    }

    /**
     * Returns every tolerance configuration, active and inactive, newest first. Use this tool to browse or audit configured tolerances; use getCycleCountTolerance instead when the toleranceId is already known. Preconditions: none. Required inputs: none; there is no paging or filtering. No events are emitted; this is a read-only projection. Returns 200 with an empty array when none are configured. 
     * List all cycle-count tolerance configurations
     */
    async listCycleCountTolerances(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<CycleCountToleranceResponse>> {
        const response = await this.listCycleCountTolerancesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Replaces a tolerance configuration\'s absolute bound, percentage bound, and active flag; the scope (productId, storageLocation) is immutable — recreate the row to change scope. Use this tool to widen, tighten, or deactivate an existing tolerance; do not use this to change scope — create a new tolerance for the new scope instead. Preconditions: the tolerance must exist; reactivating (active=false -> true) requires no other active row already occupy the same scope. Required inputs: toleranceId (UUID) path parameter, active (boolean); absoluteTolerance and percentageTolerance are set verbatim from the request, null included (PUT semantics). Emits an INVENTORY_CYCLE_COUNT_TOLERANCE_UPDATE event. Returns 404 when no tolerance exists for the id, 400 when reactivation would collide with an already-active row at the same scope. 
     * Replace a cycle-count tolerance\'s bounds and active state
     */
    async updateCycleCountToleranceRaw(requestParameters: UpdateCycleCountToleranceOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CycleCountToleranceResponse>> {
        if (requestParameters['toleranceId'] == null) {
            throw new runtime.RequiredError(
                'toleranceId',
                'Required parameter "toleranceId" was null or undefined when calling updateCycleCountTolerance().'
            );
        }

        if (requestParameters['updateCycleCountToleranceRequest'] == null) {
            throw new runtime.RequiredError(
                'updateCycleCountToleranceRequest',
                'Required parameter "updateCycleCountToleranceRequest" was null or undefined when calling updateCycleCountTolerance().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:cycle_count_tolerance:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/cycleCountTolerances/{toleranceId}`.replace(`{${"toleranceId"}}`, encodeURIComponent(String(requestParameters['toleranceId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: UpdateCycleCountToleranceRequestToJSON(requestParameters['updateCycleCountToleranceRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CycleCountToleranceResponseFromJSON(jsonValue));
    }

    /**
     * Replaces a tolerance configuration\'s absolute bound, percentage bound, and active flag; the scope (productId, storageLocation) is immutable — recreate the row to change scope. Use this tool to widen, tighten, or deactivate an existing tolerance; do not use this to change scope — create a new tolerance for the new scope instead. Preconditions: the tolerance must exist; reactivating (active=false -> true) requires no other active row already occupy the same scope. Required inputs: toleranceId (UUID) path parameter, active (boolean); absoluteTolerance and percentageTolerance are set verbatim from the request, null included (PUT semantics). Emits an INVENTORY_CYCLE_COUNT_TOLERANCE_UPDATE event. Returns 404 when no tolerance exists for the id, 400 when reactivation would collide with an already-active row at the same scope. 
     * Replace a cycle-count tolerance\'s bounds and active state
     */
    async updateCycleCountTolerance(requestParameters: UpdateCycleCountToleranceOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CycleCountToleranceResponse> {
        const response = await this.updateCycleCountToleranceRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
