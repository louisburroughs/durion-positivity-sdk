/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Inventory API
 * API documentation for the Inventory service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  LocationSyncRunResponse,
  SyncLogResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    LocationSyncRunResponseFromJSON,
    LocationSyncRunResponseToJSON,
    SyncLogResponseFromJSON,
    SyncLogResponseToJSON,
} from '../models/index';

export interface GetSyncLogRequest {
    syncLogId: string;
}

export interface ListSyncLogsRequest {
    outcome?: ListSyncLogsOutcomeEnum;
    page?: number;
    size?: number;
    pageIndex?: number;
    pageSize?: number;
}

export interface TriggerLocationSyncRequest {
    idempotencyKey?: string;
    xCorrelationId?: string;
}

/**
 * 
 */
export class LocationSyncApi extends runtime.BaseAPI {

    /**
     * Returns one location sync audit entry with its outcome, per-location counters, timing and any error message. Use this tool when the syncLogId is already known; use listSyncLogs instead to page through the audit trail. Preconditions: the sync log entry must exist. Required inputs: syncLogId (UUID) as a path parameter; there is no request body. Emits an INVENTORY_LOCATION_SYNC_LOG_GET event; no state changes. Returns 404 when no sync log entry exists for the supplied id. 
     * Get a location sync log entry
     */
    async getSyncLogRaw(requestParameters: GetSyncLogRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SyncLogResponse>> {
        if (requestParameters['syncLogId'] == null) {
            throw new runtime.RequiredError(
                'syncLogId',
                'Required parameter "syncLogId" was null or undefined when calling getSyncLog().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:location:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/sync-logs/{syncLogId}`.replace(`{${"syncLogId"}}`, encodeURIComponent(String(requestParameters['syncLogId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SyncLogResponseFromJSON(jsonValue));
    }

    /**
     * Returns one location sync audit entry with its outcome, per-location counters, timing and any error message. Use this tool when the syncLogId is already known; use listSyncLogs instead to page through the audit trail. Preconditions: the sync log entry must exist. Required inputs: syncLogId (UUID) as a path parameter; there is no request body. Emits an INVENTORY_LOCATION_SYNC_LOG_GET event; no state changes. Returns 404 when no sync log entry exists for the supplied id. 
     * Get a location sync log entry
     */
    async getSyncLog(requestParameters: GetSyncLogRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SyncLogResponse> {
        const response = await this.getSyncLogRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists one page of location sync audit entries newest first, optionally filtered by outcome. Use this tool to audit roster sync activity or find a syncLogId; use getSyncLog instead when the id is known, and use triggerLocationSync to request a new re-emit. Preconditions: none. Required inputs: none; outcome (OK, PARTIAL, FAILED, INVALID_PAYLOAD, REQUESTED) is optional, page defaults to 0 and size to 50, and pageIndex/pageSize are accepted as aliases; the response is a plain array, not a page envelope. Emits an INVENTORY_LOCATION_SYNC_LOG_LIST event; no state changes. Returns 200 with an empty array when no entries match, so an empty result is not an error condition. 
     * List location sync logs
     */
    async listSyncLogsRaw(requestParameters: ListSyncLogsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<SyncLogResponse>>> {
        const queryParameters: any = {};

        if (requestParameters['outcome'] != null) {
            queryParameters['outcome'] = requestParameters['outcome'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        if (requestParameters['pageIndex'] != null) {
            queryParameters['pageIndex'] = requestParameters['pageIndex'];
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['pageSize'] = requestParameters['pageSize'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:location:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/sync-logs`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(SyncLogResponseFromJSON));
    }

    /**
     * Lists one page of location sync audit entries newest first, optionally filtered by outcome. Use this tool to audit roster sync activity or find a syncLogId; use getSyncLog instead when the id is known, and use triggerLocationSync to request a new re-emit. Preconditions: none. Required inputs: none; outcome (OK, PARTIAL, FAILED, INVALID_PAYLOAD, REQUESTED) is optional, page defaults to 0 and size to 50, and pageIndex/pageSize are accepted as aliases; the response is a plain array, not a page envelope. Emits an INVENTORY_LOCATION_SYNC_LOG_LIST event; no state changes. Returns 200 with an empty array when no entries match, so an empty result is not an error condition. 
     * List location sync logs
     */
    async listSyncLogs(requestParameters: ListSyncLogsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<SyncLogResponse>> {
        const response = await this.listSyncLogsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Requests an administrative re-emit of pos-location\'s outbox on the location.commands.v1 topic so the location.events.v1 consumer idempotently repairs the local location roster replica. Use this tool when the roster replica looks stale or damaged; use listSyncLogs instead to inspect past runs — the repair completes asynchronously, so this call never returns repaired counts. Preconditions: the Kafka location event feed must be enabled; with the feed disabled the run is recorded as FAILED instead of REQUESTED. Required inputs: none in the body; an optional Idempotency-Key header makes retries safe — re-sending the same key returns the original run — and an optional X-Correlation-Id header is recorded on the run log. Emits an INVENTORY_LOCATION_SYNC_TRIGGER event and writes one sync-log run entry; the re-emit covers the configured lookback window (pos.inventory.location.replay-lookback, default P30D). Returns 202 with outcome REQUESTED when the replay command was published, and 202 with outcome FAILED and the error recorded in the sync log when publishing was not possible. 
     * Trigger a location roster re-sync
     */
    async triggerLocationSyncRaw(requestParameters: TriggerLocationSyncRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LocationSyncRunResponse>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (requestParameters['xCorrelationId'] != null) {
            headerParameters['X-Correlation-Id'] = String(requestParameters['xCorrelationId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:location:sync"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/locations/sync`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LocationSyncRunResponseFromJSON(jsonValue));
    }

    /**
     * Requests an administrative re-emit of pos-location\'s outbox on the location.commands.v1 topic so the location.events.v1 consumer idempotently repairs the local location roster replica. Use this tool when the roster replica looks stale or damaged; use listSyncLogs instead to inspect past runs — the repair completes asynchronously, so this call never returns repaired counts. Preconditions: the Kafka location event feed must be enabled; with the feed disabled the run is recorded as FAILED instead of REQUESTED. Required inputs: none in the body; an optional Idempotency-Key header makes retries safe — re-sending the same key returns the original run — and an optional X-Correlation-Id header is recorded on the run log. Emits an INVENTORY_LOCATION_SYNC_TRIGGER event and writes one sync-log run entry; the re-emit covers the configured lookback window (pos.inventory.location.replay-lookback, default P30D). Returns 202 with outcome REQUESTED when the replay command was published, and 202 with outcome FAILED and the error recorded in the sync log when publishing was not possible. 
     * Trigger a location roster re-sync
     */
    async triggerLocationSync(requestParameters: TriggerLocationSyncRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LocationSyncRunResponse> {
        const response = await this.triggerLocationSyncRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
  * @export
  * @enum {string}
  */
export enum ListSyncLogsOutcomeEnum {
    Ok = 'OK',
    Partial = 'PARTIAL',
    Failed = 'FAILED',
    InvalidPayload = 'INVALID_PAYLOAD',
    Requested = 'REQUESTED'
}
