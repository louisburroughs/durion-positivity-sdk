/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Inventory API
 * API documentation for the Inventory service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  ValuationReportResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    ValuationReportResponseFromJSON,
    ValuationReportResponseToJSON,
} from '../models/index';

export interface GetInventoryValuationRequest {
    locationId?: string;
    sku?: string;
    asOf?: Date;
}

/**
 * 
 */
export class InventoryValuationApi extends runtime.BaseAPI {

    /**
     * Returns on-hand inventory value per SKU with a total, computing on-hand times the current unit cost derived from each SKU\'s resolved costing method — the running average under AVERAGE, the standard price (falling back to the latest-receipt memo) under STANDARD — with uncosted SKUs reported at null unit cost and zero value rather than as errors. Use this tool for valuation reporting; use listInventoryLedger instead for the raw movements behind the numbers, and createRevaluation to change a cost rather than read it. Preconditions: none for a current valuation; an asOf request additionally requires the inventory:ledger:view authority because it replays ledger history. Required inputs: none are mandatory — locationId scopes to one site, sku restricts to one stock item, and asOf (ISO-8601 instant) reconstructs both on-hand and unit cost from the ledger as of that instant. No events are emitted and no state changes; this is a read-only projection computed on demand and never persisted. Returns 422 when asOf is in the future (AS_OF_IN_FUTURE) or when an uncapped full-catalog asOf request exceeds the configured SKU safety cap (default 1000), and 403 when asOf is requested without inventory:ledger:view. 
     * Get inventory valuation
     */
    async getInventoryValuationRaw(requestParameters: GetInventoryValuationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ValuationReportResponse>> {
        const queryParameters: any = {};

        if (requestParameters['locationId'] != null) {
            queryParameters['locationId'] = requestParameters['locationId'];
        }

        if (requestParameters['sku'] != null) {
            queryParameters['sku'] = requestParameters['sku'];
        }

        if (requestParameters['asOf'] != null) {
            queryParameters['asOf'] = (requestParameters['asOf'] as any).toISOString();
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:valuation:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/valuation`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ValuationReportResponseFromJSON(jsonValue));
    }

    /**
     * Returns on-hand inventory value per SKU with a total, computing on-hand times the current unit cost derived from each SKU\'s resolved costing method — the running average under AVERAGE, the standard price (falling back to the latest-receipt memo) under STANDARD — with uncosted SKUs reported at null unit cost and zero value rather than as errors. Use this tool for valuation reporting; use listInventoryLedger instead for the raw movements behind the numbers, and createRevaluation to change a cost rather than read it. Preconditions: none for a current valuation; an asOf request additionally requires the inventory:ledger:view authority because it replays ledger history. Required inputs: none are mandatory — locationId scopes to one site, sku restricts to one stock item, and asOf (ISO-8601 instant) reconstructs both on-hand and unit cost from the ledger as of that instant. No events are emitted and no state changes; this is a read-only projection computed on demand and never persisted. Returns 422 when asOf is in the future (AS_OF_IN_FUTURE) or when an uncapped full-catalog asOf request exceeds the configured SKU safety cap (default 1000), and 403 when asOf is requested without inventory:ledger:view. 
     * Get inventory valuation
     */
    async getInventoryValuation(requestParameters: GetInventoryValuationRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ValuationReportResponse> {
        const response = await this.getInventoryValuationRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
