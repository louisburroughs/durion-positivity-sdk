/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Inventory API
 * API documentation for the Inventory service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  SourcingStrategyConfigRequest,
  SourcingStrategyConfigResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    SourcingStrategyConfigRequestFromJSON,
    SourcingStrategyConfigRequestToJSON,
    SourcingStrategyConfigResponseFromJSON,
    SourcingStrategyConfigResponseToJSON,
} from '../models/index';

export interface DeactivateSourcingStrategyConfigRequest {
    configId: string;
}

export interface UpsertSourcingStrategyConfigRequest {
    sourcingStrategyConfigRequest: SourcingStrategyConfigRequest;
}

/**
 * 
 */
export class SourcingStrategiesApi extends runtime.BaseAPI {

    /**
     * Deactivates one sourcing-strategy configuration row so it stops participating in strategy resolution; this is a soft delete and the row is never removed. Use this tool to retire a scope override and fall back to the next precedence level; do not use upsertSourcingStrategyConfig with a different strategy when the intent is to remove the override entirely. Preconditions: the configuration row must exist; deactivating an already inactive row is a no-op that returns the row. Required inputs: configId (UUID) path parameter; there is no request body. Emits an INVENTORY_SOURCING_STRATEGY_DEACTIVATE event; subsequent decisions fall back to SITE, DEFAULT or the platform default FIFO. Returns 404 when no configuration exists for the supplied id. 
     * Deactivate sourcing strategy configuration
     */
    async deactivateSourcingStrategyConfigRaw(requestParameters: DeactivateSourcingStrategyConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SourcingStrategyConfigResponse>> {
        if (requestParameters['configId'] == null) {
            throw new runtime.RequiredError(
                'configId',
                'Required parameter "configId" was null or undefined when calling deactivateSourcingStrategyConfig().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:location:admin"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/sourcing-strategies/{configId}`.replace(`{${"configId"}}`, encodeURIComponent(String(requestParameters['configId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SourcingStrategyConfigResponseFromJSON(jsonValue));
    }

    /**
     * Deactivates one sourcing-strategy configuration row so it stops participating in strategy resolution; this is a soft delete and the row is never removed. Use this tool to retire a scope override and fall back to the next precedence level; do not use upsertSourcingStrategyConfig with a different strategy when the intent is to remove the override entirely. Preconditions: the configuration row must exist; deactivating an already inactive row is a no-op that returns the row. Required inputs: configId (UUID) path parameter; there is no request body. Emits an INVENTORY_SOURCING_STRATEGY_DEACTIVATE event; subsequent decisions fall back to SITE, DEFAULT or the platform default FIFO. Returns 404 when no configuration exists for the supplied id. 
     * Deactivate sourcing strategy configuration
     */
    async deactivateSourcingStrategyConfig(requestParameters: DeactivateSourcingStrategyConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SourcingStrategyConfigResponse> {
        const response = await this.deactivateSourcingStrategyConfigRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists every sourcing-strategy configuration row, active and inactive, ordered by scope type then scope value. Use this tool to inspect removal-strategy configuration; use upsertSourcingStrategyConfig instead to change a scope, and deactivateSourcingStrategyConfig instead to retire one. Preconditions: none; at most one row exists per scope. Required inputs: none, and there is no paging or filtering. Emits an INVENTORY_SOURCING_STRATEGY_LIST audit event; no configuration is changed. Returns 200 with an empty array when nothing is configured, in which case decision-time resolution falls through SKU_CATEGORY, SITE and DEFAULT to the platform default FIFO. 
     * List sourcing strategy configurations
     */
    async listSourcingStrategyConfigsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<SourcingStrategyConfigResponse>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:location:admin"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/sourcing-strategies`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(SourcingStrategyConfigResponseFromJSON));
    }

    /**
     * Lists every sourcing-strategy configuration row, active and inactive, ordered by scope type then scope value. Use this tool to inspect removal-strategy configuration; use upsertSourcingStrategyConfig instead to change a scope, and deactivateSourcingStrategyConfig instead to retire one. Preconditions: none; at most one row exists per scope. Required inputs: none, and there is no paging or filtering. Emits an INVENTORY_SOURCING_STRATEGY_LIST audit event; no configuration is changed. Returns 200 with an empty array when nothing is configured, in which case decision-time resolution falls through SKU_CATEGORY, SITE and DEFAULT to the platform default FIFO. 
     * List sourcing strategy configurations
     */
    async listSourcingStrategyConfigs(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<SourcingStrategyConfigResponse>> {
        const response = await this.listSourcingStrategyConfigsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates or updates the sourcing strategy for one scope and reactivates the row; at most one row exists per scopeType and scopeValue pair. Use this tool to set FIFO, FEFO, PROXIMITY or HIGHEST_STOCK at a scope; do not use deactivateSourcingStrategyConfig to change a strategy — an upsert on an inactive row reactivates it with the new strategy. Preconditions: none beyond scope-value shape; note that FEFO falls back to FIFO while the SKU has no lot-expiry data, PROXIMITY falls back to FIFO when a decision has no reference location, and SKU_CATEGORY rows are stored but unresolvable until the catalog replica carries categories. Required inputs: scopeType (SKU_CATEGORY, SITE or DEFAULT) and strategy; scopeValue must be the category string for SKU_CATEGORY, the site UUID as text for SITE, and must be omitted for DEFAULT. Emits an INVENTORY_SOURCING_STRATEGY_UPSERT event; the change affects subsequent sourcing decisions immediately. Returns 400 when scopeValue is missing for SITE or SKU_CATEGORY, is not a UUID for SITE, or is present for DEFAULT. 
     * Upsert sourcing strategy configuration
     */
    async upsertSourcingStrategyConfigRaw(requestParameters: UpsertSourcingStrategyConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SourcingStrategyConfigResponse>> {
        if (requestParameters['sourcingStrategyConfigRequest'] == null) {
            throw new runtime.RequiredError(
                'sourcingStrategyConfigRequest',
                'Required parameter "sourcingStrategyConfigRequest" was null or undefined when calling upsertSourcingStrategyConfig().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:location:admin"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/sourcing-strategies`,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: SourcingStrategyConfigRequestToJSON(requestParameters['sourcingStrategyConfigRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SourcingStrategyConfigResponseFromJSON(jsonValue));
    }

    /**
     * Creates or updates the sourcing strategy for one scope and reactivates the row; at most one row exists per scopeType and scopeValue pair. Use this tool to set FIFO, FEFO, PROXIMITY or HIGHEST_STOCK at a scope; do not use deactivateSourcingStrategyConfig to change a strategy — an upsert on an inactive row reactivates it with the new strategy. Preconditions: none beyond scope-value shape; note that FEFO falls back to FIFO while the SKU has no lot-expiry data, PROXIMITY falls back to FIFO when a decision has no reference location, and SKU_CATEGORY rows are stored but unresolvable until the catalog replica carries categories. Required inputs: scopeType (SKU_CATEGORY, SITE or DEFAULT) and strategy; scopeValue must be the category string for SKU_CATEGORY, the site UUID as text for SITE, and must be omitted for DEFAULT. Emits an INVENTORY_SOURCING_STRATEGY_UPSERT event; the change affects subsequent sourcing decisions immediately. Returns 400 when scopeValue is missing for SITE or SKU_CATEGORY, is not a UUID for SITE, or is present for DEFAULT. 
     * Upsert sourcing strategy configuration
     */
    async upsertSourcingStrategyConfig(requestParameters: UpsertSourcingStrategyConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SourcingStrategyConfigResponse> {
        const response = await this.upsertSourcingStrategyConfigRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
