/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Inventory API
 * API documentation for the Inventory service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CreateCycleCountScheduleRequest,
  CycleCountScheduleResponse,
  UpdateCycleCountScheduleRequest,
} from '../models/index';
import {
    CreateCycleCountScheduleRequestFromJSON,
    CreateCycleCountScheduleRequestToJSON,
    CycleCountScheduleResponseFromJSON,
    CycleCountScheduleResponseToJSON,
    UpdateCycleCountScheduleRequestFromJSON,
    UpdateCycleCountScheduleRequestToJSON,
} from '../models/index';

export interface CreateCycleCountScheduleOperationRequest {
    createCycleCountScheduleRequest: CreateCycleCountScheduleRequest;
}

export interface DeactivateCycleCountScheduleRequest {
    scheduleId: string;
}

export interface GetCycleCountScheduleRequest {
    scheduleId: string;
}

export interface ListCycleCountSchedulesRequest {
    locationId?: string;
    active?: boolean;
    due?: boolean;
    page?: number;
    size?: number;
}

export interface UpdateCycleCountScheduleOperationRequest {
    scheduleId: string;
    updateCycleCountScheduleRequest: UpdateCycleCountScheduleRequest;
}

/**
 * 
 */
export class CycleCountSchedulesApi extends runtime.BaseAPI {

    /**
     * Creates an active recurring cycle-count schedule for a location, optionally filtered to one zone and/or SKU category, that comes due every frequencyDays. Use this tool to institutionalise periodic counting; do not use createCycleCountPlan, which creates a single one-off plan. Preconditions: the caller must be an authenticated user (recorded as the schedule creator). Required inputs: locationId (UUID), frequencyDays (positive integer) and nextDueDate (ISO date); zoneId and skuCategory are optional filters, and autoCreatePlan defaults to false, meaning the schedule only surfaces in the due-for-count view instead of auto-creating plans. Emits an INVENTORY_CYCLE_COUNT_SCHEDULE_CREATE event; the schedule starts active and no plan is created by this call. Returns 400 when locationId or nextDueDate is missing or frequencyDays is not positive. 
     * Create cycle count schedule
     */
    async createCycleCountScheduleRaw(requestParameters: CreateCycleCountScheduleOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CycleCountScheduleResponse>> {
        if (requestParameters['createCycleCountScheduleRequest'] == null) {
            throw new runtime.RequiredError(
                'createCycleCountScheduleRequest',
                'Required parameter "createCycleCountScheduleRequest" was null or undefined when calling createCycleCountSchedule().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:cycle_count:initiate"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/cycleCountSchedules`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateCycleCountScheduleRequestToJSON(requestParameters['createCycleCountScheduleRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CycleCountScheduleResponseFromJSON(jsonValue));
    }

    /**
     * Creates an active recurring cycle-count schedule for a location, optionally filtered to one zone and/or SKU category, that comes due every frequencyDays. Use this tool to institutionalise periodic counting; do not use createCycleCountPlan, which creates a single one-off plan. Preconditions: the caller must be an authenticated user (recorded as the schedule creator). Required inputs: locationId (UUID), frequencyDays (positive integer) and nextDueDate (ISO date); zoneId and skuCategory are optional filters, and autoCreatePlan defaults to false, meaning the schedule only surfaces in the due-for-count view instead of auto-creating plans. Emits an INVENTORY_CYCLE_COUNT_SCHEDULE_CREATE event; the schedule starts active and no plan is created by this call. Returns 400 when locationId or nextDueDate is missing or frequencyDays is not positive. 
     * Create cycle count schedule
     */
    async createCycleCountSchedule(requestParameters: CreateCycleCountScheduleOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CycleCountScheduleResponse> {
        const response = await this.createCycleCountScheduleRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Deactivates a recurring cycle-count schedule — a soft delete that clears its active flag so it stops firing and drops out of the due-for-count view; the row is never removed. Use this tool to retire a schedule; to change its cadence or filters while keeping it running (or to re-activate it), use updateCycleCountSchedule instead. Preconditions: the schedule must exist; deactivating an already-inactive schedule succeeds unchanged. Required inputs: scheduleId (UUID) as a path parameter; there is no request body or confirmation flag. Emits an INVENTORY_CYCLE_COUNT_SCHEDULE_DEACTIVATE event; plans already created from the schedule are untouched. Returns 404 when no cycle count schedule exists for the supplied id. 
     * Deactivate cycle count schedule
     */
    async deactivateCycleCountScheduleRaw(requestParameters: DeactivateCycleCountScheduleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CycleCountScheduleResponse>> {
        if (requestParameters['scheduleId'] == null) {
            throw new runtime.RequiredError(
                'scheduleId',
                'Required parameter "scheduleId" was null or undefined when calling deactivateCycleCountSchedule().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:cycle_count:initiate"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/cycleCountSchedules/{scheduleId}`.replace(`{${"scheduleId"}}`, encodeURIComponent(String(requestParameters['scheduleId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CycleCountScheduleResponseFromJSON(jsonValue));
    }

    /**
     * Deactivates a recurring cycle-count schedule — a soft delete that clears its active flag so it stops firing and drops out of the due-for-count view; the row is never removed. Use this tool to retire a schedule; to change its cadence or filters while keeping it running (or to re-activate it), use updateCycleCountSchedule instead. Preconditions: the schedule must exist; deactivating an already-inactive schedule succeeds unchanged. Required inputs: scheduleId (UUID) as a path parameter; there is no request body or confirmation flag. Emits an INVENTORY_CYCLE_COUNT_SCHEDULE_DEACTIVATE event; plans already created from the schedule are untouched. Returns 404 when no cycle count schedule exists for the supplied id. 
     * Deactivate cycle count schedule
     */
    async deactivateCycleCountSchedule(requestParameters: DeactivateCycleCountScheduleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CycleCountScheduleResponse> {
        const response = await this.deactivateCycleCountScheduleRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one recurring cycle-count schedule with its filters, frequency, next due date, and active and auto-create flags. Use this tool when the scheduleId is already known; use listCycleCountSchedules instead to search by location or due state. Preconditions: the schedule must exist. Required inputs: scheduleId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no cycle count schedule exists for the supplied id. 
     * Get cycle count schedule
     */
    async getCycleCountScheduleRaw(requestParameters: GetCycleCountScheduleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CycleCountScheduleResponse>> {
        if (requestParameters['scheduleId'] == null) {
            throw new runtime.RequiredError(
                'scheduleId',
                'Required parameter "scheduleId" was null or undefined when calling getCycleCountSchedule().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:cycle_count:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/cycleCountSchedules/{scheduleId}`.replace(`{${"scheduleId"}}`, encodeURIComponent(String(requestParameters['scheduleId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CycleCountScheduleResponseFromJSON(jsonValue));
    }

    /**
     * Returns one recurring cycle-count schedule with its filters, frequency, next due date, and active and auto-create flags. Use this tool when the scheduleId is already known; use listCycleCountSchedules instead to search by location or due state. Preconditions: the schedule must exist. Required inputs: scheduleId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no cycle count schedule exists for the supplied id. 
     * Get cycle count schedule
     */
    async getCycleCountSchedule(requestParameters: GetCycleCountScheduleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CycleCountScheduleResponse> {
        const response = await this.getCycleCountScheduleRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one page of recurring cycle-count schedules, newest first, optionally filtered by location and/or active flag. Use this tool to discover scheduleIds or drive the due-for-count view via due=true, which restricts to active schedules whose nextDueDate has arrived; use getCycleCountSchedule instead when the scheduleId is already known. Preconditions: none. Required inputs: all query parameters are optional — locationId (UUID), active (boolean), due (boolean, default false), page (0-based, default 0) and size (default 50). Emits an INVENTORY_CYCLE_COUNT_SCHEDULE_LIST audit event; no schedule state changes. Returns 200 with an empty array when no schedules match, so an empty result is not an error condition. 
     * List cycle count schedules
     */
    async listCycleCountSchedulesRaw(requestParameters: ListCycleCountSchedulesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<CycleCountScheduleResponse>>> {
        const queryParameters: any = {};

        if (requestParameters['locationId'] != null) {
            queryParameters['locationId'] = requestParameters['locationId'];
        }

        if (requestParameters['active'] != null) {
            queryParameters['active'] = requestParameters['active'];
        }

        if (requestParameters['due'] != null) {
            queryParameters['due'] = requestParameters['due'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:cycle_count:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/cycleCountSchedules`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(CycleCountScheduleResponseFromJSON));
    }

    /**
     * Returns one page of recurring cycle-count schedules, newest first, optionally filtered by location and/or active flag. Use this tool to discover scheduleIds or drive the due-for-count view via due=true, which restricts to active schedules whose nextDueDate has arrived; use getCycleCountSchedule instead when the scheduleId is already known. Preconditions: none. Required inputs: all query parameters are optional — locationId (UUID), active (boolean), due (boolean, default false), page (0-based, default 0) and size (default 50). Emits an INVENTORY_CYCLE_COUNT_SCHEDULE_LIST audit event; no schedule state changes. Returns 200 with an empty array when no schedules match, so an empty result is not an error condition. 
     * List cycle count schedules
     */
    async listCycleCountSchedules(requestParameters: ListCycleCountSchedulesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<CycleCountScheduleResponse>> {
        const response = await this.listCycleCountSchedulesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Partially updates a recurring cycle-count schedule: frequencyDays, zoneId, skuCategory, nextDueDate, autoCreatePlan and active can each be changed, and null fields are left unchanged. Use this tool to retune an existing schedule; do not use updateCycleCountPlanStatus, which moves an individual plan through its lifecycle, and note that v1 offers no way to clear the zone or SKU-category filters — recreate the schedule instead. Preconditions: the schedule must exist. Required inputs: scheduleId (UUID) path parameter and a body carrying only the fields to change; frequencyDays, when supplied, must be positive. Emits an INVENTORY_CYCLE_COUNT_SCHEDULE_UPDATE event; the change takes effect on the next scheduler pass. Returns 404 when the schedule does not exist, and 400 when a supplied frequencyDays is not positive. 
     * Update cycle count schedule
     */
    async updateCycleCountScheduleRaw(requestParameters: UpdateCycleCountScheduleOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CycleCountScheduleResponse>> {
        if (requestParameters['scheduleId'] == null) {
            throw new runtime.RequiredError(
                'scheduleId',
                'Required parameter "scheduleId" was null or undefined when calling updateCycleCountSchedule().'
            );
        }

        if (requestParameters['updateCycleCountScheduleRequest'] == null) {
            throw new runtime.RequiredError(
                'updateCycleCountScheduleRequest',
                'Required parameter "updateCycleCountScheduleRequest" was null or undefined when calling updateCycleCountSchedule().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:cycle_count:initiate"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/cycleCountSchedules/{scheduleId}`.replace(`{${"scheduleId"}}`, encodeURIComponent(String(requestParameters['scheduleId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: UpdateCycleCountScheduleRequestToJSON(requestParameters['updateCycleCountScheduleRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CycleCountScheduleResponseFromJSON(jsonValue));
    }

    /**
     * Partially updates a recurring cycle-count schedule: frequencyDays, zoneId, skuCategory, nextDueDate, autoCreatePlan and active can each be changed, and null fields are left unchanged. Use this tool to retune an existing schedule; do not use updateCycleCountPlanStatus, which moves an individual plan through its lifecycle, and note that v1 offers no way to clear the zone or SKU-category filters — recreate the schedule instead. Preconditions: the schedule must exist. Required inputs: scheduleId (UUID) path parameter and a body carrying only the fields to change; frequencyDays, when supplied, must be positive. Emits an INVENTORY_CYCLE_COUNT_SCHEDULE_UPDATE event; the change takes effect on the next scheduler pass. Returns 404 when the schedule does not exist, and 400 when a supplied frequencyDays is not positive. 
     * Update cycle count schedule
     */
    async updateCycleCountSchedule(requestParameters: UpdateCycleCountScheduleOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CycleCountScheduleResponse> {
        const response = await this.updateCycleCountScheduleRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
