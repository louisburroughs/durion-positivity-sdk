/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Inventory API
 * API documentation for the Inventory service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  LocationInventoryRollupResponse,
  SiteInventoryRollupResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    LocationInventoryRollupResponseFromJSON,
    LocationInventoryRollupResponseToJSON,
    SiteInventoryRollupResponseFromJSON,
    SiteInventoryRollupResponseToJSON,
} from '../models/index';

export interface GetLocationInventoryRollupRequest {
    locationId: string;
    parentType?: string;
    sku?: string;
    expand?: string;
    depth?: number;
    includeEmpty?: boolean;
}

export interface GetSiteInventoryRollupRequest {
    siteId: string;
    sku?: string;
    depth?: number;
    includeEmpty?: boolean;
}

/**
 * 
 */
export class InventoryRollupApi extends runtime.BaseAPI {

    /**
     * Aggregates inventory totals across all descendant sites of a parent location (building, place or region) along a typed parent chain, returning per-site summaries plus a grand total. Use this tool for multi-site aggregation above the site level; do not use getSiteInventoryRollup, which walks the storage-location tree inside one site rather than across the descendant sites of a parent location. Preconditions: the parent location must exist in pos-location, whose topology is fetched at request time. Required inputs: locationId (UUID) path parameter; parentType defaults to PHYSICAL, sku is an optional filter, and expand=tree inlines each site\'s full storage-location tree with optional depth (minimum 1) and includeEmpty (default false); without expand, summaries are computed sequentially for every descendant regardless of count. No events are emitted and no state changes; this is a read-only projection. Returns 404 when the location is not found, 422 with ROLLUP_EXPANSION_TOO_LARGE when expand=tree covers more descendant sites than the configured cap (pos.inventory.rollup.expand-site-cap, default 25), 400 for an unknown parentType or an unsupported expand value, and 503 when the location service is unavailable. 
     * Get parent-location inventory rollup
     */
    async getLocationInventoryRollupRaw(requestParameters: GetLocationInventoryRollupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LocationInventoryRollupResponse>> {
        if (requestParameters['locationId'] == null) {
            throw new runtime.RequiredError(
                'locationId',
                'Required parameter "locationId" was null or undefined when calling getLocationInventoryRollup().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['parentType'] != null) {
            queryParameters['parentType'] = requestParameters['parentType'];
        }

        if (requestParameters['sku'] != null) {
            queryParameters['sku'] = requestParameters['sku'];
        }

        if (requestParameters['expand'] != null) {
            queryParameters['expand'] = requestParameters['expand'];
        }

        if (requestParameters['depth'] != null) {
            queryParameters['depth'] = requestParameters['depth'];
        }

        if (requestParameters['includeEmpty'] != null) {
            queryParameters['includeEmpty'] = requestParameters['includeEmpty'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:on_hand:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/locations/{locationId}/inventory-rollup`.replace(`{${"locationId"}}`, encodeURIComponent(String(requestParameters['locationId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LocationInventoryRollupResponseFromJSON(jsonValue));
    }

    /**
     * Aggregates inventory totals across all descendant sites of a parent location (building, place or region) along a typed parent chain, returning per-site summaries plus a grand total. Use this tool for multi-site aggregation above the site level; do not use getSiteInventoryRollup, which walks the storage-location tree inside one site rather than across the descendant sites of a parent location. Preconditions: the parent location must exist in pos-location, whose topology is fetched at request time. Required inputs: locationId (UUID) path parameter; parentType defaults to PHYSICAL, sku is an optional filter, and expand=tree inlines each site\'s full storage-location tree with optional depth (minimum 1) and includeEmpty (default false); without expand, summaries are computed sequentially for every descendant regardless of count. No events are emitted and no state changes; this is a read-only projection. Returns 404 when the location is not found, 422 with ROLLUP_EXPANSION_TOO_LARGE when expand=tree covers more descendant sites than the configured cap (pos.inventory.rollup.expand-site-cap, default 25), 400 for an unknown parentType or an unsupported expand value, and 503 when the location service is unavailable. 
     * Get parent-location inventory rollup
     */
    async getLocationInventoryRollup(requestParameters: GetLocationInventoryRollupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LocationInventoryRollupResponse> {
        const response = await this.getLocationInventoryRollupRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns on-hand, allocated and available quantities grouped and subtotaled along the storage-location hierarchy of one site, each node carrying its own quantities plus the rolled-up sum of itself and all descendants. Use this tool for the storage-location tree inside a single site; do not use getLocationInventoryRollup, which aggregates across the descendant sites of a parent location (building, place or region) rather than within one site. Preconditions: the site must exist in pos-location, whose topology is fetched at request time; ledger rows at location ids unknown to the site are excluded — there is no unassigned bucket. Required inputs: siteId (UUID) path parameter; sku is an optional filter, depth (minimum 1, where 1 returns root storage locations only) limits the returned tree, and includeEmpty defaults to false. No events are emitted and no state changes; this is a read-only projection. Returns 404 when the site is not found, 400 for invalid parameters, and 503 when the location service is unavailable. 
     * Get site inventory rollup
     */
    async getSiteInventoryRollupRaw(requestParameters: GetSiteInventoryRollupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SiteInventoryRollupResponse>> {
        if (requestParameters['siteId'] == null) {
            throw new runtime.RequiredError(
                'siteId',
                'Required parameter "siteId" was null or undefined when calling getSiteInventoryRollup().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['sku'] != null) {
            queryParameters['sku'] = requestParameters['sku'];
        }

        if (requestParameters['depth'] != null) {
            queryParameters['depth'] = requestParameters['depth'];
        }

        if (requestParameters['includeEmpty'] != null) {
            queryParameters['includeEmpty'] = requestParameters['includeEmpty'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:on_hand:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/sites/{siteId}/inventory-rollup`.replace(`{${"siteId"}}`, encodeURIComponent(String(requestParameters['siteId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SiteInventoryRollupResponseFromJSON(jsonValue));
    }

    /**
     * Returns on-hand, allocated and available quantities grouped and subtotaled along the storage-location hierarchy of one site, each node carrying its own quantities plus the rolled-up sum of itself and all descendants. Use this tool for the storage-location tree inside a single site; do not use getLocationInventoryRollup, which aggregates across the descendant sites of a parent location (building, place or region) rather than within one site. Preconditions: the site must exist in pos-location, whose topology is fetched at request time; ledger rows at location ids unknown to the site are excluded — there is no unassigned bucket. Required inputs: siteId (UUID) path parameter; sku is an optional filter, depth (minimum 1, where 1 returns root storage locations only) limits the returned tree, and includeEmpty defaults to false. No events are emitted and no state changes; this is a read-only projection. Returns 404 when the site is not found, 400 for invalid parameters, and 503 when the location service is unavailable. 
     * Get site inventory rollup
     */
    async getSiteInventoryRollup(requestParameters: GetSiteInventoryRollupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SiteInventoryRollupResponse> {
        const response = await this.getSiteInventoryRollupRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
