/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Inventory API
 * API documentation for the Inventory service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  CreateRevaluationRequest,
  RejectRevaluationRequest,
  RevaluationResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    CreateRevaluationRequestFromJSON,
    CreateRevaluationRequestToJSON,
    RejectRevaluationRequestFromJSON,
    RejectRevaluationRequestToJSON,
    RevaluationResponseFromJSON,
    RevaluationResponseToJSON,
} from '../models/index';

export interface ApproveRevaluationRequest {
    revaluationId: string;
}

export interface CreateRevaluationOperationRequest {
    createRevaluationRequest: CreateRevaluationRequest;
}

export interface GetRevaluationRequest {
    revaluationId: string;
}

export interface ListRevaluationsRequest {
    stockItemId?: string;
    status?: ListRevaluationsStatusEnum;
}

export interface RejectRevaluationOperationRequest {
    revaluationId: string;
    rejectRevaluationRequest: RejectRevaluationRequest;
}

/**
 * 
 */
export class InventoryRevaluationApi extends runtime.BaseAPI {

    /**
     * Approves a PENDING_APPROVAL revaluation and applies it: the SKU\'s cost state is restated to the recorded new unit cost and a ProductValueChangedV1 fact is emitted for the accounting journal entry. Use this tool after reviewing an above-threshold revaluation; do not use rejectRevaluation, which discards it and leaves the cost state untouched. Preconditions: the revaluation must exist and be in PENDING_APPROVAL status; the approver need not differ from the submitter. Required inputs: revaluationId (UUID) as a path parameter; there is no request body. Emits an INVENTORY_REVALUATION_APPROVE event plus the ProductValueChangedV1 fact; the new cost applies from now on and no ledger entries are rewritten. Returns 404 when the revaluation does not exist, and 409 when it is not in PENDING_APPROVAL status. 
     * Approve cost revaluation
     */
    async approveRevaluationRaw(requestParameters: ApproveRevaluationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RevaluationResponse>> {
        if (requestParameters['revaluationId'] == null) {
            throw new runtime.RequiredError(
                'revaluationId',
                'Required parameter "revaluationId" was null or undefined when calling approveRevaluation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:valuation:adjust"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/valuation/revaluations/{revaluationId}/approve`.replace(`{${"revaluationId"}}`, encodeURIComponent(String(requestParameters['revaluationId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RevaluationResponseFromJSON(jsonValue));
    }

    /**
     * Approves a PENDING_APPROVAL revaluation and applies it: the SKU\'s cost state is restated to the recorded new unit cost and a ProductValueChangedV1 fact is emitted for the accounting journal entry. Use this tool after reviewing an above-threshold revaluation; do not use rejectRevaluation, which discards it and leaves the cost state untouched. Preconditions: the revaluation must exist and be in PENDING_APPROVAL status; the approver need not differ from the submitter. Required inputs: revaluationId (UUID) as a path parameter; there is no request body. Emits an INVENTORY_REVALUATION_APPROVE event plus the ProductValueChangedV1 fact; the new cost applies from now on and no ledger entries are rewritten. Returns 404 when the revaluation does not exist, and 409 when it is not in PENDING_APPROVAL status. 
     * Approve cost revaluation
     */
    async approveRevaluation(requestParameters: ApproveRevaluationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RevaluationResponse> {
        const response = await this.approveRevaluationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Submits a manual cost revaluation for a SKU, correcting the cost its resolved costing method uses — the standard price under STANDARD, the running average under AVERAGE; below the configured value-delta thresholds it is AUTO_APPLIED (cost state restated and a ProductValueChangedV1 fact emitted) in the same transaction, and at or above them it enters PENDING_APPROVAL with no cost change. Use this tool to correct a wrong unit cost; do not use upsertCostingMethodConfig, which switches the costing method without restating values, and do not use approveRevaluation, which acts on a revaluation that already exists. Preconditions: the caller must be an authenticated user; the SKU\'s cost state row is seeded automatically when absent, so no prior costing history is required. Required inputs: stockItemId plus exactly one of newUnitCost (absolute, zero or positive) or costDelta (signed, applied to the current cost), and a reason for the audit log; the value delta gating the approval tier is the cost change multiplied by current on-hand. Emits an INVENTORY_REVALUATION_CREATE event, and the auto-apply path additionally emits the ProductValueChangedV1 fact so accounting can post the revaluation journal entry. Returns 400 when neither or both of newUnitCost and costDelta are supplied, or when the resulting unit cost would be negative. 
     * Submit cost revaluation
     */
    async createRevaluationRaw(requestParameters: CreateRevaluationOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RevaluationResponse>> {
        if (requestParameters['createRevaluationRequest'] == null) {
            throw new runtime.RequiredError(
                'createRevaluationRequest',
                'Required parameter "createRevaluationRequest" was null or undefined when calling createRevaluation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:valuation:adjust"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/valuation/revaluations`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateRevaluationRequestToJSON(requestParameters['createRevaluationRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RevaluationResponseFromJSON(jsonValue));
    }

    /**
     * Submits a manual cost revaluation for a SKU, correcting the cost its resolved costing method uses — the standard price under STANDARD, the running average under AVERAGE; below the configured value-delta thresholds it is AUTO_APPLIED (cost state restated and a ProductValueChangedV1 fact emitted) in the same transaction, and at or above them it enters PENDING_APPROVAL with no cost change. Use this tool to correct a wrong unit cost; do not use upsertCostingMethodConfig, which switches the costing method without restating values, and do not use approveRevaluation, which acts on a revaluation that already exists. Preconditions: the caller must be an authenticated user; the SKU\'s cost state row is seeded automatically when absent, so no prior costing history is required. Required inputs: stockItemId plus exactly one of newUnitCost (absolute, zero or positive) or costDelta (signed, applied to the current cost), and a reason for the audit log; the value delta gating the approval tier is the cost change multiplied by current on-hand. Emits an INVENTORY_REVALUATION_CREATE event, and the auto-apply path additionally emits the ProductValueChangedV1 fact so accounting can post the revaluation journal entry. Returns 400 when neither or both of newUnitCost and costDelta are supplied, or when the resulting unit cost would be negative. 
     * Submit cost revaluation
     */
    async createRevaluation(requestParameters: CreateRevaluationOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RevaluationResponse> {
        const response = await this.createRevaluationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one revaluation document with its old and new unit cost, on-hand snapshot, signed value delta, approval tier and lifecycle status. Use this tool when the revaluationId is already known; use listRevaluations instead to search by SKU or status. Preconditions: the revaluation must exist. Required inputs: revaluationId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no revaluation exists for the supplied id. 
     * Get revaluation details
     */
    async getRevaluationRaw(requestParameters: GetRevaluationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RevaluationResponse>> {
        if (requestParameters['revaluationId'] == null) {
            throw new runtime.RequiredError(
                'revaluationId',
                'Required parameter "revaluationId" was null or undefined when calling getRevaluation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:valuation:view", "inventory:valuation:adjust"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/valuation/revaluations/{revaluationId}`.replace(`{${"revaluationId"}}`, encodeURIComponent(String(requestParameters['revaluationId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RevaluationResponseFromJSON(jsonValue));
    }

    /**
     * Returns one revaluation document with its old and new unit cost, on-hand snapshot, signed value delta, approval tier and lifecycle status. Use this tool when the revaluationId is already known; use listRevaluations instead to search by SKU or status. Preconditions: the revaluation must exist. Required inputs: revaluationId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no revaluation exists for the supplied id. 
     * Get revaluation details
     */
    async getRevaluation(requestParameters: GetRevaluationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RevaluationResponse> {
        const response = await this.getRevaluationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns revaluation documents, newest first, optionally filtered by SKU and/or lifecycle status (PENDING_APPROVAL, AUTO_APPLIED, APPLIED, REJECTED). Use this tool to find pending approvals or audit past cost corrections; use getRevaluation instead when the revaluationId is already known. Preconditions: none. Required inputs: stockItemId and status are optional query parameters; there is no paging — the full match set is returned. No events are emitted and no state changes; this is a read-only projection. Returns 200 with an empty array when no revaluations match, so an empty result is not an error condition. 
     * List revaluations
     */
    async listRevaluationsRaw(requestParameters: ListRevaluationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<RevaluationResponse>>> {
        const queryParameters: any = {};

        if (requestParameters['stockItemId'] != null) {
            queryParameters['stockItemId'] = requestParameters['stockItemId'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:valuation:view", "inventory:valuation:adjust"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/valuation/revaluations`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(RevaluationResponseFromJSON));
    }

    /**
     * Returns revaluation documents, newest first, optionally filtered by SKU and/or lifecycle status (PENDING_APPROVAL, AUTO_APPLIED, APPLIED, REJECTED). Use this tool to find pending approvals or audit past cost corrections; use getRevaluation instead when the revaluationId is already known. Preconditions: none. Required inputs: stockItemId and status are optional query parameters; there is no paging — the full match set is returned. No events are emitted and no state changes; this is a read-only projection. Returns 200 with an empty array when no revaluations match, so an empty result is not an error condition. 
     * List revaluations
     */
    async listRevaluations(requestParameters: ListRevaluationsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<RevaluationResponse>> {
        const response = await this.listRevaluationsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Rejects a PENDING_APPROVAL revaluation with a recorded reason; the SKU cost state is untouched and the record moves to the terminal REJECTED status. Use this tool to discard a disputed cost correction; do not use approveRevaluation, which restates the SKU cost state. Preconditions: the revaluation must exist and be in PENDING_APPROVAL status; the rejecting actor is taken from the authenticated context, not the body. Required inputs: revaluationId (UUID) path parameter and rejectionReason (non-blank, max 1000 characters) in the body. Emits an INVENTORY_REVALUATION_REJECT event; no cost or ledger change is made. Returns 404 when the revaluation does not exist, and 409 when it is not in PENDING_APPROVAL status. 
     * Reject cost revaluation
     */
    async rejectRevaluationRaw(requestParameters: RejectRevaluationOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RevaluationResponse>> {
        if (requestParameters['revaluationId'] == null) {
            throw new runtime.RequiredError(
                'revaluationId',
                'Required parameter "revaluationId" was null or undefined when calling rejectRevaluation().'
            );
        }

        if (requestParameters['rejectRevaluationRequest'] == null) {
            throw new runtime.RequiredError(
                'rejectRevaluationRequest',
                'Required parameter "rejectRevaluationRequest" was null or undefined when calling rejectRevaluation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:valuation:adjust"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/valuation/revaluations/{revaluationId}/reject`.replace(`{${"revaluationId"}}`, encodeURIComponent(String(requestParameters['revaluationId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RejectRevaluationRequestToJSON(requestParameters['rejectRevaluationRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RevaluationResponseFromJSON(jsonValue));
    }

    /**
     * Rejects a PENDING_APPROVAL revaluation with a recorded reason; the SKU cost state is untouched and the record moves to the terminal REJECTED status. Use this tool to discard a disputed cost correction; do not use approveRevaluation, which restates the SKU cost state. Preconditions: the revaluation must exist and be in PENDING_APPROVAL status; the rejecting actor is taken from the authenticated context, not the body. Required inputs: revaluationId (UUID) path parameter and rejectionReason (non-blank, max 1000 characters) in the body. Emits an INVENTORY_REVALUATION_REJECT event; no cost or ledger change is made. Returns 404 when the revaluation does not exist, and 409 when it is not in PENDING_APPROVAL status. 
     * Reject cost revaluation
     */
    async rejectRevaluation(requestParameters: RejectRevaluationOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RevaluationResponse> {
        const response = await this.rejectRevaluationRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
  * @export
  * @enum {string}
  */
export enum ListRevaluationsStatusEnum {
    PendingApproval = 'PENDING_APPROVAL',
    AutoApplied = 'AUTO_APPLIED',
    Applied = 'APPLIED',
    Rejected = 'REJECTED'
}
