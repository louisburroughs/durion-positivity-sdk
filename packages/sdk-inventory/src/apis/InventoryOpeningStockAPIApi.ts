/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Inventory API
 * API documentation for the Inventory service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  BulkIngestRequestOpeningStockBulkIngestRecord,
  BulkIngestResponse,
} from '../models/index';
import {
    BulkIngestRequestOpeningStockBulkIngestRecordFromJSON,
    BulkIngestRequestOpeningStockBulkIngestRecordToJSON,
    BulkIngestResponseFromJSON,
    BulkIngestResponseToJSON,
} from '../models/index';

export interface BulkIngestOpeningStockRequest {
    bulkIngestRequestOpeningStockBulkIngestRecord: BulkIngestRequestOpeningStockBulkIngestRecord;
}

/**
 * 
 */
export class InventoryOpeningStockAPIApi extends runtime.BaseAPI {

    /**
     * Establishes opening on-hand quantities for many products at once, filing an adjustment per line and approving it so the ledger entry posts and the stock actually exists. Use this tool when commissioning a site or seeding an environment; use bulkIngestInventoryAdjustments instead to file adjustments that a human must review and approve. Preconditions: the caller holds both inventory:adjustment:create and inventory:adjustment:approve, and each line\'s storage location exists. Required inputs: jobId (UUID), locationId (UUID) and records, each with sku and a positive quantity; a record\'s own locationId names the storage location the stock sits in and overrides the batch one. Emits an INVENTORY_OPENING_STOCK_BULK_INGEST event, and one ADJUSTMENT_IN ledger entry per line, which is what hydrates the availability and replica views. Re-running the same file is safe: a line whose product already has on-hand at its destination is skipped, because adjustments are deltas and posting again would double the stock. Returns 200 with a per-record result; check each result rather than the status alone. A row the service refused carries errorCode OPENING_STOCK_INGEST_FAILED and the reason; a row lost to a server-side fault carries INTERNAL_ERROR and a correlationId to quote, with no detail of its own. 
     * Establish Opening On-Hand Stock in Bulk
     */
    async bulkIngestOpeningStockRaw(requestParameters: BulkIngestOpeningStockRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkIngestResponse>> {
        if (requestParameters['bulkIngestRequestOpeningStockBulkIngestRecord'] == null) {
            throw new runtime.RequiredError(
                'bulkIngestRequestOpeningStockBulkIngestRecord',
                'Required parameter "bulkIngestRequestOpeningStockBulkIngestRecord" was null or undefined when calling bulkIngestOpeningStock().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:adjustment:create", "inventory:adjustment:approve"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/opening-stock/bulk-ingest`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkIngestRequestOpeningStockBulkIngestRecordToJSON(requestParameters['bulkIngestRequestOpeningStockBulkIngestRecord']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkIngestResponseFromJSON(jsonValue));
    }

    /**
     * Establishes opening on-hand quantities for many products at once, filing an adjustment per line and approving it so the ledger entry posts and the stock actually exists. Use this tool when commissioning a site or seeding an environment; use bulkIngestInventoryAdjustments instead to file adjustments that a human must review and approve. Preconditions: the caller holds both inventory:adjustment:create and inventory:adjustment:approve, and each line\'s storage location exists. Required inputs: jobId (UUID), locationId (UUID) and records, each with sku and a positive quantity; a record\'s own locationId names the storage location the stock sits in and overrides the batch one. Emits an INVENTORY_OPENING_STOCK_BULK_INGEST event, and one ADJUSTMENT_IN ledger entry per line, which is what hydrates the availability and replica views. Re-running the same file is safe: a line whose product already has on-hand at its destination is skipped, because adjustments are deltas and posting again would double the stock. Returns 200 with a per-record result; check each result rather than the status alone. A row the service refused carries errorCode OPENING_STOCK_INGEST_FAILED and the reason; a row lost to a server-side fault carries INTERNAL_ERROR and a correlationId to quote, with no detail of its own. 
     * Establish Opening On-Hand Stock in Bulk
     */
    async bulkIngestOpeningStock(requestParameters: BulkIngestOpeningStockRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkIngestResponse> {
        const response = await this.bulkIngestOpeningStockRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
