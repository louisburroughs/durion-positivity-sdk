/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Inventory API
 * API documentation for the Inventory service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  BackorderResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    BackorderResponseFromJSON,
    BackorderResponseToJSON,
} from '../models/index';

export interface GetBackorderRequest {
    backorderId: string;
}

export interface ListBackordersRequest {
    status?: ListBackordersStatusEnum;
    sku?: string;
    locationId?: string;
    workorderLineId?: string;
    salesOrderLineId?: string;
}

/**
 * 
 */
export class BackordersApi extends runtime.BaseAPI {

    /**
     * Returns one backorder with its shortage quantity, lifecycle status, resolution source and timestamps. Use this tool when the backorderId is already known; use listBackorders instead to search by status, SKU, site or workorder line. Preconditions: the backorder must exist. Required inputs: backorderId (UUID) path parameter; there is no request body. No events are emitted and no state changes; auto-resolution happens asynchronously when inbound stock raises ATP at the backorder\'s site — oldest-priority-first, and only when the full quantityShort fits the remaining ATP budget (whole-backorder resolution, no partials). Returns 404 when no backorder exists for the supplied id. 
     * Get backorder details
     */
    async getBackorderRaw(requestParameters: GetBackorderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BackorderResponse>> {
        if (requestParameters['backorderId'] == null) {
            throw new runtime.RequiredError(
                'backorderId',
                'Required parameter "backorderId" was null or undefined when calling getBackorder().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:shortage:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/backorders/{backorderId}`.replace(`{${"backorderId"}}`, encodeURIComponent(String(requestParameters['backorderId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BackorderResponseFromJSON(jsonValue));
    }

    /**
     * Returns one backorder with its shortage quantity, lifecycle status, resolution source and timestamps. Use this tool when the backorderId is already known; use listBackorders instead to search by status, SKU, site or workorder line. Preconditions: the backorder must exist. Required inputs: backorderId (UUID) path parameter; there is no request body. No events are emitted and no state changes; auto-resolution happens asynchronously when inbound stock raises ATP at the backorder\'s site — oldest-priority-first, and only when the full quantityShort fits the remaining ATP budget (whole-backorder resolution, no partials). Returns 404 when no backorder exists for the supplied id. 
     * Get backorder details
     */
    async getBackorder(requestParameters: GetBackorderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BackorderResponse> {
        const response = await this.getBackorderRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists backorders — unfulfilled demand (a workorder line or, since CAP #1315, a sales-order line) held open until availability covers it — newest first. Use this tool to monitor open shortages awaiting stock; use getBackorder instead when the backorderId is already known, and note backorders are opened by the shortage flows (resolveShortage with BACKORDER) or the sales-order checkout gate, not by any direct create endpoint. Preconditions: none; an empty result is not an error. Required inputs: none — status (OPEN, RESOLVED or CANCELLED), sku, locationId, workorderLineId and salesOrderLineId are optional filters combined with AND. No events are emitted and no state changes; this is a read-only projection. Returns 200 with an empty array when nothing matches. 
     * List backorders
     */
    async listBackordersRaw(requestParameters: ListBackordersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<BackorderResponse>>> {
        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['sku'] != null) {
            queryParameters['sku'] = requestParameters['sku'];
        }

        if (requestParameters['locationId'] != null) {
            queryParameters['locationId'] = requestParameters['locationId'];
        }

        if (requestParameters['workorderLineId'] != null) {
            queryParameters['workorderLineId'] = requestParameters['workorderLineId'];
        }

        if (requestParameters['salesOrderLineId'] != null) {
            queryParameters['salesOrderLineId'] = requestParameters['salesOrderLineId'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:shortage:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/backorders`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(BackorderResponseFromJSON));
    }

    /**
     * Lists backorders — unfulfilled demand (a workorder line or, since CAP #1315, a sales-order line) held open until availability covers it — newest first. Use this tool to monitor open shortages awaiting stock; use getBackorder instead when the backorderId is already known, and note backorders are opened by the shortage flows (resolveShortage with BACKORDER) or the sales-order checkout gate, not by any direct create endpoint. Preconditions: none; an empty result is not an error. Required inputs: none — status (OPEN, RESOLVED or CANCELLED), sku, locationId, workorderLineId and salesOrderLineId are optional filters combined with AND. No events are emitted and no state changes; this is a read-only projection. Returns 200 with an empty array when nothing matches. 
     * List backorders
     */
    async listBackorders(requestParameters: ListBackordersRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<BackorderResponse>> {
        const response = await this.listBackordersRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
  * @export
  * @enum {string}
  */
export enum ListBackordersStatusEnum {
    Open = 'OPEN',
    Resolved = 'RESOLVED',
    Cancelled = 'CANCELLED'
}
