/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Inventory API
 * API documentation for the Inventory service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  SupplierStockHintView,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    SupplierStockHintViewFromJSON,
    SupplierStockHintViewToJSON,
} from '../models/index';

export interface ListSupplierStockHintsByCodeRequest {
    identityKind: ListSupplierStockHintsByCodeIdentityKindEnum;
    value: string;
}

export interface ListSupplierStockHintsByProductRequest {
    productId: string;
}

/**
 * 
 */
export class SupplierStockHintsApi extends runtime.BaseAPI {

    /**
     * Returns what each vendor last reported about an article identified by a code, whether or not the article has been resolved to a catalog product. Use this tool when the article is known by an EAN or by an article code rather than by a product id — in particular for articles no product in the catalog carries, whose hints are retained and reachable only this way. Preconditions: none; an unknown code simply matches nothing. Required inputs: identityKind (EAN, BUYER_ARTICLE or SUPPLIER_ARTICLE) and value, matched exactly after trimming. Emits INVENTORY_SUPPLIER_STOCK_HINT_LIST_BY_CODE for audit; this is a read of what vendors reported and changes no state. Returns 200 with an empty array when no vendor stated that code. The availability, snapshotAsOf and asOfSource fields carry the same meanings as on the by-product listing, and an entry with a null resolvedProductId is an article we have not tied to a catalog product, not an error. 
     * List vendor availability hints for an article code
     */
    async listSupplierStockHintsByCodeRaw(requestParameters: ListSupplierStockHintsByCodeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<SupplierStockHintView>>> {
        if (requestParameters['identityKind'] == null) {
            throw new runtime.RequiredError(
                'identityKind',
                'Required parameter "identityKind" was null or undefined when calling listSupplierStockHintsByCode().'
            );
        }

        if (requestParameters['value'] == null) {
            throw new runtime.RequiredError(
                'value',
                'Required parameter "value" was null or undefined when calling listSupplierStockHintsByCode().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['identityKind'] != null) {
            queryParameters['identityKind'] = requestParameters['identityKind'];
        }

        if (requestParameters['value'] != null) {
            queryParameters['value'] = requestParameters['value'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:supplier_stock_hint:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/supplierStockHints/byCode`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(SupplierStockHintViewFromJSON));
    }

    /**
     * Returns what each vendor last reported about an article identified by a code, whether or not the article has been resolved to a catalog product. Use this tool when the article is known by an EAN or by an article code rather than by a product id — in particular for articles no product in the catalog carries, whose hints are retained and reachable only this way. Preconditions: none; an unknown code simply matches nothing. Required inputs: identityKind (EAN, BUYER_ARTICLE or SUPPLIER_ARTICLE) and value, matched exactly after trimming. Emits INVENTORY_SUPPLIER_STOCK_HINT_LIST_BY_CODE for audit; this is a read of what vendors reported and changes no state. Returns 200 with an empty array when no vendor stated that code. The availability, snapshotAsOf and asOfSource fields carry the same meanings as on the by-product listing, and an entry with a null resolvedProductId is an article we have not tied to a catalog product, not an error. 
     * List vendor availability hints for an article code
     */
    async listSupplierStockHintsByCode(requestParameters: ListSupplierStockHintsByCodeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<SupplierStockHintView>> {
        const response = await this.listSupplierStockHintsByCodeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns what each vendor last reported about its own availability of a product, one entry per vendor, newest fetch first. Use this tool to tell a customer what a supplier says it can supply for a future need; do not use it to decide what can be promised or committed today, which is owned availability-to-promise and comes from the availability endpoints instead. Preconditions: none; the product need not exist, and hints appear only for articles that have been resolved to it. Required inputs: productId as a path parameter. Emits INVENTORY_SUPPLIER_STOCK_HINT_LIST_BY_PRODUCT for audit; this is a read of what vendors reported and changes no state. Returns 200 with an empty array when no vendor has reported the product, which means no vendor has said anything about it — NOT that it is unavailable. Read each entry\'s availability field: REPORTED carries the vendor\'s quantity (0 meaning the vendor explicitly has none), QUANTITY_NOT_STATED means the vendor listed the article without a quantity, and STALE_UNKNOWN means the vendor\'s figure is too old to repeat. Judge freshness on snapshotAsOf, the vendor\'s own figure, and check asOfSource: when it is FETCH the vendor stated no time and the age shown is only when we asked. 
     * List vendor availability hints for a product
     */
    async listSupplierStockHintsByProductRaw(requestParameters: ListSupplierStockHintsByProductRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<SupplierStockHintView>>> {
        if (requestParameters['productId'] == null) {
            throw new runtime.RequiredError(
                'productId',
                'Required parameter "productId" was null or undefined when calling listSupplierStockHintsByProduct().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:supplier_stock_hint:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/supplierStockHints/byProduct/{productId}`.replace(`{${"productId"}}`, encodeURIComponent(String(requestParameters['productId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(SupplierStockHintViewFromJSON));
    }

    /**
     * Returns what each vendor last reported about its own availability of a product, one entry per vendor, newest fetch first. Use this tool to tell a customer what a supplier says it can supply for a future need; do not use it to decide what can be promised or committed today, which is owned availability-to-promise and comes from the availability endpoints instead. Preconditions: none; the product need not exist, and hints appear only for articles that have been resolved to it. Required inputs: productId as a path parameter. Emits INVENTORY_SUPPLIER_STOCK_HINT_LIST_BY_PRODUCT for audit; this is a read of what vendors reported and changes no state. Returns 200 with an empty array when no vendor has reported the product, which means no vendor has said anything about it — NOT that it is unavailable. Read each entry\'s availability field: REPORTED carries the vendor\'s quantity (0 meaning the vendor explicitly has none), QUANTITY_NOT_STATED means the vendor listed the article without a quantity, and STALE_UNKNOWN means the vendor\'s figure is too old to repeat. Judge freshness on snapshotAsOf, the vendor\'s own figure, and check asOfSource: when it is FETCH the vendor stated no time and the age shown is only when we asked. 
     * List vendor availability hints for a product
     */
    async listSupplierStockHintsByProduct(requestParameters: ListSupplierStockHintsByProductRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<SupplierStockHintView>> {
        const response = await this.listSupplierStockHintsByProductRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
  * @export
  * @enum {string}
  */
export enum ListSupplierStockHintsByCodeIdentityKindEnum {
    Ean = 'EAN',
    BuyerArticle = 'BUYER_ARTICLE',
    SupplierArticle = 'SUPPLIER_ARTICLE'
}
