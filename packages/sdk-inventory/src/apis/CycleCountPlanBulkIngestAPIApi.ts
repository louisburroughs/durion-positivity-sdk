/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Inventory API
 * API documentation for the Inventory service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  BulkIngestRequestCycleCountPlanBulkIngestRecord,
  BulkIngestResponse,
} from '../models/index';
import {
    BulkIngestRequestCycleCountPlanBulkIngestRecordFromJSON,
    BulkIngestRequestCycleCountPlanBulkIngestRecordToJSON,
    BulkIngestResponseFromJSON,
    BulkIngestResponseToJSON,
} from '../models/index';

export interface BulkIngestCycleCountPlansRequest {
    bulkIngestRequestCycleCountPlanBulkIngestRecord: BulkIngestRequestCycleCountPlanBulkIngestRecord;
}

/**
 * 
 */
export class CycleCountPlanBulkIngestAPIApi extends runtime.BaseAPI {

    /**
     * Creates many cycle count plans at once, one per record, each in PLANNED status. Use this tool when standing up a counting programme or seeding an environment; use createCycleCountPlan instead for a single plan, and generateCycleCountTasks to turn a plan into work. Preconditions: each site must exist and each row must name at least one zone. Required inputs: jobId (UUID), locationId (UUID) and records, each with a planName and zoneIds; give either a scheduledDate strictly in the future or a scheduledDaysOut, which is counted from today so a replayed file never asks for a past date. Emits an INVENTORY_CYCLE_COUNT_PLAN_BULK_INGEST event and a plan-created event per row. Re-running the same file is safe: a plan whose name is already present at that site is recognised rather than duplicated, since nothing in the schema makes plan names unique. Returns 200 with a per-record result; check each result rather than the status alone. A row the service refused carries errorCode CYCLE_COUNT_PLAN_INGEST_FAILED and the reason; a row lost to a server-side fault carries INTERNAL_ERROR and a correlationId to quote, with no detail of its own. 
     * Create Cycle Count Plans in Bulk
     */
    async bulkIngestCycleCountPlansRaw(requestParameters: BulkIngestCycleCountPlansRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkIngestResponse>> {
        if (requestParameters['bulkIngestRequestCycleCountPlanBulkIngestRecord'] == null) {
            throw new runtime.RequiredError(
                'bulkIngestRequestCycleCountPlanBulkIngestRecord',
                'Required parameter "bulkIngestRequestCycleCountPlanBulkIngestRecord" was null or undefined when calling bulkIngestCycleCountPlans().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:cycle_count:initiate"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/cycleCountPlans/bulk-ingest`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkIngestRequestCycleCountPlanBulkIngestRecordToJSON(requestParameters['bulkIngestRequestCycleCountPlanBulkIngestRecord']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkIngestResponseFromJSON(jsonValue));
    }

    /**
     * Creates many cycle count plans at once, one per record, each in PLANNED status. Use this tool when standing up a counting programme or seeding an environment; use createCycleCountPlan instead for a single plan, and generateCycleCountTasks to turn a plan into work. Preconditions: each site must exist and each row must name at least one zone. Required inputs: jobId (UUID), locationId (UUID) and records, each with a planName and zoneIds; give either a scheduledDate strictly in the future or a scheduledDaysOut, which is counted from today so a replayed file never asks for a past date. Emits an INVENTORY_CYCLE_COUNT_PLAN_BULK_INGEST event and a plan-created event per row. Re-running the same file is safe: a plan whose name is already present at that site is recognised rather than duplicated, since nothing in the schema makes plan names unique. Returns 200 with a per-record result; check each result rather than the status alone. A row the service refused carries errorCode CYCLE_COUNT_PLAN_INGEST_FAILED and the reason; a row lost to a server-side fault carries INTERNAL_ERROR and a correlationId to quote, with no detail of its own. 
     * Create Cycle Count Plans in Bulk
     */
    async bulkIngestCycleCountPlans(requestParameters: BulkIngestCycleCountPlansRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkIngestResponse> {
        const response = await this.bulkIngestCycleCountPlansRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
