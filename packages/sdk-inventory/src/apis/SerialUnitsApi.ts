/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Inventory API
 * API documentation for the Inventory service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  SerialUnitResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    SerialUnitResponseFromJSON,
    SerialUnitResponseToJSON,
} from '../models/index';

export interface GetSerialUnitRequest {
    serialUnitId: string;
}

export interface ListSerialUnitsRequest {
    stockItemId?: string;
    status?: ListSerialUnitsStatusEnum;
    serialNumber?: string;
}

/**
 * 
 */
export class SerialUnitsApi extends runtime.BaseAPI {

    /**
     * Returns one serial unit master record with its current status, location, lot linkage and receipt, consumption and workorder linkages. Use this tool when the serialUnitId is already known; use listSerialUnits instead to search by stock item, status or serial number, and use traceSerialUnit for the full movement chain. Preconditions: the serial unit must exist. Required inputs: serialUnitId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no serial unit exists for the supplied id. 
     * Get serial unit
     */
    async getSerialUnitRaw(requestParameters: GetSerialUnitRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SerialUnitResponse>> {
        if (requestParameters['serialUnitId'] == null) {
            throw new runtime.RequiredError(
                'serialUnitId',
                'Required parameter "serialUnitId" was null or undefined when calling getSerialUnit().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:on_hand:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/serial-units/{serialUnitId}`.replace(`{${"serialUnitId"}}`, encodeURIComponent(String(requestParameters['serialUnitId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SerialUnitResponseFromJSON(jsonValue));
    }

    /**
     * Returns one serial unit master record with its current status, location, lot linkage and receipt, consumption and workorder linkages. Use this tool when the serialUnitId is already known; use listSerialUnits instead to search by stock item, status or serial number, and use traceSerialUnit for the full movement chain. Preconditions: the serial unit must exist. Required inputs: serialUnitId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no serial unit exists for the supplied id. 
     * Get serial unit
     */
    async getSerialUnit(requestParameters: GetSerialUnitRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SerialUnitResponse> {
        const response = await this.getSerialUnitRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists serial unit master records newest first, optionally filtered by stock item, lifecycle status and exact serial number — the warranty join point that resolves a part-return hold\'s serial number to a stock record. Use this tool to discover a serialUnitId or resolve a serial number; use getSerialUnit instead when the id is known, and use traceSerialUnit for the movement history. Preconditions: none; serial units are created and transitioned exclusively by the ledger posting funnel, never through this API. Required inputs: none; stockItemId (catalog product id), status (IN_STOCK, ISSUED, RETURNED, SCRAPPED) and serialNumber are optional query filters. No events are emitted and no state changes; this is a read-only projection. Returns 200 with an empty array when nothing matches, so an empty result is not an error condition. 
     * List serial units
     */
    async listSerialUnitsRaw(requestParameters: ListSerialUnitsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<SerialUnitResponse>>> {
        const queryParameters: any = {};

        if (requestParameters['stockItemId'] != null) {
            queryParameters['stockItemId'] = requestParameters['stockItemId'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['serialNumber'] != null) {
            queryParameters['serialNumber'] = requestParameters['serialNumber'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:on_hand:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/serial-units`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(SerialUnitResponseFromJSON));
    }

    /**
     * Lists serial unit master records newest first, optionally filtered by stock item, lifecycle status and exact serial number — the warranty join point that resolves a part-return hold\'s serial number to a stock record. Use this tool to discover a serialUnitId or resolve a serial number; use getSerialUnit instead when the id is known, and use traceSerialUnit for the movement history. Preconditions: none; serial units are created and transitioned exclusively by the ledger posting funnel, never through this API. Required inputs: none; stockItemId (catalog product id), status (IN_STOCK, ISSUED, RETURNED, SCRAPPED) and serialNumber are optional query filters. No events are emitted and no state changes; this is a read-only projection. Returns 200 with an empty array when nothing matches, so an empty result is not an error condition. 
     * List serial units
     */
    async listSerialUnits(requestParameters: ListSerialUnitsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<SerialUnitResponse>> {
        const response = await this.listSerialUnitsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
  * @export
  * @enum {string}
  */
export enum ListSerialUnitsStatusEnum {
    InStock = 'IN_STOCK',
    Issued = 'ISSUED',
    Returned = 'RETURNED',
    Scrapped = 'SCRAPPED'
}
