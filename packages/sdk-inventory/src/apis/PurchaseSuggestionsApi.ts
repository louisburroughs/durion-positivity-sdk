/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Inventory API
 * API documentation for the Inventory service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  ConvertPurchaseSuggestionsRequest,
  ConvertPurchaseSuggestionsResponse,
  DismissPurchaseSuggestionRequest,
  Pageable,
  PurchaseSuggestionResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    ConvertPurchaseSuggestionsRequestFromJSON,
    ConvertPurchaseSuggestionsRequestToJSON,
    ConvertPurchaseSuggestionsResponseFromJSON,
    ConvertPurchaseSuggestionsResponseToJSON,
    DismissPurchaseSuggestionRequestFromJSON,
    DismissPurchaseSuggestionRequestToJSON,
    PageableFromJSON,
    PageableToJSON,
    PurchaseSuggestionResponseFromJSON,
    PurchaseSuggestionResponseToJSON,
} from '../models/index';

export interface AcceptPurchaseSuggestionRequest {
    suggestionId: string;
}

export interface ConvertPurchaseSuggestionsOperationRequest {
    convertPurchaseSuggestionsRequest: ConvertPurchaseSuggestionsRequest;
}

export interface DismissPurchaseSuggestionOperationRequest {
    suggestionId: string;
    dismissPurchaseSuggestionRequest: DismissPurchaseSuggestionRequest;
}

export interface GetPurchaseSuggestionRequest {
    suggestionId: string;
}

export interface ListPurchaseSuggestionsRequest {
    pageable: Pageable;
    status?: string;
    sku?: string;
    locationId?: string;
}

/**
 * 
 */
export class PurchaseSuggestionsApi extends runtime.BaseAPI {

    /**
     * Accepts a purchase suggestion, moving it from SUGGESTED to ACCEPTED as the human-mandatory gate the replenishment scan never crosses on its own. Use this tool when a reviewer approves proposed replenishment; do not use convertPurchaseSuggestions before acceptance, and use dismissPurchaseSuggestion instead to reject the proposal. Preconditions: the suggestion must exist and be in SUGGESTED status. Required inputs: suggestionId (UUIDv7) path parameter; there is no request body. Emits an INVENTORY_PURCHASE_SUGGESTION_ACCEPT event; no purchase order is created and no spend is committed, that happens only through convertPurchaseSuggestions and the PO approval workflow. Returns 404 when the suggestion does not exist, and 409 when it is not SUGGESTED because it was already accepted, converted or dismissed. 
     * Accept Purchase Suggestion
     */
    async acceptPurchaseSuggestionRaw(requestParameters: AcceptPurchaseSuggestionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PurchaseSuggestionResponse>> {
        if (requestParameters['suggestionId'] == null) {
            throw new runtime.RequiredError(
                'suggestionId',
                'Required parameter "suggestionId" was null or undefined when calling acceptPurchaseSuggestion().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:replenishment:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/purchase-suggestions/{suggestionId}/accept`.replace(`{${"suggestionId"}}`, encodeURIComponent(String(requestParameters['suggestionId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PurchaseSuggestionResponseFromJSON(jsonValue));
    }

    /**
     * Accepts a purchase suggestion, moving it from SUGGESTED to ACCEPTED as the human-mandatory gate the replenishment scan never crosses on its own. Use this tool when a reviewer approves proposed replenishment; do not use convertPurchaseSuggestions before acceptance, and use dismissPurchaseSuggestion instead to reject the proposal. Preconditions: the suggestion must exist and be in SUGGESTED status. Required inputs: suggestionId (UUIDv7) path parameter; there is no request body. Emits an INVENTORY_PURCHASE_SUGGESTION_ACCEPT event; no purchase order is created and no spend is committed, that happens only through convertPurchaseSuggestions and the PO approval workflow. Returns 404 when the suggestion does not exist, and 409 when it is not SUGGESTED because it was already accepted, converted or dismissed. 
     * Accept Purchase Suggestion
     */
    async acceptPurchaseSuggestion(requestParameters: AcceptPurchaseSuggestionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PurchaseSuggestionResponse> {
        const response = await this.acceptPurchaseSuggestionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Converts ACCEPTED purchase suggestions into one multi-line DRAFT purchase order, stamping each suggestion CONVERTED with the created order\'s id; a line is keyed in the vendor\'s pack UoM when the suggestion captured a pack size the quantity divides evenly by. Use this tool after acceptPurchaseSuggestion to turn approved replenishment into an order; do not use createPurchaseOrder, the manual path that ignores suggestions, and note that conversion never approves spend, so approvePurchaseOrder must still run on the DRAFT order. Preconditions: every listed suggestion must exist, be ACCEPTED, carry a vendor reference and a feed unit cost, all must share one vendor, and all must resolve to a single ship-to site. Required inputs: suggestionIds (non-empty list of UUIDs); duplicates are collapsed, and the caller needs both inventory:replenishment:manage and inventory:purchase_order:create. Emits an INVENTORY_PURCHASE_SUGGESTION_CONVERT event and creates the DRAFT order through the standard purchase order path, with the latest expected date among the suggestions as the expected delivery date. Returns 404 when a listed suggestion does not exist, 422 when a suggestion is not ACCEPTED, lacks a vendor or unit cost, or the suggestions mix vendors or ship-to sites, and 400 when suggestionIds is empty. 
     * Convert Purchase Suggestions To Purchase Order
     */
    async convertPurchaseSuggestionsRaw(requestParameters: ConvertPurchaseSuggestionsOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ConvertPurchaseSuggestionsResponse>> {
        if (requestParameters['convertPurchaseSuggestionsRequest'] == null) {
            throw new runtime.RequiredError(
                'convertPurchaseSuggestionsRequest',
                'Required parameter "convertPurchaseSuggestionsRequest" was null or undefined when calling convertPurchaseSuggestions().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:replenishment:manage", "order:purchase_order:create"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/purchase-suggestions/convert`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ConvertPurchaseSuggestionsRequestToJSON(requestParameters['convertPurchaseSuggestionsRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ConvertPurchaseSuggestionsResponseFromJSON(jsonValue));
    }

    /**
     * Converts ACCEPTED purchase suggestions into one multi-line DRAFT purchase order, stamping each suggestion CONVERTED with the created order\'s id; a line is keyed in the vendor\'s pack UoM when the suggestion captured a pack size the quantity divides evenly by. Use this tool after acceptPurchaseSuggestion to turn approved replenishment into an order; do not use createPurchaseOrder, the manual path that ignores suggestions, and note that conversion never approves spend, so approvePurchaseOrder must still run on the DRAFT order. Preconditions: every listed suggestion must exist, be ACCEPTED, carry a vendor reference and a feed unit cost, all must share one vendor, and all must resolve to a single ship-to site. Required inputs: suggestionIds (non-empty list of UUIDs); duplicates are collapsed, and the caller needs both inventory:replenishment:manage and inventory:purchase_order:create. Emits an INVENTORY_PURCHASE_SUGGESTION_CONVERT event and creates the DRAFT order through the standard purchase order path, with the latest expected date among the suggestions as the expected delivery date. Returns 404 when a listed suggestion does not exist, 422 when a suggestion is not ACCEPTED, lacks a vendor or unit cost, or the suggestions mix vendors or ship-to sites, and 400 when suggestionIds is empty. 
     * Convert Purchase Suggestions To Purchase Order
     */
    async convertPurchaseSuggestions(requestParameters: ConvertPurchaseSuggestionsOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ConvertPurchaseSuggestionsResponse> {
        const response = await this.convertPurchaseSuggestionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Dismisses a purchase suggestion with a recorded reason, moving it to the terminal DISMISSED status. Use this tool to reject one specific proposal; dismissal is per-instance, so use the replenishment policy snooze instead to suppress future suggestions for the SKU, and do not use acceptPurchaseSuggestion, which approves the proposal. Preconditions: the suggestion must exist and be in SUGGESTED or ACCEPTED status. Required inputs: suggestionId (UUIDv7) path parameter and a body with reason, non-blank and at most 255 characters. Emits an INVENTORY_PURCHASE_SUGGESTION_DISMISS event and stores the dismissal reason on the suggestion; the replenishment scan may still raise a new suggestion later. Returns 404 when the suggestion does not exist, 409 when it is already CONVERTED or DISMISSED, and 400 when the reason is missing or blank. 
     * Dismiss Purchase Suggestion
     */
    async dismissPurchaseSuggestionRaw(requestParameters: DismissPurchaseSuggestionOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PurchaseSuggestionResponse>> {
        if (requestParameters['suggestionId'] == null) {
            throw new runtime.RequiredError(
                'suggestionId',
                'Required parameter "suggestionId" was null or undefined when calling dismissPurchaseSuggestion().'
            );
        }

        if (requestParameters['dismissPurchaseSuggestionRequest'] == null) {
            throw new runtime.RequiredError(
                'dismissPurchaseSuggestionRequest',
                'Required parameter "dismissPurchaseSuggestionRequest" was null or undefined when calling dismissPurchaseSuggestion().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:replenishment:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/purchase-suggestions/{suggestionId}/dismiss`.replace(`{${"suggestionId"}}`, encodeURIComponent(String(requestParameters['suggestionId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: DismissPurchaseSuggestionRequestToJSON(requestParameters['dismissPurchaseSuggestionRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PurchaseSuggestionResponseFromJSON(jsonValue));
    }

    /**
     * Dismisses a purchase suggestion with a recorded reason, moving it to the terminal DISMISSED status. Use this tool to reject one specific proposal; dismissal is per-instance, so use the replenishment policy snooze instead to suppress future suggestions for the SKU, and do not use acceptPurchaseSuggestion, which approves the proposal. Preconditions: the suggestion must exist and be in SUGGESTED or ACCEPTED status. Required inputs: suggestionId (UUIDv7) path parameter and a body with reason, non-blank and at most 255 characters. Emits an INVENTORY_PURCHASE_SUGGESTION_DISMISS event and stores the dismissal reason on the suggestion; the replenishment scan may still raise a new suggestion later. Returns 404 when the suggestion does not exist, 409 when it is already CONVERTED or DISMISSED, and 400 when the reason is missing or blank. 
     * Dismiss Purchase Suggestion
     */
    async dismissPurchaseSuggestion(requestParameters: DismissPurchaseSuggestionOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PurchaseSuggestionResponse> {
        const response = await this.dismissPurchaseSuggestionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one purchase suggestion with its status, suggested quantity, vendor reference, feed price and, when converted, the created purchase order id. Use this tool when the suggestionId is already known; use listPurchaseSuggestions instead to search by status, SKU or destination location. Preconditions: the purchase suggestion must exist. Required inputs: suggestionId (UUIDv7) path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no purchase suggestion exists for the supplied id. 
     * Get Purchase Suggestion
     */
    async getPurchaseSuggestionRaw(requestParameters: GetPurchaseSuggestionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PurchaseSuggestionResponse>> {
        if (requestParameters['suggestionId'] == null) {
            throw new runtime.RequiredError(
                'suggestionId',
                'Required parameter "suggestionId" was null or undefined when calling getPurchaseSuggestion().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:on_hand:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/purchase-suggestions/{suggestionId}`.replace(`{${"suggestionId"}}`, encodeURIComponent(String(requestParameters['suggestionId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PurchaseSuggestionResponseFromJSON(jsonValue));
    }

    /**
     * Returns one purchase suggestion with its status, suggested quantity, vendor reference, feed price and, when converted, the created purchase order id. Use this tool when the suggestionId is already known; use listPurchaseSuggestions instead to search by status, SKU or destination location. Preconditions: the purchase suggestion must exist. Required inputs: suggestionId (UUIDv7) path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no purchase suggestion exists for the supplied id. 
     * Get Purchase Suggestion
     */
    async getPurchaseSuggestion(requestParameters: GetPurchaseSuggestionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PurchaseSuggestionResponse> {
        const response = await this.getPurchaseSuggestionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns a page of replenishment purchase suggestions, newest first, with their lifecycle status, suggested quantity, vendor and pricing snapshot. Use this tool to discover suggestionIds or review what the replenishment scan proposed; use getPurchaseSuggestion instead when the id is already known. Preconditions: none; suggestions are created only by the replenishment scan, so an empty page means the scan proposed nothing matching the filters. Required inputs: none; status (SUGGESTED, ACCEPTED, CONVERTED or DISMISSED), sku and locationId are optional filters, and the page size defaults to 20. No events are emitted and no state changes; this is a read-only projection. Returns 400 when status is not a known lifecycle value, and 200 with an empty page when nothing matches. 
     * List Purchase Suggestions
     */
    async listPurchaseSuggestionsRaw(requestParameters: ListPurchaseSuggestionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<string>> {
        if (requestParameters['pageable'] == null) {
            throw new runtime.RequiredError(
                'pageable',
                'Required parameter "pageable" was null or undefined when calling listPurchaseSuggestions().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['sku'] != null) {
            queryParameters['sku'] = requestParameters['sku'];
        }

        if (requestParameters['locationId'] != null) {
            queryParameters['locationId'] = requestParameters['locationId'];
        }

        if (requestParameters['pageable'] != null) {
            queryParameters['pageable'] = requestParameters['pageable'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:on_hand:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/purchase-suggestions`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<string>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Returns a page of replenishment purchase suggestions, newest first, with their lifecycle status, suggested quantity, vendor and pricing snapshot. Use this tool to discover suggestionIds or review what the replenishment scan proposed; use getPurchaseSuggestion instead when the id is already known. Preconditions: none; suggestions are created only by the replenishment scan, so an empty page means the scan proposed nothing matching the filters. Required inputs: none; status (SUGGESTED, ACCEPTED, CONVERTED or DISMISSED), sku and locationId are optional filters, and the page size defaults to 20. No events are emitted and no state changes; this is a read-only projection. Returns 400 when status is not a known lifecycle value, and 200 with an empty page when nothing matches. 
     * List Purchase Suggestions
     */
    async listPurchaseSuggestions(requestParameters: ListPurchaseSuggestionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<string> {
        const response = await this.listPurchaseSuggestionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
