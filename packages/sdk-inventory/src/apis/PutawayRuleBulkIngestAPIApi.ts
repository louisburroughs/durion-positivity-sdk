/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Inventory API
 * API documentation for the Inventory service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  BulkIngestRequestPutawayRuleBulkIngestRecord,
  BulkIngestResponse,
} from '../models/index';
import {
    BulkIngestRequestPutawayRuleBulkIngestRecordFromJSON,
    BulkIngestRequestPutawayRuleBulkIngestRecordToJSON,
    BulkIngestResponseFromJSON,
    BulkIngestResponseToJSON,
} from '../models/index';

export interface BulkIngestPutawayRulesRequest {
    bulkIngestRequestPutawayRuleBulkIngestRecord: BulkIngestRequestPutawayRuleBulkIngestRecord;
}

/**
 * 
 */
export class PutawayRuleBulkIngestAPIApi extends runtime.BaseAPI {

    /**
     * Creates many putaway rules at once, one per record. Use this tool when configuring a site\'s routing or seeding an environment; use createPutawayRule instead for a single rule. Preconditions: every tier except ANY must carry a matchValue, ANY must not, and an enabled rule\'s destination must exist in the storage-location replica unless that replica is still empty. Required inputs: jobId (UUID), locationId (UUID) and records, each with a priority, a matchType and a destinationLocationId. Emits an INVENTORY_PUTAWAY_RULE_BULK_INGEST event and a rule-created event per row. At most one enabled ANY rule may exist; a second is reported as already configured, carrying the existing rule\'s id so it can be checked — an ANY rule pointing somewhere unintended refuses every unclassified line rather than catching it. Returns 200 with a per-record result; check each result rather than the status alone. 
     * Create Putaway Rules in Bulk
     */
    async bulkIngestPutawayRulesRaw(requestParameters: BulkIngestPutawayRulesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkIngestResponse>> {
        if (requestParameters['bulkIngestRequestPutawayRuleBulkIngestRecord'] == null) {
            throw new runtime.RequiredError(
                'bulkIngestRequestPutawayRuleBulkIngestRecord',
                'Required parameter "bulkIngestRequestPutawayRuleBulkIngestRecord" was null or undefined when calling bulkIngestPutawayRules().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:putaway_rule:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/putaway/bulk-ingest`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkIngestRequestPutawayRuleBulkIngestRecordToJSON(requestParameters['bulkIngestRequestPutawayRuleBulkIngestRecord']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkIngestResponseFromJSON(jsonValue));
    }

    /**
     * Creates many putaway rules at once, one per record. Use this tool when configuring a site\'s routing or seeding an environment; use createPutawayRule instead for a single rule. Preconditions: every tier except ANY must carry a matchValue, ANY must not, and an enabled rule\'s destination must exist in the storage-location replica unless that replica is still empty. Required inputs: jobId (UUID), locationId (UUID) and records, each with a priority, a matchType and a destinationLocationId. Emits an INVENTORY_PUTAWAY_RULE_BULK_INGEST event and a rule-created event per row. At most one enabled ANY rule may exist; a second is reported as already configured, carrying the existing rule\'s id so it can be checked — an ANY rule pointing somewhere unintended refuses every unclassified line rather than catching it. Returns 200 with a per-record result; check each result rather than the status alone. 
     * Create Putaway Rules in Bulk
     */
    async bulkIngestPutawayRules(requestParameters: BulkIngestPutawayRulesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkIngestResponse> {
        const response = await this.bulkIngestPutawayRulesRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
