/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Inventory API
 * API documentation for the Inventory service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  LotTraceabilityResponse,
  SerialTraceabilityResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    LotTraceabilityResponseFromJSON,
    LotTraceabilityResponseToJSON,
    SerialTraceabilityResponseFromJSON,
    SerialTraceabilityResponseToJSON,
} from '../models/index';

export interface TraceInventoryLotRequest {
    lotId: string;
}

export interface TraceSerialUnitRequest {
    serialUnitId: string;
}

/**
 * 
 */
export class TraceabilityApi extends runtime.BaseAPI {

    /**
     * Returns a lot\'s upstream origin (purchase order, ASN, goods receipt, vendor and receipt date) and its downstream movement chain (putaway, pick, consumption, return, scrap, transfer), reconstructed chronologically from the lot-tagged append-only ledger entries. Use this tool for recall or quality investigations on a lot; use getInventoryLot instead for current per-location on-hand, and use traceSerialUnit for a single serialized unit\'s chain. Preconditions: the lot must exist. Required inputs: lotId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only reconstruction from the ledger. Returns 404 when no lot exists for the supplied id. 
     * Lot traceability
     */
    async traceInventoryLotRaw(requestParameters: TraceInventoryLotRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LotTraceabilityResponse>> {
        if (requestParameters['lotId'] == null) {
            throw new runtime.RequiredError(
                'lotId',
                'Required parameter "lotId" was null or undefined when calling traceInventoryLot().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:ledger:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/lots/{lotId}/traceability`.replace(`{${"lotId"}}`, encodeURIComponent(String(requestParameters['lotId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LotTraceabilityResponseFromJSON(jsonValue));
    }

    /**
     * Returns a lot\'s upstream origin (purchase order, ASN, goods receipt, vendor and receipt date) and its downstream movement chain (putaway, pick, consumption, return, scrap, transfer), reconstructed chronologically from the lot-tagged append-only ledger entries. Use this tool for recall or quality investigations on a lot; use getInventoryLot instead for current per-location on-hand, and use traceSerialUnit for a single serialized unit\'s chain. Preconditions: the lot must exist. Required inputs: lotId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only reconstruction from the ledger. Returns 404 when no lot exists for the supplied id. 
     * Lot traceability
     */
    async traceInventoryLot(requestParameters: TraceInventoryLotRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LotTraceabilityResponse> {
        const response = await this.traceInventoryLotRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns a serial unit\'s upstream origin (purchase order, ASN and goods receipt via its lot) and its downstream chain (receipt, issue, workorder, return, scrap) from the serial\'s receipt and consumption ledger linkages, plus any warranty-hold linkage joined by serial number. Use this tool to investigate one serialized unit\'s history; use traceInventoryLot instead for a whole lot\'s chain, and use getSerialUnit for just the current status and location. Preconditions: the serial unit must exist. Required inputs: serialUnitId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only reconstruction from the ledger. Returns 404 when no serial unit exists for the supplied id. 
     * Serial traceability
     */
    async traceSerialUnitRaw(requestParameters: TraceSerialUnitRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SerialTraceabilityResponse>> {
        if (requestParameters['serialUnitId'] == null) {
            throw new runtime.RequiredError(
                'serialUnitId',
                'Required parameter "serialUnitId" was null or undefined when calling traceSerialUnit().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:ledger:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/serial-units/{serialUnitId}/traceability`.replace(`{${"serialUnitId"}}`, encodeURIComponent(String(requestParameters['serialUnitId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SerialTraceabilityResponseFromJSON(jsonValue));
    }

    /**
     * Returns a serial unit\'s upstream origin (purchase order, ASN and goods receipt via its lot) and its downstream chain (receipt, issue, workorder, return, scrap) from the serial\'s receipt and consumption ledger linkages, plus any warranty-hold linkage joined by serial number. Use this tool to investigate one serialized unit\'s history; use traceInventoryLot instead for a whole lot\'s chain, and use getSerialUnit for just the current status and location. Preconditions: the serial unit must exist. Required inputs: serialUnitId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only reconstruction from the ledger. Returns 404 when no serial unit exists for the supplied id. 
     * Serial traceability
     */
    async traceSerialUnit(requestParameters: TraceSerialUnitRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SerialTraceabilityResponse> {
        const response = await this.traceSerialUnitRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
