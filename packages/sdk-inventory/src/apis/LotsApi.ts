/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Inventory API
 * API documentation for the Inventory service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  LotDetailResponse,
  LotExpirationUpdateRequest,
  LotResponse,
  LotStatusUpdateRequest,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    LotDetailResponseFromJSON,
    LotDetailResponseToJSON,
    LotExpirationUpdateRequestFromJSON,
    LotExpirationUpdateRequestToJSON,
    LotResponseFromJSON,
    LotResponseToJSON,
    LotStatusUpdateRequestFromJSON,
    LotStatusUpdateRequestToJSON,
} from '../models/index';

export interface GetInventoryLotRequest {
    lotId: string;
}

export interface ListInventoryLotsRequest {
    stockItemId?: string;
    status?: ListInventoryLotsStatusEnum;
    lotNumber?: string;
}

export interface UpdateInventoryLotExpirationRequest {
    lotId: string;
    lotExpirationUpdateRequest: LotExpirationUpdateRequest;
}

export interface UpdateInventoryLotStatusRequest {
    lotId: string;
    lotStatusUpdateRequest: LotStatusUpdateRequest;
}

/**
 * 
 */
export class LotsApi extends runtime.BaseAPI {

    /**
     * Returns one lot master record together with its per-location on-hand and in-transit balances, served from the per-lot stock summary rows rather than by aggregating the ledger. Use this tool when the lotId is already known; use listInventoryLots instead to search by stock item, status or lot number. Preconditions: the lot must exist. Required inputs: lotId (UUID) as a path parameter; there is no request body. Emits an INVENTORY_LOT_GET event; no state changes. Returns 404 when no lot exists for the supplied id. 
     * Get lot details
     */
    async getInventoryLotRaw(requestParameters: GetInventoryLotRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LotDetailResponse>> {
        if (requestParameters['lotId'] == null) {
            throw new runtime.RequiredError(
                'lotId',
                'Required parameter "lotId" was null or undefined when calling getInventoryLot().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:on_hand:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/lots/{lotId}`.replace(`{${"lotId"}}`, encodeURIComponent(String(requestParameters['lotId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LotDetailResponseFromJSON(jsonValue));
    }

    /**
     * Returns one lot master record together with its per-location on-hand and in-transit balances, served from the per-lot stock summary rows rather than by aggregating the ledger. Use this tool when the lotId is already known; use listInventoryLots instead to search by stock item, status or lot number. Preconditions: the lot must exist. Required inputs: lotId (UUID) as a path parameter; there is no request body. Emits an INVENTORY_LOT_GET event; no state changes. Returns 404 when no lot exists for the supplied id. 
     * Get lot details
     */
    async getInventoryLot(requestParameters: GetInventoryLotRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LotDetailResponse> {
        const response = await this.getInventoryLotRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists lot master records newest received first, optionally filtered by stock item, lifecycle status and exact lot number. Use this tool to discover a lotId or survey lot statuses; use getInventoryLot instead when the id is known and per-location on-hand is needed, and use traceInventoryLot for the movement history. Preconditions: none; lots are created only by the inbound receipt paths, never through this API. Required inputs: none; stockItemId (catalog product id), status (ACTIVE, QUARANTINED, RECALLED, CONSUMED) and lotNumber are optional query filters. Emits an INVENTORY_LOT_LIST event; no state changes. Returns 200 with an empty array when nothing matches, so an empty result is not an error condition. 
     * List lots
     */
    async listInventoryLotsRaw(requestParameters: ListInventoryLotsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<LotResponse>>> {
        const queryParameters: any = {};

        if (requestParameters['stockItemId'] != null) {
            queryParameters['stockItemId'] = requestParameters['stockItemId'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['lotNumber'] != null) {
            queryParameters['lotNumber'] = requestParameters['lotNumber'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:on_hand:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/lots`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(LotResponseFromJSON));
    }

    /**
     * Lists lot master records newest received first, optionally filtered by stock item, lifecycle status and exact lot number. Use this tool to discover a lotId or survey lot statuses; use getInventoryLot instead when the id is known and per-location on-hand is needed, and use traceInventoryLot for the movement history. Preconditions: none; lots are created only by the inbound receipt paths, never through this API. Required inputs: none; stockItemId (catalog product id), status (ACTIVE, QUARANTINED, RECALLED, CONSUMED) and lotNumber are optional query filters. Emits an INVENTORY_LOT_LIST event; no state changes. Returns 200 with an empty array when nothing matches, so an empty result is not an error condition. 
     * List lots
     */
    async listInventoryLots(requestParameters: ListInventoryLotsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<LotResponse>> {
        const response = await this.listInventoryLotsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Sets or clears a lot\'s expiration date and expiring-soon alert-window date. Use this tool to re-date a lot after inspection or vendor correction; do not use updateInventoryLotStatus, which changes the lifecycle status rather than the dates. Preconditions: the lot must exist. Required inputs: lotId (UUID) path parameter plus a body with optional expirationDate and alertDate (ISO dates); a null clears the corresponding date, and alertDate null means no early alert. Emits an INVENTORY_LOT_EXPIRATION_SET event and resets the emit-once alert bookkeeping so the daily expiry scan can alert the new dates afresh. Returns 404 when the lot does not exist. 
     * Set lot expiration
     */
    async updateInventoryLotExpirationRaw(requestParameters: UpdateInventoryLotExpirationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LotResponse>> {
        if (requestParameters['lotId'] == null) {
            throw new runtime.RequiredError(
                'lotId',
                'Required parameter "lotId" was null or undefined when calling updateInventoryLotExpiration().'
            );
        }

        if (requestParameters['lotExpirationUpdateRequest'] == null) {
            throw new runtime.RequiredError(
                'lotExpirationUpdateRequest',
                'Required parameter "lotExpirationUpdateRequest" was null or undefined when calling updateInventoryLotExpiration().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:lot:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/lots/{lotId}/expiration`.replace(`{${"lotId"}}`, encodeURIComponent(String(requestParameters['lotId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: LotExpirationUpdateRequestToJSON(requestParameters['lotExpirationUpdateRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LotResponseFromJSON(jsonValue));
    }

    /**
     * Sets or clears a lot\'s expiration date and expiring-soon alert-window date. Use this tool to re-date a lot after inspection or vendor correction; do not use updateInventoryLotStatus, which changes the lifecycle status rather than the dates. Preconditions: the lot must exist. Required inputs: lotId (UUID) path parameter plus a body with optional expirationDate and alertDate (ISO dates); a null clears the corresponding date, and alertDate null means no early alert. Emits an INVENTORY_LOT_EXPIRATION_SET event and resets the emit-once alert bookkeeping so the daily expiry scan can alert the new dates afresh. Returns 404 when the lot does not exist. 
     * Set lot expiration
     */
    async updateInventoryLotExpiration(requestParameters: UpdateInventoryLotExpirationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LotResponse> {
        const response = await this.updateInventoryLotExpirationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Changes a lot\'s lifecycle status: quarantines or recalls it, or releases it back to ACTIVE; QUARANTINED and RECALLED lots are blocked from lot suggestion and outbound movement. Use this tool to gate a lot for quality or recall reasons; do not use updateInventoryLotExpiration, which only re-dates the lot, and note the status flip does not move stock — relocating units to a quarantine bin is a separate operator-driven transfer. Preconditions: the lot must exist and must not already be CONSUMED, because a CONSUMED lot holds no stock and its status is owned by the posting-funnel reconciler. Required inputs: lotId (UUID) path parameter plus a body with status (ACTIVE, QUARANTINED or RECALLED; CONSUMED is rejected) and a mandatory reason recorded for traceability. Emits an INVENTORY_LOT_STATUS_UPDATE event; no ledger entries are posted. Returns 404 when the lot does not exist, and 400 when the target status is CONSUMED, the lot is already CONSUMED, or reason is missing. 
     * Change lot status
     */
    async updateInventoryLotStatusRaw(requestParameters: UpdateInventoryLotStatusRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LotResponse>> {
        if (requestParameters['lotId'] == null) {
            throw new runtime.RequiredError(
                'lotId',
                'Required parameter "lotId" was null or undefined when calling updateInventoryLotStatus().'
            );
        }

        if (requestParameters['lotStatusUpdateRequest'] == null) {
            throw new runtime.RequiredError(
                'lotStatusUpdateRequest',
                'Required parameter "lotStatusUpdateRequest" was null or undefined when calling updateInventoryLotStatus().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["inventory:lot:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/inventory/lots/{lotId}/status`.replace(`{${"lotId"}}`, encodeURIComponent(String(requestParameters['lotId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LotStatusUpdateRequestToJSON(requestParameters['lotStatusUpdateRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LotResponseFromJSON(jsonValue));
    }

    /**
     * Changes a lot\'s lifecycle status: quarantines or recalls it, or releases it back to ACTIVE; QUARANTINED and RECALLED lots are blocked from lot suggestion and outbound movement. Use this tool to gate a lot for quality or recall reasons; do not use updateInventoryLotExpiration, which only re-dates the lot, and note the status flip does not move stock — relocating units to a quarantine bin is a separate operator-driven transfer. Preconditions: the lot must exist and must not already be CONSUMED, because a CONSUMED lot holds no stock and its status is owned by the posting-funnel reconciler. Required inputs: lotId (UUID) path parameter plus a body with status (ACTIVE, QUARANTINED or RECALLED; CONSUMED is rejected) and a mandatory reason recorded for traceability. Emits an INVENTORY_LOT_STATUS_UPDATE event; no ledger entries are posted. Returns 404 when the lot does not exist, and 400 when the target status is CONSUMED, the lot is already CONSUMED, or reason is missing. 
     * Change lot status
     */
    async updateInventoryLotStatus(requestParameters: UpdateInventoryLotStatusRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LotResponse> {
        const response = await this.updateInventoryLotStatusRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
  * @export
  * @enum {string}
  */
export enum ListInventoryLotsStatusEnum {
    Active = 'ACTIVE',
    Quarantined = 'QUARANTINED',
    Recalled = 'RECALLED',
    Consumed = 'CONSUMED'
}
