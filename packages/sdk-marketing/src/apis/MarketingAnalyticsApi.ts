/* tslint:disable */
/* eslint-disable */
/**
 * Marketing API
 * API for marketing campaigns, message templates, audience binding, send orchestration, and campaign analytics in the POS system
 *
 * The version of the OpenAPI document: 1.0
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  CampaignStatsResponse,
  ProgramStatsResponse,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    CampaignStatsResponseFromJSON,
    CampaignStatsResponseToJSON,
    ProgramStatsResponseFromJSON,
    ProgramStatsResponseToJSON,
} from '../models/index';

export interface GetCampaignStatsRequest {
    campaignId: string;
}

export interface GetProgramStatsRequest {
    campaignProgramId: string;
}

/**
 * 
 */
export class MarketingAnalyticsApi extends runtime.BaseAPI {

    /**
     * Returns one campaign\'s per-channel delivery funnel (targeted, suppressed, sent, delivered, bounced, complained, failed) plus the redemption count and discount value attributed to its campaign code; the sent figure is cumulative, counting delivered, bounced and complained messages as sent. Use this tool for aggregate campaign performance; use listCampaignSends instead to inspect individual recipient send rows, and use getProgramStats to compare the arms of a program. Preconditions: the campaign must exist; a campaign that has never dispatched reports zeroed funnels rather than an error. Required inputs: campaignId (UUID) as a path parameter; there is no request body. Emits a MARKETING_CAMPAIGN_STATS_GET audit event; no marketing state is changed and this is a read-only projection. Returns 404 when no campaign exists for the supplied id. 
     * Get Campaign Stats
     */
    async getCampaignStatsRaw(requestParameters: GetCampaignStatsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignStatsResponse>> {
        if (requestParameters['campaignId'] == null) {
            throw new runtime.RequiredError(
                'campaignId',
                'Required parameter "campaignId" was null or undefined when calling getCampaignStats().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["marketing:stats:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/marketing/campaigns/{campaignId}/stats`.replace(`{${"campaignId"}}`, encodeURIComponent(String(requestParameters['campaignId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignStatsResponseFromJSON(jsonValue));
    }

    /**
     * Returns one campaign\'s per-channel delivery funnel (targeted, suppressed, sent, delivered, bounced, complained, failed) plus the redemption count and discount value attributed to its campaign code; the sent figure is cumulative, counting delivered, bounced and complained messages as sent. Use this tool for aggregate campaign performance; use listCampaignSends instead to inspect individual recipient send rows, and use getProgramStats to compare the arms of a program. Preconditions: the campaign must exist; a campaign that has never dispatched reports zeroed funnels rather than an error. Required inputs: campaignId (UUID) as a path parameter; there is no request body. Emits a MARKETING_CAMPAIGN_STATS_GET audit event; no marketing state is changed and this is a read-only projection. Returns 404 when no campaign exists for the supplied id. 
     * Get Campaign Stats
     */
    async getCampaignStats(requestParameters: GetCampaignStatsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignStatsResponse> {
        const response = await this.getCampaignStatsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the full stats block for every campaign sharing one campaignProgramId, so the commercial and individual arms of an initiative can be compared side by side. Use this tool when comparing the arms of a program; use getCampaignStats instead for a single campaign. Preconditions: at least one campaign must reference the campaignProgramId, since the program has no record of its own in this module. Required inputs: campaignProgramId (UUID) as a path parameter; there is no request body. Emits a MARKETING_PROGRAM_STATS_GET audit event; no marketing state is changed and this is a read-only projection. Returns 404 when no campaign references the supplied campaignProgramId. 
     * Get Campaign Program Stats
     */
    async getProgramStatsRaw(requestParameters: GetProgramStatsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProgramStatsResponse>> {
        if (requestParameters['campaignProgramId'] == null) {
            throw new runtime.RequiredError(
                'campaignProgramId',
                'Required parameter "campaignProgramId" was null or undefined when calling getProgramStats().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["marketing:stats:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/marketing/programs/{campaignProgramId}/stats`.replace(`{${"campaignProgramId"}}`, encodeURIComponent(String(requestParameters['campaignProgramId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProgramStatsResponseFromJSON(jsonValue));
    }

    /**
     * Returns the full stats block for every campaign sharing one campaignProgramId, so the commercial and individual arms of an initiative can be compared side by side. Use this tool when comparing the arms of a program; use getCampaignStats instead for a single campaign. Preconditions: at least one campaign must reference the campaignProgramId, since the program has no record of its own in this module. Required inputs: campaignProgramId (UUID) as a path parameter; there is no request body. Emits a MARKETING_PROGRAM_STATS_GET audit event; no marketing state is changed and this is a read-only projection. Returns 404 when no campaign references the supplied campaignProgramId. 
     * Get Campaign Program Stats
     */
    async getProgramStats(requestParameters: GetProgramStatsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProgramStatsResponse> {
        const response = await this.getProgramStatsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
