/* tslint:disable */
/* eslint-disable */
/**
 * Marketing API
 * API for marketing campaigns, message templates, audience binding, send orchestration, and campaign analytics in the POS system
 *
 * The version of the OpenAPI document: 1.0
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  MessageTemplateResponse,
  UpsertMessageTemplateRequest,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    MessageTemplateResponseFromJSON,
    MessageTemplateResponseToJSON,
    UpsertMessageTemplateRequestFromJSON,
    UpsertMessageTemplateRequestToJSON,
} from '../models/index';

export interface CreateMessageTemplateRequest {
    upsertMessageTemplateRequest: UpsertMessageTemplateRequest;
}

export interface DeleteMessageTemplateRequest {
    templateId: string;
}

export interface GetMessageTemplateByIdRequest {
    templateId: string;
}

export interface ListMessageTemplatesRequest {
    channel?: ListMessageTemplatesChannelEnum;
    audienceType?: ListMessageTemplatesAudienceTypeEnum;
}

export interface UpdateMessageTemplateRequest {
    templateId: string;
    upsertMessageTemplateRequest: UpsertMessageTemplateRequest;
}

/**
 * 
 */
export class MarketingTemplatesApi extends runtime.BaseAPI {

    /**
     * Creates a channel-specific message template whose {{token}} placeholders are validated against the closed vocabulary for its audience at save time, so a typo costs a validation error rather than a batch of broken messages. Use this tool when authoring a new template; do not use updateMessageTemplate, which revises an existing template and cannot change its channel or audienceType. Preconditions: no template may already use the same name (compared case-insensitively); commercial-only tokens such as accountName are rejected for INDIVIDUAL audiences and vehicle tokens for COMMERCIAL ones. Required inputs: name, channel (EMAIL or SMS), audienceType (COMMERCIAL or INDIVIDUAL) and body; subject is required for EMAIL, and for SMS it must be absent and any supplied value is discarded. Emits a MARKETING_TEMPLATE_CREATE event; no campaign is affected until the template is attached to one. Returns 409 when the name is already in use, and 422 when a token is unknown or outside the audience\'s vocabulary, an email template lacks a subject, an SMS template has one, or an SMS body exceeds 320 characters. 
     * Create Message Template
     */
    async createMessageTemplateRaw(requestParameters: CreateMessageTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MessageTemplateResponse>> {
        if (requestParameters['upsertMessageTemplateRequest'] == null) {
            throw new runtime.RequiredError(
                'upsertMessageTemplateRequest',
                'Required parameter "upsertMessageTemplateRequest" was null or undefined when calling createMessageTemplate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["marketing:template:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/marketing/templates`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: UpsertMessageTemplateRequestToJSON(requestParameters['upsertMessageTemplateRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MessageTemplateResponseFromJSON(jsonValue));
    }

    /**
     * Creates a channel-specific message template whose {{token}} placeholders are validated against the closed vocabulary for its audience at save time, so a typo costs a validation error rather than a batch of broken messages. Use this tool when authoring a new template; do not use updateMessageTemplate, which revises an existing template and cannot change its channel or audienceType. Preconditions: no template may already use the same name (compared case-insensitively); commercial-only tokens such as accountName are rejected for INDIVIDUAL audiences and vehicle tokens for COMMERCIAL ones. Required inputs: name, channel (EMAIL or SMS), audienceType (COMMERCIAL or INDIVIDUAL) and body; subject is required for EMAIL, and for SMS it must be absent and any supplied value is discarded. Emits a MARKETING_TEMPLATE_CREATE event; no campaign is affected until the template is attached to one. Returns 409 when the name is already in use, and 422 when a token is unknown or outside the audience\'s vocabulary, an email template lacks a subject, an SMS template has one, or an SMS body exceeds 320 characters. 
     * Create Message Template
     */
    async createMessageTemplate(requestParameters: CreateMessageTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MessageTemplateResponse> {
        const response = await this.createMessageTemplateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Permanently deletes a message template that no campaign references. Use this tool to retire an unused template; detach it from every campaign first via updateCampaign rather than expecting the delete to cascade. Preconditions: the template must exist and must not be referenced as the email or SMS template of any campaign, because a campaign whose template vanished would fail at dispatch across its whole audience. Required inputs: templateId (UUID) as a path parameter; there is no request body and no confirmation flag. Emits a MARKETING_TEMPLATE_DELETE event; the deletion is irreversible. Returns 404 when the template does not exist, and 422 when it is still attached to at least one campaign. 
     * Delete Unused Message Template
     */
    async deleteMessageTemplateRaw(requestParameters: DeleteMessageTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling deleteMessageTemplate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["marketing:template:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/marketing/templates/{templateId}`.replace(`{${"templateId"}}`, encodeURIComponent(String(requestParameters['templateId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Permanently deletes a message template that no campaign references. Use this tool to retire an unused template; detach it from every campaign first via updateCampaign rather than expecting the delete to cascade. Preconditions: the template must exist and must not be referenced as the email or SMS template of any campaign, because a campaign whose template vanished would fail at dispatch across its whole audience. Required inputs: templateId (UUID) as a path parameter; there is no request body and no confirmation flag. Emits a MARKETING_TEMPLATE_DELETE event; the deletion is irreversible. Returns 404 when the template does not exist, and 422 when it is still attached to at least one campaign. 
     * Delete Unused Message Template
     */
    async deleteMessageTemplate(requestParameters: DeleteMessageTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteMessageTemplateRaw(requestParameters, initOverrides);
    }

    /**
     * Returns one message template with its name, channel, audience type, subject, body, and the substitution tokens available to its audience. Use this tool when the template id is already known; use listMessageTemplates instead when searching by channel or audience. Preconditions: the template must exist. Required inputs: templateId (UUID) as a path parameter; there is no request body. Emits a MARKETING_TEMPLATE_GET audit event; no marketing state is changed and this is a read-only projection. Returns 404 when no template exists for the supplied id. 
     * Get Message Template By Id
     */
    async getMessageTemplateByIdRaw(requestParameters: GetMessageTemplateByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MessageTemplateResponse>> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling getMessageTemplateById().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["marketing:template:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/marketing/templates/{templateId}`.replace(`{${"templateId"}}`, encodeURIComponent(String(requestParameters['templateId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MessageTemplateResponseFromJSON(jsonValue));
    }

    /**
     * Returns one message template with its name, channel, audience type, subject, body, and the substitution tokens available to its audience. Use this tool when the template id is already known; use listMessageTemplates instead when searching by channel or audience. Preconditions: the template must exist. Required inputs: templateId (UUID) as a path parameter; there is no request body. Emits a MARKETING_TEMPLATE_GET audit event; no marketing state is changed and this is a read-only projection. Returns 404 when no template exists for the supplied id. 
     * Get Message Template By Id
     */
    async getMessageTemplateById(requestParameters: GetMessageTemplateByIdRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MessageTemplateResponse> {
        const response = await this.getMessageTemplateByIdRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists message templates ordered by name, optionally filtered by delivery channel and audience type. Use this tool when browsing templates or finding one to attach to a campaign; do not use getMessageTemplateById, which requires a known template id and returns exactly one template. Preconditions: none; an empty result simply means no template matches the filters. Required inputs: none; channel (EMAIL or SMS) and audienceType (COMMERCIAL or INDIVIDUAL) are optional query filters that combine when both are supplied. Emits a MARKETING_TEMPLATE_LIST audit event; no marketing state is changed. Returns 200 with an empty array when nothing matches, so an empty result is not an error condition. 
     * List Message Templates
     */
    async listMessageTemplatesRaw(requestParameters: ListMessageTemplatesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<MessageTemplateResponse>>> {
        const queryParameters: any = {};

        if (requestParameters['channel'] != null) {
            queryParameters['channel'] = requestParameters['channel'];
        }

        if (requestParameters['audienceType'] != null) {
            queryParameters['audienceType'] = requestParameters['audienceType'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["marketing:template:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/marketing/templates`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(MessageTemplateResponseFromJSON));
    }

    /**
     * Lists message templates ordered by name, optionally filtered by delivery channel and audience type. Use this tool when browsing templates or finding one to attach to a campaign; do not use getMessageTemplateById, which requires a known template id and returns exactly one template. Preconditions: none; an empty result simply means no template matches the filters. Required inputs: none; channel (EMAIL or SMS) and audienceType (COMMERCIAL or INDIVIDUAL) are optional query filters that combine when both are supplied. Emits a MARKETING_TEMPLATE_LIST audit event; no marketing state is changed. Returns 200 with an empty array when nothing matches, so an empty result is not an error condition. 
     * List Message Templates
     */
    async listMessageTemplates(requestParameters: ListMessageTemplatesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<MessageTemplateResponse>> {
        const response = await this.listMessageTemplatesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Updates a template\'s name, subject and body, re-validating every {{token}} against the audience\'s vocabulary. Use this tool to revise wording; do not use createMessageTemplate for a different channel or audience, because channel and audienceType are immutable and changing either requires a new template. Preconditions: the template must exist, the channel and audienceType in the body must equal the stored values, and no other template may hold the new name. Required inputs: templateId (UUID) path parameter plus the full body (name, channel, audienceType, body); subject is required for EMAIL and discarded for SMS. Emits a MARKETING_TEMPLATE_UPDATE event; campaigns already bound to the template pick up the new wording at their next dispatch. Returns 404 when the template does not exist, 409 when the name belongs to another template, and 422 when channel or audienceType is changed or the token, subject or SMS length rules fail. 
     * Update Message Template
     */
    async updateMessageTemplateRaw(requestParameters: UpdateMessageTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MessageTemplateResponse>> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling updateMessageTemplate().'
            );
        }

        if (requestParameters['upsertMessageTemplateRequest'] == null) {
            throw new runtime.RequiredError(
                'upsertMessageTemplateRequest',
                'Required parameter "upsertMessageTemplateRequest" was null or undefined when calling updateMessageTemplate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["marketing:template:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/marketing/templates/{templateId}`.replace(`{${"templateId"}}`, encodeURIComponent(String(requestParameters['templateId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: UpsertMessageTemplateRequestToJSON(requestParameters['upsertMessageTemplateRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MessageTemplateResponseFromJSON(jsonValue));
    }

    /**
     * Updates a template\'s name, subject and body, re-validating every {{token}} against the audience\'s vocabulary. Use this tool to revise wording; do not use createMessageTemplate for a different channel or audience, because channel and audienceType are immutable and changing either requires a new template. Preconditions: the template must exist, the channel and audienceType in the body must equal the stored values, and no other template may hold the new name. Required inputs: templateId (UUID) path parameter plus the full body (name, channel, audienceType, body); subject is required for EMAIL and discarded for SMS. Emits a MARKETING_TEMPLATE_UPDATE event; campaigns already bound to the template pick up the new wording at their next dispatch. Returns 404 when the template does not exist, 409 when the name belongs to another template, and 422 when channel or audienceType is changed or the token, subject or SMS length rules fail. 
     * Update Message Template
     */
    async updateMessageTemplate(requestParameters: UpdateMessageTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MessageTemplateResponse> {
        const response = await this.updateMessageTemplateRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
  * @export
  * @enum {string}
  */
export enum ListMessageTemplatesChannelEnum {
    Email = 'EMAIL',
    Sms = 'SMS'
}
/**
  * @export
  * @enum {string}
  */
export enum ListMessageTemplatesAudienceTypeEnum {
    Commercial = 'COMMERCIAL',
    Individual = 'INDIVIDUAL'
}
