/* tslint:disable */
/* eslint-disable */
/**
 * Supplier API
 * API for supplier vendor profile administration — profiles, auth configs, commercial accounts, and capability endpoint bindings (ADR-0050)
 *
 * The version of the OpenAPI document: 1.0
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  ExchangeAuditPayloadView,
  ExchangeAuditSummary,
  PagedResponseExchangeAuditAccessView,
  PagedResponseExchangeAuditSummary,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    ExchangeAuditPayloadViewFromJSON,
    ExchangeAuditPayloadViewToJSON,
    ExchangeAuditSummaryFromJSON,
    ExchangeAuditSummaryToJSON,
    PagedResponseExchangeAuditAccessViewFromJSON,
    PagedResponseExchangeAuditAccessViewToJSON,
    PagedResponseExchangeAuditSummaryFromJSON,
    PagedResponseExchangeAuditSummaryToJSON,
} from '../models/index';

export interface GetSupplierExchangeRequest {
    exchangeAuditId: string;
}

export interface ListSupplierExchangeAccessesRequest {
    exchangeAuditId: string;
    page?: number;
    size?: number;
}

export interface ListSupplierExchangesRequest {
    vendorProfileId: string;
    from: Date;
    to: Date;
    capability?: string;
    page?: number;
    size?: number;
}

export interface ReadSupplierExchangePayloadRequest {
    exchangeAuditId: string;
}

export interface TraceSupplierCorrelationRequest {
    correlationId: string;
    page?: number;
    size?: number;
}

/**
 * 
 */
export class SupplierExchangeAuditApi extends runtime.BaseAPI {

    /**
     * Returns the metadata of a single supplier exchange attempt, without any payload content. Use this tool to check requestPayloadPresent and responsePayloadPresent before calling readSupplierExchangePayload, which is recorded; do not use it to read the documents themselves. Preconditions: the caller must hold supplier:audit:read and the exchange-audit record must exist. Required inputs: exchangeAuditId (UUIDv7) path parameter; there is no request body. Emits a SUPPLIER_AUDIT_EXCHANGE_GET event; because no content is served, no access row is written. Returns 404 when no exchange-audit record has that id. 
     * Get one exchange\'s audit metadata
     */
    async getSupplierExchangeRaw(requestParameters: GetSupplierExchangeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ExchangeAuditSummary>> {
        if (requestParameters['exchangeAuditId'] == null) {
            throw new runtime.RequiredError(
                'exchangeAuditId',
                'Required parameter "exchangeAuditId" was null or undefined when calling getSupplierExchange().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/audit/exchanges/{exchangeAuditId}`.replace(`{${"exchangeAuditId"}}`, encodeURIComponent(String(requestParameters['exchangeAuditId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ExchangeAuditSummaryFromJSON(jsonValue));
    }

    /**
     * Returns the metadata of a single supplier exchange attempt, without any payload content. Use this tool to check requestPayloadPresent and responsePayloadPresent before calling readSupplierExchangePayload, which is recorded; do not use it to read the documents themselves. Preconditions: the caller must hold supplier:audit:read and the exchange-audit record must exist. Required inputs: exchangeAuditId (UUIDv7) path parameter; there is no request body. Emits a SUPPLIER_AUDIT_EXCHANGE_GET event; because no content is served, no access row is written. Returns 404 when no exchange-audit record has that id. 
     * Get one exchange\'s audit metadata
     */
    async getSupplierExchange(requestParameters: GetSupplierExchangeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ExchangeAuditSummary> {
        const response = await this.getSupplierExchangeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the access trail for one exchange, newest first: who read its payload, when, and what the read yielded. Use this tool to audit who has seen a trading partner\'s documents; do not expect metadata browsing or denied attempts here, because a 403 disclosed nothing and is deliberately not recorded. Preconditions: the caller must hold supplier:audit:read; an unknown exchangeAuditId returns an empty page rather than 404, because access rows carry no foreign key to the exchange. Required inputs: exchangeAuditId (UUIDv7) path parameter; page defaults to 0 and size defaults to 50 with a maximum of 200. Emits a SUPPLIER_AUDIT_ACCESS_LIST event; reading this trail is not itself recorded, or every inspection would generate the noise the next one has to sift. Returns 400 when the page size is outside the permitted range. 
     * List who read one exchange\'s payload content
     */
    async listSupplierExchangeAccessesRaw(requestParameters: ListSupplierExchangeAccessesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagedResponseExchangeAuditAccessView>> {
        if (requestParameters['exchangeAuditId'] == null) {
            throw new runtime.RequiredError(
                'exchangeAuditId',
                'Required parameter "exchangeAuditId" was null or undefined when calling listSupplierExchangeAccesses().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/audit/exchanges/{exchangeAuditId}/accesses`.replace(`{${"exchangeAuditId"}}`, encodeURIComponent(String(requestParameters['exchangeAuditId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagedResponseExchangeAuditAccessViewFromJSON(jsonValue));
    }

    /**
     * Returns the access trail for one exchange, newest first: who read its payload, when, and what the read yielded. Use this tool to audit who has seen a trading partner\'s documents; do not expect metadata browsing or denied attempts here, because a 403 disclosed nothing and is deliberately not recorded. Preconditions: the caller must hold supplier:audit:read; an unknown exchangeAuditId returns an empty page rather than 404, because access rows carry no foreign key to the exchange. Required inputs: exchangeAuditId (UUIDv7) path parameter; page defaults to 0 and size defaults to 50 with a maximum of 200. Emits a SUPPLIER_AUDIT_ACCESS_LIST event; reading this trail is not itself recorded, or every inspection would generate the noise the next one has to sift. Returns 400 when the page size is outside the permitted range. 
     * List who read one exchange\'s payload content
     */
    async listSupplierExchangeAccesses(requestParameters: ListSupplierExchangeAccessesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagedResponseExchangeAuditAccessView> {
        const response = await this.listSupplierExchangeAccessesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns a page of exchange-audit metadata for one vendor profile within a half-open time window, newest first. Use this tool to find the exchangeAuditId of a supplier call; use traceSupplierCorrelation instead when every attempt of one logical call is wanted, and do not expect payload content here. Preconditions: the caller must hold supplier:audit:read, which supplier:profile:read does not imply; the vendor profile need not still exist, because audit history outlives configuration. Required inputs: vendorProfileId, from (inclusive) and to (exclusive) as ISO-8601 instants; capability is optional, page defaults to 0 and size defaults to 50 with a maximum of 200. Emits a SUPPLIER_AUDIT_EXCHANGE_LIST event; no payload content is served, so no access row is written against the exchanges listed. Returns 400 when the window end is not after its start, the capability key is unrecognised, or the page size is out of range. 
     * List supplier exchanges in a window
     */
    async listSupplierExchangesRaw(requestParameters: ListSupplierExchangesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagedResponseExchangeAuditSummary>> {
        if (requestParameters['vendorProfileId'] == null) {
            throw new runtime.RequiredError(
                'vendorProfileId',
                'Required parameter "vendorProfileId" was null or undefined when calling listSupplierExchanges().'
            );
        }

        if (requestParameters['from'] == null) {
            throw new runtime.RequiredError(
                'from',
                'Required parameter "from" was null or undefined when calling listSupplierExchanges().'
            );
        }

        if (requestParameters['to'] == null) {
            throw new runtime.RequiredError(
                'to',
                'Required parameter "to" was null or undefined when calling listSupplierExchanges().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['vendorProfileId'] != null) {
            queryParameters['vendorProfileId'] = requestParameters['vendorProfileId'];
        }

        if (requestParameters['from'] != null) {
            queryParameters['from'] = (requestParameters['from'] as any).toISOString();
        }

        if (requestParameters['to'] != null) {
            queryParameters['to'] = (requestParameters['to'] as any).toISOString();
        }

        if (requestParameters['capability'] != null) {
            queryParameters['capability'] = requestParameters['capability'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/audit/exchanges`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagedResponseExchangeAuditSummaryFromJSON(jsonValue));
    }

    /**
     * Returns a page of exchange-audit metadata for one vendor profile within a half-open time window, newest first. Use this tool to find the exchangeAuditId of a supplier call; use traceSupplierCorrelation instead when every attempt of one logical call is wanted, and do not expect payload content here. Preconditions: the caller must hold supplier:audit:read, which supplier:profile:read does not imply; the vendor profile need not still exist, because audit history outlives configuration. Required inputs: vendorProfileId, from (inclusive) and to (exclusive) as ISO-8601 instants; capability is optional, page defaults to 0 and size defaults to 50 with a maximum of 200. Emits a SUPPLIER_AUDIT_EXCHANGE_LIST event; no payload content is served, so no access row is written against the exchanges listed. Returns 400 when the window end is not after its start, the capability key is unrecognised, or the page size is out of range. 
     * List supplier exchanges in a window
     */
    async listSupplierExchanges(requestParameters: ListSupplierExchangesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagedResponseExchangeAuditSummary> {
        const response = await this.listSupplierExchangesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the stored request and response documents of one supplier exchange. Use this tool only when the documents themselves are needed; use getSupplierExchange instead for routine inspection, because this call is itself recorded and each read adds an access row naming the caller. Preconditions: the caller must hold supplier:audit:read, the record must exist, and the access row must be writable, since content is withheld when it cannot be written. Required inputs: exchangeAuditId (UUIDv7) path parameter; there is no request body and no way to request the unredacted originals. Emits a SUPPLIER_AUDIT_PAYLOAD_READ event and writes an access row in the same transaction; null payload fields are normal, meaning a METADATA_ONLY binding, a bodiless exchange, or content already purged after 400 days, and a redacted flag means sensitive fields were replaced at capture time so the originals were never stored. Returns 404 when no record has that id, and 500 when stored content exists but cannot be decrypted, in which case the read is still recorded with an UNREADABLE outcome. 
     * Read one exchange\'s stored payload content
     */
    async readSupplierExchangePayloadRaw(requestParameters: ReadSupplierExchangePayloadRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ExchangeAuditPayloadView>> {
        if (requestParameters['exchangeAuditId'] == null) {
            throw new runtime.RequiredError(
                'exchangeAuditId',
                'Required parameter "exchangeAuditId" was null or undefined when calling readSupplierExchangePayload().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/audit/exchanges/{exchangeAuditId}/payload`.replace(`{${"exchangeAuditId"}}`, encodeURIComponent(String(requestParameters['exchangeAuditId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ExchangeAuditPayloadViewFromJSON(jsonValue));
    }

    /**
     * Returns the stored request and response documents of one supplier exchange. Use this tool only when the documents themselves are needed; use getSupplierExchange instead for routine inspection, because this call is itself recorded and each read adds an access row naming the caller. Preconditions: the caller must hold supplier:audit:read, the record must exist, and the access row must be writable, since content is withheld when it cannot be written. Required inputs: exchangeAuditId (UUIDv7) path parameter; there is no request body and no way to request the unredacted originals. Emits a SUPPLIER_AUDIT_PAYLOAD_READ event and writes an access row in the same transaction; null payload fields are normal, meaning a METADATA_ONLY binding, a bodiless exchange, or content already purged after 400 days, and a redacted flag means sensitive fields were replaced at capture time so the originals were never stored. Returns 404 when no record has that id, and 500 when stored content exists but cannot be decrypted, in which case the read is still recorded with an UNREADABLE outcome. 
     * Read one exchange\'s stored payload content
     */
    async readSupplierExchangePayload(requestParameters: ReadSupplierExchangePayloadRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ExchangeAuditPayloadView> {
        const response = await this.readSupplierExchangePayloadRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every exchange attempt sharing one correlation id, oldest first, which is the order in which a retry sequence actually happened. Use this tool when a single logical supplier call needs to be reconstructed across its retries; use listSupplierExchanges instead to search a time window. Preconditions: the caller must hold supplier:audit:read; an unknown correlation id is not an error and yields an empty page. Required inputs: correlationId path parameter; page defaults to 0 and size defaults to 50 with a maximum of 200. Emits a SUPPLIER_AUDIT_EXCHANGE_TRACE event; metadata only, so no payload access is recorded. Returns 400 when the page size is outside the permitted range. 
     * Trace one logical call by correlation id
     */
    async traceSupplierCorrelationRaw(requestParameters: TraceSupplierCorrelationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagedResponseExchangeAuditSummary>> {
        if (requestParameters['correlationId'] == null) {
            throw new runtime.RequiredError(
                'correlationId',
                'Required parameter "correlationId" was null or undefined when calling traceSupplierCorrelation().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/audit/exchanges/by-correlation/{correlationId}`.replace(`{${"correlationId"}}`, encodeURIComponent(String(requestParameters['correlationId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagedResponseExchangeAuditSummaryFromJSON(jsonValue));
    }

    /**
     * Returns every exchange attempt sharing one correlation id, oldest first, which is the order in which a retry sequence actually happened. Use this tool when a single logical supplier call needs to be reconstructed across its retries; use listSupplierExchanges instead to search a time window. Preconditions: the caller must hold supplier:audit:read; an unknown correlation id is not an error and yields an empty page. Required inputs: correlationId path parameter; page defaults to 0 and size defaults to 50 with a maximum of 200. Emits a SUPPLIER_AUDIT_EXCHANGE_TRACE event; metadata only, so no payload access is recorded. Returns 400 when the page size is outside the permitted range. 
     * Trace one logical call by correlation id
     */
    async traceSupplierCorrelation(requestParameters: TraceSupplierCorrelationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagedResponseExchangeAuditSummary> {
        const response = await this.traceSupplierCorrelationRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
