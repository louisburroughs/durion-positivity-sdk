/* tslint:disable */
/* eslint-disable */
/**
 * Supplier API
 * API for supplier vendor profile administration — profiles, auth configs, commercial accounts, and capability endpoint bindings (ADR-0050)
 *
 * The version of the OpenAPI document: 1.0
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
} from '../models/index';

export interface FetchSupplierInvoicesRequest {
    supplierRef: string;
    fromDate: Date;
    toDate: Date;
}

/**
 * 
 */
export class SupplierInvoicesApi extends runtime.BaseAPI {

    /**
     * Asks a vendor for the invoices it issued between two dates and imports the ones not already held. Use this tool when somebody is chasing an invoice a vendor says it sent; do not use it to catch a schedule up, because it deliberately leaves the checkpoint alone and the scheduled run will still cover its own window. Preconditions: the vendor profile must be enabled and have an invoice-fetch binding configured; fromDate must not be after toDate. Required inputs: supplierRef path parameter, and fromDate and toDate query parameters as ISO dates. Emits a SUPPLIER_INVOICE_FETCH event and, for each invoice not already held, publishes it for the accounting domain to record as a vendor bill. Returns 200 with the number the vendor sent and the number newly imported, 422 when the vendor refused the window or could not be reached, 404 when the vendor profile is unknown, and 409 when the profile is disabled or has no invoice-fetch binding configured. 
     * Fetch Vendor Invoices For A Window
     */
    async fetchSupplierInvoicesRaw(requestParameters: FetchSupplierInvoicesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<object>> {
        if (requestParameters['supplierRef'] == null) {
            throw new runtime.RequiredError(
                'supplierRef',
                'Required parameter "supplierRef" was null or undefined when calling fetchSupplierInvoices().'
            );
        }

        if (requestParameters['fromDate'] == null) {
            throw new runtime.RequiredError(
                'fromDate',
                'Required parameter "fromDate" was null or undefined when calling fetchSupplierInvoices().'
            );
        }

        if (requestParameters['toDate'] == null) {
            throw new runtime.RequiredError(
                'toDate',
                'Required parameter "toDate" was null or undefined when calling fetchSupplierInvoices().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['fromDate'] != null) {
            queryParameters['fromDate'] = (requestParameters['fromDate'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['toDate'] != null) {
            queryParameters['toDate'] = (requestParameters['toDate'] as any).toISOString().substring(0,10);
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["supplier:invoice:fetch"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/invoices/{supplierRef}/fetches`.replace(`{${"supplierRef"}}`, encodeURIComponent(String(requestParameters['supplierRef']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Asks a vendor for the invoices it issued between two dates and imports the ones not already held. Use this tool when somebody is chasing an invoice a vendor says it sent; do not use it to catch a schedule up, because it deliberately leaves the checkpoint alone and the scheduled run will still cover its own window. Preconditions: the vendor profile must be enabled and have an invoice-fetch binding configured; fromDate must not be after toDate. Required inputs: supplierRef path parameter, and fromDate and toDate query parameters as ISO dates. Emits a SUPPLIER_INVOICE_FETCH event and, for each invoice not already held, publishes it for the accounting domain to record as a vendor bill. Returns 200 with the number the vendor sent and the number newly imported, 422 when the vendor refused the window or could not be reached, 404 when the vendor profile is unknown, and 409 when the profile is disabled or has no invoice-fetch binding configured. 
     * Fetch Vendor Invoices For A Window
     */
    async fetchSupplierInvoices(requestParameters: FetchSupplierInvoicesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<object> {
        const response = await this.fetchSupplierInvoicesRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
