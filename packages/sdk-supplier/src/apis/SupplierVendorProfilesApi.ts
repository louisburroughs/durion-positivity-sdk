/* tslint:disable */
/* eslint-disable */
/**
 * Supplier API
 * API for supplier vendor profile administration — profiles, auth configs, commercial accounts, and capability endpoint bindings (ADR-0050)
 *
 * The version of the OpenAPI document: 1.0
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  VendorProfileRequest,
  VendorProfileView,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    VendorProfileRequestFromJSON,
    VendorProfileRequestToJSON,
    VendorProfileViewFromJSON,
    VendorProfileViewToJSON,
} from '../models/index';

export interface CreateVendorProfileRequest {
    vendorProfileRequest: VendorProfileRequest;
}

export interface DeleteVendorProfileRequest {
    vendorProfileId: string;
}

export interface GetVendorProfileRequest {
    vendorProfileId: string;
}

export interface UpdateVendorProfileRequest {
    vendorProfileId: string;
    vendorProfileRequest: VendorProfileRequest;
}

/**
 * 
 */
export class SupplierVendorProfilesApi extends runtime.BaseAPI {

    /**
     * Creates an ADMIN-managed vendor profile, the row that carries one supplier connection and owns its accounts, auth config and endpoint bindings. Use this tool when onboarding a new supplier connection; do not use it to change an existing profile, which is updateVendorProfile, and note that YAML-managed profiles cannot be created here at all. Preconditions: supplierRef must not already be in use by another profile. Required inputs: supplierRef and displayName, both non-blank, plus the enabled and sandbox flags; timeouts, maxRetries, retryBackoff and sandboxBaseUrlOverride are optional and fall back to the deployment defaults when omitted. Emits a SUPPLIER_PROFILE_CREATE audit event; the profile is created with no bindings, so it resolves every capability to a not-configured outcome until bindings are added. Returns 409 when supplierRef is already in use, and 400 when supplierRef or displayName are blank or a timeout value is not greater than zero. 
     * Create vendor profile
     */
    async createVendorProfileRaw(requestParameters: CreateVendorProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<VendorProfileView>> {
        if (requestParameters['vendorProfileRequest'] == null) {
            throw new runtime.RequiredError(
                'vendorProfileRequest',
                'Required parameter "vendorProfileRequest" was null or undefined when calling createVendorProfile().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/profiles`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: VendorProfileRequestToJSON(requestParameters['vendorProfileRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => VendorProfileViewFromJSON(jsonValue));
    }

    /**
     * Creates an ADMIN-managed vendor profile, the row that carries one supplier connection and owns its accounts, auth config and endpoint bindings. Use this tool when onboarding a new supplier connection; do not use it to change an existing profile, which is updateVendorProfile, and note that YAML-managed profiles cannot be created here at all. Preconditions: supplierRef must not already be in use by another profile. Required inputs: supplierRef and displayName, both non-blank, plus the enabled and sandbox flags; timeouts, maxRetries, retryBackoff and sandboxBaseUrlOverride are optional and fall back to the deployment defaults when omitted. Emits a SUPPLIER_PROFILE_CREATE audit event; the profile is created with no bindings, so it resolves every capability to a not-configured outcome until bindings are added. Returns 409 when supplierRef is already in use, and 400 when supplierRef or displayName are blank or a timeout value is not greater than zero. 
     * Create vendor profile
     */
    async createVendorProfile(requestParameters: CreateVendorProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<VendorProfileView> {
        const response = await this.createVendorProfileRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Deletes a vendor profile together with its accounts, auth config and endpoint bindings. Use this tool only when a supplier connection is being retired permanently; to stop traffic while keeping the configuration, call updateVendorProfile with enabled cleared instead. Preconditions: the profile must exist and must be ADMIN-managed, because YAML-managed profiles are owned by the deployment configuration. Required inputs: vendorProfileId (UUIDv7) path parameter; there is no request body and no confirmation flag. Emits a SUPPLIER_PROFILE_DELETE audit event and cascades to the profile\'s child configuration; exchange audit records already written are retained. Returns 404 when the profile does not exist and 409 when it is YAML-managed. 
     * Delete vendor profile
     */
    async deleteVendorProfileRaw(requestParameters: DeleteVendorProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['vendorProfileId'] == null) {
            throw new runtime.RequiredError(
                'vendorProfileId',
                'Required parameter "vendorProfileId" was null or undefined when calling deleteVendorProfile().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/profiles/{vendorProfileId}`.replace(`{${"vendorProfileId"}}`, encodeURIComponent(String(requestParameters['vendorProfileId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes a vendor profile together with its accounts, auth config and endpoint bindings. Use this tool only when a supplier connection is being retired permanently; to stop traffic while keeping the configuration, call updateVendorProfile with enabled cleared instead. Preconditions: the profile must exist and must be ADMIN-managed, because YAML-managed profiles are owned by the deployment configuration. Required inputs: vendorProfileId (UUIDv7) path parameter; there is no request body and no confirmation flag. Emits a SUPPLIER_PROFILE_DELETE audit event and cascades to the profile\'s child configuration; exchange audit records already written are retained. Returns 404 when the profile does not exist and 409 when it is YAML-managed. 
     * Delete vendor profile
     */
    async deleteVendorProfile(requestParameters: DeleteVendorProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteVendorProfileRaw(requestParameters, initOverrides);
    }

    /**
     * Returns one vendor profile with its timeouts, retry settings, sandbox overlay and whether it is ADMIN-managed or YAML-managed. Use this tool when the vendorProfileId is already known; use listVendorProfiles instead to search by supplierRef. Preconditions: the profile must exist. Required inputs: vendorProfileId (UUIDv7) path parameter; there is no request body. Emits a SUPPLIER_PROFILE_GET audit event; no configuration is changed. Returns 404 when no profile exists for the supplied id. 
     * Get vendor profile
     */
    async getVendorProfileRaw(requestParameters: GetVendorProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<VendorProfileView>> {
        if (requestParameters['vendorProfileId'] == null) {
            throw new runtime.RequiredError(
                'vendorProfileId',
                'Required parameter "vendorProfileId" was null or undefined when calling getVendorProfile().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/profiles/{vendorProfileId}`.replace(`{${"vendorProfileId"}}`, encodeURIComponent(String(requestParameters['vendorProfileId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => VendorProfileViewFromJSON(jsonValue));
    }

    /**
     * Returns one vendor profile with its timeouts, retry settings, sandbox overlay and whether it is ADMIN-managed or YAML-managed. Use this tool when the vendorProfileId is already known; use listVendorProfiles instead to search by supplierRef. Preconditions: the profile must exist. Required inputs: vendorProfileId (UUIDv7) path parameter; there is no request body. Emits a SUPPLIER_PROFILE_GET audit event; no configuration is changed. Returns 404 when no profile exists for the supplied id. 
     * Get vendor profile
     */
    async getVendorProfile(requestParameters: GetVendorProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<VendorProfileView> {
        const response = await this.getVendorProfileRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every configured vendor profile, ordered by supplierRef, with its enabled, sandbox and source-of-truth state. Use this tool to discover a vendorProfileId before working with accounts, auth config or bindings; use getVendorProfile instead when the id is already known. Preconditions: none; the list is unfiltered and includes both ADMIN-managed and YAML-managed profiles. Required inputs: none, and there is no request body, paging or filtering. Emits a SUPPLIER_PROFILE_LIST audit event; no configuration is changed. Returns 200 with an empty array when nothing is configured, so an empty result is not an error condition. 
     * List vendor profiles
     */
    async listVendorProfilesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<VendorProfileView>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/profiles`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(VendorProfileViewFromJSON));
    }

    /**
     * Returns every configured vendor profile, ordered by supplierRef, with its enabled, sandbox and source-of-truth state. Use this tool to discover a vendorProfileId before working with accounts, auth config or bindings; use getVendorProfile instead when the id is already known. Preconditions: none; the list is unfiltered and includes both ADMIN-managed and YAML-managed profiles. Required inputs: none, and there is no request body, paging or filtering. Emits a SUPPLIER_PROFILE_LIST audit event; no configuration is changed. Returns 200 with an empty array when nothing is configured, so an empty result is not an error condition. 
     * List vendor profiles
     */
    async listVendorProfiles(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<VendorProfileView>> {
        const response = await this.listVendorProfilesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Replaces every settable field of a vendor profile, including its alias, display name, enabled and sandbox flags and default timeouts. Use this tool to change connection defaults or take a supplier out of service by clearing enabled; do not use it on YAML-managed profiles, whose source of truth is the deployment configuration instead. Preconditions: the profile must exist, must be ADMIN-managed, and the supplierRef in the body must not belong to a different profile. Required inputs: vendorProfileId (UUIDv7) path parameter plus the full body, because every field is replaced; omitting an optional field resets it to the deployment default rather than leaving the stored value. Emits a SUPPLIER_PROFILE_UPDATE audit event; disabling a profile immediately makes its bindings resolve to a typed not-configured outcome. Returns 404 when the profile does not exist, 409 when it is YAML-managed or the supplierRef is taken, and 400 when a required field is blank or a timeout is not greater than zero. 
     * Update vendor profile
     */
    async updateVendorProfileRaw(requestParameters: UpdateVendorProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<VendorProfileView>> {
        if (requestParameters['vendorProfileId'] == null) {
            throw new runtime.RequiredError(
                'vendorProfileId',
                'Required parameter "vendorProfileId" was null or undefined when calling updateVendorProfile().'
            );
        }

        if (requestParameters['vendorProfileRequest'] == null) {
            throw new runtime.RequiredError(
                'vendorProfileRequest',
                'Required parameter "vendorProfileRequest" was null or undefined when calling updateVendorProfile().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/profiles/{vendorProfileId}`.replace(`{${"vendorProfileId"}}`, encodeURIComponent(String(requestParameters['vendorProfileId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: VendorProfileRequestToJSON(requestParameters['vendorProfileRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => VendorProfileViewFromJSON(jsonValue));
    }

    /**
     * Replaces every settable field of a vendor profile, including its alias, display name, enabled and sandbox flags and default timeouts. Use this tool to change connection defaults or take a supplier out of service by clearing enabled; do not use it on YAML-managed profiles, whose source of truth is the deployment configuration instead. Preconditions: the profile must exist, must be ADMIN-managed, and the supplierRef in the body must not belong to a different profile. Required inputs: vendorProfileId (UUIDv7) path parameter plus the full body, because every field is replaced; omitting an optional field resets it to the deployment default rather than leaving the stored value. Emits a SUPPLIER_PROFILE_UPDATE audit event; disabling a profile immediately makes its bindings resolve to a typed not-configured outcome. Returns 404 when the profile does not exist, 409 when it is YAML-managed or the supplierRef is taken, and 400 when a required field is blank or a timeout is not greater than zero. 
     * Update vendor profile
     */
    async updateVendorProfile(requestParameters: UpdateVendorProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<VendorProfileView> {
        const response = await this.updateVendorProfileRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
