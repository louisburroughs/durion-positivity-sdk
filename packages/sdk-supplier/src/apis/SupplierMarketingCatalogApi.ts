/* tslint:disable */
/* eslint-disable */
/**
 * Supplier API
 * API for supplier vendor profile administration — profiles, auth configs, commercial accounts, and capability endpoint bindings (ADR-0050)
 *
 * The version of the OpenAPI document: 1.0
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  MarketingEnrichmentView,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    MarketingEnrichmentViewFromJSON,
    MarketingEnrichmentViewToJSON,
} from '../models/index';

export interface ImportMarketingCatalogRequest {
    supplierRef: string;
}

export interface ListMarketingCatalogVariantsRequest {
    supplierRef: string;
    limit?: number;
    hasUnresolvedImages?: boolean;
}

/**
 * 
 */
export class SupplierMarketingCatalogApi extends runtime.BaseAPI {

    /**
     * Reads the manufacturer marketing catalogue and publishes enrichment for every tread design variant whose content has changed since the last run. Use this tool to bring a newly configured catalogue in without waiting for its first scheduled sweep, or after fixing a configuration problem; do not use it to force republication of unchanged content, because publication is decided by content and an unchanged variant emits nothing however often it is imported. Preconditions: the vendor profile must be enabled and have a marketing-catalogue binding configured. Required inputs: supplierRef path parameter. Emits a SUPPLIER_MKTCAT_IMPORT event, and for each changed variant publishes supplier.catalog.updated for the catalog domain to attach. Returns 200 with how many variants were seen, published and skipped; 404 when the vendor profile is unknown; 409 when the profile is disabled or has no marketing-catalogue binding; and 422 when the catalogue could not be read at all. 
     * Import The Marketing Catalog
     */
    async importMarketingCatalogRaw(requestParameters: ImportMarketingCatalogRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<object>> {
        if (requestParameters['supplierRef'] == null) {
            throw new runtime.RequiredError(
                'supplierRef',
                'Required parameter "supplierRef" was null or undefined when calling importMarketingCatalog().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["supplier:mktcat:import"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/mktcat/{supplierRef}/imports`.replace(`{${"supplierRef"}}`, encodeURIComponent(String(requestParameters['supplierRef']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Reads the manufacturer marketing catalogue and publishes enrichment for every tread design variant whose content has changed since the last run. Use this tool to bring a newly configured catalogue in without waiting for its first scheduled sweep, or after fixing a configuration problem; do not use it to force republication of unchanged content, because publication is decided by content and an unchanged variant emits nothing however often it is imported. Preconditions: the vendor profile must be enabled and have a marketing-catalogue binding configured. Required inputs: supplierRef path parameter. Emits a SUPPLIER_MKTCAT_IMPORT event, and for each changed variant publishes supplier.catalog.updated for the catalog domain to attach. Returns 200 with how many variants were seen, published and skipped; 404 when the vendor profile is unknown; 409 when the profile is disabled or has no marketing-catalogue binding; and 422 when the catalogue could not be read at all. 
     * Import The Marketing Catalog
     */
    async importMarketingCatalog(requestParameters: ImportMarketingCatalogRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<object> {
        const response = await this.importMarketingCatalogRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the tread design variants a marketing catalogue last sent, most recently seen first, including which of them are still missing artwork. Use this tool when a product\'s marketing copy is not what was expected, to see what the catalogue actually published; do not read this as catalog content, because nothing here has been attached to a product and a variant may match no product at all. This list is supplier-scoped staged enrichment — one vendor\'s own catalogue submission — and is NOT the unmatched-product queue: that worklist, matched by design against products, is pos-catalog\'s listUnmatchedTreadDesigns. Preconditions: none. Required inputs: supplierRef path parameter; limit defaults to 100. Optional hasUnresolvedImages filters to variants still missing artwork (true) or not (false); omitted, every staged row is returned regardless of image state. Emits a SUPPLIER_MKTCAT_VARIANT_LIST event. Returns 200 with the staged variants, which is an empty list when the catalogue has never been imported or nothing matches the filter, 400 when hasUnresolvedImages is not a boolean, and 404 when the vendor profile is unknown. 
     * List Staged Marketing Enrichment
     */
    async listMarketingCatalogVariantsRaw(requestParameters: ListMarketingCatalogVariantsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<MarketingEnrichmentView>>> {
        if (requestParameters['supplierRef'] == null) {
            throw new runtime.RequiredError(
                'supplierRef',
                'Required parameter "supplierRef" was null or undefined when calling listMarketingCatalogVariants().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['hasUnresolvedImages'] != null) {
            queryParameters['hasUnresolvedImages'] = requestParameters['hasUnresolvedImages'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["supplier:mktcat:import"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/mktcat/{supplierRef}/variants`.replace(`{${"supplierRef"}}`, encodeURIComponent(String(requestParameters['supplierRef']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(MarketingEnrichmentViewFromJSON));
    }

    /**
     * Lists the tread design variants a marketing catalogue last sent, most recently seen first, including which of them are still missing artwork. Use this tool when a product\'s marketing copy is not what was expected, to see what the catalogue actually published; do not read this as catalog content, because nothing here has been attached to a product and a variant may match no product at all. This list is supplier-scoped staged enrichment — one vendor\'s own catalogue submission — and is NOT the unmatched-product queue: that worklist, matched by design against products, is pos-catalog\'s listUnmatchedTreadDesigns. Preconditions: none. Required inputs: supplierRef path parameter; limit defaults to 100. Optional hasUnresolvedImages filters to variants still missing artwork (true) or not (false); omitted, every staged row is returned regardless of image state. Emits a SUPPLIER_MKTCAT_VARIANT_LIST event. Returns 200 with the staged variants, which is an empty list when the catalogue has never been imported or nothing matches the filter, 400 when hasUnresolvedImages is not a boolean, and 404 when the vendor profile is unknown. 
     * List Staged Marketing Enrichment
     */
    async listMarketingCatalogVariants(requestParameters: ListMarketingCatalogVariantsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<MarketingEnrichmentView>> {
        const response = await this.listMarketingCatalogVariantsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
