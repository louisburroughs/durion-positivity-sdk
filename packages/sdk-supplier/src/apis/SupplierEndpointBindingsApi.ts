/* tslint:disable */
/* eslint-disable */
/**
 * Supplier API
 * API for supplier vendor profile administration — profiles, auth configs, commercial accounts, and capability endpoint bindings (ADR-0050)
 *
 * The version of the OpenAPI document: 1.0
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  EndpointBindingRequest,
  EndpointBindingView,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    EndpointBindingRequestFromJSON,
    EndpointBindingRequestToJSON,
    EndpointBindingViewFromJSON,
    EndpointBindingViewToJSON,
} from '../models/index';

export interface CreateEndpointBindingRequest {
    vendorProfileId: string;
    endpointBindingRequest: EndpointBindingRequest;
}

export interface DeleteEndpointBindingRequest {
    vendorProfileId: string;
    bindingId: string;
}

export interface ListEndpointBindingsRequest {
    vendorProfileId: string;
}

export interface UpdateEndpointBindingRequest {
    vendorProfileId: string;
    bindingId: string;
    endpointBindingRequest: EndpointBindingRequest;
}

/**
 * 
 */
export class SupplierEndpointBindingsApi extends runtime.BaseAPI {

    /**
     * Binds one supplier capability on a vendor profile to a protocol family, adapter version, URL and auth config, which is what makes that capability resolve at call time. Use this tool when enabling a capability for a supplier; do not use it to change an existing binding, which is updateEndpointBinding, since at most one binding per capability may exist. Preconditions: the profile must exist and be ADMIN-managed, the capability must not already be bound on it, and authConfigName must name an auth config on the same profile. Required inputs: vendorProfileId (UUIDv7) path parameter plus capability, protocolFamily, version, baseUrl, path and authConfigName; version is not validated on write, so a key with no registered codec is accepted and then resolves to CAPABILITY_NOT_CONFIGURED on every call. Emits a SUPPLIER_BINDING_CREATE audit event; captureLevel and the redaction classifications decide what the exchange-audit trail retains for this binding from now on. Returns 404 when the profile does not exist, 409 when the profile is YAML-managed or the capability is already bound, and 400 when the capability, protocol family or auth config name is unknown. 
     * Create endpoint binding
     */
    async createEndpointBindingRaw(requestParameters: CreateEndpointBindingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EndpointBindingView>> {
        if (requestParameters['vendorProfileId'] == null) {
            throw new runtime.RequiredError(
                'vendorProfileId',
                'Required parameter "vendorProfileId" was null or undefined when calling createEndpointBinding().'
            );
        }

        if (requestParameters['endpointBindingRequest'] == null) {
            throw new runtime.RequiredError(
                'endpointBindingRequest',
                'Required parameter "endpointBindingRequest" was null or undefined when calling createEndpointBinding().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/profiles/{vendorProfileId}/bindings`.replace(`{${"vendorProfileId"}}`, encodeURIComponent(String(requestParameters['vendorProfileId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: EndpointBindingRequestToJSON(requestParameters['endpointBindingRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EndpointBindingViewFromJSON(jsonValue));
    }

    /**
     * Binds one supplier capability on a vendor profile to a protocol family, adapter version, URL and auth config, which is what makes that capability resolve at call time. Use this tool when enabling a capability for a supplier; do not use it to change an existing binding, which is updateEndpointBinding, since at most one binding per capability may exist. Preconditions: the profile must exist and be ADMIN-managed, the capability must not already be bound on it, and authConfigName must name an auth config on the same profile. Required inputs: vendorProfileId (UUIDv7) path parameter plus capability, protocolFamily, version, baseUrl, path and authConfigName; version is not validated on write, so a key with no registered codec is accepted and then resolves to CAPABILITY_NOT_CONFIGURED on every call. Emits a SUPPLIER_BINDING_CREATE audit event; captureLevel and the redaction classifications decide what the exchange-audit trail retains for this binding from now on. Returns 404 when the profile does not exist, 409 when the profile is YAML-managed or the capability is already bound, and 400 when the capability, protocol family or auth config name is unknown. 
     * Create endpoint binding
     */
    async createEndpointBinding(requestParameters: CreateEndpointBindingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EndpointBindingView> {
        const response = await this.createEndpointBindingRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes an endpoint binding, which disables that one capability for the vendor profile. Use this tool when a capability is retired for a supplier; to pause it while keeping the configuration, call updateEndpointBinding with enabled cleared instead, since a disabled binding behaves exactly as an absent one. Preconditions: the profile must exist and be ADMIN-managed, and the binding must belong to it. Required inputs: vendorProfileId and bindingId (UUIDv7) path parameters; there is no request body. Emits a SUPPLIER_BINDING_DELETE audit event; the capability then resolves to the typed CAPABILITY_NOT_CONFIGURED outcome, and exchange audit rows already written are retained. Returns 404 when the profile or binding does not exist, and 409 when the profile is YAML-managed. 
     * Delete endpoint binding
     */
    async deleteEndpointBindingRaw(requestParameters: DeleteEndpointBindingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['vendorProfileId'] == null) {
            throw new runtime.RequiredError(
                'vendorProfileId',
                'Required parameter "vendorProfileId" was null or undefined when calling deleteEndpointBinding().'
            );
        }

        if (requestParameters['bindingId'] == null) {
            throw new runtime.RequiredError(
                'bindingId',
                'Required parameter "bindingId" was null or undefined when calling deleteEndpointBinding().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/profiles/{vendorProfileId}/bindings/{bindingId}`.replace(`{${"vendorProfileId"}}`, encodeURIComponent(String(requestParameters['vendorProfileId']))).replace(`{${"bindingId"}}`, encodeURIComponent(String(requestParameters['bindingId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes an endpoint binding, which disables that one capability for the vendor profile. Use this tool when a capability is retired for a supplier; to pause it while keeping the configuration, call updateEndpointBinding with enabled cleared instead, since a disabled binding behaves exactly as an absent one. Preconditions: the profile must exist and be ADMIN-managed, and the binding must belong to it. Required inputs: vendorProfileId and bindingId (UUIDv7) path parameters; there is no request body. Emits a SUPPLIER_BINDING_DELETE audit event; the capability then resolves to the typed CAPABILITY_NOT_CONFIGURED outcome, and exchange audit rows already written are retained. Returns 404 when the profile or binding does not exist, and 409 when the profile is YAML-managed. 
     * Delete endpoint binding
     */
    async deleteEndpointBinding(requestParameters: DeleteEndpointBindingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteEndpointBindingRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the capability endpoint bindings of a vendor profile, ordered by capability, each with its protocol family, version, URL, auth config name and enabled flag. Use this tool to see which capabilities a supplier actually resolves; do not infer availability from the profile itself, because an absent or disabled binding disables just that capability. Preconditions: the vendor profile must exist. Required inputs: vendorProfileId (UUIDv7) path parameter; there is no request body or filtering. Emits a SUPPLIER_BINDING_LIST audit event; no configuration is changed. Returns 404 when the vendor profile does not exist, and 200 with an empty array when the profile has no bindings, which means every capability is unconfigured. 
     * List endpoint bindings
     */
    async listEndpointBindingsRaw(requestParameters: ListEndpointBindingsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<EndpointBindingView>>> {
        if (requestParameters['vendorProfileId'] == null) {
            throw new runtime.RequiredError(
                'vendorProfileId',
                'Required parameter "vendorProfileId" was null or undefined when calling listEndpointBindings().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/profiles/{vendorProfileId}/bindings`.replace(`{${"vendorProfileId"}}`, encodeURIComponent(String(requestParameters['vendorProfileId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(EndpointBindingViewFromJSON));
    }

    /**
     * Returns the capability endpoint bindings of a vendor profile, ordered by capability, each with its protocol family, version, URL, auth config name and enabled flag. Use this tool to see which capabilities a supplier actually resolves; do not infer availability from the profile itself, because an absent or disabled binding disables just that capability. Preconditions: the vendor profile must exist. Required inputs: vendorProfileId (UUIDv7) path parameter; there is no request body or filtering. Emits a SUPPLIER_BINDING_LIST audit event; no configuration is changed. Returns 404 when the vendor profile does not exist, and 200 with an empty array when the profile has no bindings, which means every capability is unconfigured. 
     * List endpoint bindings
     */
    async listEndpointBindings(requestParameters: ListEndpointBindingsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<EndpointBindingView>> {
        const response = await this.listEndpointBindingsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Replaces every settable field of an endpoint binding, including its capability, adapter version, URL, auth config, schedule, enabled flag and audit capture level. Use this tool to repoint a capability or take it out of service by clearing enabled; do not use it to bind a second capability, which is createEndpointBinding. Preconditions: the profile must exist and be ADMIN-managed, the binding must belong to it, and a changed capability must not already be bound elsewhere on the profile. Required inputs: vendorProfileId and bindingId (UUIDv7) path parameters plus the full body, because every field is replaced; omitting captureLevel resets it to the deployment default rather than leaving the stored value. Emits a SUPPLIER_BINDING_UPDATE audit event; a disabled binding immediately reports the typed CAPABILITY_NOT_CONFIGURED outcome with HTTP 200 rather than failing. Returns 404 when the profile or binding does not exist, 409 when the profile is YAML-managed or the capability is already bound, and 400 when the capability, protocol family or auth config name is unknown. 
     * Update endpoint binding
     */
    async updateEndpointBindingRaw(requestParameters: UpdateEndpointBindingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EndpointBindingView>> {
        if (requestParameters['vendorProfileId'] == null) {
            throw new runtime.RequiredError(
                'vendorProfileId',
                'Required parameter "vendorProfileId" was null or undefined when calling updateEndpointBinding().'
            );
        }

        if (requestParameters['bindingId'] == null) {
            throw new runtime.RequiredError(
                'bindingId',
                'Required parameter "bindingId" was null or undefined when calling updateEndpointBinding().'
            );
        }

        if (requestParameters['endpointBindingRequest'] == null) {
            throw new runtime.RequiredError(
                'endpointBindingRequest',
                'Required parameter "endpointBindingRequest" was null or undefined when calling updateEndpointBinding().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/profiles/{vendorProfileId}/bindings/{bindingId}`.replace(`{${"vendorProfileId"}}`, encodeURIComponent(String(requestParameters['vendorProfileId']))).replace(`{${"bindingId"}}`, encodeURIComponent(String(requestParameters['bindingId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: EndpointBindingRequestToJSON(requestParameters['endpointBindingRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EndpointBindingViewFromJSON(jsonValue));
    }

    /**
     * Replaces every settable field of an endpoint binding, including its capability, adapter version, URL, auth config, schedule, enabled flag and audit capture level. Use this tool to repoint a capability or take it out of service by clearing enabled; do not use it to bind a second capability, which is createEndpointBinding. Preconditions: the profile must exist and be ADMIN-managed, the binding must belong to it, and a changed capability must not already be bound elsewhere on the profile. Required inputs: vendorProfileId and bindingId (UUIDv7) path parameters plus the full body, because every field is replaced; omitting captureLevel resets it to the deployment default rather than leaving the stored value. Emits a SUPPLIER_BINDING_UPDATE audit event; a disabled binding immediately reports the typed CAPABILITY_NOT_CONFIGURED outcome with HTTP 200 rather than failing. Returns 404 when the profile or binding does not exist, 409 when the profile is YAML-managed or the capability is already bound, and 400 when the capability, protocol family or auth config name is unknown. 
     * Update endpoint binding
     */
    async updateEndpointBinding(requestParameters: UpdateEndpointBindingRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EndpointBindingView> {
        const response = await this.updateEndpointBindingRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
