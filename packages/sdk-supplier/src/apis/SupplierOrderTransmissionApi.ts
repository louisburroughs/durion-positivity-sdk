/* tslint:disable */
/* eslint-disable */
/**
 * Supplier API
 * API for supplier vendor profile administration — profiles, auth configs, commercial accounts, and capability endpoint bindings (ADR-0050)
 *
 * The version of the OpenAPI document: 1.0
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  OrderTransmissionStatus,
  TransmissionResolutionRequest,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    OrderTransmissionStatusFromJSON,
    OrderTransmissionStatusToJSON,
    TransmissionResolutionRequestFromJSON,
    TransmissionResolutionRequestToJSON,
} from '../models/index';

export interface GetSupplierTransmissionRequest {
    transmissionIntentId: string;
}

export interface ListSupplierTransmissionsForPurchaseOrderRequest {
    purchaseOrderId: string;
}

export interface ResolveSupplierTransmissionRequest {
    transmissionIntentId: string;
    transmissionResolutionRequest: TransmissionResolutionRequest;
}

/**
 * 
 */
export class SupplierOrderTransmissionApi extends runtime.BaseAPI {

    /**
     * Returns the current state of one transmission intent, including the wire document id an operator would quote to a vendor and, when the transmission is stuck, why. Use this tool when working a single transmission; do not use it to survey a purchase order\'s transmission history, which is listSupplierTransmissionsForPurchaseOrder instead. Preconditions: the caller must hold supplier:transmission:read and the transmission intent must exist; a transmission is created only by pos-supplier consuming a supplier.order.requested command, never by this API. Required inputs: transmissionIntentId (UUIDv7) as a path parameter; there is no request body. Emits a SUPPLIER_TRANSMISSION_GET event. Read-only. Returns 200 with the transmission, or 404 when no transmission exists with that id. 
     * Get one vendor transmission
     */
    async getSupplierTransmissionRaw(requestParameters: GetSupplierTransmissionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OrderTransmissionStatus>> {
        if (requestParameters['transmissionIntentId'] == null) {
            throw new runtime.RequiredError(
                'transmissionIntentId',
                'Required parameter "transmissionIntentId" was null or undefined when calling getSupplierTransmission().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/transmissions/{transmissionIntentId}`.replace(`{${"transmissionIntentId"}}`, encodeURIComponent(String(requestParameters['transmissionIntentId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OrderTransmissionStatusFromJSON(jsonValue));
    }

    /**
     * Returns the current state of one transmission intent, including the wire document id an operator would quote to a vendor and, when the transmission is stuck, why. Use this tool when working a single transmission; do not use it to survey a purchase order\'s transmission history, which is listSupplierTransmissionsForPurchaseOrder instead. Preconditions: the caller must hold supplier:transmission:read and the transmission intent must exist; a transmission is created only by pos-supplier consuming a supplier.order.requested command, never by this API. Required inputs: transmissionIntentId (UUIDv7) as a path parameter; there is no request body. Emits a SUPPLIER_TRANSMISSION_GET event. Read-only. Returns 200 with the transmission, or 404 when no transmission exists with that id. 
     * Get one vendor transmission
     */
    async getSupplierTransmission(requestParameters: GetSupplierTransmissionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OrderTransmissionStatus> {
        const response = await this.getSupplierTransmissionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every transmission intent recorded for a purchase order — initial, revision, replacement and split — newest first, with what the vendor answered and, where a transmission is stuck, what a human needs to resolve it. Use this tool to answer \"did this order actually reach the vendor, and what did they say\"; do not use it to read delivery schedules or despatches, which reach the ordering domain as supplier.orderstatus.changed events and belong on the purchase-order timeline. Preconditions: the caller must hold supplier:transmission:read; the purchase order need not exist in this module, and an order that was never transmitted is a normal empty answer. Required inputs: purchaseOrderId (UUIDv7) as a path parameter; there is no request body. Emits a SUPPLIER_TRANSMISSION_LIST event. Read-only: nothing is transmitted, retried or changed. Returns 200 with an empty array when the purchase order was never transmitted — that is a normal answer, not a 404. 
     * List the vendor transmissions of one purchase order
     */
    async listSupplierTransmissionsForPurchaseOrderRaw(requestParameters: ListSupplierTransmissionsForPurchaseOrderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<OrderTransmissionStatus>>> {
        if (requestParameters['purchaseOrderId'] == null) {
            throw new runtime.RequiredError(
                'purchaseOrderId',
                'Required parameter "purchaseOrderId" was null or undefined when calling listSupplierTransmissionsForPurchaseOrder().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/transmissions/purchase-order/{purchaseOrderId}`.replace(`{${"purchaseOrderId"}}`, encodeURIComponent(String(requestParameters['purchaseOrderId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(OrderTransmissionStatusFromJSON));
    }

    /**
     * Returns every transmission intent recorded for a purchase order — initial, revision, replacement and split — newest first, with what the vendor answered and, where a transmission is stuck, what a human needs to resolve it. Use this tool to answer \"did this order actually reach the vendor, and what did they say\"; do not use it to read delivery schedules or despatches, which reach the ordering domain as supplier.orderstatus.changed events and belong on the purchase-order timeline. Preconditions: the caller must hold supplier:transmission:read; the purchase order need not exist in this module, and an order that was never transmitted is a normal empty answer. Required inputs: purchaseOrderId (UUIDv7) as a path parameter; there is no request body. Emits a SUPPLIER_TRANSMISSION_LIST event. Read-only: nothing is transmitted, retried or changed. Returns 200 with an empty array when the purchase order was never transmitted — that is a normal answer, not a 404. 
     * List the vendor transmissions of one purchase order
     */
    async listSupplierTransmissionsForPurchaseOrder(requestParameters: ListSupplierTransmissionsForPurchaseOrderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<OrderTransmissionStatus>> {
        const response = await this.listSupplierTransmissionsForPurchaseOrderRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Applies one of the three ADR-0052 §4 resolutions to a transmission whose outcome could not be established automatically: confirm-with-vendor-reference (the vendor accepted it), mark-not-received (the vendor never got it — the intent ends and is never re-sent), or cancel. Use this tool only after establishing the facts with the vendor; do not use it to retry a transmission, which no endpoint offers because a blind re-send is how a purchase order becomes two deliveries. Re-ordering after mark-not-received is a fresh request from the ordering domain, which mints a new transmission intent and a new vendor document id. Preconditions: the caller must hold supplier:transmission:resolve, the transmission must exist and be in state MANUAL_REVIEW, and the operator must have established the facts with the vendor — every other state either has a definite outcome already or is still being worked on automatically. Required inputs: transmissionIntentId (UUIDv7) as a path parameter, and a body carrying the action plus free-text evidence; supplierOrderNumber is additionally required when confirming. Emits a SUPPLIER_TRANSMISSION_RESOLVE event, records the actor and their evidence on the transmission, and publishes supplier.order.confirmed or supplier.order.rejected for the purchase-order timeline. The change is permanent — resolved transmissions are terminal. Returns 200 with the resolved transmission, 400 when the evidence or vendor reference is missing, 404 when no such transmission exists, and 409 when the transmission is not awaiting manual review. 
     * Resolve a transmission awaiting manual review
     */
    async resolveSupplierTransmissionRaw(requestParameters: ResolveSupplierTransmissionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OrderTransmissionStatus>> {
        if (requestParameters['transmissionIntentId'] == null) {
            throw new runtime.RequiredError(
                'transmissionIntentId',
                'Required parameter "transmissionIntentId" was null or undefined when calling resolveSupplierTransmission().'
            );
        }

        if (requestParameters['transmissionResolutionRequest'] == null) {
            throw new runtime.RequiredError(
                'transmissionResolutionRequest',
                'Required parameter "transmissionResolutionRequest" was null or undefined when calling resolveSupplierTransmission().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/transmissions/{transmissionIntentId}/resolve`.replace(`{${"transmissionIntentId"}}`, encodeURIComponent(String(requestParameters['transmissionIntentId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: TransmissionResolutionRequestToJSON(requestParameters['transmissionResolutionRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OrderTransmissionStatusFromJSON(jsonValue));
    }

    /**
     * Applies one of the three ADR-0052 §4 resolutions to a transmission whose outcome could not be established automatically: confirm-with-vendor-reference (the vendor accepted it), mark-not-received (the vendor never got it — the intent ends and is never re-sent), or cancel. Use this tool only after establishing the facts with the vendor; do not use it to retry a transmission, which no endpoint offers because a blind re-send is how a purchase order becomes two deliveries. Re-ordering after mark-not-received is a fresh request from the ordering domain, which mints a new transmission intent and a new vendor document id. Preconditions: the caller must hold supplier:transmission:resolve, the transmission must exist and be in state MANUAL_REVIEW, and the operator must have established the facts with the vendor — every other state either has a definite outcome already or is still being worked on automatically. Required inputs: transmissionIntentId (UUIDv7) as a path parameter, and a body carrying the action plus free-text evidence; supplierOrderNumber is additionally required when confirming. Emits a SUPPLIER_TRANSMISSION_RESOLVE event, records the actor and their evidence on the transmission, and publishes supplier.order.confirmed or supplier.order.rejected for the purchase-order timeline. The change is permanent — resolved transmissions are terminal. Returns 200 with the resolved transmission, 400 when the evidence or vendor reference is missing, 404 when no such transmission exists, and 409 when the transmission is not awaiting manual review. 
     * Resolve a transmission awaiting manual review
     */
    async resolveSupplierTransmission(requestParameters: ResolveSupplierTransmissionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OrderTransmissionStatus> {
        const response = await this.resolveSupplierTransmissionRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
