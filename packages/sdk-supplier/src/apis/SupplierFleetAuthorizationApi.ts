/* tslint:disable */
/* eslint-disable */
/**
 * Supplier API
 * API for supplier vendor profile administration — profiles, auth configs, commercial accounts, and capability endpoint bindings (ADR-0050)
 *
 * The version of the OpenAPI document: 1.0
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  FleetAuthorizationRequestBody,
  FleetAuthorizationResponse,
  FleetContract,
  FleetPolicy,
  FleetVehicle,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    FleetAuthorizationRequestBodyFromJSON,
    FleetAuthorizationRequestBodyToJSON,
    FleetAuthorizationResponseFromJSON,
    FleetAuthorizationResponseToJSON,
    FleetContractFromJSON,
    FleetContractToJSON,
    FleetPolicyFromJSON,
    FleetPolicyToJSON,
    FleetVehicleFromJSON,
    FleetVehicleToJSON,
} from '../models/index';

export interface GetFleetWorkorderAuthorizationRequest {
    supplierRef: string;
    workorderId: string;
}

export interface ListFleetAuthorizationsNeedingReviewRequest {
    limit?: number;
}

export interface ListFleetContractsRequest {
    supplierRef: string;
}

export interface ListFleetPoliciesRequest {
    supplierRef: string;
}

export interface LookupFleetVehicleRequest {
    supplierRef: string;
    vehicleIdent: string;
}

export interface RequestFleetWorkorderAuthorizationRequest {
    supplierRef: string;
    fleetAuthorizationRequestBody: FleetAuthorizationRequestBody;
}

/**
 * 
 */
export class SupplierFleetAuthorizationApi extends runtime.BaseAPI {

    /**
     * Returns the fleet authorization recorded for a workorder at a vendor, including the vendor\'s reason where it gave one. Use this tool to check the outcome of an authorization that came back PENDING; do not call requestFleetWorkorderAuthorization again for that, because it can open a second authorization. Preconditions: none. Required inputs: supplierRef and workorderId path parameters. Emits a SUPPLIER_WORKORDER_AUTHORIZATION_READ event. Returns 200 with the authorization, and 404 when the vendor profile is unknown or no authorization was ever requested for this workorder. 
     * Read A Fleet Authorization
     */
    async getFleetWorkorderAuthorizationRaw(requestParameters: GetFleetWorkorderAuthorizationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FleetAuthorizationResponse>> {
        if (requestParameters['supplierRef'] == null) {
            throw new runtime.RequiredError(
                'supplierRef',
                'Required parameter "supplierRef" was null or undefined when calling getFleetWorkorderAuthorization().'
            );
        }

        if (requestParameters['workorderId'] == null) {
            throw new runtime.RequiredError(
                'workorderId',
                'Required parameter "workorderId" was null or undefined when calling getFleetWorkorderAuthorization().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["supplier:workorderauth:request"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/fleet/{supplierRef}/authorizations/{workorderId}`.replace(`{${"supplierRef"}}`, encodeURIComponent(String(requestParameters['supplierRef']))).replace(`{${"workorderId"}}`, encodeURIComponent(String(requestParameters['workorderId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FleetAuthorizationResponseFromJSON(jsonValue));
    }

    /**
     * Returns the fleet authorization recorded for a workorder at a vendor, including the vendor\'s reason where it gave one. Use this tool to check the outcome of an authorization that came back PENDING; do not call requestFleetWorkorderAuthorization again for that, because it can open a second authorization. Preconditions: none. Required inputs: supplierRef and workorderId path parameters. Emits a SUPPLIER_WORKORDER_AUTHORIZATION_READ event. Returns 200 with the authorization, and 404 when the vendor profile is unknown or no authorization was ever requested for this workorder. 
     * Read A Fleet Authorization
     */
    async getFleetWorkorderAuthorization(requestParameters: GetFleetWorkorderAuthorizationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FleetAuthorizationResponse> {
        const response = await this.getFleetWorkorderAuthorizationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists fleet authorizations and completions that could not be resolved with a vendor and are waiting on a person, newest first. Use this tool to work the queue of stuck fleet jobs; the reviewReason says what went wrong and approvalStatus distinguishes an authorization nobody could obtain from a completion nobody could get signed off. Do not read a row here as a vendor decision — nothing in this list has been refused by a fleet, only left unresolved — and do not use it to check one workorder, because getFleetWorkorderAuthorization answers that directly instead. Preconditions: none. Required inputs: none; limit defaults to 100. Emits a SUPPLIER_WORKORDER_AUTHORIZATION_REVIEW_LIST event. Returns 200 with the rows waiting on a person, which is an empty list when nothing is stuck. 
     * List Fleet Authorizations Needing Review
     */
    async listFleetAuthorizationsNeedingReviewRaw(requestParameters: ListFleetAuthorizationsNeedingReviewRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<FleetAuthorizationResponse>>> {
        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["supplier:workorderauth:review"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/fleet/authorizations/reviews`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(FleetAuthorizationResponseFromJSON));
    }

    /**
     * Lists fleet authorizations and completions that could not be resolved with a vendor and are waiting on a person, newest first. Use this tool to work the queue of stuck fleet jobs; the reviewReason says what went wrong and approvalStatus distinguishes an authorization nobody could obtain from a completion nobody could get signed off. Do not read a row here as a vendor decision — nothing in this list has been refused by a fleet, only left unresolved — and do not use it to check one workorder, because getFleetWorkorderAuthorization answers that directly instead. Preconditions: none. Required inputs: none; limit defaults to 100. Emits a SUPPLIER_WORKORDER_AUTHORIZATION_REVIEW_LIST event. Returns 200 with the rows waiting on a person, which is an empty list when nothing is stuck. 
     * List Fleet Authorizations Needing Review
     */
    async listFleetAuthorizationsNeedingReview(requestParameters: ListFleetAuthorizationsNeedingReviewRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<FleetAuthorizationResponse>> {
        const response = await this.listFleetAuthorizationsNeedingReviewRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the fleet contracts this service provider may claim work under at a vendor. Use this tool to populate a contract choice before requesting authorization; do not use the contract dates to decide whether work is covered, because only the vendor decides that when authorization is requested. Preconditions: the vendor profile must be enabled with a workorder-authorization binding. Required inputs: supplierRef path parameter. Emits a SUPPLIER_FLEET_CONTRACT_LIST event. Returns 200 with the contracts as stated, 404 when the vendor profile is unknown, 409 when the profile is disabled or unconfigured, and 422 when the vendor could not be reached. 
     * List Fleet Contracts
     */
    async listFleetContractsRaw(requestParameters: ListFleetContractsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<FleetContract>>> {
        if (requestParameters['supplierRef'] == null) {
            throw new runtime.RequiredError(
                'supplierRef',
                'Required parameter "supplierRef" was null or undefined when calling listFleetContracts().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["supplier:workorderauth:request"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/fleet/{supplierRef}/contracts`.replace(`{${"supplierRef"}}`, encodeURIComponent(String(requestParameters['supplierRef']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(FleetContractFromJSON));
    }

    /**
     * Lists the fleet contracts this service provider may claim work under at a vendor. Use this tool to populate a contract choice before requesting authorization; do not use the contract dates to decide whether work is covered, because only the vendor decides that when authorization is requested. Preconditions: the vendor profile must be enabled with a workorder-authorization binding. Required inputs: supplierRef path parameter. Emits a SUPPLIER_FLEET_CONTRACT_LIST event. Returns 200 with the contracts as stated, 404 when the vendor profile is unknown, 409 when the profile is disabled or unconfigured, and 422 when the vendor could not be reached. 
     * List Fleet Contracts
     */
    async listFleetContracts(requestParameters: ListFleetContractsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<FleetContract>> {
        const response = await this.listFleetContractsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists what a fleet program says it will and will not pay for. Use this tool to narrow what is offered to a fleet customer; do not use it as permission, because it is advisory and only requestFleetWorkorderAuthorization obtains a decision. Preconditions: the vendor profile must be enabled with a workorder-authorization binding. Required inputs: supplierRef path parameter. Emits a SUPPLIER_FLEET_POLICY_LIST event. Returns 200 with the policies as stated — an empty list when the vendor states none or answers in a shape this adapter does not recognise — 404 when the vendor profile is unknown, 409 when the profile is disabled or unconfigured, and 422 when the vendor could not be reached. 
     * List Fleet Policies
     */
    async listFleetPoliciesRaw(requestParameters: ListFleetPoliciesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<FleetPolicy>>> {
        if (requestParameters['supplierRef'] == null) {
            throw new runtime.RequiredError(
                'supplierRef',
                'Required parameter "supplierRef" was null or undefined when calling listFleetPolicies().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["supplier:workorderauth:request"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/fleet/{supplierRef}/policies`.replace(`{${"supplierRef"}}`, encodeURIComponent(String(requestParameters['supplierRef']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(FleetPolicyFromJSON));
    }

    /**
     * Lists what a fleet program says it will and will not pay for. Use this tool to narrow what is offered to a fleet customer; do not use it as permission, because it is advisory and only requestFleetWorkorderAuthorization obtains a decision. Preconditions: the vendor profile must be enabled with a workorder-authorization binding. Required inputs: supplierRef path parameter. Emits a SUPPLIER_FLEET_POLICY_LIST event. Returns 200 with the policies as stated — an empty list when the vendor states none or answers in a shape this adapter does not recognise — 404 when the vendor profile is unknown, 409 when the profile is disabled or unconfigured, and 422 when the vendor could not be reached. 
     * List Fleet Policies
     */
    async listFleetPolicies(requestParameters: ListFleetPoliciesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<FleetPolicy>> {
        const response = await this.listFleetPoliciesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Asks a vendor\'s fleet program what it knows about a vehicle, by license plate, VIN, fleet number or the vendor\'s own vehicle id. Use this tool before requesting authorization, to confirm the vehicle is on a fleet contract and to obtain the vendor vehicle id; do not treat the answer as authorization, because only requestFleetWorkorderAuthorization obtains that. Preconditions: the vendor profile must be enabled with a workorder-authorization binding. Required inputs: supplierRef and vehicleIdent path parameters. Emits a SUPPLIER_FLEET_VEHICLE_LOOKUP event. Returns 200 with the vehicle as the fleet holds it, 404 when the fleet does not know the vehicle or the vendor profile is unknown, 409 when the profile is disabled or unconfigured, and 422 when the vendor could not be reached or answered unreadably. 
     * Look Up A Fleet Vehicle
     */
    async lookupFleetVehicleRaw(requestParameters: LookupFleetVehicleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FleetVehicle>> {
        if (requestParameters['supplierRef'] == null) {
            throw new runtime.RequiredError(
                'supplierRef',
                'Required parameter "supplierRef" was null or undefined when calling lookupFleetVehicle().'
            );
        }

        if (requestParameters['vehicleIdent'] == null) {
            throw new runtime.RequiredError(
                'vehicleIdent',
                'Required parameter "vehicleIdent" was null or undefined when calling lookupFleetVehicle().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["supplier:workorderauth:request"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/fleet/{supplierRef}/vehicles/{vehicleIdent}`.replace(`{${"supplierRef"}}`, encodeURIComponent(String(requestParameters['supplierRef']))).replace(`{${"vehicleIdent"}}`, encodeURIComponent(String(requestParameters['vehicleIdent']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FleetVehicleFromJSON(jsonValue));
    }

    /**
     * Asks a vendor\'s fleet program what it knows about a vehicle, by license plate, VIN, fleet number or the vendor\'s own vehicle id. Use this tool before requesting authorization, to confirm the vehicle is on a fleet contract and to obtain the vendor vehicle id; do not treat the answer as authorization, because only requestFleetWorkorderAuthorization obtains that. Preconditions: the vendor profile must be enabled with a workorder-authorization binding. Required inputs: supplierRef and vehicleIdent path parameters. Emits a SUPPLIER_FLEET_VEHICLE_LOOKUP event. Returns 200 with the vehicle as the fleet holds it, 404 when the fleet does not know the vehicle or the vendor profile is unknown, 409 when the profile is disabled or unconfigured, and 422 when the vendor could not be reached or answered unreadably. 
     * Look Up A Fleet Vehicle
     */
    async lookupFleetVehicle(requestParameters: LookupFleetVehicleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FleetVehicle> {
        const response = await this.lookupFleetVehicleRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Asks a vendor\'s fleet program to authorize the work on a workorder, and records what it said. Use this tool once the vehicle and the work are known; do not call it repeatedly to check for an answer, because asking again can open a second authorization against the same contract — use getFleetWorkorderAuthorization to read the outcome instead. Preconditions: the vendor profile must be enabled with a workorder-authorization binding and a billing account number, and the request must identify the vehicle by vendor vehicle id, license plate, VIN or fleet number. Required inputs: supplierRef path parameter, workorderId, and at least one vehicle identifier; contractId, odometerValue and lines are optional. Emits a SUPPLIER_WORKORDER_AUTHORIZATION_REQUEST event, and on a decision publishes supplier.workorderauth.granted or supplier.workorderauth.denied for the workorder domain. Returns 200 with the authorization, whose status is GRANTED, DENIED, NOT_FOUND, PENDING when the vendor accepted the request and will decide later, or MANUAL_REVIEW when no decision could be obtained at all; 400 when no vehicle identifier is given; 404 when the vendor profile is unknown; and 409 when the profile is disabled or has no workorder-authorization binding configured. 
     * Request Fleet Authorization For A Workorder
     */
    async requestFleetWorkorderAuthorizationRaw(requestParameters: RequestFleetWorkorderAuthorizationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FleetAuthorizationResponse>> {
        if (requestParameters['supplierRef'] == null) {
            throw new runtime.RequiredError(
                'supplierRef',
                'Required parameter "supplierRef" was null or undefined when calling requestFleetWorkorderAuthorization().'
            );
        }

        if (requestParameters['fleetAuthorizationRequestBody'] == null) {
            throw new runtime.RequiredError(
                'fleetAuthorizationRequestBody',
                'Required parameter "fleetAuthorizationRequestBody" was null or undefined when calling requestFleetWorkorderAuthorization().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["supplier:workorderauth:request"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/fleet/{supplierRef}/authorizations`.replace(`{${"supplierRef"}}`, encodeURIComponent(String(requestParameters['supplierRef']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: FleetAuthorizationRequestBodyToJSON(requestParameters['fleetAuthorizationRequestBody']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FleetAuthorizationResponseFromJSON(jsonValue));
    }

    /**
     * Asks a vendor\'s fleet program to authorize the work on a workorder, and records what it said. Use this tool once the vehicle and the work are known; do not call it repeatedly to check for an answer, because asking again can open a second authorization against the same contract — use getFleetWorkorderAuthorization to read the outcome instead. Preconditions: the vendor profile must be enabled with a workorder-authorization binding and a billing account number, and the request must identify the vehicle by vendor vehicle id, license plate, VIN or fleet number. Required inputs: supplierRef path parameter, workorderId, and at least one vehicle identifier; contractId, odometerValue and lines are optional. Emits a SUPPLIER_WORKORDER_AUTHORIZATION_REQUEST event, and on a decision publishes supplier.workorderauth.granted or supplier.workorderauth.denied for the workorder domain. Returns 200 with the authorization, whose status is GRANTED, DENIED, NOT_FOUND, PENDING when the vendor accepted the request and will decide later, or MANUAL_REVIEW when no decision could be obtained at all; 400 when no vehicle identifier is given; 404 when the vendor profile is unknown; and 409 when the profile is disabled or has no workorder-authorization binding configured. 
     * Request Fleet Authorization For A Workorder
     */
    async requestFleetWorkorderAuthorization(requestParameters: RequestFleetWorkorderAuthorizationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FleetAuthorizationResponse> {
        const response = await this.requestFleetWorkorderAuthorizationRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
