/* tslint:disable */
/* eslint-disable */
/**
 * Supplier API
 * API for supplier vendor profile administration — profiles, auth configs, commercial accounts, and capability endpoint bindings (ADR-0050)
 *
 * The version of the OpenAPI document: 1.0
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  CommercialAccountRequest,
  CommercialAccountView,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    CommercialAccountRequestFromJSON,
    CommercialAccountRequestToJSON,
    CommercialAccountViewFromJSON,
    CommercialAccountViewToJSON,
} from '../models/index';

export interface CreateCommercialAccountRequest {
    vendorProfileId: string;
    commercialAccountRequest: CommercialAccountRequest;
}

export interface DeleteCommercialAccountRequest {
    vendorProfileId: string;
    accountId: string;
}

export interface ListCommercialAccountsRequest {
    vendorProfileId: string;
}

export interface UpdateCommercialAccountRequest {
    vendorProfileId: string;
    accountId: string;
    commercialAccountRequest: CommercialAccountRequest;
}

/**
 * 
 */
export class SupplierCommercialAccountsApi extends runtime.BaseAPI {

    /**
     * Adds a commercial account to a vendor profile, either the profile\'s single BILLING account or a DELIVERY account mapping one pos-location to its vendor account number. Use this tool when a location is first authorised to order from a supplier; do not use it to correct an account number, which is updateCommercialAccount. Preconditions: the vendor profile must exist and be ADMIN-managed, and the slot must be free — one BILLING account per profile and one DELIVERY account per profile and location pair. Required inputs: vendorProfileId (UUIDv7) path parameter plus role and accountNumber in the body; deliveryLocationId is required for DELIVERY and must be absent for BILLING, and agencyCode is optional. Emits a SUPPLIER_ACCOUNT_CREATE audit event; no order or invoice records are touched. Returns 404 when the profile does not exist, 409 when the profile is YAML-managed or the account slot is already taken, and 400 when accountNumber is blank or deliveryLocationId does not match the role. 
     * Create commercial account
     */
    async createCommercialAccountRaw(requestParameters: CreateCommercialAccountRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CommercialAccountView>> {
        if (requestParameters['vendorProfileId'] == null) {
            throw new runtime.RequiredError(
                'vendorProfileId',
                'Required parameter "vendorProfileId" was null or undefined when calling createCommercialAccount().'
            );
        }

        if (requestParameters['commercialAccountRequest'] == null) {
            throw new runtime.RequiredError(
                'commercialAccountRequest',
                'Required parameter "commercialAccountRequest" was null or undefined when calling createCommercialAccount().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/profiles/{vendorProfileId}/accounts`.replace(`{${"vendorProfileId"}}`, encodeURIComponent(String(requestParameters['vendorProfileId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CommercialAccountRequestToJSON(requestParameters['commercialAccountRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CommercialAccountViewFromJSON(jsonValue));
    }

    /**
     * Adds a commercial account to a vendor profile, either the profile\'s single BILLING account or a DELIVERY account mapping one pos-location to its vendor account number. Use this tool when a location is first authorised to order from a supplier; do not use it to correct an account number, which is updateCommercialAccount. Preconditions: the vendor profile must exist and be ADMIN-managed, and the slot must be free — one BILLING account per profile and one DELIVERY account per profile and location pair. Required inputs: vendorProfileId (UUIDv7) path parameter plus role and accountNumber in the body; deliveryLocationId is required for DELIVERY and must be absent for BILLING, and agencyCode is optional. Emits a SUPPLIER_ACCOUNT_CREATE audit event; no order or invoice records are touched. Returns 404 when the profile does not exist, 409 when the profile is YAML-managed or the account slot is already taken, and 400 when accountNumber is blank or deliveryLocationId does not match the role. 
     * Create commercial account
     */
    async createCommercialAccount(requestParameters: CreateCommercialAccountRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CommercialAccountView> {
        const response = await this.createCommercialAccountRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes a commercial account from a vendor profile, freeing its BILLING or per-location DELIVERY slot. Use this tool when a location stops ordering from the supplier; to change the account number instead, call updateCommercialAccount, which preserves the slot. Preconditions: the profile must exist and be ADMIN-managed, and the account must belong to that profile. Required inputs: vendorProfileId and accountId (UUIDv7) path parameters; there is no request body. Emits a SUPPLIER_ACCOUNT_DELETE audit event; ordering for the mapped location resolves to a not-configured outcome from that point on. Returns 404 when the profile or account does not exist, and 409 when the profile is YAML-managed. 
     * Delete commercial account
     */
    async deleteCommercialAccountRaw(requestParameters: DeleteCommercialAccountRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['vendorProfileId'] == null) {
            throw new runtime.RequiredError(
                'vendorProfileId',
                'Required parameter "vendorProfileId" was null or undefined when calling deleteCommercialAccount().'
            );
        }

        if (requestParameters['accountId'] == null) {
            throw new runtime.RequiredError(
                'accountId',
                'Required parameter "accountId" was null or undefined when calling deleteCommercialAccount().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/profiles/{vendorProfileId}/accounts/{accountId}`.replace(`{${"vendorProfileId"}}`, encodeURIComponent(String(requestParameters['vendorProfileId']))).replace(`{${"accountId"}}`, encodeURIComponent(String(requestParameters['accountId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes a commercial account from a vendor profile, freeing its BILLING or per-location DELIVERY slot. Use this tool when a location stops ordering from the supplier; to change the account number instead, call updateCommercialAccount, which preserves the slot. Preconditions: the profile must exist and be ADMIN-managed, and the account must belong to that profile. Required inputs: vendorProfileId and accountId (UUIDv7) path parameters; there is no request body. Emits a SUPPLIER_ACCOUNT_DELETE audit event; ordering for the mapped location resolves to a not-configured outcome from that point on. Returns 404 when the profile or account does not exist, and 409 when the profile is YAML-managed. 
     * Delete commercial account
     */
    async deleteCommercialAccount(requestParameters: DeleteCommercialAccountRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteCommercialAccountRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the commercial accounts configured on a vendor profile, the BILLING account first and then the per-location DELIVERY accounts. Use this tool to find the accountId or the vendor account number for a location; do not use it to read credentials, which are never returned here because account numbers are ordinary configuration data. Preconditions: the vendor profile must exist. Required inputs: vendorProfileId (UUIDv7) path parameter; there is no request body or filtering. Emits a SUPPLIER_ACCOUNT_LIST audit event; no configuration is changed. Returns 404 when the vendor profile does not exist, and 200 with an empty array when the profile exists but has no accounts. 
     * List commercial accounts
     */
    async listCommercialAccountsRaw(requestParameters: ListCommercialAccountsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<CommercialAccountView>>> {
        if (requestParameters['vendorProfileId'] == null) {
            throw new runtime.RequiredError(
                'vendorProfileId',
                'Required parameter "vendorProfileId" was null or undefined when calling listCommercialAccounts().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/profiles/{vendorProfileId}/accounts`.replace(`{${"vendorProfileId"}}`, encodeURIComponent(String(requestParameters['vendorProfileId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(CommercialAccountViewFromJSON));
    }

    /**
     * Returns the commercial accounts configured on a vendor profile, the BILLING account first and then the per-location DELIVERY accounts. Use this tool to find the accountId or the vendor account number for a location; do not use it to read credentials, which are never returned here because account numbers are ordinary configuration data. Preconditions: the vendor profile must exist. Required inputs: vendorProfileId (UUIDv7) path parameter; there is no request body or filtering. Emits a SUPPLIER_ACCOUNT_LIST audit event; no configuration is changed. Returns 404 when the vendor profile does not exist, and 200 with an empty array when the profile exists but has no accounts. 
     * List commercial accounts
     */
    async listCommercialAccounts(requestParameters: ListCommercialAccountsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<CommercialAccountView>> {
        const response = await this.listCommercialAccountsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Replaces the role, account number, agency code and location mapping of an existing commercial account. Use this tool to correct a vendor-assigned account number or move a DELIVERY account to another location; do not use it to add a second account, which is createCommercialAccount. Preconditions: the profile must exist and be ADMIN-managed, the account must belong to that profile, and the target slot must not already be occupied by another account. Required inputs: vendorProfileId and accountId (UUIDv7) path parameters plus the full body, because every field is replaced; omitting agencyCode clears it. Emits a SUPPLIER_ACCOUNT_UPDATE audit event; orders already transmitted under the previous account number are unaffected. Returns 404 when the profile or account does not exist, 409 when the profile is YAML-managed or the account slot is taken, and 400 when accountNumber is blank or deliveryLocationId does not match the role. 
     * Update commercial account
     */
    async updateCommercialAccountRaw(requestParameters: UpdateCommercialAccountRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CommercialAccountView>> {
        if (requestParameters['vendorProfileId'] == null) {
            throw new runtime.RequiredError(
                'vendorProfileId',
                'Required parameter "vendorProfileId" was null or undefined when calling updateCommercialAccount().'
            );
        }

        if (requestParameters['accountId'] == null) {
            throw new runtime.RequiredError(
                'accountId',
                'Required parameter "accountId" was null or undefined when calling updateCommercialAccount().'
            );
        }

        if (requestParameters['commercialAccountRequest'] == null) {
            throw new runtime.RequiredError(
                'commercialAccountRequest',
                'Required parameter "commercialAccountRequest" was null or undefined when calling updateCommercialAccount().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/profiles/{vendorProfileId}/accounts/{accountId}`.replace(`{${"vendorProfileId"}}`, encodeURIComponent(String(requestParameters['vendorProfileId']))).replace(`{${"accountId"}}`, encodeURIComponent(String(requestParameters['accountId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: CommercialAccountRequestToJSON(requestParameters['commercialAccountRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CommercialAccountViewFromJSON(jsonValue));
    }

    /**
     * Replaces the role, account number, agency code and location mapping of an existing commercial account. Use this tool to correct a vendor-assigned account number or move a DELIVERY account to another location; do not use it to add a second account, which is createCommercialAccount. Preconditions: the profile must exist and be ADMIN-managed, the account must belong to that profile, and the target slot must not already be occupied by another account. Required inputs: vendorProfileId and accountId (UUIDv7) path parameters plus the full body, because every field is replaced; omitting agencyCode clears it. Emits a SUPPLIER_ACCOUNT_UPDATE audit event; orders already transmitted under the previous account number are unaffected. Returns 404 when the profile or account does not exist, 409 when the profile is YAML-managed or the account slot is taken, and 400 when accountNumber is blank or deliveryLocationId does not match the role. 
     * Update commercial account
     */
    async updateCommercialAccount(requestParameters: UpdateCommercialAccountRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CommercialAccountView> {
        const response = await this.updateCommercialAccountRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
