/* tslint:disable */
/* eslint-disable */
/**
 * Supplier API
 * API for supplier vendor profile administration — profiles, auth configs, commercial accounts, and capability endpoint bindings (ADR-0050)
 *
 * The version of the OpenAPI document: 1.0
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  AuthConfigRequest,
  AuthConfigView,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    AuthConfigRequestFromJSON,
    AuthConfigRequestToJSON,
    AuthConfigViewFromJSON,
    AuthConfigViewToJSON,
} from '../models/index';

export interface CreateAuthConfigRequest {
    vendorProfileId: string;
    authConfigRequest: AuthConfigRequest;
}

export interface DeleteAuthConfigRequest {
    vendorProfileId: string;
    authConfigId: string;
}

export interface ListAuthConfigsRequest {
    vendorProfileId: string;
}

export interface UpdateAuthConfigRequest {
    vendorProfileId: string;
    authConfigId: string;
    authConfigRequest: AuthConfigRequest;
}

/**
 * 
 */
export class SupplierAuthConfigsApi extends runtime.BaseAPI {

    /**
     * Adds a named auth config to a vendor profile, describing one credential scheme as a set of secret references resolved at call time. Use this tool when a supplier connection needs credentials; do not put plaintext credentials in any field, because every secret field takes a scheme-prefixed reference such as env:VAR_NAME and plaintext is rejected. Preconditions: the profile must exist and be ADMIN-managed, and the name must not already be used by another auth config on the same profile. Required inputs: vendorProfileId (UUIDv7) path parameter plus name and type in the body; the type fixes which reference fields are required and which must be absent, so BASIC_PLUS_APIKEY needs usernameRef, passwordRef and apiKeyRef while OAUTH2_CLIENT_CREDENTIALS needs tokenUrlRef, clientIdRef and clientSecretRef. Emits a SUPPLIER_AUTHCONFIG_CREATE audit event; the response omits every reference value. Returns 404 when the profile does not exist, 409 when the profile is YAML-managed or the name is taken, and 400 when the reference set does not match the declared type or a reference is malformed. 
     * Create auth config
     */
    async createAuthConfigRaw(requestParameters: CreateAuthConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AuthConfigView>> {
        if (requestParameters['vendorProfileId'] == null) {
            throw new runtime.RequiredError(
                'vendorProfileId',
                'Required parameter "vendorProfileId" was null or undefined when calling createAuthConfig().'
            );
        }

        if (requestParameters['authConfigRequest'] == null) {
            throw new runtime.RequiredError(
                'authConfigRequest',
                'Required parameter "authConfigRequest" was null or undefined when calling createAuthConfig().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/profiles/{vendorProfileId}/auth-configs`.replace(`{${"vendorProfileId"}}`, encodeURIComponent(String(requestParameters['vendorProfileId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AuthConfigRequestToJSON(requestParameters['authConfigRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AuthConfigViewFromJSON(jsonValue));
    }

    /**
     * Adds a named auth config to a vendor profile, describing one credential scheme as a set of secret references resolved at call time. Use this tool when a supplier connection needs credentials; do not put plaintext credentials in any field, because every secret field takes a scheme-prefixed reference such as env:VAR_NAME and plaintext is rejected. Preconditions: the profile must exist and be ADMIN-managed, and the name must not already be used by another auth config on the same profile. Required inputs: vendorProfileId (UUIDv7) path parameter plus name and type in the body; the type fixes which reference fields are required and which must be absent, so BASIC_PLUS_APIKEY needs usernameRef, passwordRef and apiKeyRef while OAUTH2_CLIENT_CREDENTIALS needs tokenUrlRef, clientIdRef and clientSecretRef. Emits a SUPPLIER_AUTHCONFIG_CREATE audit event; the response omits every reference value. Returns 404 when the profile does not exist, 409 when the profile is YAML-managed or the name is taken, and 400 when the reference set does not match the declared type or a reference is malformed. 
     * Create auth config
     */
    async createAuthConfig(requestParameters: CreateAuthConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AuthConfigView> {
        const response = await this.createAuthConfigRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes an auth config from a vendor profile. Use this tool when a credential scheme is retired; do not call it to rotate credentials, where updateAuthConfig repoints the references instead, and repoint or delete the endpoint bindings that name it first, because deletion is rejected while any binding still references it. Preconditions: the profile must exist and be ADMIN-managed, the config must belong to it, and no endpoint binding may reference it. Required inputs: vendorProfileId and authConfigId (UUIDv7) path parameters; there is no request body. Emits a SUPPLIER_AUTHCONFIG_DELETE audit event; the referenced secrets themselves are untouched, since only the reference is stored here. Returns 404 when the profile or config does not exist, and 409 when the profile is YAML-managed or a binding still references the config. 
     * Delete auth config
     */
    async deleteAuthConfigRaw(requestParameters: DeleteAuthConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['vendorProfileId'] == null) {
            throw new runtime.RequiredError(
                'vendorProfileId',
                'Required parameter "vendorProfileId" was null or undefined when calling deleteAuthConfig().'
            );
        }

        if (requestParameters['authConfigId'] == null) {
            throw new runtime.RequiredError(
                'authConfigId',
                'Required parameter "authConfigId" was null or undefined when calling deleteAuthConfig().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/profiles/{vendorProfileId}/auth-configs/{authConfigId}`.replace(`{${"vendorProfileId"}}`, encodeURIComponent(String(requestParameters['vendorProfileId']))).replace(`{${"authConfigId"}}`, encodeURIComponent(String(requestParameters['authConfigId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes an auth config from a vendor profile. Use this tool when a credential scheme is retired; do not call it to rotate credentials, where updateAuthConfig repoints the references instead, and repoint or delete the endpoint bindings that name it first, because deletion is rejected while any binding still references it. Preconditions: the profile must exist and be ADMIN-managed, the config must belong to it, and no endpoint binding may reference it. Required inputs: vendorProfileId and authConfigId (UUIDv7) path parameters; there is no request body. Emits a SUPPLIER_AUTHCONFIG_DELETE audit event; the referenced secrets themselves are untouched, since only the reference is stored here. Returns 404 when the profile or config does not exist, and 409 when the profile is YAML-managed or a binding still references the config. 
     * Delete auth config
     */
    async deleteAuthConfig(requestParameters: DeleteAuthConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteAuthConfigRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the auth configs of a vendor profile, ordered by name, showing the credential scheme and which reference fields are populated. Use this tool to find the auth config name an endpoint binding should reference; do not use it to read credentials, because secret references are write-only and never appear in a response. Preconditions: the vendor profile must exist. Required inputs: vendorProfileId (UUIDv7) path parameter; there is no request body or filtering. Emits a SUPPLIER_AUTHCONFIG_LIST audit event; no configuration is changed. Returns 404 when the vendor profile does not exist, and 200 with an empty array when the profile has no auth configs. 
     * List auth configs
     */
    async listAuthConfigsRaw(requestParameters: ListAuthConfigsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<AuthConfigView>>> {
        if (requestParameters['vendorProfileId'] == null) {
            throw new runtime.RequiredError(
                'vendorProfileId',
                'Required parameter "vendorProfileId" was null or undefined when calling listAuthConfigs().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/profiles/{vendorProfileId}/auth-configs`.replace(`{${"vendorProfileId"}}`, encodeURIComponent(String(requestParameters['vendorProfileId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(AuthConfigViewFromJSON));
    }

    /**
     * Returns the auth configs of a vendor profile, ordered by name, showing the credential scheme and which reference fields are populated. Use this tool to find the auth config name an endpoint binding should reference; do not use it to read credentials, because secret references are write-only and never appear in a response. Preconditions: the vendor profile must exist. Required inputs: vendorProfileId (UUIDv7) path parameter; there is no request body or filtering. Emits a SUPPLIER_AUTHCONFIG_LIST audit event; no configuration is changed. Returns 404 when the vendor profile does not exist, and 200 with an empty array when the profile has no auth configs. 
     * List auth configs
     */
    async listAuthConfigs(requestParameters: ListAuthConfigsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<AuthConfigView>> {
        const response = await this.listAuthConfigsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Replaces the name, credential scheme and secret references of an existing auth config. Use this tool to rotate which references a connection resolves or to switch credential scheme; do not use it to rename a config that endpoint bindings still reference, because bindings resolve it by name and the rename is rejected. Preconditions: the profile must exist and be ADMIN-managed, the auth config must belong to it, and a new name must be free and unreferenced. Required inputs: vendorProfileId and authConfigId (UUIDv7) path parameters plus the full body, because every field is replaced; omitting a reference field clears it, which fails validation when the declared type requires it. Emits a SUPPLIER_AUTHCONFIG_UPDATE audit event; the new references take effect on the next outbound call, and the response omits every reference value. Returns 404 when the profile or config does not exist, 409 when the profile is YAML-managed or the name is in use or still referenced, and 400 when the reference set does not match the declared type. 
     * Update auth config
     */
    async updateAuthConfigRaw(requestParameters: UpdateAuthConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AuthConfigView>> {
        if (requestParameters['vendorProfileId'] == null) {
            throw new runtime.RequiredError(
                'vendorProfileId',
                'Required parameter "vendorProfileId" was null or undefined when calling updateAuthConfig().'
            );
        }

        if (requestParameters['authConfigId'] == null) {
            throw new runtime.RequiredError(
                'authConfigId',
                'Required parameter "authConfigId" was null or undefined when calling updateAuthConfig().'
            );
        }

        if (requestParameters['authConfigRequest'] == null) {
            throw new runtime.RequiredError(
                'authConfigRequest',
                'Required parameter "authConfigRequest" was null or undefined when calling updateAuthConfig().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/supplier/admin/profiles/{vendorProfileId}/auth-configs/{authConfigId}`.replace(`{${"vendorProfileId"}}`, encodeURIComponent(String(requestParameters['vendorProfileId']))).replace(`{${"authConfigId"}}`, encodeURIComponent(String(requestParameters['authConfigId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: AuthConfigRequestToJSON(requestParameters['authConfigRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AuthConfigViewFromJSON(jsonValue));
    }

    /**
     * Replaces the name, credential scheme and secret references of an existing auth config. Use this tool to rotate which references a connection resolves or to switch credential scheme; do not use it to rename a config that endpoint bindings still reference, because bindings resolve it by name and the rename is rejected. Preconditions: the profile must exist and be ADMIN-managed, the auth config must belong to it, and a new name must be free and unreferenced. Required inputs: vendorProfileId and authConfigId (UUIDv7) path parameters plus the full body, because every field is replaced; omitting a reference field clears it, which fails validation when the declared type requires it. Emits a SUPPLIER_AUTHCONFIG_UPDATE audit event; the new references take effect on the next outbound call, and the response omits every reference value. Returns 404 when the profile or config does not exist, 409 when the profile is YAML-managed or the name is in use or still referenced, and 400 when the reference set does not match the declared type. 
     * Update auth config
     */
    async updateAuthConfig(requestParameters: UpdateAuthConfigRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AuthConfigView> {
        const response = await this.updateAuthConfigRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
