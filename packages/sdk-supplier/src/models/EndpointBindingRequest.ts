/* tslint:disable */
/* eslint-disable */
/**
 * Supplier API
 * API for supplier vendor profile administration — profiles, auth configs, commercial accounts, and capability endpoint bindings (ADR-0050)
 *
 * The version of the OpenAPI document: 1.0
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * Create/update payload for a capability endpoint binding (ADR-0050 §3): at most one binding per capability per profile. An absent or disabled binding means the capability is disabled for the supplier and resolves to the typed CAPABILITY_NOT_CONFIGURED status -- a value of the capability response's own status enum, returned with HTTP 200, not an error. Do not confuse it with the ApiError code SUPPLIER_CAPABILITY_NOT_CONFIGURED, which is the same condition escaping as a 4xx and indicates a defect, not a normal outcome.
 * @export
 * @interface EndpointBindingRequest
 */
export interface EndpointBindingRequest {
    /**
     * Name of the auth config on the same profile that this binding authenticates with. Must already exist.
     * @type {string}
     * @memberof EndpointBindingRequest
     */
    authConfigName?: string;
    /**
     * Base URL of the vendor endpoint for this capability.
     * @type {string}
     * @memberof EndpointBindingRequest
     */
    baseUrl?: string;
    /**
     * Canonical supplier capability key this binding serves. Unknown keys are rejected with SUPPLIER_UNKNOWN_CAPABILITY.
     * @type {string}
     * @memberof EndpointBindingRequest
     */
    capability?: string;
    /**
     * Exchange-audit payload capture level for this binding (ADR-0050 §7). Exchange metadata is always retained regardless. Omit to use the deployment default.
     * @type {string}
     * @memberof EndpointBindingRequest
     */
    captureLevel?: EndpointBindingRequestCaptureLevelEnum;
    /**
     * Whether THIS binding resolves. A disabled binding behaves exactly as an absent one: only this capability stops resolving for this supplier, and it reports the typed CAPABILITY_NOT_CONFIGURED outcome. This is a per-capability toggle, NOT a supplier-wide kill switch -- for that, disable the vendor profile itself.
     * @type {boolean}
     * @memberof EndpointBindingRequest
     */
    enabled?: boolean;
    /**
     * Path appended to the base URL for this capability.
     * @type {string}
     * @memberof EndpointBindingRequest
     */
    path?: string;
    /**
     * Canonical protocol family key of the adapter to use. Unknown keys are rejected with SUPPLIER_UNKNOWN_PROTOCOL_FAMILY.
     * @type {string}
     * @memberof EndpointBindingRequest
     */
    protocolFamily?: string;
    /**
     * Data classifications whose named fields are additionally redacted from REDACTED captures of this binding (ADR-0050 §7 minimization). Credential redaction always applies and cannot be configured away; classifications only narrow what a REDACTED capture retains. Name-based: for positional vendor formats (EDIFACT segments) they redact nothing and METADATA_ONLY is the only level guaranteeing no content is retained. Omit for credential redaction only.
     * @type {Set<string>}
     * @memberof EndpointBindingRequest
     */
    redactionClassifications?: Set<EndpointBindingRequestRedactionClassificationsEnum>;
    /**
     * Cron expression driving scheduled runs of this binding. Omit for request-driven capabilities.
     * @type {string}
     * @memberof EndpointBindingRequest
     */
    schedule?: string;
    /**
     * Adapter version key within the protocol family. Free-form data, deliberately not an enum (ADR-0051 §3) so a vendor's new norm needs no code change. NOT validated on write: a key with no registered codec is accepted here and then resolves to CAPABILITY_NOT_CONFIGURED on every call, so it must match a codec exactly. Keys shipped today: A2_5, B2_1, B3_3, B4_0, C1_0, C1_1, C1_2, S2S_V1. At most 64 characters: the exchange-audit trail stores this key at that width, and a longer one would make every audit write for the binding fail.
     * @type {string}
     * @memberof EndpointBindingRequest
     */
    version?: string;
}

/**
* @export
* @enum {string}
*/
export enum EndpointBindingRequestCaptureLevelEnum {
    Full = 'FULL',
    Redacted = 'REDACTED',
    MetadataOnly = 'METADATA_ONLY'
}
/**
* @export
* @enum {string}
*/
export enum EndpointBindingRequestRedactionClassificationsEnum {
    CustomerIdentifier = 'CUSTOMER_IDENTIFIER',
    CommercialPricing = 'COMMERCIAL_PRICING'
}


/**
 * Check if a given object implements the EndpointBindingRequest interface.
 */
export function instanceOfEndpointBindingRequest(value: object): boolean {
    return true;
}

export function EndpointBindingRequestFromJSON(json: any): EndpointBindingRequest {
    return EndpointBindingRequestFromJSONTyped(json, false);
}

export function EndpointBindingRequestFromJSONTyped(json: any, ignoreDiscriminator: boolean): EndpointBindingRequest {
    if (json == null) {
        return json;
    }
    return {
        
        'authConfigName': json['authConfigName'] == null ? undefined : json['authConfigName'],
        'baseUrl': json['baseUrl'] == null ? undefined : json['baseUrl'],
        'capability': json['capability'] == null ? undefined : json['capability'],
        'captureLevel': json['captureLevel'] == null ? undefined : json['captureLevel'],
        'enabled': json['enabled'] == null ? undefined : json['enabled'],
        'path': json['path'] == null ? undefined : json['path'],
        'protocolFamily': json['protocolFamily'] == null ? undefined : json['protocolFamily'],
        'redactionClassifications': json['redactionClassifications'] == null ? undefined : json['redactionClassifications'],
        'schedule': json['schedule'] == null ? undefined : json['schedule'],
        'version': json['version'] == null ? undefined : json['version'],
    };
}

export function EndpointBindingRequestToJSON(value?: EndpointBindingRequest | null): any {
    if (value == null) {
        return value;
    }
    return {
        
        'authConfigName': value['authConfigName'],
        'baseUrl': value['baseUrl'],
        'capability': value['capability'],
        'captureLevel': value['captureLevel'],
        'enabled': value['enabled'],
        'path': value['path'],
        'protocolFamily': value['protocolFamily'],
        'redactionClassifications': value['redactionClassifications'] == null ? undefined : Array.from(value['redactionClassifications'] as Set<any>),
        'schedule': value['schedule'],
        'version': value['version'],
    };
}

