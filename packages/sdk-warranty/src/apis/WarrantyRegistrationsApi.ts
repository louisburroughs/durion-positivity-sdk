/* tslint:disable */
/* eslint-disable */
/**
 * Warranty API
 * API for warranty policies, claims, settlements, reimbursements, and part returns in the POS system
 *
 * The version of the OpenAPI document: 1.0
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  RegistrationRequest,
  RegistrationResponse,
} from '../models/index';
import {
    RegistrationRequestFromJSON,
    RegistrationRequestToJSON,
    RegistrationResponseFromJSON,
    RegistrationResponseToJSON,
} from '../models/index';

export interface CreateRegistrationRequest {
    registrationRequest: RegistrationRequest;
}

export interface GetRegistrationRequest {
    id: string;
}

export interface SearchRegistrationsRequest {
    customerId?: string;
    vehicleId?: string;
    status?: SearchRegistrationsStatusEnum;
}

export interface UpdateRegistrationRequest {
    id: string;
    registrationRequest: RegistrationRequest;
}

/**
 * 
 */
export class WarrantyRegistrationsApi extends runtime.BaseAPI {

    /**
     * Records sold/instantiated coverage — a registration binding a governing policy to a customer, and optionally to a vehicle and the originating invoice line. Use this tool when coverage such as a road-hazard add-on or extended plan is sold; do not use createPolicy, which defines the reusable program a registration instantiates. Preconditions: the governing policy must exist; policies flagged autoRegister create registrations automatically from invoiced sales, so manual creation covers the remaining cases. Required inputs: policyId (UUID), customerId (UUID), and purchaseDate (ISO date); vehicleId, source invoice references, contractNumber, and expiresAt are optional, and status defaults to ACTIVE. Emits a WARRANTY_REGISTRATION_CREATE event. Returns 404 when the referenced policy cannot be resolved. 
     * Create registration
     */
    async createRegistrationRaw(requestParameters: CreateRegistrationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RegistrationResponse>> {
        if (requestParameters['registrationRequest'] == null) {
            throw new runtime.RequiredError(
                'registrationRequest',
                'Required parameter "registrationRequest" was null or undefined when calling createRegistration().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/warranty/registrations`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RegistrationRequestToJSON(requestParameters['registrationRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RegistrationResponseFromJSON(jsonValue));
    }

    /**
     * Records sold/instantiated coverage — a registration binding a governing policy to a customer, and optionally to a vehicle and the originating invoice line. Use this tool when coverage such as a road-hazard add-on or extended plan is sold; do not use createPolicy, which defines the reusable program a registration instantiates. Preconditions: the governing policy must exist; policies flagged autoRegister create registrations automatically from invoiced sales, so manual creation covers the remaining cases. Required inputs: policyId (UUID), customerId (UUID), and purchaseDate (ISO date); vehicleId, source invoice references, contractNumber, and expiresAt are optional, and status defaults to ACTIVE. Emits a WARRANTY_REGISTRATION_CREATE event. Returns 404 when the referenced policy cannot be resolved. 
     * Create registration
     */
    async createRegistration(requestParameters: CreateRegistrationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RegistrationResponse> {
        const response = await this.createRegistrationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one warranty registration — the sold/instantiated coverage binding a policy to a customer, and optionally to a vehicle and its originating invoice line. Use this tool when the registration id is already known; use searchRegistrations instead to locate coverage by customer, vehicle, or status. Preconditions: the registration must exist. Required inputs: id (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no registration exists for the supplied id. 
     * Get registration
     */
    async getRegistrationRaw(requestParameters: GetRegistrationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RegistrationResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getRegistration().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/warranty/registrations/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RegistrationResponseFromJSON(jsonValue));
    }

    /**
     * Returns one warranty registration — the sold/instantiated coverage binding a policy to a customer, and optionally to a vehicle and its originating invoice line. Use this tool when the registration id is already known; use searchRegistrations instead to locate coverage by customer, vehicle, or status. Preconditions: the registration must exist. Required inputs: id (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no registration exists for the supplied id. 
     * Get registration
     */
    async getRegistration(requestParameters: GetRegistrationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RegistrationResponse> {
        const response = await this.getRegistrationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Searches sold-coverage registrations, optionally filtered by customer, vehicle, and lifecycle status. Use this tool to find a customer\'s road-hazard or extended-plan coverage; use getRegistration instead when the registration id is already known. Preconditions: none — an empty list is returned when nothing matches. Required inputs: customerId, vehicleId, and status (ACTIVE, EXPIRED, CANCELLED, EXHAUSTED) are all optional query parameters; with no filters every registration is returned. No events are emitted and no state changes; this is a read-only projection. Returns 200 with the list, which is empty rather than 404 when nothing matches. 
     * Search registrations
     */
    async searchRegistrationsRaw(requestParameters: SearchRegistrationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<RegistrationResponse>>> {
        const queryParameters: any = {};

        if (requestParameters['customerId'] != null) {
            queryParameters['customerId'] = requestParameters['customerId'];
        }

        if (requestParameters['vehicleId'] != null) {
            queryParameters['vehicleId'] = requestParameters['vehicleId'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/warranty/registrations`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(RegistrationResponseFromJSON));
    }

    /**
     * Searches sold-coverage registrations, optionally filtered by customer, vehicle, and lifecycle status. Use this tool to find a customer\'s road-hazard or extended-plan coverage; use getRegistration instead when the registration id is already known. Preconditions: none — an empty list is returned when nothing matches. Required inputs: customerId, vehicleId, and status (ACTIVE, EXPIRED, CANCELLED, EXHAUSTED) are all optional query parameters; with no filters every registration is returned. No events are emitted and no state changes; this is a read-only projection. Returns 200 with the list, which is empty rather than 404 when nothing matches. 
     * Search registrations
     */
    async searchRegistrations(requestParameters: SearchRegistrationsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<RegistrationResponse>> {
        const response = await this.searchRegistrationsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Fully replaces an existing warranty registration\'s fields, including the policy binding, source references, and coverage dates. Use this tool to correct or expire sold coverage; do not use createRegistration, which records new coverage. Preconditions: the registration and the referenced policy must exist; omitting status keeps the current value, while the other fields are rewritten from the request and null clears them. Required inputs: id (UUID) as a path parameter, plus policyId, customerId, and purchaseDate; vehicleId, source invoice references, contractNumber, expiresAt, and status (ACTIVE, EXPIRED, CANCELLED, EXHAUSTED) are optional. Emits a WARRANTY_REGISTRATION_UPDATE event. Returns 404 when the registration or the referenced policy cannot be resolved. 
     * Update registration
     */
    async updateRegistrationRaw(requestParameters: UpdateRegistrationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RegistrationResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updateRegistration().'
            );
        }

        if (requestParameters['registrationRequest'] == null) {
            throw new runtime.RequiredError(
                'registrationRequest',
                'Required parameter "registrationRequest" was null or undefined when calling updateRegistration().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/warranty/registrations/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: RegistrationRequestToJSON(requestParameters['registrationRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RegistrationResponseFromJSON(jsonValue));
    }

    /**
     * Fully replaces an existing warranty registration\'s fields, including the policy binding, source references, and coverage dates. Use this tool to correct or expire sold coverage; do not use createRegistration, which records new coverage. Preconditions: the registration and the referenced policy must exist; omitting status keeps the current value, while the other fields are rewritten from the request and null clears them. Required inputs: id (UUID) as a path parameter, plus policyId, customerId, and purchaseDate; vehicleId, source invoice references, contractNumber, expiresAt, and status (ACTIVE, EXPIRED, CANCELLED, EXHAUSTED) are optional. Emits a WARRANTY_REGISTRATION_UPDATE event. Returns 404 when the registration or the referenced policy cannot be resolved. 
     * Update registration
     */
    async updateRegistration(requestParameters: UpdateRegistrationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RegistrationResponse> {
        const response = await this.updateRegistrationRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
  * @export
  * @enum {string}
  */
export enum SearchRegistrationsStatusEnum {
    Active = 'ACTIVE',
    Expired = 'EXPIRED',
    Cancelled = 'CANCELLED',
    Exhausted = 'EXHAUSTED'
}
