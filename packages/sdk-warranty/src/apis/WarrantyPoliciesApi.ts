/* tslint:disable */
/* eslint-disable */
/**
 * Warranty API
 * API for warranty policies, claims, settlements, reimbursements, and part returns in the POS system
 *
 * The version of the OpenAPI document: 1.0
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  PolicyRequest,
  PolicyResponse,
} from '../models/index';
import {
    PolicyRequestFromJSON,
    PolicyRequestToJSON,
    PolicyResponseFromJSON,
    PolicyResponseToJSON,
} from '../models/index';

export interface CreatePolicyRequest {
    policyRequest: PolicyRequest;
}

export interface FindApplicablePoliciesRequest {
    saleDate: Date;
    productEntityId?: string;
    manufacturerId?: string;
    categoryId?: string;
    coverageType?: FindApplicablePoliciesCoverageTypeEnum;
}

export interface GetPolicyRequest {
    id: string;
}

export interface ListPoliciesRequest {
    providerId?: string;
    coverageType?: ListPoliciesCoverageTypeEnum;
}

export interface UpdatePolicyRequest {
    id: string;
    policyRequest: PolicyRequest;
}

/**
 * 
 */
export class WarrantyPoliciesApi extends runtime.BaseAPI {

    /**
     * Creates a warranty policy — one row of structured coverage terms owned by a provider, governing eligibility, proration, and evidence requirements for claims. Use this tool to configure a new coverage program; do not use updatePolicy, which fully replaces an existing policy, and do not use createRegistration, which records coverage sold to one customer under a policy. Preconditions: the owning provider must exist; the appliesTo scope must be internally consistent — PRODUCT_LIST needs a non-empty appliesToProductEntityIds, CATEGORY needs appliesToCategoryId, MANUFACTURER needs appliesToManufacturerId — and effectiveTo must not precede effectiveFrom. Required inputs: providerId (UUID), name, coverageType, appliesToType, effectiveFrom (ISO date), and prorationMethod (NONE, TREAD_DEPTH, MILEAGE, or TIME); laborCovered, requiresPartReturn, requiresPhotoEvidence, transferable, and autoRegister all default to false. Emits a WARRANTY_POLICY_CREATE event; no claims or registrations are touched. Returns 404 when the referenced provider cannot be resolved, and 400 when the appliesTo scope keys or the effective window are inconsistent. 
     * Create policy
     */
    async createPolicyRaw(requestParameters: CreatePolicyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PolicyResponse>> {
        if (requestParameters['policyRequest'] == null) {
            throw new runtime.RequiredError(
                'policyRequest',
                'Required parameter "policyRequest" was null or undefined when calling createPolicy().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/warranty/policies`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PolicyRequestToJSON(requestParameters['policyRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PolicyResponseFromJSON(jsonValue));
    }

    /**
     * Creates a warranty policy — one row of structured coverage terms owned by a provider, governing eligibility, proration, and evidence requirements for claims. Use this tool to configure a new coverage program; do not use updatePolicy, which fully replaces an existing policy, and do not use createRegistration, which records coverage sold to one customer under a policy. Preconditions: the owning provider must exist; the appliesTo scope must be internally consistent — PRODUCT_LIST needs a non-empty appliesToProductEntityIds, CATEGORY needs appliesToCategoryId, MANUFACTURER needs appliesToManufacturerId — and effectiveTo must not precede effectiveFrom. Required inputs: providerId (UUID), name, coverageType, appliesToType, effectiveFrom (ISO date), and prorationMethod (NONE, TREAD_DEPTH, MILEAGE, or TIME); laborCovered, requiresPartReturn, requiresPhotoEvidence, transferable, and autoRegister all default to false. Emits a WARRANTY_POLICY_CREATE event; no claims or registrations are touched. Returns 404 when the referenced provider cannot be resolved, and 400 when the appliesTo scope keys or the effective window are inconsistent. 
     * Create policy
     */
    async createPolicy(requestParameters: CreatePolicyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PolicyResponse> {
        const response = await this.createPolicyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Resolves the policies in effect on the original sale date whose appliesTo scope matches any provided catalog key, ordered most-specific-first (PRODUCT_LIST, then MANUFACTURER, then CATEGORY, then ALL). Use this tool during intake or eligibility work to find the governing policy for a product; do not use listPolicies, which lists policies without effective-date or scope matching. Preconditions: policies must exist with an effectiveFrom/effectiveTo window covering the sale date; ALL-scoped policies always match, while scoped policies match only when the corresponding key is provided and equal. Required inputs: saleDate (ISO-8601 date) is required; productEntityId, manufacturerId, and categoryId are optional pos-catalog keys, and coverageType optionally restricts the result to one coverage type. No events are emitted and no state changes; this is a read-only projection. Returns 200 with matching policies most specific first (empty when none match), and 400 when saleDate is missing or malformed. 
     * Find applicable policies
     */
    async findApplicablePoliciesRaw(requestParameters: FindApplicablePoliciesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<PolicyResponse>>> {
        if (requestParameters['saleDate'] == null) {
            throw new runtime.RequiredError(
                'saleDate',
                'Required parameter "saleDate" was null or undefined when calling findApplicablePolicies().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['productEntityId'] != null) {
            queryParameters['productEntityId'] = requestParameters['productEntityId'];
        }

        if (requestParameters['manufacturerId'] != null) {
            queryParameters['manufacturerId'] = requestParameters['manufacturerId'];
        }

        if (requestParameters['categoryId'] != null) {
            queryParameters['categoryId'] = requestParameters['categoryId'];
        }

        if (requestParameters['saleDate'] != null) {
            queryParameters['saleDate'] = (requestParameters['saleDate'] as any).toISOString().substring(0,10);
        }

        if (requestParameters['coverageType'] != null) {
            queryParameters['coverageType'] = requestParameters['coverageType'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/warranty/policies/applicable`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(PolicyResponseFromJSON));
    }

    /**
     * Resolves the policies in effect on the original sale date whose appliesTo scope matches any provided catalog key, ordered most-specific-first (PRODUCT_LIST, then MANUFACTURER, then CATEGORY, then ALL). Use this tool during intake or eligibility work to find the governing policy for a product; do not use listPolicies, which lists policies without effective-date or scope matching. Preconditions: policies must exist with an effectiveFrom/effectiveTo window covering the sale date; ALL-scoped policies always match, while scoped policies match only when the corresponding key is provided and equal. Required inputs: saleDate (ISO-8601 date) is required; productEntityId, manufacturerId, and categoryId are optional pos-catalog keys, and coverageType optionally restricts the result to one coverage type. No events are emitted and no state changes; this is a read-only projection. Returns 200 with matching policies most specific first (empty when none match), and 400 when saleDate is missing or malformed. 
     * Find applicable policies
     */
    async findApplicablePolicies(requestParameters: FindApplicablePoliciesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<PolicyResponse>> {
        const response = await this.findApplicablePoliciesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one warranty policy with its full structured coverage terms, including proration method, caps, and evidence requirements. Use this tool when the policy id is already known; use listPolicies or findApplicablePolicies instead to locate a policy. Preconditions: the policy must exist. Required inputs: id (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no policy exists for the supplied id. 
     * Get policy
     */
    async getPolicyRaw(requestParameters: GetPolicyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PolicyResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getPolicy().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/warranty/policies/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PolicyResponseFromJSON(jsonValue));
    }

    /**
     * Returns one warranty policy with its full structured coverage terms, including proration method, caps, and evidence requirements. Use this tool when the policy id is already known; use listPolicies or findApplicablePolicies instead to locate a policy. Preconditions: the policy must exist. Required inputs: id (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no policy exists for the supplied id. 
     * Get policy
     */
    async getPolicy(requestParameters: GetPolicyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PolicyResponse> {
        const response = await this.getPolicyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns warranty policies, optionally filtered by owning provider and/or coverage type. Use this tool to browse configured coverage programs; use findApplicablePolicies instead to resolve which policies cover a specific product on a specific sale date. Preconditions: none — an empty list is returned when nothing matches. Required inputs: providerId and coverageType are optional query parameters; when both are given the provider filter is applied first and coverage type filters the result. No events are emitted and no state changes; this is a read-only projection. Returns 200 with the list, which is empty rather than 404 when no policy matches. 
     * List policies
     */
    async listPoliciesRaw(requestParameters: ListPoliciesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<PolicyResponse>>> {
        const queryParameters: any = {};

        if (requestParameters['providerId'] != null) {
            queryParameters['providerId'] = requestParameters['providerId'];
        }

        if (requestParameters['coverageType'] != null) {
            queryParameters['coverageType'] = requestParameters['coverageType'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/warranty/policies`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(PolicyResponseFromJSON));
    }

    /**
     * Returns warranty policies, optionally filtered by owning provider and/or coverage type. Use this tool to browse configured coverage programs; use findApplicablePolicies instead to resolve which policies cover a specific product on a specific sale date. Preconditions: none — an empty list is returned when nothing matches. Required inputs: providerId and coverageType are optional query parameters; when both are given the provider filter is applied first and coverage type filters the result. No events are emitted and no state changes; this is a read-only projection. Returns 200 with the list, which is empty rather than 404 when no policy matches. 
     * List policies
     */
    async listPolicies(requestParameters: ListPoliciesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<PolicyResponse>> {
        const response = await this.listPoliciesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Fully replaces an existing warranty policy\'s structured coverage terms; every field is rewritten from the request, and omitted optional booleans reset to false. Use this tool to change a coverage program; do not use createPolicy, which adds a new program — claims already decided keep their frozen proration inputs regardless of policy edits. Preconditions: the policy and the referenced provider must exist, the appliesTo scope keys must be consistent with appliesToType, and effectiveTo must not precede effectiveFrom. Required inputs: id (UUID) as a path parameter and the same full body as createPolicy — this is full-update semantics, so omitting an optional field clears it or resets its default. Emits a WARRANTY_POLICY_UPDATE event. Returns 404 when the policy or the referenced provider cannot be resolved, and 400 when the appliesTo scope keys or the effective window are inconsistent. 
     * Update policy
     */
    async updatePolicyRaw(requestParameters: UpdatePolicyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PolicyResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updatePolicy().'
            );
        }

        if (requestParameters['policyRequest'] == null) {
            throw new runtime.RequiredError(
                'policyRequest',
                'Required parameter "policyRequest" was null or undefined when calling updatePolicy().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/warranty/policies/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: PolicyRequestToJSON(requestParameters['policyRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PolicyResponseFromJSON(jsonValue));
    }

    /**
     * Fully replaces an existing warranty policy\'s structured coverage terms; every field is rewritten from the request, and omitted optional booleans reset to false. Use this tool to change a coverage program; do not use createPolicy, which adds a new program — claims already decided keep their frozen proration inputs regardless of policy edits. Preconditions: the policy and the referenced provider must exist, the appliesTo scope keys must be consistent with appliesToType, and effectiveTo must not precede effectiveFrom. Required inputs: id (UUID) as a path parameter and the same full body as createPolicy — this is full-update semantics, so omitting an optional field clears it or resets its default. Emits a WARRANTY_POLICY_UPDATE event. Returns 404 when the policy or the referenced provider cannot be resolved, and 400 when the appliesTo scope keys or the effective window are inconsistent. 
     * Update policy
     */
    async updatePolicy(requestParameters: UpdatePolicyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PolicyResponse> {
        const response = await this.updatePolicyRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
  * @export
  * @enum {string}
  */
export enum FindApplicablePoliciesCoverageTypeEnum {
    ManufacturerDefect = 'MANUFACTURER_DEFECT',
    DealerWorkmanship = 'DEALER_WORKMANSHIP',
    RoadHazard = 'ROAD_HAZARD',
    ExtendedPlan = 'EXTENDED_PLAN'
}
/**
  * @export
  * @enum {string}
  */
export enum ListPoliciesCoverageTypeEnum {
    ManufacturerDefect = 'MANUFACTURER_DEFECT',
    DealerWorkmanship = 'DEALER_WORKMANSHIP',
    RoadHazard = 'ROAD_HAZARD',
    ExtendedPlan = 'EXTENDED_PLAN'
}
