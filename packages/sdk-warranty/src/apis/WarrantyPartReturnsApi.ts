/* tslint:disable */
/* eslint-disable */
/**
 * Warranty API
 * API for warranty policies, claims, settlements, reimbursements, and part returns in the POS system
 *
 * The version of the OpenAPI document: 1.0
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  PartReturnCreateRequest,
  PartReturnResponse,
  PartReturnUpdateRequest,
} from '../models/index';
import {
    PartReturnCreateRequestFromJSON,
    PartReturnCreateRequestToJSON,
    PartReturnResponseFromJSON,
    PartReturnResponseToJSON,
    PartReturnUpdateRequestFromJSON,
    PartReturnUpdateRequestToJSON,
} from '../models/index';

export interface CreatePartReturnRequest {
    id: string;
    partReturnCreateRequest: PartReturnCreateRequest;
}

export interface ListPartReturnsRequest {
    status?: ListPartReturnsStatusEnum;
}

export interface UpdatePartReturnRequest {
    id: string;
    partReturnUpdateRequest: PartReturnUpdateRequest;
}

/**
 * 
 */
export class WarrantyPartReturnsApi extends runtime.BaseAPI {

    /**
     * Opens the defective-part RMA lifecycle for one claim line, creating a part return in AWAITING_PART with the chosen disposition. Use this tool when the governing policy requires the part back or staff choose to hold or return it; do not use updatePartReturn, which advances or annotates an RMA that already exists — at most one part return is allowed per claim line. Preconditions: the claim must exist and not be CANCELLED or CLOSED, the line must belong to the claim, and no part return may already exist for that line; no policy check gates creation because staff choice is always permitted. Required inputs: claim id (UUID) as a path parameter and a body with claimLineId (UUID) and disposition (HOLD_FOR_INSPECTION, RETURN_TO_VENDOR, SCRAP_AUTHORIZED, or CUSTOMER_RETAINED); rmaNumber and holdLocationNote are optional. Emits a WARRANTY_PART_RETURN_CREATE event, enqueues warranty.part-return.requested so pos-inventory can quarantine the defective unit, and republishes the claim snapshot. Returns 404 when the claim or the line cannot be found, and 409 when the line already has a part return or the claim is terminal. 
     * Create part return (RMA)
     */
    async createPartReturnRaw(requestParameters: CreatePartReturnRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PartReturnResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling createPartReturn().'
            );
        }

        if (requestParameters['partReturnCreateRequest'] == null) {
            throw new runtime.RequiredError(
                'partReturnCreateRequest',
                'Required parameter "partReturnCreateRequest" was null or undefined when calling createPartReturn().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/warranty/claims/{id}/part-returns`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PartReturnCreateRequestToJSON(requestParameters['partReturnCreateRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PartReturnResponseFromJSON(jsonValue));
    }

    /**
     * Opens the defective-part RMA lifecycle for one claim line, creating a part return in AWAITING_PART with the chosen disposition. Use this tool when the governing policy requires the part back or staff choose to hold or return it; do not use updatePartReturn, which advances or annotates an RMA that already exists — at most one part return is allowed per claim line. Preconditions: the claim must exist and not be CANCELLED or CLOSED, the line must belong to the claim, and no part return may already exist for that line; no policy check gates creation because staff choice is always permitted. Required inputs: claim id (UUID) as a path parameter and a body with claimLineId (UUID) and disposition (HOLD_FOR_INSPECTION, RETURN_TO_VENDOR, SCRAP_AUTHORIZED, or CUSTOMER_RETAINED); rmaNumber and holdLocationNote are optional. Emits a WARRANTY_PART_RETURN_CREATE event, enqueues warranty.part-return.requested so pos-inventory can quarantine the defective unit, and republishes the claim snapshot. Returns 404 when the claim or the line cannot be found, and 409 when the line already has a part return or the claim is terminal. 
     * Create part return (RMA)
     */
    async createPartReturn(requestParameters: CreatePartReturnRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PartReturnResponse> {
        const response = await this.createPartReturnRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the hold-shelf and shipping worklist of part returns across all claims, optionally filtered by status. Use this tool to drive the back-office RMA worklist; use getClaim instead to see the part returns of one specific claim. Preconditions: none — an empty list is returned when nothing matches. Required inputs: status is an optional query parameter (AWAITING_PART, ON_HOLD, SHIPPED, RECEIVED_BY_VENDOR, SCRAPPED, CLOSED); omitting it returns every part return. No events are emitted and no state changes; this is a read-only projection. Returns 200 with the list, which is empty rather than 404 when no part return matches. 
     * Part-return worklist
     */
    async listPartReturnsRaw(requestParameters: ListPartReturnsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<PartReturnResponse>>> {
        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/warranty/part-returns`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(PartReturnResponseFromJSON));
    }

    /**
     * Returns the hold-shelf and shipping worklist of part returns across all claims, optionally filtered by status. Use this tool to drive the back-office RMA worklist; use getClaim instead to see the part returns of one specific claim. Preconditions: none — an empty list is returned when nothing matches. Required inputs: status is an optional query parameter (AWAITING_PART, ON_HOLD, SHIPPED, RECEIVED_BY_VENDOR, SCRAPPED, CLOSED); omitting it returns every part return. No events are emitted and no state changes; this is a read-only projection. Returns 200 with the list, which is empty rather than 404 when no part return matches. 
     * Part-return worklist
     */
    async listPartReturns(requestParameters: ListPartReturnsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<PartReturnResponse>> {
        const response = await this.listPartReturnsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Advances a part return through its child state machine — AWAITING_PART to ON_HOLD to SHIPPED to RECEIVED_BY_VENDOR, or to SCRAPPED/CLOSED per disposition, where ON_HOLD may be skipped — and/or updates RMA details without a lifecycle move. Use this tool for every change after creation; do not use createPartReturn, which opens a new RMA and rejects a line that already has one. Preconditions: the part return must exist and not be terminal; moving to SHIPPED or RECEIVED_BY_VENDOR requires disposition RETURN_TO_VENDOR, and moving to SCRAPPED requires disposition SCRAP_AUTHORIZED (either already set or set in the same request). Required inputs: part return id (UUID) as a path parameter; every body field is optional — omit status for a detail-only update, and shippedAt defaults to now when entering SHIPPED. Emits a WARRANTY_PART_RETURN_UPDATE event, enqueues warranty.part-return.shipped when the return enters SHIPPED, and republishes the claim snapshot. Returns 404 when the part return does not exist, and 409 when the transition is illegal, the disposition does not permit the target status, or the return is already terminal. 
     * Update part return (RMA)
     */
    async updatePartReturnRaw(requestParameters: UpdatePartReturnRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PartReturnResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updatePartReturn().'
            );
        }

        if (requestParameters['partReturnUpdateRequest'] == null) {
            throw new runtime.RequiredError(
                'partReturnUpdateRequest',
                'Required parameter "partReturnUpdateRequest" was null or undefined when calling updatePartReturn().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/warranty/part-returns/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: PartReturnUpdateRequestToJSON(requestParameters['partReturnUpdateRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PartReturnResponseFromJSON(jsonValue));
    }

    /**
     * Advances a part return through its child state machine — AWAITING_PART to ON_HOLD to SHIPPED to RECEIVED_BY_VENDOR, or to SCRAPPED/CLOSED per disposition, where ON_HOLD may be skipped — and/or updates RMA details without a lifecycle move. Use this tool for every change after creation; do not use createPartReturn, which opens a new RMA and rejects a line that already has one. Preconditions: the part return must exist and not be terminal; moving to SHIPPED or RECEIVED_BY_VENDOR requires disposition RETURN_TO_VENDOR, and moving to SCRAPPED requires disposition SCRAP_AUTHORIZED (either already set or set in the same request). Required inputs: part return id (UUID) as a path parameter; every body field is optional — omit status for a detail-only update, and shippedAt defaults to now when entering SHIPPED. Emits a WARRANTY_PART_RETURN_UPDATE event, enqueues warranty.part-return.shipped when the return enters SHIPPED, and republishes the claim snapshot. Returns 404 when the part return does not exist, and 409 when the transition is illegal, the disposition does not permit the target status, or the return is already terminal. 
     * Update part return (RMA)
     */
    async updatePartReturn(requestParameters: UpdatePartReturnRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PartReturnResponse> {
        const response = await this.updatePartReturnRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
  * @export
  * @enum {string}
  */
export enum ListPartReturnsStatusEnum {
    AwaitingPart = 'AWAITING_PART',
    OnHold = 'ON_HOLD',
    Shipped = 'SHIPPED',
    ReceivedByVendor = 'RECEIVED_BY_VENDOR',
    Scrapped = 'SCRAPPED',
    Closed = 'CLOSED'
}
