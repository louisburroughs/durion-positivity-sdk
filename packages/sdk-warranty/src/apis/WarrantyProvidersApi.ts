/* tslint:disable */
/* eslint-disable */
/**
 * Warranty API
 * API for warranty policies, claims, settlements, reimbursements, and part returns in the POS system
 *
 * The version of the OpenAPI document: 1.0
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ProviderRequest,
  ProviderResponse,
} from '../models/index';
import {
    ProviderRequestFromJSON,
    ProviderRequestToJSON,
    ProviderResponseFromJSON,
    ProviderResponseToJSON,
} from '../models/index';

export interface CreateProviderRequest {
    providerRequest: ProviderRequest;
}

export interface GetProviderRequest {
    id: string;
}

export interface ListProvidersRequest {
    status?: string;
    providerType?: ListProvidersProviderTypeEnum;
}

export interface UpdateProviderRequest {
    id: string;
    providerRequest: ProviderRequest;
}

/**
 * 
 */
export class WarrantyProvidersApi extends runtime.BaseAPI {

    /**
     * Creates a warranty provider — the party that funds a warranty program, from a manufacturer or third-party administrator to the dealer itself. Use this tool to register a new payer before configuring its policies with createPolicy; do not use updateProvider, which rewrites an existing provider. Preconditions: none — there is no duplicate-name check, so repeating the call creates a second provider. Required inputs: name and providerType (MANUFACTURER, DISTRIBUTOR_PROGRAM, THIRD_PARTY_ADMIN, or DEALER — DEALER means self-funded, which later makes vendor reimbursement NOT_APPLICABLE); status defaults to ACTIVE, and apVendorId optionally links the pos-accounting vendor for credit matching. Emits a WARRANTY_PROVIDER_CREATE event; no policies or claims are touched. Returns 201 with the created provider on success. 
     * Create provider
     */
    async createProviderRaw(requestParameters: CreateProviderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProviderResponse>> {
        if (requestParameters['providerRequest'] == null) {
            throw new runtime.RequiredError(
                'providerRequest',
                'Required parameter "providerRequest" was null or undefined when calling createProvider().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/warranty/providers`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ProviderRequestToJSON(requestParameters['providerRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProviderResponseFromJSON(jsonValue));
    }

    /**
     * Creates a warranty provider — the party that funds a warranty program, from a manufacturer or third-party administrator to the dealer itself. Use this tool to register a new payer before configuring its policies with createPolicy; do not use updateProvider, which rewrites an existing provider. Preconditions: none — there is no duplicate-name check, so repeating the call creates a second provider. Required inputs: name and providerType (MANUFACTURER, DISTRIBUTOR_PROGRAM, THIRD_PARTY_ADMIN, or DEALER — DEALER means self-funded, which later makes vendor reimbursement NOT_APPLICABLE); status defaults to ACTIVE, and apVendorId optionally links the pos-accounting vendor for credit matching. Emits a WARRANTY_PROVIDER_CREATE event; no policies or claims are touched. Returns 201 with the created provider on success. 
     * Create provider
     */
    async createProvider(requestParameters: CreateProviderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProviderResponse> {
        const response = await this.createProviderRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one warranty provider, including its funding type, AP vendor link, and claim-submission contact details. Use this tool when the provider id is already known; use listProviders instead to locate a provider by status or type. Preconditions: the provider must exist. Required inputs: id (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no provider exists for the supplied id. 
     * Get provider
     */
    async getProviderRaw(requestParameters: GetProviderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProviderResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getProvider().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/warranty/providers/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProviderResponseFromJSON(jsonValue));
    }

    /**
     * Returns one warranty provider, including its funding type, AP vendor link, and claim-submission contact details. Use this tool when the provider id is already known; use listProviders instead to locate a provider by status or type. Preconditions: the provider must exist. Required inputs: id (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no provider exists for the supplied id. 
     * Get provider
     */
    async getProvider(requestParameters: GetProviderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProviderResponse> {
        const response = await this.getProviderRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns warranty providers, optionally filtered by lifecycle status and/or provider type. Use this tool to browse who funds warranty programs; use getProvider instead when the provider id is already known. Preconditions: none — an empty list is returned when nothing matches. Required inputs: status (ACTIVE or INACTIVE) and providerType (MANUFACTURER, DISTRIBUTOR_PROGRAM, THIRD_PARTY_ADMIN, DEALER) are optional query parameters; when both are given the status filter is applied first and provider type filters the result. No events are emitted and no state changes; this is a read-only projection. Returns 200 with the list, which is empty rather than 404 when no provider matches. 
     * List providers
     */
    async listProvidersRaw(requestParameters: ListProvidersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ProviderResponse>>> {
        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['providerType'] != null) {
            queryParameters['providerType'] = requestParameters['providerType'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/warranty/providers`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ProviderResponseFromJSON));
    }

    /**
     * Returns warranty providers, optionally filtered by lifecycle status and/or provider type. Use this tool to browse who funds warranty programs; use getProvider instead when the provider id is already known. Preconditions: none — an empty list is returned when nothing matches. Required inputs: status (ACTIVE or INACTIVE) and providerType (MANUFACTURER, DISTRIBUTOR_PROGRAM, THIRD_PARTY_ADMIN, DEALER) are optional query parameters; when both are given the status filter is applied first and provider type filters the result. No events are emitted and no state changes; this is a read-only projection. Returns 200 with the list, which is empty rather than 404 when no provider matches. 
     * List providers
     */
    async listProviders(requestParameters: ListProvidersRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ProviderResponse>> {
        const response = await this.listProvidersRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Fully replaces an existing warranty provider\'s fields, including funding type, contact details, and the AP vendor link. Use this tool to change provider data; do not use createProvider, which adds a new provider. Preconditions: the provider must exist; omitting status keeps the current value, while every other field is rewritten from the request and null clears it. Required inputs: id (UUID) as a path parameter, plus name and providerType; apVendorId, manufacturerId, claimSubmissionMethod, and the contact fields are optional. Emits a WARRANTY_PROVIDER_UPDATE event. Returns 404 when no provider exists for the supplied id. 
     * Update provider
     */
    async updateProviderRaw(requestParameters: UpdateProviderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProviderResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updateProvider().'
            );
        }

        if (requestParameters['providerRequest'] == null) {
            throw new runtime.RequiredError(
                'providerRequest',
                'Required parameter "providerRequest" was null or undefined when calling updateProvider().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/warranty/providers/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: ProviderRequestToJSON(requestParameters['providerRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProviderResponseFromJSON(jsonValue));
    }

    /**
     * Fully replaces an existing warranty provider\'s fields, including funding type, contact details, and the AP vendor link. Use this tool to change provider data; do not use createProvider, which adds a new provider. Preconditions: the provider must exist; omitting status keeps the current value, while every other field is rewritten from the request and null clears it. Required inputs: id (UUID) as a path parameter, plus name and providerType; apVendorId, manufacturerId, claimSubmissionMethod, and the contact fields are optional. Emits a WARRANTY_PROVIDER_UPDATE event. Returns 404 when no provider exists for the supplied id. 
     * Update provider
     */
    async updateProvider(requestParameters: UpdateProviderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProviderResponse> {
        const response = await this.updateProviderRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
  * @export
  * @enum {string}
  */
export enum ListProvidersProviderTypeEnum {
    Manufacturer = 'MANUFACTURER',
    DistributorProgram = 'DISTRIBUTOR_PROGRAM',
    ThirdPartyAdmin = 'THIRD_PARTY_ADMIN',
    Dealer = 'DEALER'
}
