/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Tenant API
 * Tenant registry and the accounts that own each tenancy (ADR-0062 section 7). Platform-tenant callers only; reached via the API Gateway under /tenant.
 *
 * The version of the OpenAPI document: v1
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  TenantCreateRequest,
  TenantResponse,
  TenantUpdateRequest,
} from '../models/index';
import {
    TenantCreateRequestFromJSON,
    TenantCreateRequestToJSON,
    TenantResponseFromJSON,
    TenantResponseToJSON,
    TenantUpdateRequestFromJSON,
    TenantUpdateRequestToJSON,
} from '../models/index';

export interface CreateTenantRequest {
    tenantCreateRequest: TenantCreateRequest;
}

export interface DecommissionTenantRequest {
    id: string;
}

export interface GetTenantRequest {
    id: string;
}

export interface ListTenantsRequest {
    status?: ListTenantsStatusEnum;
}

export interface ReactivateTenantRequest {
    id: string;
}

export interface SuspendTenantRequest {
    id: string;
}

export interface UpdateTenantRequest {
    id: string;
    tenantUpdateRequest: TenantUpdateRequest;
}

/**
 * 
 */
export class PlatformTenantAPIApi extends runtime.BaseAPI {

    /**
     * Registers a tenant under an existing account in status PENDING and publishes tenant.created on tenant.events.v1; pos-security-service provisions the role template and the initial administrator named by initialAdminEmail, then answers tenant.provisioned, which moves the tenant to ACTIVE. Use this tool once per customer tenancy, after createAccount when the owning account does not exist; do not use it to change an existing tenant, use updateTenant or the lifecycle operations instead. Preconditions: the account exists, and neither the slug nor the display name is taken. Required inputs: slug, accountId and initialAdminEmail; cell and displayName are optional. Omitting displayName seeds it from the account\'s legal name, suffixed \" #2\", \" #3\" and so on only where that would collide — a fallback, so prefer naming the tenant explicitly. Emits a TENANT_CREATE event. Returns 201 with the tenant, 404 when the account is unknown and 409 when the slug or the display name is taken. 
     * Register a Tenant
     */
    async createTenantRaw(requestParameters: CreateTenantRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TenantResponse>> {
        if (requestParameters['tenantCreateRequest'] == null) {
            throw new runtime.RequiredError(
                'tenantCreateRequest',
                'Required parameter "tenantCreateRequest" was null or undefined when calling createTenant().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["platform:tenant:create"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/platform/tenants`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: TenantCreateRequestToJSON(requestParameters['tenantCreateRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TenantResponseFromJSON(jsonValue));
    }

    /**
     * Registers a tenant under an existing account in status PENDING and publishes tenant.created on tenant.events.v1; pos-security-service provisions the role template and the initial administrator named by initialAdminEmail, then answers tenant.provisioned, which moves the tenant to ACTIVE. Use this tool once per customer tenancy, after createAccount when the owning account does not exist; do not use it to change an existing tenant, use updateTenant or the lifecycle operations instead. Preconditions: the account exists, and neither the slug nor the display name is taken. Required inputs: slug, accountId and initialAdminEmail; cell and displayName are optional. Omitting displayName seeds it from the account\'s legal name, suffixed \" #2\", \" #3\" and so on only where that would collide — a fallback, so prefer naming the tenant explicitly. Emits a TENANT_CREATE event. Returns 201 with the tenant, 404 when the account is unknown and 409 when the slug or the display name is taken. 
     * Register a Tenant
     */
    async createTenant(requestParameters: CreateTenantRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TenantResponse> {
        const response = await this.createTenantRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Moves a tenant to the terminal DECOMMISSIONED status and publishes tenant.decommissioned; the tenant can never be reactivated or edited again. Use this tool when a tenancy ends; do not use it for a reversible hold, use suspendTenant instead. Preconditions: the tenant is not already DECOMMISSIONED. Required inputs: id (UUID) as a path parameter; no body. Emits a TENANT_DECOMMISSION event. Returns 200 with the tenant, 404 when it does not exist and 409 when it is already decommissioned. 
     * Decommission a Tenant
     */
    async decommissionTenantRaw(requestParameters: DecommissionTenantRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TenantResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling decommissionTenant().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["platform:tenant:decommission"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/platform/tenants/{id}/decommission`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TenantResponseFromJSON(jsonValue));
    }

    /**
     * Moves a tenant to the terminal DECOMMISSIONED status and publishes tenant.decommissioned; the tenant can never be reactivated or edited again. Use this tool when a tenancy ends; do not use it for a reversible hold, use suspendTenant instead. Preconditions: the tenant is not already DECOMMISSIONED. Required inputs: id (UUID) as a path parameter; no body. Emits a TENANT_DECOMMISSION event. Returns 200 with the tenant, 404 when it does not exist and 409 when it is already decommissioned. 
     * Decommission a Tenant
     */
    async decommissionTenant(requestParameters: DecommissionTenantRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TenantResponse> {
        const response = await this.decommissionTenantRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one tenant\'s registry record, including its account, cell and lifecycle timestamps. Use this tool when the tenant id is known; do not use it to search by slug or status, use listTenants instead. Preconditions: the tenant exists. Required inputs: id (UUID) as a path parameter. Emits a TENANT_GET event. Returns 200 with the tenant and 404 when it does not exist. 
     * Get a Tenant
     */
    async getTenantRaw(requestParameters: GetTenantRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TenantResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getTenant().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["platform:tenant:read"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/platform/tenants/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TenantResponseFromJSON(jsonValue));
    }

    /**
     * Returns one tenant\'s registry record, including its account, cell and lifecycle timestamps. Use this tool when the tenant id is known; do not use it to search by slug or status, use listTenants instead. Preconditions: the tenant exists. Required inputs: id (UUID) as a path parameter. Emits a TENANT_GET event. Returns 200 with the tenant and 404 when it does not exist. 
     * Get a Tenant
     */
    async getTenant(requestParameters: GetTenantRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TenantResponse> {
        const response = await this.getTenantRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists every tenant in the registry, oldest first, optionally filtered by lifecycle status. Use this tool to find a tenant id or slug; do not use it for one tenant\'s full record, use getTenant instead. Preconditions: none beyond the platform:tenant:read authority. Required inputs: none; status is an optional filter (PENDING, ACTIVE, SUSPENDED, DECOMMISSIONED). Emits a TENANT_LIST event. Returns 200 with the unpaginated list. 
     * List Tenants
     */
    async listTenantsRaw(requestParameters: ListTenantsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<TenantResponse>>> {
        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["platform:tenant:read"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/platform/tenants`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(TenantResponseFromJSON));
    }

    /**
     * Lists every tenant in the registry, oldest first, optionally filtered by lifecycle status. Use this tool to find a tenant id or slug; do not use it for one tenant\'s full record, use getTenant instead. Preconditions: none beyond the platform:tenant:read authority. Required inputs: none; status is an optional filter (PENDING, ACTIVE, SUSPENDED, DECOMMISSIONED). Emits a TENANT_LIST event. Returns 200 with the unpaginated list. 
     * List Tenants
     */
    async listTenants(requestParameters: ListTenantsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<TenantResponse>> {
        const response = await this.listTenantsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Moves a SUSPENDED tenant back to ACTIVE and publishes tenant.reactivated. Use this tool to lift a hold placed by suspendTenant; do not use it on a PENDING tenant, which becomes ACTIVE through tenant.provisioned instead. Preconditions: the tenant is SUSPENDED. Required inputs: id (UUID) as a path parameter; no body. Emits a TENANT_REACTIVATE event. Returns 200 with the tenant, 404 when it does not exist and 409 when it is not SUSPENDED. 
     * Reactivate a Tenant
     */
    async reactivateTenantRaw(requestParameters: ReactivateTenantRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TenantResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling reactivateTenant().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["platform:tenant:reactivate"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/platform/tenants/{id}/reactivate`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TenantResponseFromJSON(jsonValue));
    }

    /**
     * Moves a SUSPENDED tenant back to ACTIVE and publishes tenant.reactivated. Use this tool to lift a hold placed by suspendTenant; do not use it on a PENDING tenant, which becomes ACTIVE through tenant.provisioned instead. Preconditions: the tenant is SUSPENDED. Required inputs: id (UUID) as a path parameter; no body. Emits a TENANT_REACTIVATE event. Returns 200 with the tenant, 404 when it does not exist and 409 when it is not SUSPENDED. 
     * Reactivate a Tenant
     */
    async reactivateTenant(requestParameters: ReactivateTenantRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TenantResponse> {
        const response = await this.reactivateTenantRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Moves an ACTIVE tenant to SUSPENDED and publishes tenant.suspended; logins for the tenant are refused until reactivateTenant. Use this tool for non-payment or abuse holds; do not use it for a permanent end, use decommissionTenant instead. Preconditions: the tenant is ACTIVE. Required inputs: id (UUID) as a path parameter; no body. Emits a TENANT_SUSPEND event. Returns 200 with the tenant, 404 when it does not exist and 409 when it is not ACTIVE. 
     * Suspend a Tenant
     */
    async suspendTenantRaw(requestParameters: SuspendTenantRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TenantResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling suspendTenant().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["platform:tenant:suspend"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/platform/tenants/{id}/suspend`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TenantResponseFromJSON(jsonValue));
    }

    /**
     * Moves an ACTIVE tenant to SUSPENDED and publishes tenant.suspended; logins for the tenant are refused until reactivateTenant. Use this tool for non-payment or abuse holds; do not use it for a permanent end, use decommissionTenant instead. Preconditions: the tenant is ACTIVE. Required inputs: id (UUID) as a path parameter; no body. Emits a TENANT_SUSPEND event. Returns 200 with the tenant, 404 when it does not exist and 409 when it is not ACTIVE. 
     * Suspend a Tenant
     */
    async suspendTenant(requestParameters: SuspendTenantRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TenantResponse> {
        const response = await this.suspendTenantRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Changes a tenant\'s display name or cell and publishes tenant.updated; slug, account and status are never changed here. Use this tool for descriptive edits; do not use it to change status, use suspendTenant, reactivateTenant or decommissionTenant instead. Preconditions: the tenant exists, is not DECOMMISSIONED, and the new display name is not already held by another tenant. Required inputs: id (UUID) as a path parameter and a body with displayName and/or cell; a null field leaves the value unchanged. Emits a TENANT_UPDATE event. Returns 200 with the tenant, 404 when it does not exist and 409 when it is decommissioned or the display name is taken. 
     * Update a Tenant
     */
    async updateTenantRaw(requestParameters: UpdateTenantRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TenantResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updateTenant().'
            );
        }

        if (requestParameters['tenantUpdateRequest'] == null) {
            throw new runtime.RequiredError(
                'tenantUpdateRequest',
                'Required parameter "tenantUpdateRequest" was null or undefined when calling updateTenant().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["platform:tenant:update"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/platform/tenants/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: TenantUpdateRequestToJSON(requestParameters['tenantUpdateRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TenantResponseFromJSON(jsonValue));
    }

    /**
     * Changes a tenant\'s display name or cell and publishes tenant.updated; slug, account and status are never changed here. Use this tool for descriptive edits; do not use it to change status, use suspendTenant, reactivateTenant or decommissionTenant instead. Preconditions: the tenant exists, is not DECOMMISSIONED, and the new display name is not already held by another tenant. Required inputs: id (UUID) as a path parameter and a body with displayName and/or cell; a null field leaves the value unchanged. Emits a TENANT_UPDATE event. Returns 200 with the tenant, 404 when it does not exist and 409 when it is decommissioned or the display name is taken. 
     * Update a Tenant
     */
    async updateTenant(requestParameters: UpdateTenantRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TenantResponse> {
        const response = await this.updateTenantRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
  * @export
  * @enum {string}
  */
export enum ListTenantsStatusEnum {
    Pending = 'PENDING',
    Active = 'ACTIVE',
    Suspended = 'SUSPENDED',
    Decommissioned = 'DECOMMISSIONED'
}
