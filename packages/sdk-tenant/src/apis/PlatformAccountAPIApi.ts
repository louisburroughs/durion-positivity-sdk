/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Tenant API
 * Tenant registry and the accounts that own each tenancy (ADR-0062 section 7). Platform-tenant callers only; reached via the API Gateway under /tenant.
 *
 * The version of the OpenAPI document: v1
 * Contact: platform@durionpos.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AccountContactRequest,
  AccountContactResponse,
  AccountCreateRequest,
  AccountResponse,
  AccountUpdateRequest,
  BillingProfileRequest,
  BillingProfileResponse,
} from '../models/index';
import {
    AccountContactRequestFromJSON,
    AccountContactRequestToJSON,
    AccountContactResponseFromJSON,
    AccountContactResponseToJSON,
    AccountCreateRequestFromJSON,
    AccountCreateRequestToJSON,
    AccountResponseFromJSON,
    AccountResponseToJSON,
    AccountUpdateRequestFromJSON,
    AccountUpdateRequestToJSON,
    BillingProfileRequestFromJSON,
    BillingProfileRequestToJSON,
    BillingProfileResponseFromJSON,
    BillingProfileResponseToJSON,
} from '../models/index';

export interface AddAccountContactRequest {
    id: string;
    accountContactRequest: AccountContactRequest;
}

export interface CreateAccountRequest {
    accountCreateRequest: AccountCreateRequest;
}

export interface GetAccountRequest {
    id: string;
}

export interface PutAccountBillingProfileRequest {
    id: string;
    billingProfileRequest: BillingProfileRequest;
}

export interface RemoveAccountContactRequest {
    id: string;
    contactId: string;
}

export interface UpdateAccountRequest {
    id: string;
    accountUpdateRequest: AccountUpdateRequest;
}

export interface UpdateAccountContactRequest {
    id: string;
    contactId: string;
    accountContactRequest: AccountContactRequest;
}

/**
 * 
 */
export class PlatformAccountAPIApi extends runtime.BaseAPI {

    /**
     * Adds an OWNER, BILLING or TECHNICAL contact to an account; an account may carry several of each role. Use this tool for a new person; do not use it to change an existing contact, use updateAccountContact instead. Preconditions: the account exists. Required inputs: id (UUID) as a path parameter and a body with name, role and email; phone is optional. Emits an ACCOUNT_CONTACT_ADD event. Returns 201 with the contact and 404 when the account does not exist. 
     * Add a Contact to an Account
     */
    async addAccountContactRaw(requestParameters: AddAccountContactRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccountContactResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling addAccountContact().'
            );
        }

        if (requestParameters['accountContactRequest'] == null) {
            throw new runtime.RequiredError(
                'accountContactRequest',
                'Required parameter "accountContactRequest" was null or undefined when calling addAccountContact().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["platform:account:update"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/platform/accounts/{id}/contacts`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AccountContactRequestToJSON(requestParameters['accountContactRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccountContactResponseFromJSON(jsonValue));
    }

    /**
     * Adds an OWNER, BILLING or TECHNICAL contact to an account; an account may carry several of each role. Use this tool for a new person; do not use it to change an existing contact, use updateAccountContact instead. Preconditions: the account exists. Required inputs: id (UUID) as a path parameter and a body with name, role and email; phone is optional. Emits an ACCOUNT_CONTACT_ADD event. Returns 201 with the contact and 404 when the account does not exist. 
     * Add a Contact to an Account
     */
    async addAccountContact(requestParameters: AddAccountContactRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccountContactResponse> {
        const response = await this.addAccountContactRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates the customer account that will own one or more tenants. Use this tool before createTenant for a new customer; do not use it to add a tenant to an existing customer. Preconditions: the legal name is not already registered. Required inputs: legalName, homeCountry (ISO 3166-1 alpha-2) and homeCurrency (ISO 4217); tradingName and taxId are optional. Emits an ACCOUNT_CREATE event. Returns 201 with the account and 409 when the legal name is taken. 
     * Create an Account
     */
    async createAccountRaw(requestParameters: CreateAccountRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccountResponse>> {
        if (requestParameters['accountCreateRequest'] == null) {
            throw new runtime.RequiredError(
                'accountCreateRequest',
                'Required parameter "accountCreateRequest" was null or undefined when calling createAccount().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["platform:account:create"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/platform/accounts`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AccountCreateRequestToJSON(requestParameters['accountCreateRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccountResponseFromJSON(jsonValue));
    }

    /**
     * Creates the customer account that will own one or more tenants. Use this tool before createTenant for a new customer; do not use it to add a tenant to an existing customer. Preconditions: the legal name is not already registered. Required inputs: legalName, homeCountry (ISO 3166-1 alpha-2) and homeCurrency (ISO 4217); tradingName and taxId are optional. Emits an ACCOUNT_CREATE event. Returns 201 with the account and 409 when the legal name is taken. 
     * Create an Account
     */
    async createAccount(requestParameters: CreateAccountRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccountResponse> {
        const response = await this.createAccountRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one account with its contacts, billing profile and the ids of the tenants it owns. Use this tool when the account id is known; do not use it to find an account by name, use listAccounts instead. Preconditions: the account exists. Required inputs: id (UUID) as a path parameter. Emits an ACCOUNT_GET event. Returns 200 with the account and 404 when it does not exist. 
     * Get an Account
     */
    async getAccountRaw(requestParameters: GetAccountRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccountResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getAccount().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["platform:account:read"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/platform/accounts/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccountResponseFromJSON(jsonValue));
    }

    /**
     * Returns one account with its contacts, billing profile and the ids of the tenants it owns. Use this tool when the account id is known; do not use it to find an account by name, use listAccounts instead. Preconditions: the account exists. Required inputs: id (UUID) as a path parameter. Emits an ACCOUNT_GET event. Returns 200 with the account and 404 when it does not exist. 
     * Get an Account
     */
    async getAccount(requestParameters: GetAccountRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccountResponse> {
        const response = await this.getAccountRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists every account with its contacts, billing profile and tenant ids, ordered by legal name. Use this tool to find an account id before createTenant; do not use it for one account, use getAccount instead. Preconditions: none beyond the platform:account:read authority. Required inputs: none. Emits an ACCOUNT_LIST event. Returns 200 with the unpaginated list. 
     * List Accounts
     */
    async listAccountsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<AccountResponse>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["platform:account:read"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/platform/accounts`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(AccountResponseFromJSON));
    }

    /**
     * Lists every account with its contacts, billing profile and tenant ids, ordered by legal name. Use this tool to find an account id before createTenant; do not use it for one account, use getAccount instead. Preconditions: none beyond the platform:account:read authority. Required inputs: none. Emits an ACCOUNT_LIST event. Returns 200 with the unpaginated list. 
     * List Accounts
     */
    async listAccounts(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<AccountResponse>> {
        const response = await this.listAccountsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates or replaces the account\'s single billing profile: billing address, payment terms, invoicing email and the payment processor\'s customer token. Card numbers are never accepted or stored. Use this tool whenever billing details change; do not use it for the legal name or home currency, use updateAccount instead. The response reports only whether a processor token is on file. Preconditions: the account exists. Required inputs: id (UUID) as a path parameter and a body with addressLine1, city, country, paymentTerms and invoicingEmail; the rest is optional. Emits an ACCOUNT_BILLING_PROFILE_PUT event. Returns 200 with the billing profile and 404 when the account does not exist. 
     * Set an Account\'s Billing Profile
     */
    async putAccountBillingProfileRaw(requestParameters: PutAccountBillingProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BillingProfileResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling putAccountBillingProfile().'
            );
        }

        if (requestParameters['billingProfileRequest'] == null) {
            throw new runtime.RequiredError(
                'billingProfileRequest',
                'Required parameter "billingProfileRequest" was null or undefined when calling putAccountBillingProfile().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["platform:account:update"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/platform/accounts/{id}/billing-profile`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: BillingProfileRequestToJSON(requestParameters['billingProfileRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BillingProfileResponseFromJSON(jsonValue));
    }

    /**
     * Creates or replaces the account\'s single billing profile: billing address, payment terms, invoicing email and the payment processor\'s customer token. Card numbers are never accepted or stored. Use this tool whenever billing details change; do not use it for the legal name or home currency, use updateAccount instead. The response reports only whether a processor token is on file. Preconditions: the account exists. Required inputs: id (UUID) as a path parameter and a body with addressLine1, city, country, paymentTerms and invoicingEmail; the rest is optional. Emits an ACCOUNT_BILLING_PROFILE_PUT event. Returns 200 with the billing profile and 404 when the account does not exist. 
     * Set an Account\'s Billing Profile
     */
    async putAccountBillingProfile(requestParameters: PutAccountBillingProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BillingProfileResponse> {
        const response = await this.putAccountBillingProfileRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Deletes a contact from an account. Use this tool when a person no longer represents the customer; use updateAccountContact to change their role instead. Preconditions: the contact exists on the account. Required inputs: id and contactId (UUIDs) as path parameters. Emits an ACCOUNT_CONTACT_REMOVE event. Returns 204 and 404 when the account or contact does not exist. 
     * Remove an Account Contact
     */
    async removeAccountContactRaw(requestParameters: RemoveAccountContactRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling removeAccountContact().'
            );
        }

        if (requestParameters['contactId'] == null) {
            throw new runtime.RequiredError(
                'contactId',
                'Required parameter "contactId" was null or undefined when calling removeAccountContact().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["platform:account:update"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/platform/accounts/{id}/contacts/{contactId}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))).replace(`{${"contactId"}}`, encodeURIComponent(String(requestParameters['contactId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes a contact from an account. Use this tool when a person no longer represents the customer; use updateAccountContact to change their role instead. Preconditions: the contact exists on the account. Required inputs: id and contactId (UUIDs) as path parameters. Emits an ACCOUNT_CONTACT_REMOVE event. Returns 204 and 404 when the account or contact does not exist. 
     * Remove an Account Contact
     */
    async removeAccountContact(requestParameters: RemoveAccountContactRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.removeAccountContactRaw(requestParameters, initOverrides);
    }

    /**
     * Applies a partial update to an account\'s legal name, trading name, status, tax id, home country or home currency. Use this tool for account descriptors; do not use it for contacts or billing details, use the contact and billing-profile operations instead. Preconditions: the account exists; a new legal name must not be taken. Required inputs: id (UUID) as a path parameter and a body where every field is optional and null leaves the value unchanged. Emits an ACCOUNT_UPDATE event. Returns 200 with the account, 404 when it does not exist and 409 when the legal name is taken. 
     * Update an Account
     */
    async updateAccountRaw(requestParameters: UpdateAccountRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccountResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updateAccount().'
            );
        }

        if (requestParameters['accountUpdateRequest'] == null) {
            throw new runtime.RequiredError(
                'accountUpdateRequest',
                'Required parameter "accountUpdateRequest" was null or undefined when calling updateAccount().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["platform:account:update"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/platform/accounts/{id}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: AccountUpdateRequestToJSON(requestParameters['accountUpdateRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccountResponseFromJSON(jsonValue));
    }

    /**
     * Applies a partial update to an account\'s legal name, trading name, status, tax id, home country or home currency. Use this tool for account descriptors; do not use it for contacts or billing details, use the contact and billing-profile operations instead. Preconditions: the account exists; a new legal name must not be taken. Required inputs: id (UUID) as a path parameter and a body where every field is optional and null leaves the value unchanged. Emits an ACCOUNT_UPDATE event. Returns 200 with the account, 404 when it does not exist and 409 when the legal name is taken. 
     * Update an Account
     */
    async updateAccount(requestParameters: UpdateAccountRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccountResponse> {
        const response = await this.updateAccountRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Replaces a contact\'s name, role, email and phone. Use this tool to correct an existing contact; do not use it to add or drop one, use addAccountContact or removeAccountContact instead. Preconditions: the contact exists on the account. Required inputs: id and contactId (UUIDs) as path parameters and a full contact body. Emits an ACCOUNT_CONTACT_UPDATE event. Returns 200 with the contact and 404 when the account or contact does not exist. 
     * Update an Account Contact
     */
    async updateAccountContactRaw(requestParameters: UpdateAccountContactRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccountContactResponse>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updateAccountContact().'
            );
        }

        if (requestParameters['contactId'] == null) {
            throw new runtime.RequiredError(
                'contactId',
                'Required parameter "contactId" was null or undefined when calling updateAccountContact().'
            );
        }

        if (requestParameters['accountContactRequest'] == null) {
            throw new runtime.RequiredError(
                'accountContactRequest',
                'Required parameter "accountContactRequest" was null or undefined when calling updateAccountContact().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["platform:account:update"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/platform/accounts/{id}/contacts/{contactId}`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))).replace(`{${"contactId"}}`, encodeURIComponent(String(requestParameters['contactId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: AccountContactRequestToJSON(requestParameters['accountContactRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccountContactResponseFromJSON(jsonValue));
    }

    /**
     * Replaces a contact\'s name, role, email and phone. Use this tool to correct an existing contact; do not use it to add or drop one, use addAccountContact or removeAccountContact instead. Preconditions: the contact exists on the account. Required inputs: id and contactId (UUIDs) as path parameters and a full contact body. Emits an ACCOUNT_CONTACT_UPDATE event. Returns 200 with the contact and 404 when the account or contact does not exist. 
     * Update an Account Contact
     */
    async updateAccountContact(requestParameters: UpdateAccountContactRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccountContactResponse> {
        const response = await this.updateAccountContactRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
