/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Location API
 * API documentation for the Shop Location service, accessible via the API Gateway. Shop locations are Maintenance Bays and Mobile Shops.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  BulkIngestRequestMobileUnitBulkIngestRecord,
  BulkIngestResponse,
} from '../models/index';
import {
    BulkIngestRequestMobileUnitBulkIngestRecordFromJSON,
    BulkIngestRequestMobileUnitBulkIngestRecordToJSON,
    BulkIngestResponseFromJSON,
    BulkIngestResponseToJSON,
} from '../models/index';

export interface BulkIngestMobileUnitsRequest {
    bulkIngestRequestMobileUnitBulkIngestRecord: BulkIngestRequestMobileUnitBulkIngestRecord;
}

/**
 * 
 */
export class MobileUnitBulkIngestAPIApi extends runtime.BaseAPI {

    /**
     * Creates many mobile units at once, one per record. Use this tool when commissioning a fleet or seeding an environment; use createMobileUnit instead for a single unit. Preconditions: each row\'s base location must exist. A unit created ACTIVE must also carry a travel buffer policy, capabilities and coverage rules, which this record does not express — so seed units INACTIVE and activate them through createMobileUnit or a later update. Required inputs: jobId (UUID), locationId (UUID) and records, each with a name; a record\'s own baseLocationId overrides the batch one. Emits a LOCATION_MOBILE_UNIT_MANAGE event and a unit-created event per row. Re-running the same file is safe: a name already present at its base location is reported as already existing rather than as a failure. Returns 200 with a per-record result; check each result\'s errorCode rather than the status alone, which is MOBILE_UNIT_INGEST_FAILED with the reason for a row the service refused, or INTERNAL_ERROR with a correlationId to quote for a row lost to a server-side fault. 
     * Create Mobile Units in Bulk
     */
    async bulkIngestMobileUnitsRaw(requestParameters: BulkIngestMobileUnitsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkIngestResponse>> {
        if (requestParameters['bulkIngestRequestMobileUnitBulkIngestRecord'] == null) {
            throw new runtime.RequiredError(
                'bulkIngestRequestMobileUnitBulkIngestRecord',
                'Required parameter "bulkIngestRequestMobileUnitBulkIngestRecord" was null or undefined when calling bulkIngestMobileUnits().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["location:mobile-unit:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/mobile-units/bulk-ingest`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkIngestRequestMobileUnitBulkIngestRecordToJSON(requestParameters['bulkIngestRequestMobileUnitBulkIngestRecord']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkIngestResponseFromJSON(jsonValue));
    }

    /**
     * Creates many mobile units at once, one per record. Use this tool when commissioning a fleet or seeding an environment; use createMobileUnit instead for a single unit. Preconditions: each row\'s base location must exist. A unit created ACTIVE must also carry a travel buffer policy, capabilities and coverage rules, which this record does not express — so seed units INACTIVE and activate them through createMobileUnit or a later update. Required inputs: jobId (UUID), locationId (UUID) and records, each with a name; a record\'s own baseLocationId overrides the batch one. Emits a LOCATION_MOBILE_UNIT_MANAGE event and a unit-created event per row. Re-running the same file is safe: a name already present at its base location is reported as already existing rather than as a failure. Returns 200 with a per-record result; check each result\'s errorCode rather than the status alone, which is MOBILE_UNIT_INGEST_FAILED with the reason for a row the service refused, or INTERNAL_ERROR with a correlationId to quote for a row lost to a server-side fault. 
     * Create Mobile Units in Bulk
     */
    async bulkIngestMobileUnits(requestParameters: BulkIngestMobileUnitsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkIngestResponse> {
        const response = await this.bulkIngestMobileUnitsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
