/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Location API
 * API documentation for the Shop Location service, accessible via the API Gateway. Shop locations are Maintenance Bays and Mobile Shops.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  BulkIngestRequestBayBulkIngestRecord,
  BulkIngestResponse,
} from '../models/index';
import {
    BulkIngestRequestBayBulkIngestRecordFromJSON,
    BulkIngestRequestBayBulkIngestRecordToJSON,
    BulkIngestResponseFromJSON,
    BulkIngestResponseToJSON,
} from '../models/index';

export interface BulkIngestBaysRequest {
    bulkIngestRequestBayBulkIngestRecord: BulkIngestRequestBayBulkIngestRecord;
}

/**
 * 
 */
export class BayBulkIngestAPIApi extends runtime.BaseAPI {

    /**
     * Creates many service bays at once, one per record. Use this tool when commissioning a location or seeding an environment; use createBay instead for a single bay. Preconditions: each row\'s location must exist, and bayType must name a known bay type. Required inputs: jobId (UUID), locationId (UUID) and records, each with a name, a bayType and maxConcurrentVehicles; a record\'s own locationId overrides the batch one. Emits a LOCATION_BAY_BULK_INGEST event and a bay-created event per row. Re-running the same file is safe: a bay name already present at its location is reported as already existing rather than as a failure. Returns 200 with a per-record result; check each result rather than the status alone. A row the service refused carries errorCode BAY_INGEST_FAILED and the reason; a row lost to a server-side fault carries INTERNAL_ERROR and a correlationId to quote, with no detail of its own. 
     * Create Service Bays in Bulk
     */
    async bulkIngestBaysRaw(requestParameters: BulkIngestBaysRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkIngestResponse>> {
        if (requestParameters['bulkIngestRequestBayBulkIngestRecord'] == null) {
            throw new runtime.RequiredError(
                'bulkIngestRequestBayBulkIngestRecord',
                'Required parameter "bulkIngestRequestBayBulkIngestRecord" was null or undefined when calling bulkIngestBays().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["location:bay:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/locations/bays/bulk-ingest`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkIngestRequestBayBulkIngestRecordToJSON(requestParameters['bulkIngestRequestBayBulkIngestRecord']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkIngestResponseFromJSON(jsonValue));
    }

    /**
     * Creates many service bays at once, one per record. Use this tool when commissioning a location or seeding an environment; use createBay instead for a single bay. Preconditions: each row\'s location must exist, and bayType must name a known bay type. Required inputs: jobId (UUID), locationId (UUID) and records, each with a name, a bayType and maxConcurrentVehicles; a record\'s own locationId overrides the batch one. Emits a LOCATION_BAY_BULK_INGEST event and a bay-created event per row. Re-running the same file is safe: a bay name already present at its location is reported as already existing rather than as a failure. Returns 200 with a per-record result; check each result rather than the status alone. A row the service refused carries errorCode BAY_INGEST_FAILED and the reason; a row lost to a server-side fault carries INTERNAL_ERROR and a correlationId to quote, with no detail of its own. 
     * Create Service Bays in Bulk
     */
    async bulkIngestBays(requestParameters: BulkIngestBaysRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkIngestResponse> {
        const response = await this.bulkIngestBaysRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
