/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Location API
 * API documentation for the Shop Location service, accessible via the API Gateway. Shop locations are Maintenance Bays and Mobile Shops.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  BulkIngestRequestStorageLocationBulkIngestRecord,
  BulkIngestResponse,
} from '../models/index';
import {
    BulkIngestRequestStorageLocationBulkIngestRecordFromJSON,
    BulkIngestRequestStorageLocationBulkIngestRecordToJSON,
    BulkIngestResponseFromJSON,
    BulkIngestResponseToJSON,
} from '../models/index';

export interface BulkIngestStorageLocationsRequest {
    bulkIngestRequestStorageLocationBulkIngestRecord: BulkIngestRequestStorageLocationBulkIngestRecord;
}

/**
 * 
 */
export class StorageLocationBulkIngestAPIApi extends runtime.BaseAPI {

    /**
     * Creates many storage locations at once, resolving each row\'s parent by name and applying a non-default status as a follow-up update. Use this tool when commissioning a site or seeding an environment; use createStorageLocation instead for a single location, and patchStorageLocation to change one that already exists. Preconditions: each row\'s site must exist, and a row naming a parent must either follow that parent in the same batch or name one already at the site. Required inputs: jobId (UUID), locationId (UUID) and records, each with a name and a type; a record\'s own siteId overrides the batch one. Emits a LOCATION_STORAGE_LOCATION_BULK_INGEST event, and a storage-location fact per created row, which is what hydrates the pos-inventory replica. Re-running the same file is safe: a name already present at its site is skipped, not failed, and an existing location is never modified — applying a changed status or capacity to a live location is an operator\'s update, not a reseed. Returns 200 with a per-record result; check each result rather than the status alone. 
     * Create a Site\'s Storage Topology in Bulk
     */
    async bulkIngestStorageLocationsRaw(requestParameters: BulkIngestStorageLocationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkIngestResponse>> {
        if (requestParameters['bulkIngestRequestStorageLocationBulkIngestRecord'] == null) {
            throw new runtime.RequiredError(
                'bulkIngestRequestStorageLocationBulkIngestRecord',
                'Required parameter "bulkIngestRequestStorageLocationBulkIngestRecord" was null or undefined when calling bulkIngestStorageLocations().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["location:write"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/locations/storage-locations/bulk-ingest`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkIngestRequestStorageLocationBulkIngestRecordToJSON(requestParameters['bulkIngestRequestStorageLocationBulkIngestRecord']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkIngestResponseFromJSON(jsonValue));
    }

    /**
     * Creates many storage locations at once, resolving each row\'s parent by name and applying a non-default status as a follow-up update. Use this tool when commissioning a site or seeding an environment; use createStorageLocation instead for a single location, and patchStorageLocation to change one that already exists. Preconditions: each row\'s site must exist, and a row naming a parent must either follow that parent in the same batch or name one already at the site. Required inputs: jobId (UUID), locationId (UUID) and records, each with a name and a type; a record\'s own siteId overrides the batch one. Emits a LOCATION_STORAGE_LOCATION_BULK_INGEST event, and a storage-location fact per created row, which is what hydrates the pos-inventory replica. Re-running the same file is safe: a name already present at its site is skipped, not failed, and an existing location is never modified — applying a changed status or capacity to a live location is an operator\'s update, not a reseed. Returns 200 with a per-record result; check each result rather than the status alone. 
     * Create a Site\'s Storage Topology in Bulk
     */
    async bulkIngestStorageLocations(requestParameters: BulkIngestStorageLocationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkIngestResponse> {
        const response = await this.bulkIngestStorageLocationsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
