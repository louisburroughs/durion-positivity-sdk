/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Order API
 * API documentation for the Order service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  ApprovePurchaseOrderRequest,
  CreatePurchaseOrderRequest,
  PagePurchaseOrderResponse,
  ProcurementAvailability,
  PurchaseOrderResponse,
  RevisePurchaseOrderRequest,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    ApprovePurchaseOrderRequestFromJSON,
    ApprovePurchaseOrderRequestToJSON,
    CreatePurchaseOrderRequestFromJSON,
    CreatePurchaseOrderRequestToJSON,
    PagePurchaseOrderResponseFromJSON,
    PagePurchaseOrderResponseToJSON,
    ProcurementAvailabilityFromJSON,
    ProcurementAvailabilityToJSON,
    PurchaseOrderResponseFromJSON,
    PurchaseOrderResponseToJSON,
    RevisePurchaseOrderRequestFromJSON,
    RevisePurchaseOrderRequestToJSON,
} from '../models/index';

export interface ApprovePurchaseOrderOperationRequest {
    poId: string;
    approvePurchaseOrderRequest: ApprovePurchaseOrderRequest;
}

export interface CancelPurchaseOrderRequest {
    poId: string;
}

export interface CreatePurchaseOrderOperationRequest {
    createPurchaseOrderRequest: CreatePurchaseOrderRequest;
}

export interface GetPurchaseOrderRequest {
    poId: string;
}

export interface GetPurchaseOrderSupplierAvailabilityRequest {
    poId: string;
    vendorProfileId: string;
    deliveryLocationId: string;
}

export interface ListPurchaseOrdersRequest {
    vendorId?: string;
    status?: ListPurchaseOrdersStatusEnum;
    currency?: string;
    locationId?: string;
    page?: number;
    size?: number;
    sort?: Array<string>;
}

export interface RevisePurchaseOrderOperationRequest {
    poId: string;
    revisePurchaseOrderRequest: RevisePurchaseOrderRequest;
}

export interface TransmitPurchaseOrderRequest {
    poId: string;
}

/**
 * 
 */
export class PurchaseOrdersApi extends runtime.BaseAPI {

    /**
     * Approves a DRAFT purchase order, moving it to APPROVED and recording the approver, timestamp and optional notes. Use this tool to release a drafted order for receiving; do not use cancelPurchaseOrder, which terminates the order, and note that receivePurchaseOrder rejects orders that were never approved. Preconditions: the purchase order must exist and be in DRAFT status; any other status is a conflict. Required inputs: poId (UUIDv7) path parameter and a JSON body where approvalNotes is optional, so an empty object is acceptable. Emits an ORDER_PURCHASE_ORDER_APPROVE event; the order becomes eligible for ASNs, goods receipts and receiving sessions. Returns 404 when the purchase order does not exist, and 409 when it is not in DRAFT status. 
     * Approve Purchase Order
     */
    async approvePurchaseOrderRaw(requestParameters: ApprovePurchaseOrderOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PurchaseOrderResponse>> {
        if (requestParameters['poId'] == null) {
            throw new runtime.RequiredError(
                'poId',
                'Required parameter "poId" was null or undefined when calling approvePurchaseOrder().'
            );
        }

        if (requestParameters['approvePurchaseOrderRequest'] == null) {
            throw new runtime.RequiredError(
                'approvePurchaseOrderRequest',
                'Required parameter "approvePurchaseOrderRequest" was null or undefined when calling approvePurchaseOrder().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["order:purchase_order:approve"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/orders/purchase-orders/{poId}/approve`.replace(`{${"poId"}}`, encodeURIComponent(String(requestParameters['poId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ApprovePurchaseOrderRequestToJSON(requestParameters['approvePurchaseOrderRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PurchaseOrderResponseFromJSON(jsonValue));
    }

    /**
     * Approves a DRAFT purchase order, moving it to APPROVED and recording the approver, timestamp and optional notes. Use this tool to release a drafted order for receiving; do not use cancelPurchaseOrder, which terminates the order, and note that receivePurchaseOrder rejects orders that were never approved. Preconditions: the purchase order must exist and be in DRAFT status; any other status is a conflict. Required inputs: poId (UUIDv7) path parameter and a JSON body where approvalNotes is optional, so an empty object is acceptable. Emits an ORDER_PURCHASE_ORDER_APPROVE event; the order becomes eligible for ASNs, goods receipts and receiving sessions. Returns 404 when the purchase order does not exist, and 409 when it is not in DRAFT status. 
     * Approve Purchase Order
     */
    async approvePurchaseOrder(requestParameters: ApprovePurchaseOrderOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PurchaseOrderResponse> {
        const response = await this.approvePurchaseOrderRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Cancels a purchase order, moving it to CANCELLED so it can no longer be approved or received. Use this tool when an order is no longer needed; do not use revisePurchaseOrder, which keeps the order alive with changed lines. Preconditions: the purchase order must exist and must not be FULLY_RECEIVED or CLOSED; any other status, including DRAFT, may be cancelled. Required inputs: poId (UUIDv7) path parameter; there is no request body and no confirmation flag. Emits an ORDER_PURCHASE_ORDER_CANCEL event and publishes the order\'s new state; a cancelled order is no longer counted as incoming supply by the modules that track it. Returns 404 when the purchase order does not exist, and 409 when it is FULLY_RECEIVED or CLOSED. 
     * Cancel Purchase Order
     */
    async cancelPurchaseOrderRaw(requestParameters: CancelPurchaseOrderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PurchaseOrderResponse>> {
        if (requestParameters['poId'] == null) {
            throw new runtime.RequiredError(
                'poId',
                'Required parameter "poId" was null or undefined when calling cancelPurchaseOrder().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["order:purchase_order:approve"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/orders/purchase-orders/{poId}/cancel`.replace(`{${"poId"}}`, encodeURIComponent(String(requestParameters['poId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PurchaseOrderResponseFromJSON(jsonValue));
    }

    /**
     * Cancels a purchase order, moving it to CANCELLED so it can no longer be approved or received. Use this tool when an order is no longer needed; do not use revisePurchaseOrder, which keeps the order alive with changed lines. Preconditions: the purchase order must exist and must not be FULLY_RECEIVED or CLOSED; any other status, including DRAFT, may be cancelled. Required inputs: poId (UUIDv7) path parameter; there is no request body and no confirmation flag. Emits an ORDER_PURCHASE_ORDER_CANCEL event and publishes the order\'s new state; a cancelled order is no longer counted as incoming supply by the modules that track it. Returns 404 when the purchase order does not exist, and 409 when it is FULLY_RECEIVED or CLOSED. 
     * Cancel Purchase Order
     */
    async cancelPurchaseOrder(requestParameters: CancelPurchaseOrderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PurchaseOrderResponse> {
        const response = await this.cancelPurchaseOrderRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a DRAFT purchase order with a generated PO number, computing subtotal, tax and grand total in minor currency units and initialising the open balance to the grand total. Use this tool when ordering stock from a vendor manually; do not use revisePurchaseOrder, which replaces the lines of an existing order, and do not use convertPurchaseSuggestions, which turns accepted replenishment suggestions into orders. Preconditions: none; the order starts in DRAFT and must pass approvePurchaseOrder before any receiving. Required inputs: vendorId (UUID), poDate, currency (ISO 4217) and lines (non-empty), each naming lineNumber, description and unitCostMinor plus either quantity in base UoM or the documentUom/documentQuantity pair; tax is derived from the configured default rate. Emits an ORDER_PURCHASE_ORDER_CREATE event; no stock, ledger or vendor-side state is touched. Returns 400 when a line\'s quantity is missing without the document-UoM pair, and 422 when a documentUom has no conversion path to the product\'s base UoM. 
     * Create Purchase Order
     */
    async createPurchaseOrderRaw(requestParameters: CreatePurchaseOrderOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PurchaseOrderResponse>> {
        if (requestParameters['createPurchaseOrderRequest'] == null) {
            throw new runtime.RequiredError(
                'createPurchaseOrderRequest',
                'Required parameter "createPurchaseOrderRequest" was null or undefined when calling createPurchaseOrder().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["order:purchase_order:create"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/orders/purchase-orders`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreatePurchaseOrderRequestToJSON(requestParameters['createPurchaseOrderRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PurchaseOrderResponseFromJSON(jsonValue));
    }

    /**
     * Creates a DRAFT purchase order with a generated PO number, computing subtotal, tax and grand total in minor currency units and initialising the open balance to the grand total. Use this tool when ordering stock from a vendor manually; do not use revisePurchaseOrder, which replaces the lines of an existing order, and do not use convertPurchaseSuggestions, which turns accepted replenishment suggestions into orders. Preconditions: none; the order starts in DRAFT and must pass approvePurchaseOrder before any receiving. Required inputs: vendorId (UUID), poDate, currency (ISO 4217) and lines (non-empty), each naming lineNumber, description and unitCostMinor plus either quantity in base UoM or the documentUom/documentQuantity pair; tax is derived from the configured default rate. Emits an ORDER_PURCHASE_ORDER_CREATE event; no stock, ledger or vendor-side state is touched. Returns 400 when a line\'s quantity is missing without the document-UoM pair, and 422 when a documentUom has no conversion path to the product\'s base UoM. 
     * Create Purchase Order
     */
    async createPurchaseOrder(requestParameters: CreatePurchaseOrderOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PurchaseOrderResponse> {
        const response = await this.createPurchaseOrderRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one purchase order with its lifecycle status, totals, open balance and lines with open quantities. Use this tool when the poId is already known; use listPurchaseOrders instead to search by vendor, status, currency or ship-to location. Preconditions: the purchase order must exist. Required inputs: poId (UUIDv7) path parameter; there is no request body. Emits an ORDER_PURCHASE_ORDER_GET audit event; no stock state changes, this is a read-only projection. Returns 404 when no purchase order exists for the supplied id. 
     * Get Purchase Order
     */
    async getPurchaseOrderRaw(requestParameters: GetPurchaseOrderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PurchaseOrderResponse>> {
        if (requestParameters['poId'] == null) {
            throw new runtime.RequiredError(
                'poId',
                'Required parameter "poId" was null or undefined when calling getPurchaseOrder().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["order:purchase_order:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/orders/purchase-orders/{poId}`.replace(`{${"poId"}}`, encodeURIComponent(String(requestParameters['poId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PurchaseOrderResponseFromJSON(jsonValue));
    }

    /**
     * Returns one purchase order with its lifecycle status, totals, open balance and lines with open quantities. Use this tool when the poId is already known; use listPurchaseOrders instead to search by vendor, status, currency or ship-to location. Preconditions: the purchase order must exist. Required inputs: poId (UUIDv7) path parameter; there is no request body. Emits an ORDER_PURCHASE_ORDER_GET audit event; no stock state changes, this is a read-only projection. Returns 404 when no purchase order exists for the supplied id. 
     * Get Purchase Order
     */
    async getPurchaseOrder(requestParameters: GetPurchaseOrderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PurchaseOrderResponse> {
        const response = await this.getPurchaseOrderRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Asks a vendor what it can supply against every line of a purchase order, in one inquiry, and returns the answer per line. Use this tool when a buyer is deciding what to order and needs what the vendor says now; do not use the availability endpoints for this, which report owned stock rather than what a supplier could send. Preconditions: the purchase order must exist; the vendor profile and delivery location are both required, because availability is consignee-specific and answering for another location answers a different question. Required inputs: poId path parameter, and vendorProfileId and deliveryLocationId query parameters. Emits an ORDER_PURCHASE_ORDER_AVAILABILITY audit event; no state changes, and a supplier that cannot be reached degrades the answer rather than failing the request. Returns 200 with a document status of SUPPLIER_UNAVAILABLE and no line quantities when the vendor could not be asked; availableQuantity is null where the vendor stated nothing and zero only where it stated it has none, and 404 when the purchase order does not exist. 
     * Read Live Vendor Availability For A Purchase Order
     */
    async getPurchaseOrderSupplierAvailabilityRaw(requestParameters: GetPurchaseOrderSupplierAvailabilityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProcurementAvailability>> {
        if (requestParameters['poId'] == null) {
            throw new runtime.RequiredError(
                'poId',
                'Required parameter "poId" was null or undefined when calling getPurchaseOrderSupplierAvailability().'
            );
        }

        if (requestParameters['vendorProfileId'] == null) {
            throw new runtime.RequiredError(
                'vendorProfileId',
                'Required parameter "vendorProfileId" was null or undefined when calling getPurchaseOrderSupplierAvailability().'
            );
        }

        if (requestParameters['deliveryLocationId'] == null) {
            throw new runtime.RequiredError(
                'deliveryLocationId',
                'Required parameter "deliveryLocationId" was null or undefined when calling getPurchaseOrderSupplierAvailability().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['vendorProfileId'] != null) {
            queryParameters['vendorProfileId'] = requestParameters['vendorProfileId'];
        }

        if (requestParameters['deliveryLocationId'] != null) {
            queryParameters['deliveryLocationId'] = requestParameters['deliveryLocationId'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["order:purchase_order:availability_view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/orders/purchase-orders/{poId}/supplier-availability`.replace(`{${"poId"}}`, encodeURIComponent(String(requestParameters['poId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProcurementAvailabilityFromJSON(jsonValue));
    }

    /**
     * Asks a vendor what it can supply against every line of a purchase order, in one inquiry, and returns the answer per line. Use this tool when a buyer is deciding what to order and needs what the vendor says now; do not use the availability endpoints for this, which report owned stock rather than what a supplier could send. Preconditions: the purchase order must exist; the vendor profile and delivery location are both required, because availability is consignee-specific and answering for another location answers a different question. Required inputs: poId path parameter, and vendorProfileId and deliveryLocationId query parameters. Emits an ORDER_PURCHASE_ORDER_AVAILABILITY audit event; no state changes, and a supplier that cannot be reached degrades the answer rather than failing the request. Returns 200 with a document status of SUPPLIER_UNAVAILABLE and no line quantities when the vendor could not be asked; availableQuantity is null where the vendor stated nothing and zero only where it stated it has none, and 404 when the purchase order does not exist. 
     * Read Live Vendor Availability For A Purchase Order
     */
    async getPurchaseOrderSupplierAvailability(requestParameters: GetPurchaseOrderSupplierAvailabilityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProcurementAvailability> {
        const response = await this.getPurchaseOrderSupplierAvailabilityRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns a page of purchase orders matching the optional filter criteria, with lifecycle status, totals and open balances. Use this tool to discover a poId or survey open orders; use getPurchaseOrder instead when the id is already known. Preconditions: none; every filter is optional and an unfiltered call pages over all orders. Required inputs: none; vendorId, status, currency and locationId narrow the result, and standard page, size and sort parameters control pagination. Emits an ORDER_PURCHASE_ORDER_LIST audit event; no stock state changes, this is a read-only projection. Returns 200 with an empty page when nothing matches, so an empty result is not an error condition. 
     * List Purchase Orders
     */
    async listPurchaseOrdersRaw(requestParameters: ListPurchaseOrdersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PagePurchaseOrderResponse>> {
        const queryParameters: any = {};

        if (requestParameters['vendorId'] != null) {
            queryParameters['vendorId'] = requestParameters['vendorId'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['currency'] != null) {
            queryParameters['currency'] = requestParameters['currency'];
        }

        if (requestParameters['locationId'] != null) {
            queryParameters['locationId'] = requestParameters['locationId'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        if (requestParameters['sort'] != null) {
            queryParameters['sort'] = requestParameters['sort'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["order:purchase_order:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/orders/purchase-orders`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PagePurchaseOrderResponseFromJSON(jsonValue));
    }

    /**
     * Returns a page of purchase orders matching the optional filter criteria, with lifecycle status, totals and open balances. Use this tool to discover a poId or survey open orders; use getPurchaseOrder instead when the id is already known. Preconditions: none; every filter is optional and an unfiltered call pages over all orders. Required inputs: none; vendorId, status, currency and locationId narrow the result, and standard page, size and sort parameters control pagination. Emits an ORDER_PURCHASE_ORDER_LIST audit event; no stock state changes, this is a read-only projection. Returns 200 with an empty page when nothing matches, so an empty result is not an error condition. 
     * List Purchase Orders
     */
    async listPurchaseOrders(requestParameters: ListPurchaseOrdersRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PagePurchaseOrderResponse> {
        const response = await this.listPurchaseOrdersRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Revises an existing purchase order in place: the version number increments, the header fields and every line are replaced, totals are recomputed, and the open balance resets to the value of the new lines. Use this tool to change quantities, costs or delivery terms on an order; do not use createPurchaseOrder, which starts a new order, and use cancelPurchaseOrder instead when the order should be terminated rather than changed. Preconditions: the purchase order must exist; the service applies no status gate, so a revision replaces lines and open balance in any lifecycle status, discarding received-quantity bookkeeping on the replaced lines. Required inputs: poId (UUIDv7) path parameter plus poDate, revisionReason and the full replacement lines list, because every line is replaced rather than merged; paymentTermsId, expectedDeliveryDate, shipToLocationId, requestedBy and comment overwrite the stored values even when omitted. Emits an ORDER_PURCHASE_ORDER_REVISE event carrying the field-level delta and the revision reason. Returns 404 when the purchase order does not exist, and 422 when a line\'s documentUom has no conversion path to the product\'s base UoM. 
     * Revise Purchase Order
     */
    async revisePurchaseOrderRaw(requestParameters: RevisePurchaseOrderOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PurchaseOrderResponse>> {
        if (requestParameters['poId'] == null) {
            throw new runtime.RequiredError(
                'poId',
                'Required parameter "poId" was null or undefined when calling revisePurchaseOrder().'
            );
        }

        if (requestParameters['revisePurchaseOrderRequest'] == null) {
            throw new runtime.RequiredError(
                'revisePurchaseOrderRequest',
                'Required parameter "revisePurchaseOrderRequest" was null or undefined when calling revisePurchaseOrder().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["order:purchase_order:create"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/orders/purchase-orders/{poId}/revisions`.replace(`{${"poId"}}`, encodeURIComponent(String(requestParameters['poId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RevisePurchaseOrderRequestToJSON(requestParameters['revisePurchaseOrderRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PurchaseOrderResponseFromJSON(jsonValue));
    }

    /**
     * Revises an existing purchase order in place: the version number increments, the header fields and every line are replaced, totals are recomputed, and the open balance resets to the value of the new lines. Use this tool to change quantities, costs or delivery terms on an order; do not use createPurchaseOrder, which starts a new order, and use cancelPurchaseOrder instead when the order should be terminated rather than changed. Preconditions: the purchase order must exist; the service applies no status gate, so a revision replaces lines and open balance in any lifecycle status, discarding received-quantity bookkeeping on the replaced lines. Required inputs: poId (UUIDv7) path parameter plus poDate, revisionReason and the full replacement lines list, because every line is replaced rather than merged; paymentTermsId, expectedDeliveryDate, shipToLocationId, requestedBy and comment overwrite the stored values even when omitted. Emits an ORDER_PURCHASE_ORDER_REVISE event carrying the field-level delta and the revision reason. Returns 404 when the purchase order does not exist, and 422 when a line\'s documentUom has no conversion path to the product\'s base UoM. 
     * Revise Purchase Order
     */
    async revisePurchaseOrder(requestParameters: RevisePurchaseOrderOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PurchaseOrderResponse> {
        const response = await this.revisePurchaseOrderRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Sends an approved purchase order to the supplier named on it, asking pos-supplier to transmit it and report back what the vendor says. Use this tool to actually place an order with a vendor; do not use approvePurchaseOrder for this, which commits the spend internally and sends nothing to anyone. Preconditions: the order must be APPROVED or PARTIALLY_RECEIVED, must name a supplierRef, must have no transmission already in flight or awaiting review, and every line must carry an article code the vendor would recognise. Required inputs: poId (UUIDv7) path parameter; there is no request body, because what is sent is the order as it stands. Emits an ORDER_PURCHASE_ORDER_TRANSMIT event and queues a supplier order command; the vendor\'s answer arrives later and appears on the order\'s transmission state and timeline. Returns 404 when the purchase order does not exist, and 422 with a code naming the obstacle when the order cannot be sent: SUPPLIER_REF_MISSING, PURCHASE_ORDER_NOT_APPROVED, TRANSMISSION_IN_FLIGHT, TRANSMISSION_AWAITING_REVIEW, ARTICLE_NOT_IDENTIFIABLE or FRACTIONAL_QUANTITY. 
     * Transmit Purchase Order To Vendor
     */
    async transmitPurchaseOrderRaw(requestParameters: TransmitPurchaseOrderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['poId'] == null) {
            throw new runtime.RequiredError(
                'poId',
                'Required parameter "poId" was null or undefined when calling transmitPurchaseOrder().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["order:purchase_order:transmit"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/orders/purchase-orders/{poId}/transmit`.replace(`{${"poId"}}`, encodeURIComponent(String(requestParameters['poId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Sends an approved purchase order to the supplier named on it, asking pos-supplier to transmit it and report back what the vendor says. Use this tool to actually place an order with a vendor; do not use approvePurchaseOrder for this, which commits the spend internally and sends nothing to anyone. Preconditions: the order must be APPROVED or PARTIALLY_RECEIVED, must name a supplierRef, must have no transmission already in flight or awaiting review, and every line must carry an article code the vendor would recognise. Required inputs: poId (UUIDv7) path parameter; there is no request body, because what is sent is the order as it stands. Emits an ORDER_PURCHASE_ORDER_TRANSMIT event and queues a supplier order command; the vendor\'s answer arrives later and appears on the order\'s transmission state and timeline. Returns 404 when the purchase order does not exist, and 422 with a code naming the obstacle when the order cannot be sent: SUPPLIER_REF_MISSING, PURCHASE_ORDER_NOT_APPROVED, TRANSMISSION_IN_FLIGHT, TRANSMISSION_AWAITING_REVIEW, ARTICLE_NOT_IDENTIFIABLE or FRACTIONAL_QUANTITY. 
     * Transmit Purchase Order To Vendor
     */
    async transmitPurchaseOrder(requestParameters: TransmitPurchaseOrderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.transmitPurchaseOrderRaw(requestParameters, initOverrides);
    }

}

/**
  * @export
  * @enum {string}
  */
export enum ListPurchaseOrdersStatusEnum {
    Draft = 'DRAFT',
    Approved = 'APPROVED',
    PartiallyReceived = 'PARTIALLY_RECEIVED',
    FullyReceived = 'FULLY_RECEIVED',
    Closed = 'CLOSED',
    Cancelled = 'CANCELLED'
}
