/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Order API
 * API documentation for the Order service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  BeginCloseRequest,
  CashMovementRequest,
  CashMovementResponse,
  OpenSessionRequest,
  RegisterSessionResponse,
  SessionReportResponse,
} from '../models/index';
import {
    BeginCloseRequestFromJSON,
    BeginCloseRequestToJSON,
    CashMovementRequestFromJSON,
    CashMovementRequestToJSON,
    CashMovementResponseFromJSON,
    CashMovementResponseToJSON,
    OpenSessionRequestFromJSON,
    OpenSessionRequestToJSON,
    RegisterSessionResponseFromJSON,
    RegisterSessionResponseToJSON,
    SessionReportResponseFromJSON,
    SessionReportResponseToJSON,
} from '../models/index';

export interface BeginSessionCloseRequest {
    sessionId: string;
    beginCloseRequest: BeginCloseRequest;
}

export interface ConfirmSessionCloseRequest {
    sessionId: string;
}

export interface GetCurrentRegisterSessionRequest {
    terminalId: string;
}

export interface GetRegisterSessionRequest {
    sessionId: string;
}

export interface GetSessionXReportRequest {
    sessionId: string;
}

export interface GetSessionZReportRequest {
    sessionId: string;
}

export interface ListCashMovementsRequest {
    sessionId: string;
}

export interface OpenRegisterSessionRequest {
    openSessionRequest: OpenSessionRequest;
}

export interface RecordCashMovementRequest {
    sessionId: string;
    cashMovementRequest: CashMovementRequest;
}

/**
 * 
 */
export class RegisterSessionsApi extends runtime.BaseAPI {

    /**
     * Records the physically counted drawer cash and moves the register session to CLOSING, freezing the terminal against new orders. Use this tool to start the drawer count at end of shift; do not use confirmSessionClose, which finalizes a session already in CLOSING. Preconditions: the session must exist, must not already be CLOSED, and none of its orders may be in PENDING_PAYMENT. Required inputs: countedCash (zero or greater) in the body and sessionId (UUID) as a path parameter. Emits an ORDER_SESSION_BEGIN_CLOSE event. Returns 200 with the CLOSING session, 404 when the session does not exist, and 409 when the session is already closed or an order on the session is still awaiting payment. 
     * Begin Closing a Register Session
     */
    async beginSessionCloseRaw(requestParameters: BeginSessionCloseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RegisterSessionResponse>> {
        if (requestParameters['sessionId'] == null) {
            throw new runtime.RequiredError(
                'sessionId',
                'Required parameter "sessionId" was null or undefined when calling beginSessionClose().'
            );
        }

        if (requestParameters['beginCloseRequest'] == null) {
            throw new runtime.RequiredError(
                'beginCloseRequest',
                'Required parameter "beginCloseRequest" was null or undefined when calling beginSessionClose().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/orders/sessions/{sessionId}/begin-close`.replace(`{${"sessionId"}}`, encodeURIComponent(String(requestParameters['sessionId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BeginCloseRequestToJSON(requestParameters['beginCloseRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RegisterSessionResponseFromJSON(jsonValue));
    }

    /**
     * Records the physically counted drawer cash and moves the register session to CLOSING, freezing the terminal against new orders. Use this tool to start the drawer count at end of shift; do not use confirmSessionClose, which finalizes a session already in CLOSING. Preconditions: the session must exist, must not already be CLOSED, and none of its orders may be in PENDING_PAYMENT. Required inputs: countedCash (zero or greater) in the body and sessionId (UUID) as a path parameter. Emits an ORDER_SESSION_BEGIN_CLOSE event. Returns 200 with the CLOSING session, 404 when the session does not exist, and 409 when the session is already closed or an order on the session is still awaiting payment. 
     * Begin Closing a Register Session
     */
    async beginSessionClose(requestParameters: BeginSessionCloseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RegisterSessionResponse> {
        const response = await this.beginSessionCloseRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Finalizes a CLOSING register session: snapshots theoretical cash (opening float plus net CASH settlements plus signed cash movements), computes the over/short against the counted drawer, and moves the session to CLOSED. Use this tool to finish the close after the count; do not use beginSessionClose, which records the count and must run first. Preconditions: the session must be in CLOSING, and no order on the session may have re-entered PENDING_PAYMENT since the count began. Required inputs: sessionId (UUID) as a path parameter; there is no request body — an over/short beyond the authorized difference limit (default 5.00, configurable via pos.order.session.authorized-difference-limit) additionally requires the order:session:approve_variance permission. Emits an ORDER_SESSION_CONFIRM_CLOSE event and publishes a register-session-closed fact carrying per-tender totals and the reconciliation figures. Returns 200 with the CLOSED session, 403 when the variance exceeds the limit without the approval permission, 404 when the session does not exist, and 409 when the session is not in CLOSING or an order is still awaiting payment. 
     * Confirm a Register Session Close
     */
    async confirmSessionCloseRaw(requestParameters: ConfirmSessionCloseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RegisterSessionResponse>> {
        if (requestParameters['sessionId'] == null) {
            throw new runtime.RequiredError(
                'sessionId',
                'Required parameter "sessionId" was null or undefined when calling confirmSessionClose().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/orders/sessions/{sessionId}/confirm-close`.replace(`{${"sessionId"}}`, encodeURIComponent(String(requestParameters['sessionId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RegisterSessionResponseFromJSON(jsonValue));
    }

    /**
     * Finalizes a CLOSING register session: snapshots theoretical cash (opening float plus net CASH settlements plus signed cash movements), computes the over/short against the counted drawer, and moves the session to CLOSED. Use this tool to finish the close after the count; do not use beginSessionClose, which records the count and must run first. Preconditions: the session must be in CLOSING, and no order on the session may have re-entered PENDING_PAYMENT since the count began. Required inputs: sessionId (UUID) as a path parameter; there is no request body — an over/short beyond the authorized difference limit (default 5.00, configurable via pos.order.session.authorized-difference-limit) additionally requires the order:session:approve_variance permission. Emits an ORDER_SESSION_CONFIRM_CLOSE event and publishes a register-session-closed fact carrying per-tender totals and the reconciliation figures. Returns 200 with the CLOSED session, 403 when the variance exceeds the limit without the approval permission, 404 when the session does not exist, and 409 when the session is not in CLOSING or an order is still awaiting payment. 
     * Confirm a Register Session Close
     */
    async confirmSessionClose(requestParameters: ConfirmSessionCloseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RegisterSessionResponse> {
        const response = await this.confirmSessionCloseRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the active register session on a terminal, preferring an OPEN session and falling back to one in CLOSING. Use this tool to resolve which drawer a terminal is on; use getRegisterSession instead when the session id is already known. Preconditions: none — a terminal without an active session is a normal outcome. Required inputs: terminalId as a query parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 200 with the OPEN or CLOSING session, and 204 when the terminal has no active session. 
     * Get the Current Session for a Terminal
     */
    async getCurrentRegisterSessionRaw(requestParameters: GetCurrentRegisterSessionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RegisterSessionResponse>> {
        if (requestParameters['terminalId'] == null) {
            throw new runtime.RequiredError(
                'terminalId',
                'Required parameter "terminalId" was null or undefined when calling getCurrentRegisterSession().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['terminalId'] != null) {
            queryParameters['terminalId'] = requestParameters['terminalId'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/orders/sessions/current`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RegisterSessionResponseFromJSON(jsonValue));
    }

    /**
     * Returns the active register session on a terminal, preferring an OPEN session and falling back to one in CLOSING. Use this tool to resolve which drawer a terminal is on; use getRegisterSession instead when the session id is already known. Preconditions: none — a terminal without an active session is a normal outcome. Required inputs: terminalId as a query parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 200 with the OPEN or CLOSING session, and 204 when the terminal has no active session. 
     * Get the Current Session for a Terminal
     */
    async getCurrentRegisterSession(requestParameters: GetCurrentRegisterSessionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RegisterSessionResponse> {
        const response = await this.getCurrentRegisterSessionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns a register session with its status, opening float, counted and theoretical cash, over/short, and lifecycle timestamps. Use this tool when the session id is already known; use getCurrentRegisterSession instead to resolve the active session from a terminal id. Preconditions: the session must exist. Required inputs: sessionId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no register session exists for the supplied id. 
     * Get a Register Session
     */
    async getRegisterSessionRaw(requestParameters: GetRegisterSessionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RegisterSessionResponse>> {
        if (requestParameters['sessionId'] == null) {
            throw new runtime.RequiredError(
                'sessionId',
                'Required parameter "sessionId" was null or undefined when calling getRegisterSession().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/orders/sessions/{sessionId}`.replace(`{${"sessionId"}}`, encodeURIComponent(String(requestParameters['sessionId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RegisterSessionResponseFromJSON(jsonValue));
    }

    /**
     * Returns a register session with its status, opening float, counted and theoretical cash, over/short, and lifecycle timestamps. Use this tool when the session id is already known; use getCurrentRegisterSession instead to resolve the active session from a terminal id. Preconditions: the session must exist. Required inputs: sessionId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no register session exists for the supplied id. 
     * Get a Register Session
     */
    async getRegisterSession(requestParameters: GetRegisterSessionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RegisterSessionResponse> {
        const response = await this.getRegisterSessionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns an interim X-report for a register session: opening float, per-tender totals, cash settlements, cash movements, theoretical cash, and over/short when a count has been recorded. Use this tool for mid-shift figures while the session is open; use getSessionZReport instead for the end-of-session close summary. Preconditions: the session must exist; figures are computed live from the session\'s current ledger. Required inputs: sessionId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only report projection. Returns 404 when no register session exists for the supplied id. 
     * X-Report for a Register Session
     */
    async getSessionXReportRaw(requestParameters: GetSessionXReportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SessionReportResponse>> {
        if (requestParameters['sessionId'] == null) {
            throw new runtime.RequiredError(
                'sessionId',
                'Required parameter "sessionId" was null or undefined when calling getSessionXReport().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/orders/sessions/{sessionId}/x-report`.replace(`{${"sessionId"}}`, encodeURIComponent(String(requestParameters['sessionId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SessionReportResponseFromJSON(jsonValue));
    }

    /**
     * Returns an interim X-report for a register session: opening float, per-tender totals, cash settlements, cash movements, theoretical cash, and over/short when a count has been recorded. Use this tool for mid-shift figures while the session is open; use getSessionZReport instead for the end-of-session close summary. Preconditions: the session must exist; figures are computed live from the session\'s current ledger. Required inputs: sessionId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only report projection. Returns 404 when no register session exists for the supplied id. 
     * X-Report for a Register Session
     */
    async getSessionXReport(requestParameters: GetSessionXReportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SessionReportResponse> {
        const response = await this.getSessionXReportRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the Z-report close summary for a register session, including per-tender totals, cash movements, theoretical cash, counted cash, and the over/short variance. Use this tool for the end-of-session summary after close; use getSessionXReport instead for interim mid-shift figures. Preconditions: the session must exist; the report reflects the session\'s current ledger, so it is authoritative once the session is CLOSED. Required inputs: sessionId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only report projection. Returns 404 when no register session exists for the supplied id. 
     * Z-Report for a Register Session
     */
    async getSessionZReportRaw(requestParameters: GetSessionZReportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SessionReportResponse>> {
        if (requestParameters['sessionId'] == null) {
            throw new runtime.RequiredError(
                'sessionId',
                'Required parameter "sessionId" was null or undefined when calling getSessionZReport().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/orders/sessions/{sessionId}/z-report`.replace(`{${"sessionId"}}`, encodeURIComponent(String(requestParameters['sessionId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SessionReportResponseFromJSON(jsonValue));
    }

    /**
     * Returns the Z-report close summary for a register session, including per-tender totals, cash movements, theoretical cash, counted cash, and the over/short variance. Use this tool for the end-of-session summary after close; use getSessionXReport instead for interim mid-shift figures. Preconditions: the session must exist; the report reflects the session\'s current ledger, so it is authoritative once the session is CLOSED. Required inputs: sessionId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only report projection. Returns 404 when no register session exists for the supplied id. 
     * Z-Report for a Register Session
     */
    async getSessionZReport(requestParameters: GetSessionZReportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SessionReportResponse> {
        const response = await this.getSessionZReportRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists every recorded cash movement for a register session in the order they occurred. Use this tool to review drawer ins and outs; use getSessionXReport instead for the aggregated mid-day figures that include tender totals and theoretical cash. Preconditions: the session must exist. Required inputs: sessionId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 200 with a possibly empty list, and 404 when the session does not exist. 
     * List a Session\'s Cash Movements
     */
    async listCashMovementsRaw(requestParameters: ListCashMovementsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<CashMovementResponse>>> {
        if (requestParameters['sessionId'] == null) {
            throw new runtime.RequiredError(
                'sessionId',
                'Required parameter "sessionId" was null or undefined when calling listCashMovements().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/orders/sessions/{sessionId}/cash-movements`.replace(`{${"sessionId"}}`, encodeURIComponent(String(requestParameters['sessionId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(CashMovementResponseFromJSON));
    }

    /**
     * Lists every recorded cash movement for a register session in the order they occurred. Use this tool to review drawer ins and outs; use getSessionXReport instead for the aggregated mid-day figures that include tender totals and theoretical cash. Preconditions: the session must exist. Required inputs: sessionId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 200 with a possibly empty list, and 404 when the session does not exist. 
     * List a Session\'s Cash Movements
     */
    async listCashMovements(requestParameters: ListCashMovementsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<CashMovementResponse>> {
        const response = await this.listCashMovementsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Opens an OPEN register (drawer) session on a terminal; sales orders created on the terminal while it is open bind to it, and it supplies their location by default. Use this tool at the start of a drawer shift; do not use recordCashMovement, which requires a session that is already open. Preconditions: the terminal must have no session in OPEN or CLOSING — one drawer per terminal. Required inputs: terminalId and openedByClerkId; openingFloat defaults to the terminal\'s previous counted close (else zero) when omitted, and locationId defaults from the terminal\'s previous session. Emits an ORDER_SESSION_OPEN event. Returns 201 with the new session, and 409 when the terminal already has an active session. 
     * Open a Register Session
     */
    async openRegisterSessionRaw(requestParameters: OpenRegisterSessionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RegisterSessionResponse>> {
        if (requestParameters['openSessionRequest'] == null) {
            throw new runtime.RequiredError(
                'openSessionRequest',
                'Required parameter "openSessionRequest" was null or undefined when calling openRegisterSession().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/orders/sessions`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: OpenSessionRequestToJSON(requestParameters['openSessionRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RegisterSessionResponseFromJSON(jsonValue));
    }

    /**
     * Opens an OPEN register (drawer) session on a terminal; sales orders created on the terminal while it is open bind to it, and it supplies their location by default. Use this tool at the start of a drawer shift; do not use recordCashMovement, which requires a session that is already open. Preconditions: the terminal must have no session in OPEN or CLOSING — one drawer per terminal. Required inputs: terminalId and openedByClerkId; openingFloat defaults to the terminal\'s previous counted close (else zero) when omitted, and locationId defaults from the terminal\'s previous session. Emits an ORDER_SESSION_OPEN event. Returns 201 with the new session, and 409 when the terminal already has an active session. 
     * Open a Register Session
     */
    async openRegisterSession(requestParameters: OpenRegisterSessionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RegisterSessionResponse> {
        const response = await this.openRegisterSessionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Records a PAID_IN or PAID_OUT cash movement against an OPEN register session; movements feed the theoretical-cash calculation at close. Use this tool for non-sale drawer cash such as petty cash or bank drops; do not use beginSessionClose, which records the final counted drawer instead. Preconditions: the session must exist and be OPEN — movements are rejected once closing has begun. Required inputs: movementType (PAID_IN or PAID_OUT), a positive amount, reason, and clerkId. Emits an ORDER_SESSION_CASH_MOVEMENT event. Returns 201 with the recorded movement, 400 when the amount is not positive or the movement type is unknown, 404 when the session does not exist, and 409 when the session is not OPEN. 
     * Record a Drawer Cash Movement
     */
    async recordCashMovementRaw(requestParameters: RecordCashMovementRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CashMovementResponse>> {
        if (requestParameters['sessionId'] == null) {
            throw new runtime.RequiredError(
                'sessionId',
                'Required parameter "sessionId" was null or undefined when calling recordCashMovement().'
            );
        }

        if (requestParameters['cashMovementRequest'] == null) {
            throw new runtime.RequiredError(
                'cashMovementRequest',
                'Required parameter "cashMovementRequest" was null or undefined when calling recordCashMovement().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/orders/sessions/{sessionId}/cash-movements`.replace(`{${"sessionId"}}`, encodeURIComponent(String(requestParameters['sessionId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CashMovementRequestToJSON(requestParameters['cashMovementRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CashMovementResponseFromJSON(jsonValue));
    }

    /**
     * Records a PAID_IN or PAID_OUT cash movement against an OPEN register session; movements feed the theoretical-cash calculation at close. Use this tool for non-sale drawer cash such as petty cash or bank drops; do not use beginSessionClose, which records the final counted drawer instead. Preconditions: the session must exist and be OPEN — movements are rejected once closing has begun. Required inputs: movementType (PAID_IN or PAID_OUT), a positive amount, reason, and clerkId. Emits an ORDER_SESSION_CASH_MOVEMENT event. Returns 201 with the recorded movement, 400 when the amount is not positive or the movement type is unknown, 404 when the session does not exist, and 409 when the session is not OPEN. 
     * Record a Drawer Cash Movement
     */
    async recordCashMovement(requestParameters: RecordCashMovementRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CashMovementResponse> {
        const response = await this.recordCashMovementRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
