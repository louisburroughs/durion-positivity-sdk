/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Product API
 * API documentation for the Product service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  BulkIngestRequestServicePackageMemberBulkIngestRecord,
  BulkIngestResponse,
} from '../models/index';
import {
    BulkIngestRequestServicePackageMemberBulkIngestRecordFromJSON,
    BulkIngestRequestServicePackageMemberBulkIngestRecordToJSON,
    BulkIngestResponseFromJSON,
    BulkIngestResponseToJSON,
} from '../models/index';

export interface BulkIngestServicePackageMembersRequest {
    bulkIngestRequestServicePackageMemberBulkIngestRecord: BulkIngestRequestServicePackageMemberBulkIngestRecord;
}

/**
 * 
 */
export class ServicePackageMemberBulkIngestAPIApi extends runtime.BaseAPI {

    /**
     * Applies a batch of package memberships, naming the package by its code and the member by its Durion operation code, with a per-row success or failure verdict. Use this tool after bulkIngestServicePackages has loaded the packages themselves; do not use addServicePackageMember, which takes ids rather than codes and refuses an operation the package already carries instead of restating its sequence and quantity. Preconditions: both the packageCode and the operationCode must already exist, or that row fails while the rest of the batch proceeds. Required inputs: jobId (UUID), locationId (UUID) and a non-empty records array; per record, packageCode and operationCode, with sequence, quantity and required optional. Emits a CATALOG_SERVICE_PACKAGE_MEMBER_BULK_INGEST event; a membership that already exists has its sequence, quantity and required flag replaced rather than being duplicated. Returns 200 even when rows fail, so callers must inspect successCount, failureCount and the per-row results rather than the HTTP status. A row the catalog refused carries errorCode SERVICE_PACKAGE_MEMBER_INGEST_FAILED and the reason; a row lost to a server-side fault carries INTERNAL_ERROR and a correlationId to quote. 
     * Bulk Import Service Package Membership
     */
    async bulkIngestServicePackageMembersRaw(requestParameters: BulkIngestServicePackageMembersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkIngestResponse>> {
        if (requestParameters['bulkIngestRequestServicePackageMemberBulkIngestRecord'] == null) {
            throw new runtime.RequiredError(
                'bulkIngestRequestServicePackageMemberBulkIngestRecord',
                'Required parameter "bulkIngestRequestServicePackageMemberBulkIngestRecord" was null or undefined when calling bulkIngestServicePackageMembers().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:service_package:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/service-package-members/bulk-ingest`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkIngestRequestServicePackageMemberBulkIngestRecordToJSON(requestParameters['bulkIngestRequestServicePackageMemberBulkIngestRecord']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkIngestResponseFromJSON(jsonValue));
    }

    /**
     * Applies a batch of package memberships, naming the package by its code and the member by its Durion operation code, with a per-row success or failure verdict. Use this tool after bulkIngestServicePackages has loaded the packages themselves; do not use addServicePackageMember, which takes ids rather than codes and refuses an operation the package already carries instead of restating its sequence and quantity. Preconditions: both the packageCode and the operationCode must already exist, or that row fails while the rest of the batch proceeds. Required inputs: jobId (UUID), locationId (UUID) and a non-empty records array; per record, packageCode and operationCode, with sequence, quantity and required optional. Emits a CATALOG_SERVICE_PACKAGE_MEMBER_BULK_INGEST event; a membership that already exists has its sequence, quantity and required flag replaced rather than being duplicated. Returns 200 even when rows fail, so callers must inspect successCount, failureCount and the per-row results rather than the HTTP status. A row the catalog refused carries errorCode SERVICE_PACKAGE_MEMBER_INGEST_FAILED and the reason; a row lost to a server-side fault carries INTERNAL_ERROR and a correlationId to quote. 
     * Bulk Import Service Package Membership
     */
    async bulkIngestServicePackageMembers(requestParameters: BulkIngestServicePackageMembersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkIngestResponse> {
        const response = await this.bulkIngestServicePackageMembersRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
