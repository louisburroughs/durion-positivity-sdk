/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Product API
 * API documentation for the Product service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ProductUomCreateRequestDto,
  ProductUomDto,
  ProductUomUpdateRequestDto,
} from '../models/index';
import {
    ProductUomCreateRequestDtoFromJSON,
    ProductUomCreateRequestDtoToJSON,
    ProductUomDtoFromJSON,
    ProductUomDtoToJSON,
    ProductUomUpdateRequestDtoFromJSON,
    ProductUomUpdateRequestDtoToJSON,
} from '../models/index';

export interface AddProductUomRequest {
    productId: string;
    productUomCreateRequestDto: ProductUomCreateRequestDto;
}

export interface DeleteProductUomRequest {
    productId: string;
    uomId: string;
}

export interface ListProductUomsRequest {
    productId: string;
}

export interface UpdateProductUomRequest {
    productId: string;
    uomId: string;
    productUomUpdateRequestDto: ProductUomUpdateRequestDto;
}

/**
 * 
 */
export class ProductUoMAPIApi extends runtime.BaseAPI {

    /**
     * Adds a unit-of-measure entry to a product\'s conversion set, binding a UoM code to its factor relative to the product\'s base unit; the code is normalised to upper case and the factor stored at six decimal places. Use this tool to define how a purchase or pack unit converts for one product; do not use createUomConversion, which defines global cross-unit conversions not tied to a product. Preconditions: the product must exist and must not already define the UoM code; a BASE-type entry must carry factorToBase exactly 1. Required inputs: productId (UUID) path parameter plus uomCode, uomType (BASE, PURCHASE or PACK) and a positive factorToBase; precisionScale is optional. Emits a CATALOG_PRODUCT_UOM_CREATE event and re-publishes the product fact with a bumped aggregate version so downstream replicas refresh. Returns 404 when the product does not exist, 409 when the UoM code is already defined for the product, and 400 when factorToBase is not positive or a BASE entry\'s factor is not 1. 
     * Add Product UoM Conversion
     */
    async addProductUomRaw(requestParameters: AddProductUomRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProductUomDto>> {
        if (requestParameters['productId'] == null) {
            throw new runtime.RequiredError(
                'productId',
                'Required parameter "productId" was null or undefined when calling addProductUom().'
            );
        }

        if (requestParameters['productUomCreateRequestDto'] == null) {
            throw new runtime.RequiredError(
                'productUomCreateRequestDto',
                'Required parameter "productUomCreateRequestDto" was null or undefined when calling addProductUom().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "ROLE_CATALOG_EDIT"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/products/{productId}/uoms`.replace(`{${"productId"}}`, encodeURIComponent(String(requestParameters['productId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ProductUomCreateRequestDtoToJSON(requestParameters['productUomCreateRequestDto']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProductUomDtoFromJSON(jsonValue));
    }

    /**
     * Adds a unit-of-measure entry to a product\'s conversion set, binding a UoM code to its factor relative to the product\'s base unit; the code is normalised to upper case and the factor stored at six decimal places. Use this tool to define how a purchase or pack unit converts for one product; do not use createUomConversion, which defines global cross-unit conversions not tied to a product. Preconditions: the product must exist and must not already define the UoM code; a BASE-type entry must carry factorToBase exactly 1. Required inputs: productId (UUID) path parameter plus uomCode, uomType (BASE, PURCHASE or PACK) and a positive factorToBase; precisionScale is optional. Emits a CATALOG_PRODUCT_UOM_CREATE event and re-publishes the product fact with a bumped aggregate version so downstream replicas refresh. Returns 404 when the product does not exist, 409 when the UoM code is already defined for the product, and 400 when factorToBase is not positive or a BASE entry\'s factor is not 1. 
     * Add Product UoM Conversion
     */
    async addProductUom(requestParameters: AddProductUomRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProductUomDto> {
        const response = await this.addProductUomRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes one unit-of-measure entry from a product\'s conversion set permanently; there is no soft delete. Use this tool when a purchase or pack unit no longer applies to the product; do not use updateProductUom, which changes an entry\'s factor or type while keeping it. Preconditions: the product must exist and the UoM entry must belong to it; no guard prevents deleting the BASE entry, so callers must not orphan the conversion set. Required inputs: productId and uomId (UUIDs) as path parameters; there is no request body. Emits a CATALOG_PRODUCT_UOM_DELETE event and re-publishes the product fact with a bumped aggregate version so downstream replicas refresh. Returns 204 on success, and 404 when the product or the UoM entry does not exist under that product. 
     * Delete Product UoM Conversion
     */
    async deleteProductUomRaw(requestParameters: DeleteProductUomRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['productId'] == null) {
            throw new runtime.RequiredError(
                'productId',
                'Required parameter "productId" was null or undefined when calling deleteProductUom().'
            );
        }

        if (requestParameters['uomId'] == null) {
            throw new runtime.RequiredError(
                'uomId',
                'Required parameter "uomId" was null or undefined when calling deleteProductUom().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "ROLE_CATALOG_EDIT"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/products/{productId}/uoms/{uomId}`.replace(`{${"productId"}}`, encodeURIComponent(String(requestParameters['productId']))).replace(`{${"uomId"}}`, encodeURIComponent(String(requestParameters['uomId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Removes one unit-of-measure entry from a product\'s conversion set permanently; there is no soft delete. Use this tool when a purchase or pack unit no longer applies to the product; do not use updateProductUom, which changes an entry\'s factor or type while keeping it. Preconditions: the product must exist and the UoM entry must belong to it; no guard prevents deleting the BASE entry, so callers must not orphan the conversion set. Required inputs: productId and uomId (UUIDs) as path parameters; there is no request body. Emits a CATALOG_PRODUCT_UOM_DELETE event and re-publishes the product fact with a bumped aggregate version so downstream replicas refresh. Returns 204 on success, and 404 when the product or the UoM entry does not exist under that product. 
     * Delete Product UoM Conversion
     */
    async deleteProductUom(requestParameters: DeleteProductUomRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteProductUomRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the product\'s unit-of-measure conversion set ordered by UoM code, each entry carrying its type, factor to base and precision scale. Use this tool to read a product\'s unit conversions; use listUomConversions instead for the global, product-independent conversion table. Preconditions: the product must exist. Required inputs: productId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when the product does not exist, and 200 with an empty array when the product defines no UoM entries. 
     * List Product UoM Conversions
     */
    async listProductUomsRaw(requestParameters: ListProductUomsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ProductUomDto>>> {
        if (requestParameters['productId'] == null) {
            throw new runtime.RequiredError(
                'productId',
                'Required parameter "productId" was null or undefined when calling listProductUoms().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "ROLE_CATALOG_VIEW"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/products/{productId}/uoms`.replace(`{${"productId"}}`, encodeURIComponent(String(requestParameters['productId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ProductUomDtoFromJSON));
    }

    /**
     * Returns the product\'s unit-of-measure conversion set ordered by UoM code, each entry carrying its type, factor to base and precision scale. Use this tool to read a product\'s unit conversions; use listUomConversions instead for the global, product-independent conversion table. Preconditions: the product must exist. Required inputs: productId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when the product does not exist, and 200 with an empty array when the product defines no UoM entries. 
     * List Product UoM Conversions
     */
    async listProductUoms(requestParameters: ListProductUomsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ProductUomDto>> {
        const response = await this.listProductUomsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Updates the conversion factor, precision scale and optionally the type of one product UoM entry; the UoM code itself is immutable. Use this tool to correct a factor or reclassify an entry; do not use addProductUom, which adds a new code, or deleteProductUom, which removes one. Preconditions: the product must exist and the UoM entry must belong to it; changing the type to BASE requires factorToBase exactly 1, and omitting uomType keeps the current type. Required inputs: productId and uomId (UUIDs) path parameters plus a positive factorToBase; uomType and precisionScale are optional. Emits a CATALOG_PRODUCT_UOM_UPDATE event and re-publishes the product fact with a bumped aggregate version so downstream replicas refresh. Returns 404 when the product or the UoM entry does not exist under that product, and 400 when factorToBase is not positive or a BASE entry\'s factor is not 1. 
     * Update Product UoM Conversion
     */
    async updateProductUomRaw(requestParameters: UpdateProductUomRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProductUomDto>> {
        if (requestParameters['productId'] == null) {
            throw new runtime.RequiredError(
                'productId',
                'Required parameter "productId" was null or undefined when calling updateProductUom().'
            );
        }

        if (requestParameters['uomId'] == null) {
            throw new runtime.RequiredError(
                'uomId',
                'Required parameter "uomId" was null or undefined when calling updateProductUom().'
            );
        }

        if (requestParameters['productUomUpdateRequestDto'] == null) {
            throw new runtime.RequiredError(
                'productUomUpdateRequestDto',
                'Required parameter "productUomUpdateRequestDto" was null or undefined when calling updateProductUom().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "ROLE_CATALOG_EDIT"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/products/{productId}/uoms/{uomId}`.replace(`{${"productId"}}`, encodeURIComponent(String(requestParameters['productId']))).replace(`{${"uomId"}}`, encodeURIComponent(String(requestParameters['uomId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: ProductUomUpdateRequestDtoToJSON(requestParameters['productUomUpdateRequestDto']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProductUomDtoFromJSON(jsonValue));
    }

    /**
     * Updates the conversion factor, precision scale and optionally the type of one product UoM entry; the UoM code itself is immutable. Use this tool to correct a factor or reclassify an entry; do not use addProductUom, which adds a new code, or deleteProductUom, which removes one. Preconditions: the product must exist and the UoM entry must belong to it; changing the type to BASE requires factorToBase exactly 1, and omitting uomType keeps the current type. Required inputs: productId and uomId (UUIDs) path parameters plus a positive factorToBase; uomType and precisionScale are optional. Emits a CATALOG_PRODUCT_UOM_UPDATE event and re-publishes the product fact with a bumped aggregate version so downstream replicas refresh. Returns 404 when the product or the UoM entry does not exist under that product, and 400 when factorToBase is not positive or a BASE entry\'s factor is not 1. 
     * Update Product UoM Conversion
     */
    async updateProductUom(requestParameters: UpdateProductUomRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProductUomDto> {
        const response = await this.updateProductUomRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
