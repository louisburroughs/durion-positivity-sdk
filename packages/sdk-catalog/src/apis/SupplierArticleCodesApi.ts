/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Product API
 * API documentation for the Product service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  SupplierArticleCodeReplayResultDto,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    SupplierArticleCodeReplayResultDtoFromJSON,
    SupplierArticleCodeReplayResultDtoToJSON,
} from '../models/index';

export interface ReplaySupplierArticleCodeFactsRequest {
    afterId?: string;
    updatedSince?: Date;
    limit?: number;
}

/**
 * 
 */
export class SupplierArticleCodesApi extends runtime.BaseAPI {

    /**
     * Re-publishes catalog.supplier-article-code.updated facts for one bounded page of vendor-article-code rows so that event-fed replicas in other modules can be seeded or repaired, returning what it emitted and a cursor for the next page. Use this tool to fill a consumer\'s replica after a first deployment or a consumer outage longer than broker retention; do not use it to fix one row, which republishes itself on its next PRICAT import. Preconditions: Kafka publication must be enabled, or the facts queue in the outbox and reach nobody; replayed facts are indistinguishable from live ones, so consumers apply them through their normal path and their stale guard prevents an older fact regressing newer state. Required inputs: none; afterId resumes a previous page, updatedSince restricts to rows changed at or after an instant, and limit bounds the page at 1000. Emits a CATALOG_SUPPLIER_ARTICLE_CODE_REPLAY event and queues one fact per row in the page; no catalog state changes. Returns 200 with complete=true and a null cursor once the table end is reached, and 400 when limit is out of range or a parameter is malformed. 
     * Re-emit Vendor Article Code Facts for Replica Consumers
     */
    async replaySupplierArticleCodeFactsRaw(requestParameters: ReplaySupplierArticleCodeFactsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SupplierArticleCodeReplayResultDto>> {
        const queryParameters: any = {};

        if (requestParameters['afterId'] != null) {
            queryParameters['afterId'] = requestParameters['afterId'];
        }

        if (requestParameters['updatedSince'] != null) {
            queryParameters['updatedSince'] = (requestParameters['updatedSince'] as any).toISOString();
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "ROLE_CATALOG_EDIT"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/catalog/supplier-article-codes/replay`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SupplierArticleCodeReplayResultDtoFromJSON(jsonValue));
    }

    /**
     * Re-publishes catalog.supplier-article-code.updated facts for one bounded page of vendor-article-code rows so that event-fed replicas in other modules can be seeded or repaired, returning what it emitted and a cursor for the next page. Use this tool to fill a consumer\'s replica after a first deployment or a consumer outage longer than broker retention; do not use it to fix one row, which republishes itself on its next PRICAT import. Preconditions: Kafka publication must be enabled, or the facts queue in the outbox and reach nobody; replayed facts are indistinguishable from live ones, so consumers apply them through their normal path and their stale guard prevents an older fact regressing newer state. Required inputs: none; afterId resumes a previous page, updatedSince restricts to rows changed at or after an instant, and limit bounds the page at 1000. Emits a CATALOG_SUPPLIER_ARTICLE_CODE_REPLAY event and queues one fact per row in the page; no catalog state changes. Returns 200 with complete=true and a null cursor once the table end is reached, and 400 when limit is out of range or a parameter is malformed. 
     * Re-emit Vendor Article Code Facts for Replica Consumers
     */
    async replaySupplierArticleCodeFacts(requestParameters: ReplaySupplierArticleCodeFactsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SupplierArticleCodeReplayResultDto> {
        const response = await this.replaySupplierArticleCodeFactsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
