/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Product API
 * API documentation for the Product service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  SubstitutionGroupCreateRequestDto,
  SubstitutionGroupDto,
  SubstitutionGroupMemberRequestDto,
} from '../models/index';
import {
    SubstitutionGroupCreateRequestDtoFromJSON,
    SubstitutionGroupCreateRequestDtoToJSON,
    SubstitutionGroupDtoFromJSON,
    SubstitutionGroupDtoToJSON,
    SubstitutionGroupMemberRequestDtoFromJSON,
    SubstitutionGroupMemberRequestDtoToJSON,
} from '../models/index';

export interface AddSubstitutionGroupMemberRequest {
    groupId: string;
    substitutionGroupMemberRequestDto: SubstitutionGroupMemberRequestDto;
}

export interface CreateSubstitutionGroupRequest {
    substitutionGroupCreateRequestDto: SubstitutionGroupCreateRequestDto;
}

export interface DeleteSubstitutionGroupRequest {
    groupId: string;
}

export interface GetSubstitutionGroupRequest {
    groupId: string;
}

export interface RemoveSubstitutionGroupMemberRequest {
    groupId: string;
    productId: string;
}

/**
 * 
 */
export class SubstitutionGroupAPIApi extends runtime.BaseAPI {

    /**
     * Adds a product to a substitution group, making it mutually interchangeable with every other member; membership is exclusive, a product can belong to only one group at a time. Use this tool to grow an interchangeability set; do not use addProductReplacement, which records a one-way successor for a discontinued product rather than a symmetric substitute. Preconditions: the group and the product must both exist, and the product must not already belong to any substitution group, including this one. Required inputs: groupId (UUID) path parameter and productId (UUID) in the body. Emits a CATALOG_SUBSTITUTION_GROUP_MEMBER_ADD event and re-publishes the product fact for every member of the group so downstream replicas learn the new association. Returns 404 when the group or the product does not exist, and 409 when the product already belongs to a substitution group. 
     * Add Substitution Group Member
     */
    async addSubstitutionGroupMemberRaw(requestParameters: AddSubstitutionGroupMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SubstitutionGroupDto>> {
        if (requestParameters['groupId'] == null) {
            throw new runtime.RequiredError(
                'groupId',
                'Required parameter "groupId" was null or undefined when calling addSubstitutionGroupMember().'
            );
        }

        if (requestParameters['substitutionGroupMemberRequestDto'] == null) {
            throw new runtime.RequiredError(
                'substitutionGroupMemberRequestDto',
                'Required parameter "substitutionGroupMemberRequestDto" was null or undefined when calling addSubstitutionGroupMember().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:substitution_group:edit"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/products/substitution-groups/{groupId}/members`.replace(`{${"groupId"}}`, encodeURIComponent(String(requestParameters['groupId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SubstitutionGroupMemberRequestDtoToJSON(requestParameters['substitutionGroupMemberRequestDto']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SubstitutionGroupDtoFromJSON(jsonValue));
    }

    /**
     * Adds a product to a substitution group, making it mutually interchangeable with every other member; membership is exclusive, a product can belong to only one group at a time. Use this tool to grow an interchangeability set; do not use addProductReplacement, which records a one-way successor for a discontinued product rather than a symmetric substitute. Preconditions: the group and the product must both exist, and the product must not already belong to any substitution group, including this one. Required inputs: groupId (UUID) path parameter and productId (UUID) in the body. Emits a CATALOG_SUBSTITUTION_GROUP_MEMBER_ADD event and re-publishes the product fact for every member of the group so downstream replicas learn the new association. Returns 404 when the group or the product does not exist, and 409 when the product already belongs to a substitution group. 
     * Add Substitution Group Member
     */
    async addSubstitutionGroupMember(requestParameters: AddSubstitutionGroupMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SubstitutionGroupDto> {
        const response = await this.addSubstitutionGroupMemberRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates an empty, named substitution group — a set of mutually interchangeable products in which each product may belong to at most one group. Use this tool to establish the group before populating it; do not use addSubstitutionGroupMember, which adds products to a group that already exists, and do not confuse groups with addProductReplacement, which records one-way successors for discontinued products. Preconditions: none; group names are not checked for uniqueness. Required inputs: name (non-blank, trimmed on save); notes are optional. Emits a CATALOG_SUBSTITUTION_GROUP_CREATE event; no product facts are re-published until members are added. Returns 201 with the group and an empty productIds list, and 400 when name is missing or blank. 
     * Create Substitution Group
     */
    async createSubstitutionGroupRaw(requestParameters: CreateSubstitutionGroupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SubstitutionGroupDto>> {
        if (requestParameters['substitutionGroupCreateRequestDto'] == null) {
            throw new runtime.RequiredError(
                'substitutionGroupCreateRequestDto',
                'Required parameter "substitutionGroupCreateRequestDto" was null or undefined when calling createSubstitutionGroup().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:substitution_group:edit"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/products/substitution-groups`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SubstitutionGroupCreateRequestDtoToJSON(requestParameters['substitutionGroupCreateRequestDto']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SubstitutionGroupDtoFromJSON(jsonValue));
    }

    /**
     * Creates an empty, named substitution group — a set of mutually interchangeable products in which each product may belong to at most one group. Use this tool to establish the group before populating it; do not use addSubstitutionGroupMember, which adds products to a group that already exists, and do not confuse groups with addProductReplacement, which records one-way successors for discontinued products. Preconditions: none; group names are not checked for uniqueness. Required inputs: name (non-blank, trimmed on save); notes are optional. Emits a CATALOG_SUBSTITUTION_GROUP_CREATE event; no product facts are re-published until members are added. Returns 201 with the group and an empty productIds list, and 400 when name is missing or blank. 
     * Create Substitution Group
     */
    async createSubstitutionGroup(requestParameters: CreateSubstitutionGroupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SubstitutionGroupDto> {
        const response = await this.createSubstitutionGroupRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Deletes a substitution group and all of its memberships permanently, freeing every former member to join another group; the products themselves are untouched. Use this tool to dissolve a whole group; use removeSubstitutionGroupMember instead to take a single product out while keeping the group. Preconditions: the group must exist; there is no soft delete or archive. Required inputs: groupId (UUID) as a path parameter; there is no request body. Emits a CATALOG_SUBSTITUTION_GROUP_DELETE event and re-publishes the product fact for every former member so downstream replicas drop the association. Returns 204 on success, and 404 when no substitution group exists for the supplied id. 
     * Delete Substitution Group
     */
    async deleteSubstitutionGroupRaw(requestParameters: DeleteSubstitutionGroupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['groupId'] == null) {
            throw new runtime.RequiredError(
                'groupId',
                'Required parameter "groupId" was null or undefined when calling deleteSubstitutionGroup().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:substitution_group:edit"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/products/substitution-groups/{groupId}`.replace(`{${"groupId"}}`, encodeURIComponent(String(requestParameters['groupId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes a substitution group and all of its memberships permanently, freeing every former member to join another group; the products themselves are untouched. Use this tool to dissolve a whole group; use removeSubstitutionGroupMember instead to take a single product out while keeping the group. Preconditions: the group must exist; there is no soft delete or archive. Required inputs: groupId (UUID) as a path parameter; there is no request body. Emits a CATALOG_SUBSTITUTION_GROUP_DELETE event and re-publishes the product fact for every former member so downstream replicas drop the association. Returns 204 on success, and 404 when no substitution group exists for the supplied id. 
     * Delete Substitution Group
     */
    async deleteSubstitutionGroup(requestParameters: DeleteSubstitutionGroupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteSubstitutionGroupRaw(requestParameters, initOverrides);
    }

    /**
     * Returns one substitution group with its name, notes and member product ids ordered by when each member joined. Use this tool when the groupId is already known; use listSubstitutionGroups instead to discover groups. Preconditions: the group must exist. Required inputs: groupId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no substitution group exists for the supplied id. 
     * Get Substitution Group
     */
    async getSubstitutionGroupRaw(requestParameters: GetSubstitutionGroupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SubstitutionGroupDto>> {
        if (requestParameters['groupId'] == null) {
            throw new runtime.RequiredError(
                'groupId',
                'Required parameter "groupId" was null or undefined when calling getSubstitutionGroup().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:substitution_group:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/products/substitution-groups/{groupId}`.replace(`{${"groupId"}}`, encodeURIComponent(String(requestParameters['groupId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SubstitutionGroupDtoFromJSON(jsonValue));
    }

    /**
     * Returns one substitution group with its name, notes and member product ids ordered by when each member joined. Use this tool when the groupId is already known; use listSubstitutionGroups instead to discover groups. Preconditions: the group must exist. Required inputs: groupId (UUID) as a path parameter; there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 404 when no substitution group exists for the supplied id. 
     * Get Substitution Group
     */
    async getSubstitutionGroup(requestParameters: GetSubstitutionGroupRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SubstitutionGroupDto> {
        const response = await this.getSubstitutionGroupRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns every substitution group with its name, notes and member product ids ordered by when each member joined. Use this tool to discover a groupId or survey interchangeability sets; use getSubstitutionGroup instead when the id is already known. Preconditions: none; the list is unfiltered and unpaged. Required inputs: none, and there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 200 with an empty array when no groups exist, so an empty result is not an error condition. 
     * List Substitution Groups
     */
    async listSubstitutionGroupsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<SubstitutionGroupDto>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:substitution_group:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/products/substitution-groups`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(SubstitutionGroupDtoFromJSON));
    }

    /**
     * Returns every substitution group with its name, notes and member product ids ordered by when each member joined. Use this tool to discover a groupId or survey interchangeability sets; use getSubstitutionGroup instead when the id is already known. Preconditions: none; the list is unfiltered and unpaged. Required inputs: none, and there is no request body. No events are emitted and no state changes; this is a read-only projection. Returns 200 with an empty array when no groups exist, so an empty result is not an error condition. 
     * List Substitution Groups
     */
    async listSubstitutionGroups(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<SubstitutionGroupDto>> {
        const response = await this.listSubstitutionGroupsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Removes a product from a substitution group, ending its interchangeability with the remaining members and freeing it to join another group. Use this tool to take one product out; use deleteSubstitutionGroup instead to dissolve the entire group at once. Preconditions: the group must exist and the product must currently be a member of that specific group. Required inputs: groupId and productId (UUIDs) as path parameters; there is no request body. Emits a CATALOG_SUBSTITUTION_GROUP_MEMBER_REMOVE event and re-publishes the product fact for the removed product and every remaining member. Returns 200 with the group\'s remaining membership, and 404 when the group does not exist or the product is not a member of it. 
     * Remove Substitution Group Member
     */
    async removeSubstitutionGroupMemberRaw(requestParameters: RemoveSubstitutionGroupMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SubstitutionGroupDto>> {
        if (requestParameters['groupId'] == null) {
            throw new runtime.RequiredError(
                'groupId',
                'Required parameter "groupId" was null or undefined when calling removeSubstitutionGroupMember().'
            );
        }

        if (requestParameters['productId'] == null) {
            throw new runtime.RequiredError(
                'productId',
                'Required parameter "productId" was null or undefined when calling removeSubstitutionGroupMember().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:substitution_group:edit"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/products/substitution-groups/{groupId}/members/{productId}`.replace(`{${"groupId"}}`, encodeURIComponent(String(requestParameters['groupId']))).replace(`{${"productId"}}`, encodeURIComponent(String(requestParameters['productId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SubstitutionGroupDtoFromJSON(jsonValue));
    }

    /**
     * Removes a product from a substitution group, ending its interchangeability with the remaining members and freeing it to join another group. Use this tool to take one product out; use deleteSubstitutionGroup instead to dissolve the entire group at once. Preconditions: the group must exist and the product must currently be a member of that specific group. Required inputs: groupId and productId (UUIDs) as path parameters; there is no request body. Emits a CATALOG_SUBSTITUTION_GROUP_MEMBER_REMOVE event and re-publishes the product fact for the removed product and every remaining member. Returns 200 with the group\'s remaining membership, and 404 when the group does not exist or the product is not a member of it. 
     * Remove Substitution Group Member
     */
    async removeSubstitutionGroupMember(requestParameters: RemoveSubstitutionGroupMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SubstitutionGroupDto> {
        const response = await this.removeSubstitutionGroupMemberRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
