/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Product API
 * API documentation for the Product service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  LaborTimeQuoteRequest,
  LaborTimeQuoteResponse,
} from '../models/index';
import {
    LaborTimeQuoteRequestFromJSON,
    LaborTimeQuoteRequestToJSON,
    LaborTimeQuoteResponseFromJSON,
    LaborTimeQuoteResponseToJSON,
} from '../models/index';

export interface ResolveLaborTimeRequest {
    laborTimeQuoteRequest: LaborTimeQuoteRequest;
}

/**
 * 
 */
export class LaborTimeResolutionApi extends runtime.BaseAPI {

    /**
     * Answers the one applicable estimated labor time for a catalog service operation on the described vehicle: stored vehicle-keyed standards first (most specific match wins, source precedence is policy data), then live QUERY_ONLY guides under a license-bounded cache, then the service\'s vehicle-agnostic default hours, each answer carrying source, revision and match grade. Use this tool from the approved pos-workorder client to prefill a LABOR estimate line; do not use it to browse what times exist, which is the labor-standards listing\'s job. Preconditions: the service must exist; unknown vehicle fields are sent null and widen the match. Required inputs: a body with serviceId; vehicle fields and preferredTimeType are optional. Emits a CATALOG_LABOR_TIME_RESOLVE event; no state changes. Returns 200 always for a well-formed request — a miss is the typed NO_TIME_AVAILABLE or SOURCE_UNAVAILABLE status, never an error, and callers degrade to a blank prefill. 
     * Resolve the Labor Time for an Operation on a Vehicle
     */
    async resolveLaborTimeRaw(requestParameters: ResolveLaborTimeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LaborTimeQuoteResponse>> {
        if (requestParameters['laborTimeQuoteRequest'] == null) {
            throw new runtime.RequiredError(
                'laborTimeQuoteRequest',
                'Required parameter "laborTimeQuoteRequest" was null or undefined when calling resolveLaborTime().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:labor_time:resolve"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/catalog/labor-times/resolve`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LaborTimeQuoteRequestToJSON(requestParameters['laborTimeQuoteRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LaborTimeQuoteResponseFromJSON(jsonValue));
    }

    /**
     * Answers the one applicable estimated labor time for a catalog service operation on the described vehicle: stored vehicle-keyed standards first (most specific match wins, source precedence is policy data), then live QUERY_ONLY guides under a license-bounded cache, then the service\'s vehicle-agnostic default hours, each answer carrying source, revision and match grade. Use this tool from the approved pos-workorder client to prefill a LABOR estimate line; do not use it to browse what times exist, which is the labor-standards listing\'s job. Preconditions: the service must exist; unknown vehicle fields are sent null and widen the match. Required inputs: a body with serviceId; vehicle fields and preferredTimeType are optional. Emits a CATALOG_LABOR_TIME_RESOLVE event; no state changes. Returns 200 always for a well-formed request — a miss is the typed NO_TIME_AVAILABLE or SOURCE_UNAVAILABLE status, never an error, and callers degrade to a blank prefill. 
     * Resolve the Labor Time for an Operation on a Vehicle
     */
    async resolveLaborTime(requestParameters: ResolveLaborTimeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LaborTimeQuoteResponse> {
        const response = await this.resolveLaborTimeRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
