/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Product API
 * API documentation for the Product service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  ServiceDto,
  ServiceRequirementsRequest,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    ServiceDtoFromJSON,
    ServiceDtoToJSON,
    ServiceRequirementsRequestFromJSON,
    ServiceRequirementsRequestToJSON,
} from '../models/index';

export interface SetServiceRequirementsRequest {
    serviceId: string;
    serviceRequirementsRequest: ServiceRequirementsRequest;
}

/**
 * 
 */
export class ServiceRequirementsApi extends runtime.BaseAPI {

    /**
     * Replaces the whole skill requirement declaration of one catalog service, each required skill scoped to a GVWR class range (1-8) or to ANY class when both bounds are omitted. Use this tool when a service needs competence that differs by vehicle class — the same brake job requiring A-series brakes on classes 1-3 and T-series on 4-8 — or to declare a service unconstrained by sending an empty list; do not create a second SKU for a competence-only difference, fork at the SKU only when labor time or price differs. Preconditions: the service exists, and every skillId is an active row of the skill registry (read it at GET /v1/people/skills) whose own class range covers the range declared here. Required inputs: serviceId (UUID) as a path parameter and a requiredSkills list, possibly empty, of {skillId, minGvwrClass, maxGvwrClass}. Emits a CATALOG_SERVICE_REQUIREMENTS_SET audit event and a catalog.service.updated fact (schema v3) carrying requirementsConfiguredAt and requiredSkills, so schedulers resolve the requirement from their replica without calling back. Returns 200 with the service, 404 when the service does not exist, and 422 with SKILL_UNKNOWN, SKILL_RETIRED, SKILL_DUPLICATE or SKILL_CLASS_RANGE_INVALID naming the offending value. 
     * Declare the Skills a Service Requires
     */
    async setServiceRequirementsRaw(requestParameters: SetServiceRequirementsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ServiceDto>> {
        if (requestParameters['serviceId'] == null) {
            throw new runtime.RequiredError(
                'serviceId',
                'Required parameter "serviceId" was null or undefined when calling setServiceRequirements().'
            );
        }

        if (requestParameters['serviceRequirementsRequest'] == null) {
            throw new runtime.RequiredError(
                'serviceRequirementsRequest',
                'Required parameter "serviceRequirementsRequest" was null or undefined when calling setServiceRequirements().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:service_requirement:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/products/services/{serviceId}/requirements`.replace(`{${"serviceId"}}`, encodeURIComponent(String(requestParameters['serviceId']))),
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: ServiceRequirementsRequestToJSON(requestParameters['serviceRequirementsRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ServiceDtoFromJSON(jsonValue));
    }

    /**
     * Replaces the whole skill requirement declaration of one catalog service, each required skill scoped to a GVWR class range (1-8) or to ANY class when both bounds are omitted. Use this tool when a service needs competence that differs by vehicle class — the same brake job requiring A-series brakes on classes 1-3 and T-series on 4-8 — or to declare a service unconstrained by sending an empty list; do not create a second SKU for a competence-only difference, fork at the SKU only when labor time or price differs. Preconditions: the service exists, and every skillId is an active row of the skill registry (read it at GET /v1/people/skills) whose own class range covers the range declared here. Required inputs: serviceId (UUID) as a path parameter and a requiredSkills list, possibly empty, of {skillId, minGvwrClass, maxGvwrClass}. Emits a CATALOG_SERVICE_REQUIREMENTS_SET audit event and a catalog.service.updated fact (schema v3) carrying requirementsConfiguredAt and requiredSkills, so schedulers resolve the requirement from their replica without calling back. Returns 200 with the service, 404 when the service does not exist, and 422 with SKILL_UNKNOWN, SKILL_RETIRED, SKILL_DUPLICATE or SKILL_CLASS_RANGE_INVALID naming the offending value. 
     * Declare the Skills a Service Requires
     */
    async setServiceRequirements(requestParameters: SetServiceRequirementsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ServiceDto> {
        const response = await this.setServiceRequirementsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
