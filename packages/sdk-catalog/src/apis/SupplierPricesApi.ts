/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Product API
 * API documentation for the Product service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  Page,
  SupplierPriceEntryDto,
  SupplierPriceImportStatusDto,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    PageFromJSON,
    PageToJSON,
    SupplierPriceEntryDtoFromJSON,
    SupplierPriceEntryDtoToJSON,
    SupplierPriceImportStatusDtoFromJSON,
    SupplierPriceImportStatusDtoToJSON,
} from '../models/index';

export interface GetLatestSupplierPriceRequest {
    productId: string;
    countryCode: string;
    currency: string;
    vendorProfileId?: string;
    asOf?: Date;
}

export interface ListSupplierPriceHistoryRequest {
    productId: string;
    vendorProfileId?: string;
    page?: number;
    size?: number;
}

/**
 * 
 */
export class SupplierPricesApi extends runtime.BaseAPI {

    /**
     * Returns the vendor price that applies to a product in one market as at a day: the greatest effective-from date not after that day, with ties broken by the vendor document\'s date and then by fetch time so two readers never disagree about today\'s cost. Use this tool for purchasing and margin context; do not use it as a pricing input — supplier cost participates in no sell-price resolution, and the suggested retail price it carries is vendor reference data that must be labelled with its PRICAT source wherever it is shown. Preconditions: a PRICAT import must have applied for that vendor, product and market; a product with no vendor price simply has none, which is not an error. Required inputs: productId, countryCode and currency; vendorProfileId narrows to one vendor and asOf evaluates a past day, both optional, with asOf defaulting to today. Emits a CATALOG_SUPPLIER_PRICE_LATEST event; no state changes. Returns 404 when no entry applies, and 400 when a parameter is malformed. Money fields may be null, meaning the vendor did not state that value — null is never zero. 
     * Get the Applicable Vendor Price for a Product
     */
    async getLatestSupplierPriceRaw(requestParameters: GetLatestSupplierPriceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SupplierPriceEntryDto>> {
        if (requestParameters['productId'] == null) {
            throw new runtime.RequiredError(
                'productId',
                'Required parameter "productId" was null or undefined when calling getLatestSupplierPrice().'
            );
        }

        if (requestParameters['countryCode'] == null) {
            throw new runtime.RequiredError(
                'countryCode',
                'Required parameter "countryCode" was null or undefined when calling getLatestSupplierPrice().'
            );
        }

        if (requestParameters['currency'] == null) {
            throw new runtime.RequiredError(
                'currency',
                'Required parameter "currency" was null or undefined when calling getLatestSupplierPrice().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['productId'] != null) {
            queryParameters['productId'] = requestParameters['productId'];
        }

        if (requestParameters['countryCode'] != null) {
            queryParameters['countryCode'] = requestParameters['countryCode'];
        }

        if (requestParameters['currency'] != null) {
            queryParameters['currency'] = requestParameters['currency'];
        }

        if (requestParameters['vendorProfileId'] != null) {
            queryParameters['vendorProfileId'] = requestParameters['vendorProfileId'];
        }

        if (requestParameters['asOf'] != null) {
            queryParameters['asOf'] = (requestParameters['asOf'] as any).toISOString().substring(0,10);
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:supplier_cost:read"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/catalog/supplier-prices/latest`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SupplierPriceEntryDtoFromJSON(jsonValue));
    }

    /**
     * Returns the vendor price that applies to a product in one market as at a day: the greatest effective-from date not after that day, with ties broken by the vendor document\'s date and then by fetch time so two readers never disagree about today\'s cost. Use this tool for purchasing and margin context; do not use it as a pricing input — supplier cost participates in no sell-price resolution, and the suggested retail price it carries is vendor reference data that must be labelled with its PRICAT source wherever it is shown. Preconditions: a PRICAT import must have applied for that vendor, product and market; a product with no vendor price simply has none, which is not an error. Required inputs: productId, countryCode and currency; vendorProfileId narrows to one vendor and asOf evaluates a past day, both optional, with asOf defaulting to today. Emits a CATALOG_SUPPLIER_PRICE_LATEST event; no state changes. Returns 404 when no entry applies, and 400 when a parameter is malformed. Money fields may be null, meaning the vendor did not state that value — null is never zero. 
     * Get the Applicable Vendor Price for a Product
     */
    async getLatestSupplierPrice(requestParameters: GetLatestSupplierPriceRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SupplierPriceEntryDto> {
        const response = await this.getLatestSupplierPriceRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns PRICAT imports whose completion event declared more chunks than this module applied, newest first, each with the counts that disagree. Use this tool when vendor prices look stale or partial; do not use it to check a specific product, which is listSupplierPriceHistory. Preconditions: none; an empty list is the healthy state and means every import applied whole. Required inputs: none. Emits a CATALOG_SUPPLIER_PRICE_IMPORT_GAPS event; no state changes. A re-emit was already requested on the supplier command topic when each gap was detected, so an entry here is a feed that did not self-heal rather than one nobody has chased. Returns 200 with an empty array when there are no gaps. 
     * List Vendor Price Imports This Module Could Not Complete
     */
    async listIncompleteSupplierPriceImportsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<SupplierPriceImportStatusDto>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:supplier_cost:read"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/catalog/supplier-prices/imports/incomplete`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(SupplierPriceImportStatusDtoFromJSON));
    }

    /**
     * Returns PRICAT imports whose completion event declared more chunks than this module applied, newest first, each with the counts that disagree. Use this tool when vendor prices look stale or partial; do not use it to check a specific product, which is listSupplierPriceHistory. Preconditions: none; an empty list is the healthy state and means every import applied whole. Required inputs: none. Emits a CATALOG_SUPPLIER_PRICE_IMPORT_GAPS event; no state changes. A re-emit was already requested on the supplier command topic when each gap was detected, so an entry here is a feed that did not self-heal rather than one nobody has chased. Returns 200 with an empty array when there are no gaps. 
     * List Vendor Price Imports This Module Could Not Complete
     */
    async listIncompleteSupplierPriceImports(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<SupplierPriceImportStatusDto>> {
        const response = await this.listIncompleteSupplierPriceImportsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns every vendor price recorded for a product, newest effective date first, including superseded entries with the vendor document and import each came from. Use this tool to answer what a vendor charged over time and where a cost came from; do not use it to find today\'s cost, which is getLatestSupplierPrice and applies the effective-dating rules. Preconditions: none; a product with no imported vendor prices returns an empty page. Required inputs: productId; vendorProfileId narrows to one vendor, and page and size are optional with size defaulting to 50 and capped at 200. Emits a CATALOG_SUPPLIER_PRICE_HISTORY event; no state changes. Returns 200 with an empty items array when nothing has been imported, so an empty result is not an error condition, and 400 when the page size is out of range. 
     * List Vendor Price History for a Product
     */
    async listSupplierPriceHistoryRaw(requestParameters: ListSupplierPriceHistoryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Page>> {
        if (requestParameters['productId'] == null) {
            throw new runtime.RequiredError(
                'productId',
                'Required parameter "productId" was null or undefined when calling listSupplierPriceHistory().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['productId'] != null) {
            queryParameters['productId'] = requestParameters['productId'];
        }

        if (requestParameters['vendorProfileId'] != null) {
            queryParameters['vendorProfileId'] = requestParameters['vendorProfileId'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:supplier_cost:read"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/catalog/supplier-prices/history`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PageFromJSON(jsonValue));
    }

    /**
     * Returns every vendor price recorded for a product, newest effective date first, including superseded entries with the vendor document and import each came from. Use this tool to answer what a vendor charged over time and where a cost came from; do not use it to find today\'s cost, which is getLatestSupplierPrice and applies the effective-dating rules. Preconditions: none; a product with no imported vendor prices returns an empty page. Required inputs: productId; vendorProfileId narrows to one vendor, and page and size are optional with size defaulting to 50 and capped at 200. Emits a CATALOG_SUPPLIER_PRICE_HISTORY event; no state changes. Returns 200 with an empty items array when nothing has been imported, so an empty result is not an error condition, and 400 when the page size is out of range. 
     * List Vendor Price History for a Product
     */
    async listSupplierPriceHistory(requestParameters: ListSupplierPriceHistoryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Page> {
        const response = await this.listSupplierPriceHistoryRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
