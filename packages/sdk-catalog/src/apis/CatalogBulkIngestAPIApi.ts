/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Product API
 * API documentation for the Product service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  BulkIngestRequestCatalogBulkIngestRecord,
  BulkIngestResponse,
} from '../models/index';
import {
    BulkIngestRequestCatalogBulkIngestRecordFromJSON,
    BulkIngestRequestCatalogBulkIngestRecordToJSON,
    BulkIngestResponseFromJSON,
    BulkIngestResponseToJSON,
} from '../models/index';

export interface BulkIngestCatalogProductsRequest {
    bulkIngestRequestCatalogBulkIngestRecord: BulkIngestRequestCatalogBulkIngestRecord;
}

/**
 * 
 */
export class CatalogBulkIngestAPIApi extends runtime.BaseAPI {

    /**
     * Imports a batch of catalog products in one call, creating each record through the same governed path as createProduct and reporting a per-row success or failure verdict. Use this tool to load many products at once; do not use createProduct, which registers a single product and surfaces its errors as HTTP statuses rather than row results. Preconditions: each row\'s SKU must not already exist (case-insensitive), or that row fails with CATALOG_INGEST_FAILED while the rest of the batch proceeds. Required inputs: jobId (UUID), locationId (UUID) and a non-empty records array; per record, unitOfMeasure defaults to EA, description defaults to the name, and mpn falls back to the sku then the name. manufacturerName, manufacturerBrand, countryOfOrigin, and type are carried onto the product (the manufacturer fields also travel on the product fact for replica consumers), while price is accepted but ignored until catalog pricing lands. categoryName and subcategoryName are resolved against the seeded catalog reference data, trimmed and matched case-insensitively; omit them to leave the product unclassified. A supplied name that matches no seeded record fails that row with CATALOG_INGEST_FAILED rather than landing the product uncategorized, so a typo cannot silently produce a product that no category-based rule can match. Emits a CATALOG_BULK_INGEST event; each successful row also publishes a product fact carrying the resolved category and subcategory, and invalidates the product-detail cache. Returns 200 even when rows fail, so callers must inspect successCount, failureCount and the per-row results rather than the HTTP status. 
     * Bulk Import Catalog Products
     */
    async bulkIngestCatalogProductsRaw(requestParameters: BulkIngestCatalogProductsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkIngestResponse>> {
        if (requestParameters['bulkIngestRequestCatalogBulkIngestRecord'] == null) {
            throw new runtime.RequiredError(
                'bulkIngestRequestCatalogBulkIngestRecord',
                'Required parameter "bulkIngestRequestCatalogBulkIngestRecord" was null or undefined when calling bulkIngestCatalogProducts().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["catalog:product:create"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/catalog/bulk-ingest`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkIngestRequestCatalogBulkIngestRecordToJSON(requestParameters['bulkIngestRequestCatalogBulkIngestRecord']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkIngestResponseFromJSON(jsonValue));
    }

    /**
     * Imports a batch of catalog products in one call, creating each record through the same governed path as createProduct and reporting a per-row success or failure verdict. Use this tool to load many products at once; do not use createProduct, which registers a single product and surfaces its errors as HTTP statuses rather than row results. Preconditions: each row\'s SKU must not already exist (case-insensitive), or that row fails with CATALOG_INGEST_FAILED while the rest of the batch proceeds. Required inputs: jobId (UUID), locationId (UUID) and a non-empty records array; per record, unitOfMeasure defaults to EA, description defaults to the name, and mpn falls back to the sku then the name. manufacturerName, manufacturerBrand, countryOfOrigin, and type are carried onto the product (the manufacturer fields also travel on the product fact for replica consumers), while price is accepted but ignored until catalog pricing lands. categoryName and subcategoryName are resolved against the seeded catalog reference data, trimmed and matched case-insensitively; omit them to leave the product unclassified. A supplied name that matches no seeded record fails that row with CATALOG_INGEST_FAILED rather than landing the product uncategorized, so a typo cannot silently produce a product that no category-based rule can match. Emits a CATALOG_BULK_INGEST event; each successful row also publishes a product fact carrying the resolved category and subcategory, and invalidates the product-detail cache. Returns 200 even when rows fail, so callers must inspect successCount, failureCount and the per-row results rather than the HTTP status. 
     * Bulk Import Catalog Products
     */
    async bulkIngestCatalogProducts(requestParameters: BulkIngestCatalogProductsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkIngestResponse> {
        const response = await this.bulkIngestCatalogProductsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
