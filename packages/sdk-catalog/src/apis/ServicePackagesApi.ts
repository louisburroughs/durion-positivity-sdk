/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Product API
 * API documentation for the Product service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ServicePackageMemberRequestDto,
  ServicePackageRequestDto,
  ServicePackageResponseDto,
} from '../models/index';
import {
    ServicePackageMemberRequestDtoFromJSON,
    ServicePackageMemberRequestDtoToJSON,
    ServicePackageRequestDtoFromJSON,
    ServicePackageRequestDtoToJSON,
    ServicePackageResponseDtoFromJSON,
    ServicePackageResponseDtoToJSON,
} from '../models/index';

export interface AddServicePackageMemberRequest {
    packageId: string;
    servicePackageMemberRequestDto: ServicePackageMemberRequestDto;
}

export interface CreateServicePackageRequest {
    servicePackageRequestDto: ServicePackageRequestDto;
}

export interface GetServicePackageRequest {
    packageId: string;
}

export interface ListServicePackagesRequest {
    locationId?: string;
    fleetPartyId?: string;
    includeFleetPackages?: boolean;
}

export interface RemoveServicePackageMemberRequest {
    packageId: string;
    memberId: string;
}

/**
 * 
 */
export class ServicePackagesApi extends runtime.BaseAPI {

    /**
     * Adds one catalog service operation to a package, required by default. Use this tool to compose a package; required=false marks an operation the package offers as an upsell rather than includes by definition — which is what separates a fleet requirement from a suggestion. Preconditions: the package and the service must both exist, and the service must not already be a member. Wanting two of an operation is a quantity, not a second membership. Required inputs: serviceId; sequence, quantity and required are optional, and an omitted sequence appends to the end. Emits a CATALOG_SERVICE_PACKAGE_MEMBER_ADD event. Returns 200 with the whole package including its updated member list, 404 when the package or service does not exist, and 409 when the service is already a member. 
     * Add An Operation To A Package
     */
    async addServicePackageMemberRaw(requestParameters: AddServicePackageMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ServicePackageResponseDto>> {
        if (requestParameters['packageId'] == null) {
            throw new runtime.RequiredError(
                'packageId',
                'Required parameter "packageId" was null or undefined when calling addServicePackageMember().'
            );
        }

        if (requestParameters['servicePackageMemberRequestDto'] == null) {
            throw new runtime.RequiredError(
                'servicePackageMemberRequestDto',
                'Required parameter "servicePackageMemberRequestDto" was null or undefined when calling addServicePackageMember().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:service_package:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/service-packages/{packageId}/members`.replace(`{${"packageId"}}`, encodeURIComponent(String(requestParameters['packageId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ServicePackageMemberRequestDtoToJSON(requestParameters['servicePackageMemberRequestDto']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ServicePackageResponseDtoFromJSON(jsonValue));
    }

    /**
     * Adds one catalog service operation to a package, required by default. Use this tool to compose a package; required=false marks an operation the package offers as an upsell rather than includes by definition — which is what separates a fleet requirement from a suggestion. Preconditions: the package and the service must both exist, and the service must not already be a member. Wanting two of an operation is a quantity, not a second membership. Required inputs: serviceId; sequence, quantity and required are optional, and an omitted sequence appends to the end. Emits a CATALOG_SERVICE_PACKAGE_MEMBER_ADD event. Returns 200 with the whole package including its updated member list, 404 when the package or service does not exist, and 409 when the service is already a member. 
     * Add An Operation To A Package
     */
    async addServicePackageMember(requestParameters: AddServicePackageMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ServicePackageResponseDto> {
        const response = await this.addServicePackageMemberRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a named set of operations sold together — a four-tyre install with the balance and TPMS reset, a fleet PM interval, a seasonal changeover — or, with fleetPartyId set, one fleet account\'s requirement set. Use this tool to define what a shop offers as a unit; do not use it to add work to a workorder, which is addEstimateItem. Preconditions: packageCode must be unique platform-wide. packageLaborHours is AUTHORED, not derived from the members — the overlap arithmetic that turns member times into a total lives in the workorder, and a shop prices a package as a number it chose. Required inputs: packageCode and name; ownerScope, ownerLocationId, fleetPartyId, packageLaborHours, active and the effective window are optional. Emits a CATALOG_SERVICE_PACKAGE_CREATE event. The package is created empty; add operations with addServicePackageMember. Returns 201 with the stored package, 409 when the code is taken, and 422 when a field is not storable as described. 
     * Create A Service Package
     */
    async createServicePackageRaw(requestParameters: CreateServicePackageRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ServicePackageResponseDto>> {
        if (requestParameters['servicePackageRequestDto'] == null) {
            throw new runtime.RequiredError(
                'servicePackageRequestDto',
                'Required parameter "servicePackageRequestDto" was null or undefined when calling createServicePackage().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:service_package:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/service-packages`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ServicePackageRequestDtoToJSON(requestParameters['servicePackageRequestDto']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ServicePackageResponseDtoFromJSON(jsonValue));
    }

    /**
     * Creates a named set of operations sold together — a four-tyre install with the balance and TPMS reset, a fleet PM interval, a seasonal changeover — or, with fleetPartyId set, one fleet account\'s requirement set. Use this tool to define what a shop offers as a unit; do not use it to add work to a workorder, which is addEstimateItem. Preconditions: packageCode must be unique platform-wide. packageLaborHours is AUTHORED, not derived from the members — the overlap arithmetic that turns member times into a total lives in the workorder, and a shop prices a package as a number it chose. Required inputs: packageCode and name; ownerScope, ownerLocationId, fleetPartyId, packageLaborHours, active and the effective window are optional. Emits a CATALOG_SERVICE_PACKAGE_CREATE event. The package is created empty; add operations with addServicePackageMember. Returns 201 with the stored package, 409 when the code is taken, and 422 when a field is not storable as described. 
     * Create A Service Package
     */
    async createServicePackage(requestParameters: CreateServicePackageRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ServicePackageResponseDto> {
        const response = await this.createServicePackageRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns one package with its member operations in presentation order, including inactive packages. Use this tool when you already hold the package id; do not use it to discover what a location sells, which is listServicePackages. Preconditions: the package must exist. Required inputs: packageId. Emits a CATALOG_SERVICE_PACKAGE_GET event; no state changes. Returns 200 with the package, and 404 when no package has that id. 
     * Get A Service Package
     */
    async getServicePackageRaw(requestParameters: GetServicePackageRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ServicePackageResponseDto>> {
        if (requestParameters['packageId'] == null) {
            throw new runtime.RequiredError(
                'packageId',
                'Required parameter "packageId" was null or undefined when calling getServicePackage().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:service_package:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/service-packages/{packageId}`.replace(`{${"packageId"}}`, encodeURIComponent(String(requestParameters['packageId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ServicePackageResponseDtoFromJSON(jsonValue));
    }

    /**
     * Returns one package with its member operations in presentation order, including inactive packages. Use this tool when you already hold the package id; do not use it to discover what a location sells, which is listServicePackages. Preconditions: the package must exist. Required inputs: packageId. Emits a CATALOG_SERVICE_PACKAGE_GET event; no state changes. Returns 200 with the package, and 404 when no package has that id. 
     * Get A Service Package
     */
    async getServicePackage(requestParameters: GetServicePackageRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ServicePackageResponseDto> {
        const response = await this.getServicePackageRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Lists the active packages a location may sell — its own plus every platform package, its own first — each with its member operations in presentation order. Use this tool to show what can be added to a job as a unit, or with fleetPartyId to show what one fleet account requires on every visit; do not use it to look up a single package you already have the id for, which is getServicePackage. Preconditions: none. Fleet requirement sets are excluded from a general listing because they belong to one account and are not on offer — naming a fleetPartyId includes them, as does includeFleetPackages. Required inputs: none; locationId, fleetPartyId and includeFleetPackages are optional. Omitting locationId lists platform packages only, which is the right answer for a caller quoting as the platform. Emits a CATALOG_SERVICE_PACKAGE_LIST event; no state changes. Returns 200 with the packages, and an empty list when none match. 
     * List Service Packages
     */
    async listServicePackagesRaw(requestParameters: ListServicePackagesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ServicePackageResponseDto>>> {
        const queryParameters: any = {};

        if (requestParameters['locationId'] != null) {
            queryParameters['locationId'] = requestParameters['locationId'];
        }

        if (requestParameters['fleetPartyId'] != null) {
            queryParameters['fleetPartyId'] = requestParameters['fleetPartyId'];
        }

        if (requestParameters['includeFleetPackages'] != null) {
            queryParameters['includeFleetPackages'] = requestParameters['includeFleetPackages'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:service_package:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/service-packages`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ServicePackageResponseDtoFromJSON));
    }

    /**
     * Lists the active packages a location may sell — its own plus every platform package, its own first — each with its member operations in presentation order. Use this tool to show what can be added to a job as a unit, or with fleetPartyId to show what one fleet account requires on every visit; do not use it to look up a single package you already have the id for, which is getServicePackage. Preconditions: none. Fleet requirement sets are excluded from a general listing because they belong to one account and are not on offer — naming a fleetPartyId includes them, as does includeFleetPackages. Required inputs: none; locationId, fleetPartyId and includeFleetPackages are optional. Omitting locationId lists platform packages only, which is the right answer for a caller quoting as the platform. Emits a CATALOG_SERVICE_PACKAGE_LIST event; no state changes. Returns 200 with the packages, and an empty list when none match. 
     * List Service Packages
     */
    async listServicePackages(requestParameters: ListServicePackagesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ServicePackageResponseDto>> {
        const response = await this.listServicePackagesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Removes one operation\'s membership of a package. Use this tool to correct a package\'s composition; do not use it to change how much of an operation a package includes, which is a quantity on the membership rather than a removal. Removing a member does not affect any workorder already quoted from the package, which snapshots its own lines. Preconditions: the membership must exist and belong to the named package. Required inputs: packageId and memberId. Emits a CATALOG_SERVICE_PACKAGE_MEMBER_REMOVE event. Returns 200 with the whole package including its updated member list, and 404 when the membership does not exist in that package. 
     * Remove An Operation From A Package
     */
    async removeServicePackageMemberRaw(requestParameters: RemoveServicePackageMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ServicePackageResponseDto>> {
        if (requestParameters['packageId'] == null) {
            throw new runtime.RequiredError(
                'packageId',
                'Required parameter "packageId" was null or undefined when calling removeServicePackageMember().'
            );
        }

        if (requestParameters['memberId'] == null) {
            throw new runtime.RequiredError(
                'memberId',
                'Required parameter "memberId" was null or undefined when calling removeServicePackageMember().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:service_package:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/service-packages/{packageId}/members/{memberId}`.replace(`{${"packageId"}}`, encodeURIComponent(String(requestParameters['packageId']))).replace(`{${"memberId"}}`, encodeURIComponent(String(requestParameters['memberId']))),
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ServicePackageResponseDtoFromJSON(jsonValue));
    }

    /**
     * Removes one operation\'s membership of a package. Use this tool to correct a package\'s composition; do not use it to change how much of an operation a package includes, which is a quantity on the membership rather than a removal. Removing a member does not affect any workorder already quoted from the package, which snapshots its own lines. Preconditions: the membership must exist and belong to the named package. Required inputs: packageId and memberId. Emits a CATALOG_SERVICE_PACKAGE_MEMBER_REMOVE event. Returns 200 with the whole package including its updated member list, and 404 when the membership does not exist in that package. 
     * Remove An Operation From A Package
     */
    async removeServicePackageMember(requestParameters: RemoveServicePackageMemberRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ServicePackageResponseDto> {
        const response = await this.removeServicePackageMemberRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
