/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Product API
 * API documentation for the Product service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  BulkIngestRequestServiceLaborStandardImportRequestDto,
  BulkIngestResponse,
} from '../models/index';
import {
    BulkIngestRequestServiceLaborStandardImportRequestDtoFromJSON,
    BulkIngestRequestServiceLaborStandardImportRequestDtoToJSON,
    BulkIngestResponseFromJSON,
    BulkIngestResponseToJSON,
} from '../models/index';

export interface BulkIngestLaborStandardsRequest {
    bulkIngestRequestServiceLaborStandardImportRequestDto: BulkIngestRequestServiceLaborStandardImportRequestDto;
}

/**
 * 
 */
export class LaborStandardBulkIngestAPIApi extends runtime.BaseAPI {

    /**
     * Applies a batch of vehicle-keyed labor standards, each addressed by its service\'s operation code and carrying the source and revision that published it, with a per-row verdict. Use this tool to load a labor-time pack from a source that pushes its times; do not use it for a STORE-licensed feed the platform pulls, which arrives through runLaborGuideImport instead. Preconditions: every row\'s operationCode must already name a service, and a row whose vehicle key is already held by an active row of the same source and revision is applied as a no-op. Required inputs: jobId (UUID), locationId (UUID) and a non-empty records array; per record, operationCode, sourceCode, sourceRevision and laborHours, with the vehicle key, timeType, overlapGroup, includedOpCodes, ownership and publishedAt optional. Emits a CATALOG_LABOR_STANDARD_BULK_INGEST event; a row that changes an active standard supersedes it and inserts the replacement, leaving the old row readable for audit. Returns 200 even when rows fail, so callers must inspect successCount, failureCount and the per-row results rather than the HTTP status. A row the catalog refused carries errorCode LABOR_STANDARD_INGEST_FAILED and the reason; a row lost to a server-side fault carries INTERNAL_ERROR and a correlationId to quote. 
     * Bulk Import Labor Standards
     */
    async bulkIngestLaborStandardsRaw(requestParameters: BulkIngestLaborStandardsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkIngestResponse>> {
        if (requestParameters['bulkIngestRequestServiceLaborStandardImportRequestDto'] == null) {
            throw new runtime.RequiredError(
                'bulkIngestRequestServiceLaborStandardImportRequestDto',
                'Required parameter "bulkIngestRequestServiceLaborStandardImportRequestDto" was null or undefined when calling bulkIngestLaborStandards().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:labor_standard:import"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/catalog/labor-standards/bulk-ingest`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkIngestRequestServiceLaborStandardImportRequestDtoToJSON(requestParameters['bulkIngestRequestServiceLaborStandardImportRequestDto']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkIngestResponseFromJSON(jsonValue));
    }

    /**
     * Applies a batch of vehicle-keyed labor standards, each addressed by its service\'s operation code and carrying the source and revision that published it, with a per-row verdict. Use this tool to load a labor-time pack from a source that pushes its times; do not use it for a STORE-licensed feed the platform pulls, which arrives through runLaborGuideImport instead. Preconditions: every row\'s operationCode must already name a service, and a row whose vehicle key is already held by an active row of the same source and revision is applied as a no-op. Required inputs: jobId (UUID), locationId (UUID) and a non-empty records array; per record, operationCode, sourceCode, sourceRevision and laborHours, with the vehicle key, timeType, overlapGroup, includedOpCodes, ownership and publishedAt optional. Emits a CATALOG_LABOR_STANDARD_BULK_INGEST event; a row that changes an active standard supersedes it and inserts the replacement, leaving the old row readable for audit. Returns 200 even when rows fail, so callers must inspect successCount, failureCount and the per-row results rather than the HTTP status. A row the catalog refused carries errorCode LABOR_STANDARD_INGEST_FAILED and the reason; a row lost to a server-side fault carries INTERNAL_ERROR and a correlationId to quote. 
     * Bulk Import Labor Standards
     */
    async bulkIngestLaborStandards(requestParameters: BulkIngestLaborStandardsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkIngestResponse> {
        const response = await this.bulkIngestLaborStandardsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
