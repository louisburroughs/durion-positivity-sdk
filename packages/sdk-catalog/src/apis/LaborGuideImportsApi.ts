/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Product API
 * API documentation for the Product service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  LaborGuideImportSummaryDto,
  LaborGuideUnmappedOperationDto,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    LaborGuideImportSummaryDtoFromJSON,
    LaborGuideImportSummaryDtoToJSON,
    LaborGuideUnmappedOperationDtoFromJSON,
    LaborGuideUnmappedOperationDtoToJSON,
} from '../models/index';

export interface RunLaborGuideImportRequest {
    sourceCode: string;
}

/**
 * 
 */
export class LaborGuideImportsApi extends runtime.BaseAPI {

    /**
     * Returns every import whose status is APPLYING or INCOMPLETE — revisions where chunks or lines have not reconciled against the vendor\'s manifest, newest first. Use this tool to decide whether to re-run an import (a re-run resumes from the first missing chunk); do not use it as an import history, since completed revisions are deliberately not listed. Preconditions: none. Required inputs: none. Emits a CATALOG_LABOR_GUIDE_IMPORT_GAPS event; no state changes. Returns 200 with an empty array when every recorded import is complete, and 403 when the caller lacks the view permission. 
     * List Labor-Guide Imports Not Confirmed Complete
     */
    async listIncompleteLaborGuideImportsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<LaborGuideImportSummaryDto>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:labor_standard:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/catalog/labor-guide-imports/incomplete`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(LaborGuideImportSummaryDtoFromJSON));
    }

    /**
     * Returns every import whose status is APPLYING or INCOMPLETE — revisions where chunks or lines have not reconciled against the vendor\'s manifest, newest first. Use this tool to decide whether to re-run an import (a re-run resumes from the first missing chunk); do not use it as an import history, since completed revisions are deliberately not listed. Preconditions: none. Required inputs: none. Emits a CATALOG_LABOR_GUIDE_IMPORT_GAPS event; no state changes. Returns 200 with an empty array when every recorded import is complete, and 403 when the caller lacks the view permission. 
     * List Labor-Guide Imports Not Confirmed Complete
     */
    async listIncompleteLaborGuideImports(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<LaborGuideImportSummaryDto>> {
        const response = await this.listIncompleteLaborGuideImportsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Returns vendor operation codes that imports carried but no service_operation_xref row maps, most recently seen first, with occurrence counts so a curator can drain the queue by impact. Use this tool for mapping curation after an import; do not expect entries to clear themselves — adding the xref row is deliberate human work, and the next import then lands the lines. Preconditions: none. Required inputs: none. Emits a CATALOG_LABOR_GUIDE_UNMAPPED_LIST event; no state changes. Returns 200 with an empty array when every code the feeds carry is mapped, and 403 when the caller lacks the view permission. 
     * List Vendor Operation Codes Awaiting Curation
     */
    async listUnmappedLaborGuideOperationsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<LaborGuideUnmappedOperationDto>>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:labor_standard:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/catalog/labor-guide-imports/unmapped`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(LaborGuideUnmappedOperationDtoFromJSON));
    }

    /**
     * Returns vendor operation codes that imports carried but no service_operation_xref row maps, most recently seen first, with occurrence counts so a curator can drain the queue by impact. Use this tool for mapping curation after an import; do not expect entries to clear themselves — adding the xref row is deliberate human work, and the next import then lands the lines. Preconditions: none. Required inputs: none. Emits a CATALOG_LABOR_GUIDE_UNMAPPED_LIST event; no state changes. Returns 200 with an empty array when every code the feeds carry is mapped, and 403 when the caller lacks the view permission. 
     * List Vendor Operation Codes Awaiting Curation
     */
    async listUnmappedLaborGuideOperations(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<LaborGuideUnmappedOperationDto>> {
        const response = await this.listUnmappedLaborGuideOperationsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Opens the named STORE-licensed source\'s current feed revision and applies every not-yet-applied chunk: mapped lines upsert vehicle-keyed labor standards (unchanged lines skip, changed lines supersede the active row), and vendor codes with no cross-reference land in the unmapped curation queue while the import continues. Use this tool after a vendor publishes a new revision, or to resume an import that stopped mid-run; do not use it for QUERY_ONLY sources, whose license forbids persisting their times. Preconditions: the source must be configured under pos.catalog.labor-guide.providers with STORE mode. Required inputs: the sourceCode query parameter; there is no request body. Emits a CATALOG_LABOR_GUIDE_IMPORT event; re-running an already-complete revision is a recorded no-op. Returns 404 for an unconfigured source and 409 for a QUERY_ONLY source or an unreachable vendor. 
     * Import the Current Labor-Guide Feed Revision
     */
    async runLaborGuideImportRaw(requestParameters: RunLaborGuideImportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LaborGuideImportSummaryDto>> {
        if (requestParameters['sourceCode'] == null) {
            throw new runtime.RequiredError(
                'sourceCode',
                'Required parameter "sourceCode" was null or undefined when calling runLaborGuideImport().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['sourceCode'] != null) {
            queryParameters['sourceCode'] = requestParameters['sourceCode'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:labor_standard:import"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/catalog/labor-guide-imports`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LaborGuideImportSummaryDtoFromJSON(jsonValue));
    }

    /**
     * Opens the named STORE-licensed source\'s current feed revision and applies every not-yet-applied chunk: mapped lines upsert vehicle-keyed labor standards (unchanged lines skip, changed lines supersede the active row), and vendor codes with no cross-reference land in the unmapped curation queue while the import continues. Use this tool after a vendor publishes a new revision, or to resume an import that stopped mid-run; do not use it for QUERY_ONLY sources, whose license forbids persisting their times. Preconditions: the source must be configured under pos.catalog.labor-guide.providers with STORE mode. Required inputs: the sourceCode query parameter; there is no request body. Emits a CATALOG_LABOR_GUIDE_IMPORT event; re-running an already-complete revision is a recorded no-op. Returns 404 for an unconfigured source and 409 for a QUERY_ONLY source or an unreachable vendor. 
     * Import the Current Labor-Guide Feed Revision
     */
    async runLaborGuideImport(requestParameters: RunLaborGuideImportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LaborGuideImportSummaryDto> {
        const response = await this.runLaborGuideImportRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
