/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Product API
 * API documentation for the Product service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ApiError,
  ServiceLaborStandardRequestDto,
  ServiceLaborStandardResponseDto,
} from '../models/index';
import {
    ApiErrorFromJSON,
    ApiErrorToJSON,
    ServiceLaborStandardRequestDtoFromJSON,
    ServiceLaborStandardRequestDtoToJSON,
    ServiceLaborStandardResponseDtoFromJSON,
    ServiceLaborStandardResponseDtoToJSON,
} from '../models/index';

export interface CreateServiceLaborStandardRequest {
    serviceId: string;
    serviceLaborStandardRequestDto: ServiceLaborStandardRequestDto;
}

export interface ListServiceLaborStandardsRequest {
    serviceId: string;
    includeSuperseded?: boolean;
}

export interface SupersedeServiceLaborStandardRequest {
    serviceId: string;
    standardId: string;
    serviceLaborStandardRequestDto: ServiceLaborStandardRequestDto;
}

/**
 * 
 */
export class ServiceLaborStandardsApi extends runtime.BaseAPI {

    /**
     * Creates an active DURION-source labor standard: the estimated (book) time for this service operation on the vehicles the key fields describe, in decimal hours in tenths (0.1 hr = 6 min). Use this tool for hand-authored times — dealer-created operations or shop-decided standards; do not use it to transcribe a licensed labor guide, which enters through the guide import path with its own provenance. Preconditions: the service must exist, and no active standard may already cover the same vehicle key and time type — correct a wrong number by superseding it, not by adding a duplicate. Required inputs: serviceId path parameter and a body with laborHours; vehicle-key fields left null are wildcards, and timeType defaults to DURION_STANDARD. Emits a CATALOG_LABOR_STANDARD_CREATE event. Returns 400 for malformed hours (not in tenths, non-positive) or codes, 404 for an unknown service, and 409 when an active row already covers the same vehicle key and time type. 
     * Author a Labor Standard for a Service
     */
    async createServiceLaborStandardRaw(requestParameters: CreateServiceLaborStandardRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ServiceLaborStandardResponseDto>> {
        if (requestParameters['serviceId'] == null) {
            throw new runtime.RequiredError(
                'serviceId',
                'Required parameter "serviceId" was null or undefined when calling createServiceLaborStandard().'
            );
        }

        if (requestParameters['serviceLaborStandardRequestDto'] == null) {
            throw new runtime.RequiredError(
                'serviceLaborStandardRequestDto',
                'Required parameter "serviceLaborStandardRequestDto" was null or undefined when calling createServiceLaborStandard().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:labor_standard:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/catalog-items/service/{serviceId}/labor-standards`.replace(`{${"serviceId"}}`, encodeURIComponent(String(requestParameters['serviceId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ServiceLaborStandardRequestDtoToJSON(requestParameters['serviceLaborStandardRequestDto']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ServiceLaborStandardResponseDtoFromJSON(jsonValue));
    }

    /**
     * Creates an active DURION-source labor standard: the estimated (book) time for this service operation on the vehicles the key fields describe, in decimal hours in tenths (0.1 hr = 6 min). Use this tool for hand-authored times — dealer-created operations or shop-decided standards; do not use it to transcribe a licensed labor guide, which enters through the guide import path with its own provenance. Preconditions: the service must exist, and no active standard may already cover the same vehicle key and time type — correct a wrong number by superseding it, not by adding a duplicate. Required inputs: serviceId path parameter and a body with laborHours; vehicle-key fields left null are wildcards, and timeType defaults to DURION_STANDARD. Emits a CATALOG_LABOR_STANDARD_CREATE event. Returns 400 for malformed hours (not in tenths, non-positive) or codes, 404 for an unknown service, and 409 when an active row already covers the same vehicle key and time type. 
     * Author a Labor Standard for a Service
     */
    async createServiceLaborStandard(requestParameters: CreateServiceLaborStandardRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ServiceLaborStandardResponseDto> {
        const response = await this.createServiceLaborStandardRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the service\'s labor standards — vehicle-keyed book times with source and revision — active rows only by default, oldest first. Use this tool to see what estimated times exist for an operation and where each came from; do not use it to resolve the one applicable time for a specific vehicle, which is the resolution endpoint\'s job once it exists (sourcing plan §3.4). Preconditions: the service must exist. Required inputs: serviceId path parameter; includeSuperseded=true adds replaced rows for audit. Emits a CATALOG_LABOR_STANDARD_LIST event; no state changes. Returns 404 when the service does not exist. 
     * List a Service\'s Labor Standards
     */
    async listServiceLaborStandardsRaw(requestParameters: ListServiceLaborStandardsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ServiceLaborStandardResponseDto>>> {
        if (requestParameters['serviceId'] == null) {
            throw new runtime.RequiredError(
                'serviceId',
                'Required parameter "serviceId" was null or undefined when calling listServiceLaborStandards().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['includeSuperseded'] != null) {
            queryParameters['includeSuperseded'] = requestParameters['includeSuperseded'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:labor_standard:view"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/catalog-items/service/{serviceId}/labor-standards`.replace(`{${"serviceId"}}`, encodeURIComponent(String(requestParameters['serviceId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ServiceLaborStandardResponseDtoFromJSON));
    }

    /**
     * Returns the service\'s labor standards — vehicle-keyed book times with source and revision — active rows only by default, oldest first. Use this tool to see what estimated times exist for an operation and where each came from; do not use it to resolve the one applicable time for a specific vehicle, which is the resolution endpoint\'s job once it exists (sourcing plan §3.4). Preconditions: the service must exist. Required inputs: serviceId path parameter; includeSuperseded=true adds replaced rows for audit. Emits a CATALOG_LABOR_STANDARD_LIST event; no state changes. Returns 404 when the service does not exist. 
     * List a Service\'s Labor Standards
     */
    async listServiceLaborStandards(requestParameters: ListServiceLaborStandardsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ServiceLaborStandardResponseDto>> {
        const response = await this.listServiceLaborStandardsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Replaces an active DURION-source standard: the old row is marked superseded and kept for audit, and the body becomes the new active row, returned with fresh provenance. Use this tool to correct a hand-authored time; do not use it on imported guide rows, which are corrected by their source\'s next import — the API refuses them. Preconditions: the standard must belong to the service, still be active, and carry source DURION. Required inputs: serviceId and standardId path parameters, plus a full replacement body — this is a replacement, not a patch, so omitted vehicle-key fields become wildcards. Emits a CATALOG_LABOR_STANDARD_SUPERSEDE event. Returns 400 for a malformed body, 404 when the standard is not this service\'s, and 409 when the row is already superseded or not DURION-source. 
     * Supersede a Labor Standard with a Correction
     */
    async supersedeServiceLaborStandardRaw(requestParameters: SupersedeServiceLaborStandardRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ServiceLaborStandardResponseDto>> {
        if (requestParameters['serviceId'] == null) {
            throw new runtime.RequiredError(
                'serviceId',
                'Required parameter "serviceId" was null or undefined when calling supersedeServiceLaborStandard().'
            );
        }

        if (requestParameters['standardId'] == null) {
            throw new runtime.RequiredError(
                'standardId',
                'Required parameter "standardId" was null or undefined when calling supersedeServiceLaborStandard().'
            );
        }

        if (requestParameters['serviceLaborStandardRequestDto'] == null) {
            throw new runtime.RequiredError(
                'serviceLaborStandardRequestDto',
                'Required parameter "serviceLaborStandardRequestDto" was null or undefined when calling supersedeServiceLaborStandard().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:labor_standard:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/catalog-items/service/{serviceId}/labor-standards/{standardId}/supersede`.replace(`{${"serviceId"}}`, encodeURIComponent(String(requestParameters['serviceId']))).replace(`{${"standardId"}}`, encodeURIComponent(String(requestParameters['standardId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ServiceLaborStandardRequestDtoToJSON(requestParameters['serviceLaborStandardRequestDto']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ServiceLaborStandardResponseDtoFromJSON(jsonValue));
    }

    /**
     * Replaces an active DURION-source standard: the old row is marked superseded and kept for audit, and the body becomes the new active row, returned with fresh provenance. Use this tool to correct a hand-authored time; do not use it on imported guide rows, which are corrected by their source\'s next import — the API refuses them. Preconditions: the standard must belong to the service, still be active, and carry source DURION. Required inputs: serviceId and standardId path parameters, plus a full replacement body — this is a replacement, not a patch, so omitted vehicle-key fields become wildcards. Emits a CATALOG_LABOR_STANDARD_SUPERSEDE event. Returns 400 for a malformed body, 404 when the standard is not this service\'s, and 409 when the row is already superseded or not DURION-source. 
     * Supersede a Labor Standard with a Correction
     */
    async supersedeServiceLaborStandard(requestParameters: SupersedeServiceLaborStandardRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ServiceLaborStandardResponseDto> {
        const response = await this.supersedeServiceLaborStandardRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
