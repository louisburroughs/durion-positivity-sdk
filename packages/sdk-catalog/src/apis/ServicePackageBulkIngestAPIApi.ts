/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Product API
 * API documentation for the Product service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  BulkIngestRequestServicePackageBulkIngestRecord,
  BulkIngestResponse,
} from '../models/index';
import {
    BulkIngestRequestServicePackageBulkIngestRecordFromJSON,
    BulkIngestRequestServicePackageBulkIngestRecordToJSON,
    BulkIngestResponseFromJSON,
    BulkIngestResponseToJSON,
} from '../models/index';

export interface BulkIngestServicePackagesRequest {
    bulkIngestRequestServicePackageBulkIngestRecord: BulkIngestRequestServicePackageBulkIngestRecord;
}

/**
 * 
 */
export class ServicePackageBulkIngestAPIApi extends runtime.BaseAPI {

    /**
     * Imports a batch of service packages in one call, creating or updating each by its package code, with a per-row success or failure verdict. Use this tool to load a pack of packages and fleet requirement sets; do not use createServicePackage, which authors one package at a time and refuses a code that already exists rather than updating it. Preconditions: each row\'s packageCode must be uppercase alphanumeric segments joined by single dashes, and a SHOP-scoped row must name its owning location while a PLATFORM one must not. Required inputs: jobId (UUID), locationId (UUID) and a non-empty records array; per record, packageCode, name and packageLaborHours, with description, ownership, fleetPartyId, active and the effective window optional. Emits a CATALOG_SERVICE_PACKAGE_BULK_INGEST event; membership is untouched here, so a package\'s members survive a re-load and arrive through bulkIngestServicePackageMembers. Returns 200 even when rows fail, so callers must inspect successCount, failureCount and the per-row results rather than the HTTP status. A row the catalog refused carries errorCode SERVICE_PACKAGE_INGEST_FAILED and the reason; a row lost to a server-side fault carries INTERNAL_ERROR and a correlationId to quote. 
     * Bulk Import Service Packages
     */
    async bulkIngestServicePackagesRaw(requestParameters: BulkIngestServicePackagesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkIngestResponse>> {
        if (requestParameters['bulkIngestRequestServicePackageBulkIngestRecord'] == null) {
            throw new runtime.RequiredError(
                'bulkIngestRequestServicePackageBulkIngestRecord',
                'Required parameter "bulkIngestRequestServicePackageBulkIngestRecord" was null or undefined when calling bulkIngestServicePackages().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:service_package:manage"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/service-packages/bulk-ingest`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkIngestRequestServicePackageBulkIngestRecordToJSON(requestParameters['bulkIngestRequestServicePackageBulkIngestRecord']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkIngestResponseFromJSON(jsonValue));
    }

    /**
     * Imports a batch of service packages in one call, creating or updating each by its package code, with a per-row success or failure verdict. Use this tool to load a pack of packages and fleet requirement sets; do not use createServicePackage, which authors one package at a time and refuses a code that already exists rather than updating it. Preconditions: each row\'s packageCode must be uppercase alphanumeric segments joined by single dashes, and a SHOP-scoped row must name its owning location while a PLATFORM one must not. Required inputs: jobId (UUID), locationId (UUID) and a non-empty records array; per record, packageCode, name and packageLaborHours, with description, ownership, fleetPartyId, active and the effective window optional. Emits a CATALOG_SERVICE_PACKAGE_BULK_INGEST event; membership is untouched here, so a package\'s members survive a re-load and arrive through bulkIngestServicePackageMembers. Returns 200 even when rows fail, so callers must inspect successCount, failureCount and the per-row results rather than the HTTP status. A row the catalog refused carries errorCode SERVICE_PACKAGE_INGEST_FAILED and the reason; a row lost to a server-side fault carries INTERNAL_ERROR and a correlationId to quote. 
     * Bulk Import Service Packages
     */
    async bulkIngestServicePackages(requestParameters: BulkIngestServicePackagesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkIngestResponse> {
        const response = await this.bulkIngestServicePackagesRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
