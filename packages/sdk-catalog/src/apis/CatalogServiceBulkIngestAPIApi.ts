/* tslint:disable */
/* eslint-disable */
/**
 * Positivity Product API
 * API documentation for the Product service, accessible via the API Gateway.
 *
 * The version of the OpenAPI document: v1
 * Contact: louis.burroughs@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  BulkIngestRequestCatalogServiceBulkIngestRecord,
  BulkIngestResponse,
} from '../models/index';
import {
    BulkIngestRequestCatalogServiceBulkIngestRecordFromJSON,
    BulkIngestRequestCatalogServiceBulkIngestRecordToJSON,
    BulkIngestResponseFromJSON,
    BulkIngestResponseToJSON,
} from '../models/index';

export interface BulkIngestCatalogServicesRequest {
    bulkIngestRequestCatalogServiceBulkIngestRecord: BulkIngestRequestCatalogServiceBulkIngestRecord;
}

/**
 * 
 */
export class CatalogServiceBulkIngestAPIApi extends runtime.BaseAPI {

    /**
     * Imports a batch of service operations in one call, creating or updating each by its Durion operation code and publishing a catalog service fact per row, with a per-row success or failure verdict. Use this tool to load a pack of operations, such as the Tier 0 tire, Michelin and fleet operations; do not use addCatalogItem, which registers one service at a time and reports its errors as HTTP statuses rather than row results. Preconditions: each row\'s operationCode must be uppercase alphanumeric segments joined by single dashes, and a code already in the catalog updates that service in place instead of failing the row. Required inputs: jobId (UUID), locationId (UUID) and a non-empty records array; per record, operationCode and name, with shortDescription, longDescription, operationCategory and defaultLaborHours optional. Emits a CATALOG_SERVICE_BULK_INGEST event, and each successful row publishes the service fact that hydrates every ext_catalog_service replica. Returns 200 even when rows fail, so callers must inspect successCount, failureCount and the per-row results rather than the HTTP status. A row the catalog refused carries errorCode CATALOG_SERVICE_INGEST_FAILED and the reason; a row lost to a server-side fault carries INTERNAL_ERROR and a correlationId to quote. 
     * Bulk Import Service Operations
     */
    async bulkIngestCatalogServicesRaw(requestParameters: BulkIngestCatalogServicesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BulkIngestResponse>> {
        if (requestParameters['bulkIngestRequestCatalogServiceBulkIngestRecord'] == null) {
            throw new runtime.RequiredError(
                'bulkIngestRequestCatalogServiceBulkIngestRecord',
                'Required parameter "bulkIngestRequestCatalogServiceBulkIngestRecord" was null or undefined when calling bulkIngestCatalogServices().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", ["ROLE_ADMIN", "catalog:service:ingest"]);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/v1/catalog/services/bulk-ingest`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BulkIngestRequestCatalogServiceBulkIngestRecordToJSON(requestParameters['bulkIngestRequestCatalogServiceBulkIngestRecord']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BulkIngestResponseFromJSON(jsonValue));
    }

    /**
     * Imports a batch of service operations in one call, creating or updating each by its Durion operation code and publishing a catalog service fact per row, with a per-row success or failure verdict. Use this tool to load a pack of operations, such as the Tier 0 tire, Michelin and fleet operations; do not use addCatalogItem, which registers one service at a time and reports its errors as HTTP statuses rather than row results. Preconditions: each row\'s operationCode must be uppercase alphanumeric segments joined by single dashes, and a code already in the catalog updates that service in place instead of failing the row. Required inputs: jobId (UUID), locationId (UUID) and a non-empty records array; per record, operationCode and name, with shortDescription, longDescription, operationCategory and defaultLaborHours optional. Emits a CATALOG_SERVICE_BULK_INGEST event, and each successful row publishes the service fact that hydrates every ext_catalog_service replica. Returns 200 even when rows fail, so callers must inspect successCount, failureCount and the per-row results rather than the HTTP status. A row the catalog refused carries errorCode CATALOG_SERVICE_INGEST_FAILED and the reason; a row lost to a server-side fault carries INTERNAL_ERROR and a correlationId to quote. 
     * Bulk Import Service Operations
     */
    async bulkIngestCatalogServices(requestParameters: BulkIngestCatalogServicesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BulkIngestResponse> {
        const response = await this.bulkIngestCatalogServicesRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
